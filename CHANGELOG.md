# Changelog

All notable AIDraw changes are recorded here. The project follows Semantic Versioning; prerelease versions remain explicitly pre-v1 until every release gate in the feature tracker is satisfied.

## [Unreleased]

- Continue the audited v1 critical path in `docs/FEATURE_TRACKER.md`.
Let bounded illustration observations reuse a complete validated materialized paint cache without allocating the full artboard. The renderer still scans the bounded cache index, but decodes and copies only 256×256 tiles intersecting the established four-pixel-expanded backing; exact production tests match full-artboard raw crops across two regional offsets and opaque/transparent backgrounds, and a one-pixel request at the far edge of an 8,192×8,192 cached paint document now completes under a separate 100 ms tripwire. This path requires the cache to cover every editable stroke inside default full-opacity/normal layers. An appended live stroke, absent/incomplete cache, invalid tile arithmetic, mask, filter, blend, opacity, or richer vector content retains the full-artboard render plus raw crop. Existing cache validation, materialization, canonical strokes, save/export/interchange, observation limits/results, and the straight-primitive eligibility remain unchanged; this is not a generic paint clipper, packaged/native proof, Level 3, RC, or stable-v1 claim.
- Extend bounded targeted illustration observation from square-corner rectangles to the exact additional straight-edge Canvas subset proven phase-stable: integer-positioned/sized polygons, stars, and integer-translated structural groups. A requested region still renders on the established four-pixel-expanded backing and raw-crops byte-for-byte against a full-artboard render across a deterministic matrix of sizes, positions, side counts, backing offsets, opaque/transparent backgrounds, and translucent overlap; the 8,192×8,192 performance fixture now uses a filled star and remains under the unchanged 100 ms budget without allocating the 64-megapixel artboard. Ellipses and a rounded-rectangle candidate both produced real raw-channel differences and were explicitly rejected from this fast path. Rounded/fractional geometry, gradients, strokes, paths, text, images, paint, masks, opacity/blend isolation, effects, rotation/scale/skew, and every unproved curve retain the established full-artboard render plus raw crop. This changes no canonical model, observation budget/result, save/export, interchange, packaged behavior, Level 3, RC, or stable-v1 claim.
- Make ordinary Electron/OS quit honor the documented graceful engine boundary. A `before-quit` event now prevents process exit, coalesces repeated quit requests onto one stop, and calls `app.quit()` again only after MCP shutdown, utility cancellation, and final document/workspace recovery compaction succeed; the second event then passes through. A failed stop keeps quit cancelled, emits a bounded stderr diagnostic, and remains retryable. `EngineRuntime.stop()` now shares concurrent callers and does not mark itself stopped until final recovery succeeds, so a compaction failure can be retried instead of turning the next quit into a false success. Existing explicit quit confirmation, CLI/no-runtime exits, shutdown order, recovery formats, and headless lifecycle remain unchanged. This adds no force-quit UI, timeout, crash/power-loss/directory-fsync guarantee, packaged OS-quit acceptance, Level 3, RC, or stable-v1 claim.
- Serialize start-at-login mutations in invocation order and publish Linux XDG autostart enablement through the shared private flushed atomic writer. Concurrent enable/disable requests in one process can no longer race through one PID-named temporary and leave the older intent active; the later request deterministically wins. A failed Linux replacement preserves the prior entry, removes its unique temporary, and leaves the queue usable for retry, while successful POSIX output remains mode `0600`. Windows/macOS keep their existing native login-item calls and every platform retains existing status and executable/argument semantics. This adds no cross-process/multiple-instance coordination, desktop-environment compatibility expansion, directory-fsync/power-loss guarantee, packaged login/reboot proof, Level 3, RC, or stable-v1 claim.
- Make consented automatic Codex, Claude Code, OpenCode, and Antigravity configuration one process-serialized private atomic workflow. Concurrent setup requests now update in invocation order; each existing version receives a distinct timestamp-based mode-0600 backup even when requests share a timestamp, and the complete replacement is flushed before it becomes visible. A replacement failure preserves prior config bytes, removes its temporary, and leaves later setup usable. The saved client config is authoritative over the separate start-at-login follow-up: if enabling startup fails, the result remains `configured`, retains its config and backup paths, and tells the user to start AIDraw manually instead of falsely reporting that configuration failed or rolling it back. Existing consent, per-client schemas, unrelated settings/comments, generic manual output, and isolated packaged-validation behavior remain unchanged. This adds no config byte ceiling, cross-process/multiple-instance locking or compare-and-swap guarantee, directory-fsync/power-loss durability, live client restart/discovery, packaged interaction, Level 3, RC, or stable-v1 claim.
- Publish the explicit headless MCP connection handoff as one complete private atomic artifact instead of direct-writing the live plaintext bearer token. The documented nonempty absolute-path requirement now fails closed instead of silently resolving a relative destination. A pre-existing destination remains byte-exact until a unique same-directory mode-0600 temporary has been completely written, flushed, closed, and atomically replaced; the final POSIX file is private even if the previous file was permissive. Replacement failure preserves the prior artifact and removes the temporary. The version-one JSON fields, absolute output pathname/overwrite authorization, process-lifetime folder grants, server/token values, client import flow, and deletion/redaction responsibility remain unchanged. The final named filesystem entry is replaced rather than written through, which is the private-publication boundary; this adds no parent-path canonicalization, cross-process writer lock, directory-fsync/power-loss guarantee, packaged launcher proof, Level 3, RC, or stable-v1 readiness.
- Make localhost MCP bearer-token creation and corrupt-secret rotation one serialized private atomic store operation. Concurrent requests to one store now converge on the first persisted token instead of generating different credentials; missing, oversized, malformed, unknown-field, noncanonical-ciphertext, decrypt-failed, or noncanonical-token state rotates through a unique mode-0600 same-directory temporary that is completely written, flushed, closed, and atomically replaced before the new token is returned. Invalid encryption or replacement failure preserves prior bytes, removes the temporary, returns no unusable token, and leaves the queue available for retry. Existing 32-byte base64url token generation, 256 KiB encrypted-file ceiling, `safeStorage` backend requirement, corrupt-secret rotation, connection authority, and renderer exposure remain unchanged. This adds no cross-process/multiple-store locking, directory-fsync/power-loss guarantee, native backend or packaged restart proof, Level 3, RC, or stable-v1 readiness.
- Keep valid native artwork open when its auxiliary transaction-trace history cannot be imported into local storage. Open now reports the `.aidraw` path as opened, preserves every native parsing warning, emits the existing trace-error signal, and adds one path-scoped warning that trace history may be incomplete; immediate edits still enter ordinary recovery. Previously the document and recovery state were installed but the outer error path falsely omitted the file from `opened`. Artwork remains authoritative over advisory trace history, consistent with malformed-record omission and nonblocking trace-record failures. This changes no archive or trace schema, malformed-record policy, canonical artwork, save/recovery format, trace retry/completeness guarantee, cross-process durability, packaged warning presentation, Level 3, RC, or stable-v1 readiness.
- Serialize canonical apply, undo, redo, and checkpoint-restore requests in invocation order for each document while preserving independent progress in other documents. A transaction delayed by supervised image validation can no longer be overtaken by a later same-document rename or history command; its original document incarnation and any human lock are rechecked before commit, so closing and replacing that ID makes the old request conflict instead of overwriting replacement work. Undo invoked behind a pending commit now acts on that commit, redo follows undo, and a queued restore first preserves the exact committed branch. Revision-bound saves remain on their separate invocation-snapshot queue and continue to permit editing. This changes no reducer conflict, actor/history, journal/trace, playback, native schema, save, or cross-document behavior and adds no global/cross-process ordering, cancellation/coalescing, persistent undo history, packaged interaction, Level 3, RC, or stable-v1 claim.
- Treat a canonical document ID as one unique open-workspace identity. Reopening the exact already-open path remains idempotent and preserves its live unsaved state, while opening a different `.aidraw` file carrying that ID now returns an explicit warning instead of replacing the open tab, history, checkpoints, trace association, or recovery state. Multi-document import results preflight all IDs and clone the complete set before one workspace publication, so an in-use or within-result duplicate rejects the set before any sibling is admitted. Users can close the existing document and then open its copied file; AIDraw does not silently re-key, merge, alias, or overwrite either identity. This changes no native schema or stored ID, ordinary unique-ID import/open, exact-path reopen, file authority, or save behavior, and adds no realpath/symlink identity, cross-process coordination, packaged dialog acceptance, Level 3, RC, or stable-v1 readiness.
- Bind every native save to one immutable invocation-time document revision. Same-document saves now serialize in request order and recover after a rejected predecessor; artwork, checkpoint records, preview geometry, and preview rendering come from the captured revision, while only trace entries at or below that revision enter its archive. An edit committed while save work is pending therefore cannot leak into the older archive or be marked saved: the completed destination becomes the live document's file association, safe persisted paint-cache prefixes are reused, the newer revision stays dirty, and recovery is compacted with those unsaved edits. An unchanged revision becomes clean normally. A completion from an earlier closed/replaced document incarnation cannot mutate or resurrect its replacement. Existing archive schema, atomic replacement, overwrite behavior, checkpoint/trace formats, oversized-preview fallback, save paths, and cross-document concurrency remain unchanged. This adds no save cancellation/coalescing, cross-process locking, directory-fsync/power-loss guarantee, packaged interaction evidence, Level 3, RC, or stable-v1 readiness.
- Make reusable document presets and interchange-report history transactional within one running engine. Every list/get/save/delete/record request now serializes in invocation order; a state-changing request builds copied state, writes the complete JSON through one unique exclusive mode-0600 temporary, flushes and closes it, atomically replaces the destination, and only then publishes the copied state in memory. Concurrent preset saves and report records therefore retain both results instead of allowing a delayed older replacement to discard the later one. An update, delete, or report replacement failure best-effort removes its temporary and leaves the prior file and live state exact so later requests can continue. Existing preset/report version-one shapes, normalization/recovery behavior, 64-preset and 200-report caps, structured fidelity reasons, and UI/API contracts remain unchanged. This adds no cross-process or multi-instance locking, store byte ceiling, directory-fsync/power-loss guarantee, packaged persistence evidence, broader interchange claim, Level 3, RC, or stable-v1 readiness.
- Make durable MCP batches transactional across their private progress ledger. Every new batch sequence now requires a `clientOperationId` not already committed by that batch, while an exact retry at its original sequence remains an idempotent duplicate; reusing the old key at the next sequence returns a conflict before canonical mutation. Startup admits only exact version-one writer-shaped ledger roots and records, discards malformed records, and clears stale resumability when the root is invalid. Start, resume, prepare, finish, and cancel build replacement state, write it through a unique mode-0600 temporary, flush, close, and atomically replace the ledger before publishing the new live job; a rejected replacement leaves the prior disk and memory state intact so prepare/finish can retry. Progress additionally requires a durable transaction identity. Resume tokens remain hashed, canonical drawing data stays in the journal/trace, and valid restart/crash-window recovery is unchanged. This adds no batch-file size ceiling, directory-fsync or power-loss guarantee, cross-batch idempotency policy, packaged restart evidence, Level 3, RC, or stable-v1 claim.
- Fail closed when application-owned MCP control settings are not the exact version-one shapes AIDraw writes. Persistent folder authority now requires an object containing only `version` and a unique array of absolute, NUL-free folder paths; every path is canonicalized into a temporary set before any authority is installed, and each engine start replaces rather than accumulates the prior in-memory set. A malformed scalar can no longer be iterated as path fragments, and a corrupt restart cannot retain stale authority. The preferred-port file likewise accepts only an integer from 48200 through 48231, so strings cannot become named-pipe/listen targets. Both settings files now use private mode-0600 temporary files, flush before close, and atomically replace their destination; persistent grants serialize, enter memory only after replacement, and drain during engine stop. Valid persistent trust, session/launch trust, overwrite approvals, path canonicalization, and the first-available-port scan remain unchanged. This does not add a settings byte/count ceiling, authenticate local files, revoke trust through UI, prove packaged restart behavior, or establish Level 3, RC, or stable-v1 readiness.
- Make the shared **256 KiB encrypted-credential file ceiling** effective before allocation for both hosted-provider ciphertext and the localhost MCP bearer token. Each file now uses the handle-bound nonblocking reader, so an over-limit or observably changing file is never passed to JSON parsing or Electron `safeStorage`. Provider status retains the established corruption recovery and reports no stored key until an explicit rotation writes a valid file; the MCP token retains its established corruption recovery and rotates to a fresh encrypted token. No ciphertext or token is exposed, no prefix is accepted, and secure-storage availability, provider removal/rotation, connection authority, native backend behavior, and file locations remain unchanged. This does not bound unrelated user-data stores or prove packaged DPAPI/Keychain/libsecret behavior, exact peak RSS, Level 3, RC, or stable-v1 readiness.
- Make existing local-input byte ceilings effective during the file read itself. Root/companion interchange imports, direct sprite sheets, native `.aidraw` archives, human and approved-agent palettes, and project-link relinks now share one regular-file reader: it checks the path, opens nonblocking, repeats the check on the handle, allocates only the handle-declared size plus one sentinel byte, and rejects observable growth or shrinkage before parser/decode/canonical work. The extra byte detects post-check growth and is never returned. Exact-boundary input remains byte-for-byte unchanged; every existing 1 MiB/1.5 MB/16 MiB/256 MiB/512 MiB limit and existing authority/parser/codec/canonical behavior remains. This is bounded whole-file admission, not streaming; it adds no new format/size promise, symlink/file approval, path-identity or arbitrary mutation snapshot guarantee, exact peak-RSS, packaged/native UI, Level 3, RC, or stable-v1 claim.
- Move MCP generation-approval source and mask thumbnails out of the Electron browser process and into the supervised raster lane, separate from potentially long provider work. Main first admits the already-canonical asset's exact bytes, SHA-256, MIME/header, display geometry, 1,500,000-byte source ceiling, and 8,192-side/16MP image limits. The worker then returns only a no-enlarge static PNG at the existing 180×120 fit under 256 KiB; main verifies canonical base64, PNG chunks/CRCs, exact preview geometry, and one complete RGBA decode before the image enters an approval card. A malformed result retires its worker and queued work recovers fresh. Thumbnail failure remains advisory so the exact source/mask IDs and potentially paid request can still be reviewed without granting or denying authority. This changes no provider request, approval fields/options/expiry, source bytes, canonical state, network dispatch, format semantics, or file authority; packaged approval presentation, provider behavior, exact worker peak RSS, Level 3, RC, and stable-v1 readiness remain unproved.
- Move sprite-sheet selection, source reads, codec decode, and preview PNG generation out of the Electron browser process and into the existing supervised raster lane. The picker now applies the established display-oriented JPEG geometry, derives PNG/JPEG/WebP MIME from the file header rather than its extension, and returns only a main-admitted static PNG at the existing no-enlarge 640-wide/480-high preview shape under a 4 MiB result ceiling. Both human selection and approved-agent slicing read the source inside the worker under the unchanged 1,500,000-byte retained-asset limit; a human import additionally rechecks the exact selection hash/MIME before slicing. Malformed preview results retire their worker before renderer use, while a changed source fails before canonical mutation. The frozen orientation-6 JPEG now previews at 16×24 and slices into four 8×12 frames while retaining its exact bytes. Display geometry was chosen over encoded geometry so the visible grid and imported pixels agree. This changes no slicing options, quantization, canonical schema, selection TTL/cap, file authority, supported picker extensions, animation interpretation, or broad raster compatibility claim; packaged dialog behavior, arbitrary-producer fidelity, exact worker peak RSS, Level 3, RC, and stable-v1 readiness remain unproved.
- Make generation comparison sources truthful and fail closed before any job or provider dispatch. A selected canonical PNG/APNG/JPEG/WebP/GIF now keeps its exact bytes, real MIME, and header/display geometry without an Electron `nativeImage` re-decode; the in-memory comparison type is distinct from provider outputs so animated source formats are no longer mislabeled as PNG. When no source image is selected, comparison rendering uses an isolated document snapshot through the supervised PNG export lane, retains the established full-size static-raster guard, and admits only copied PNG bytes whose header dimensions exactly match that snapshot under the existing utility-result ceiling. Malformed or contradictory renderer output creates no job and sends no provider request. Exact source animation/bytes are preserved instead of frozen or transcoded, while adaptive previews remain a separate product decision. Provider payloads, calls, canonical documents/assets, generation acceptance, persistence schemas, whole-raster limits, and network authority remain unchanged; packaged comparison presentation, real-provider behavior, arbitrary-producer compatibility, exact utility peak RSS, Level 3, RC, and stable-v1 readiness are not claimed.
- Move production `.aidraw` preview generation out of the Electron browser process and through the existing supervised PNG export lane. Save takes an isolated document snapshot, retains the established full-size static-raster guard and transparent 1×1 fallback for valid oversized documents, and admits only a static PNG whose header geometry exactly matches that snapshot. Malformed or contradictory worker output fails before any temporary file or destination replacement; AIDraw does not fall back to an unsupervised browser-process render or silently substitute 1×1 for an in-bounds preview. Canonical document/checkpoint/trace/assets, save paths, whole-raster limits, and the native format schema remain unchanged. The auxiliary `preview.png` may use the ordinary supervised PNG encoder, so exact archive/preview byte identity is not claimed; neither are packaged error presentation, exact utility peak RSS, adaptive previews, Level 3, RC, or stable-v1 readiness.
- Move checkpoint-comparison PNG rendering out of the Electron browser process and into the existing supervised raster export lane. Current and saved immutable snapshots render sequentially so one comparison never owns two native render tasks at once; each returned PNG must match the exact snapshot dimensions and static PNG identity before its unchanged revision/size/data-URL IPC result is returned. A malformed or contradictory utility result fails the comparison instead of reaching the image UI. The full-size export path is retained rather than substituting the lower 4 MP observation ceiling, so this changes no comparison/merge/checkpoint/document semantics or existing whole-raster limit. It adds no adaptive preview, native-save-preview isolation, packaged dialog acceptance, utility peak-RSS proof, Level 3, RC, or stable-v1 claim.
- Bound clipboard interchange and move its raster work onto the supervised utility lane. New copies carry a version-1 `data-aidraw` HTML envelope with canonical base64, SHA-256 identity, a 2 MiB decoded-fragment ceiling, and a 16 MiB complete-HTML ceiling; the exact legacy marker remains readable. If clipboard HTML claims AIDraw ownership but is malformed, duplicated, oversized, noncanonical, or hash-invalid, paste now reports a conflict instead of silently substituting the bitmap fallback. Ordinary clipboard content without that marker can still paste PNG after its bytes, header, hash, native/decoded geometry, 8,192-side/16MP ceiling, and existing 1,500,000-byte editable-asset limit agree. Clipboard copy rendering and pixel-mode bitmap quantization now use the established supervised raster worker; Electron native images remain only at the OS clipboard boundary. Standard SVG/JSON text and PNG fallbacks remain, and no custom OS MIME, native-file schema change, cross-application/packaged acceptance, or broad interchange claim is introduced.
- Give both renderer popovers explicit keyboard and focus ownership. Opening All documents focuses the active item; Up/Down wraps through every document and the three batch commands, Home/End reaches an edge, and every selection or Escape returns focus to the trigger. Export is now a named non-modal dialog—not an ARIA menu around form fields—that focuses its first available control, dismisses on outside pointer interaction, and returns to its trigger after Escape or an export choice. Escape is captured so closing a popup cannot also clear pixel-canvas state. These adapters change no document, batch, export, or file-dialog command and add no menu typeahead or focus trap. Source/static evidence does not claim packaged focus order/restoration, screen-reader announcements, 200% layout, Level 3, RC, or stable-v1 readiness; UX-08 remains Partial and UX-09 remains Working.
- Make open-document tabs a bounded keyboard navigation surface. The active tab is the tablist's single ordinary tab stop; unmodified Left/Right wraps, Home/End selects an edge, and focus movement automatically activates the destination document. The close affordance is now a sibling native button instead of a nested interactive element, only the active tab's close action enters sequential focus, and a successful active-tab close restores focus to the replacement active tab. The searchable shortcut guide names the navigation keys. This changes no document lifecycle, close/save prompt, direct document shortcut, all-documents menu behavior, or canonical state. Source/static evidence does not claim packaged focus restoration, screen-reader behavior, 200% layout, complete focus order, Level 3, RC, or stable-v1 readiness; UX-08 remains Partial and UX-09 remains Working.
- Add one bounded keyboard-discoverability and focus-navigation surface. F1 or ? opens a searchable, current-mode shortcut guide whose tool entries come from the active tool catalog; the same guide is reachable from a named top-bar button. The vertical tool rail now uses one roving tab stop with Up/Down/Home/End focus movement and per-tool `aria-keyshortcuts`, while a focus-visible Skip to canvas control targets either named drawing surface. New-document, tool, and global editing/history shortcuts no longer act behind any open modal, and tool letters also ignore selects and editable content. The guide documents the existing fixed commands but intentionally does not add shortcut remapping, a keyboard alternative for direct canvas drawing, or new canonical operations. Source/static evidence does not claim packaged focus order, native key-layout behavior, screen-reader acceptance, 200% layout, Level 3, RC, or stable-v1 readiness; UX-08 remains Partial.
- Add bounded in-canvas content editing for canonical illustration text. With the Select tool, users can double-click visible unlocked text or select it and press Enter; the multiline editor follows the text box through its object and ancestor-group affine transforms. Blur or Ctrl/Cmd+Enter rechecks visibility/lock state and applies one revision-checked styled-text replacement, Escape discards the draft, and either a conflict or a newly ineligible hierarchy keeps the draft available. Existing character styles survive content replacement, while per-range typography remains in the inspector; a plain selection click no longer records a zero-distance move. The DOM editor intentionally presents the first-character style instead of becoming a second rich-text layout engine. This changes no text schema, stored transforms, Canvas/headless rendering, font embedding/interchange behavior, transformed-group hit-testing contract, packaged interaction evidence, Level 3, RC, or stable-v1 readiness.
- - Make unsupported PSD blend-mode loss explicit on import. Illustration and pixel imports still map modes outside AIDraw's closed canonical blend set to `normal`, but now emit one summary warning and one bounded `blend-mode-substitution` fidelity reason tied to each exact generated layer identity. The optional list is validated before and after the utility-process boundary, shares the existing 1 MiB aggregate report envelope, and reaches both human and approved-agent Activity reports; an unknown code retires the worker before caller use. Generated `pass through` and `color` controls prove exact order, identity, details, and identical illustration/pixel behavior. This does not implement those modes, change canonical state or PSD bytes, code raster/effect/font/lock losses comprehensively, prove external-producer visuals or packaged Activity presentation, or claim broad PSD compatibility, Level 3, RC, or stable-v1 readiness.
- Preserve partial-alpha RGB through ordinary fractional-opacity pixel composites in PNG, APNG, and sprite-sheet output. The exact normal compositor now accepts bounded path-multiplied group/layer opacity, reproduces the pinned renderer's 0–1 to byte-floor rule before combining palette alpha, and performs the existing deterministic unpremultiplied source-over. A single leaf retains its authored RGB even when opacity lowers alpha; multi-layer fully transparent nonzero colors and every non-normal blend still use Canvas. Generated pure and two-frame production controls preserve `#4080c080` at half opacity as `[64,128,192,64]` and an overlapping fractional normal composite across PNG/APNG/sheet bytes. GIF explicitly retains its full-effective-opacity eligibility and whole-animation quantizer, even when a fractional composite would finish opaque. This changes no schema, canonical pixels/layers, opacity values, frame/tag/timing, sheet geometry/metadata, encoder profile, alpha threshold, non-normal blend behavior, GIF policy, external-producer or broad compatibility claim, packaged/native behavior, Level 3, RC, or stable-v1 claim.
- Preserve representable pixel PSD leaf colors exactly through one shared indexed palette. A bounded streaming planner unions raw raster-leaf RGBA in canonical tree/raster discovery order after the established alpha threshold, keeps matching document colors at their nonzero indexes, and assigns novel colors to the lowest unused slots when the complete union fits 255 visible colors. Every leaf then indexes against that same plan; layers cannot receive conflicting palette overrides. If the union reaches 256 colors, the complete import retains the established default-palette quantizer and emits an explicit warning instead of mixing exact and quantized leaves. Generated pure boundaries plus an AIDraw custom-color PSD export → import → re-export preserve `#123456` and `#abcdef` exactly. This changes no PSD bytes/parser, canonical schema, hierarchy, layer composite/lock/crop semantics, alpha threshold/dither fallback, original PSD index identity, animation policy, external-producer or broad compatibility claim, packaged/native behavior, Level 3, RC, or stable-v1 claim.
- Restore supported pixel PSD structure into one current-frame sprite instead of creating one unrelated sprite per raster leaf. Decoder-authoritative folders, including empty folders, become canonical pixel groups in exact order; raster leaves retain supported visibility, normalized opacity, blend, binary lock-all, and signed decoded `left`/`top` placement before the unchanged indexed-palette quantization. AIDraw pixel PSD export → pixel import → re-export now preserves the generated layer/group tree and hidden raw leaf pixels, and a separate generated crop proves negative offsets without new origin metadata. Non-raster leaves remain omitted with an exact warning, partial PSD locks remain unlocked with the established warning, and a file with no decoded raster leaf keeps the ordinary editable fallback layer. This changes no PSD parser, canonical schema, palette/alpha/dither policy, record/depth/pixel ceiling, illustration import, composite-fallback policy, animation behavior, external-producer or broad PSD compatibility claim, packaged/native behavior, Level 3, RC, or stable-v1 claim.
- Replay strict editable-GIF patch decoding for the existing palette-planning and cel-writing passes instead of retaining every frame patch at once. The visitor exposes one fresh bounded RGBA patch at a time; production composites it, releases its temporary Canvas, and reparses/redecodes the retained source for the next pass. Even when exact palette planning becomes ineligible, remaining frames still receive strict LZW/palette validation without further compositing. The former patch array could reference up to 1,024,000,000 RGBA bytes under the unchanged 256-million expanded-pixel ceiling; the new production path references at most one 64 MiB frame patch at a time, alongside the already-required full-frame compositor and optional disposal restore. A deterministic two-frame replay control plus maintained animation regressions preserve order, fresh patch identity, timing, disposal, pixels, palette/fallback behavior, and source retention. This changes no byte/dimension/frame/expanded-pixel ceiling, canonical state, export, illustration/native GIF path, external-producer claim, exact peak-RSS evidence, packaged/native behavior, Level 3, RC, or stable-v1 claim.
- Reject malformed editable GIF pixels instead of accepting decoder-synthesized artwork. Pixel-mode GIF import keeps the pinned `gifuct-js` container parser but now materializes each patch through a bounded strict LZW and palette step that requires an end-of-information code, the exact declared pixel count, valid dictionary codes, an active color table, and in-range color and transparency indexes. Deterministic controls cover code-width growth and dictionary resets plus short, overlong, unterminated, invalid-code, missing-table, and out-of-range-index streams. A malformed editable GIF fails the complete import rather than padding pixels with palette index zero, substituting black, or falling back to a native first-frame composite. Valid timing, disposal, compositing, palette planning, source retention, and the illustration/native GIF path remain unchanged. This adds no partial-alpha policy, external-producer corpus, broad GIF compatibility, packaged/native behavior, Level 3, RC, or stable-v1 claim.
- Enforce the established 1,500,000-byte canonical inline-asset ceiling before retained foreign rasters can become import-created document assets. Top-level PNG/APNG/JPEG/WebP/GIF files, metadata sprite-sheet companions, and direct sliced sheets now fail before parser/native decode work when their exact source bytes cannot be persisted; the shared asset constructor repeats the check before SHA-256/base64 construction for rendered PDF pages, PSD raster fallbacks, and successfully decoded Tiled companions. Exact-boundary and production-path tests cover ordinary, animated, and sheet ingress. The complete import fails with a specific editable-asset error rather than silently resizing, recompressing, dropping the reproducibility source, or constructing a document that the later canonical gate must reject. Corrupt Tiled companion warning/omission remains unchanged. This adds no new byte ceiling, codec/MIME/geometry policy, Tiled bounds/origin decision, external-producer compatibility, packaged/native behavior, Level 3, RC, or stable-v1 claim.
- Admit decoded PSD output through the established canonical document contract before direct import returns. Both illustration and pixel-mode results now use the normal migration/normalized-pixel gate, and editable PSD strings share the canonical 100,000-code-unit ceiling plus exact lower-bound accounting against the unchanged 512 MiB imported-document envelope before text-object construction. Exact-boundary and hostile decoder-boundary tests accept one maximum-length text run, reject the next code unit, non-finite font size/opacity, and an overlong pixel-layer name, while maintained hierarchy/text/fallback interchange stays green. Out-of-model decoded content fails the complete import rather than being clamped, split, renamed, or partly omitted because those alternatives would invent recovery and fidelity policy. This changes no PSD bytes, parser/decoder authority, raster fallback, opacity/lock/text/hierarchy semantics, canonical or resource ceiling, external-producer claim, broad compatibility, packaged/native behavior, Level 3, RC, or stable-v1 claim.
- Admit PDF extracted text through the existing canonical illustration contract before direct import returns, and apply the established imported-document envelope before the utility worker transfers any import result. A text run above the canonical 100,000-code-unit limit or with out-of-model numeric/font geometry now fails with one PDF-specific integrity error; exact JSON-string accounting also rejects as soon as retained text alone proves the existing 512 MiB document envelope cannot fit. The completed page document is schema-validated, and the worker checks the same byte/node/depth/warning envelope that the main process independently repeats. Boundary tests use near-limit arithmetic rather than a ceiling-sized allocation and prove canonical success, overlong/non-finite failure, cleanup-error precedence, cycle/depth rejection, and unchanged maintained PDF interchange. This deliberately does not clamp, split, or silently omit extracted text; it changes no PDF bytes, raster fallback, searchable-text representation, parser/recovery policy, canonical ceiling, utility trust authority, broad compatibility, packaged/native behavior, Level 3, RC, or stable-v1 claim.
- Bound sequential PDF page import resources without changing pdf.js recovery or interchange semantics. The existing all-page geometry pass still enforces page-count, per-page, and 64-megapixel aggregate limits before any render. After that preflight, each page Canvas is reset to 1×1 immediately after PNG encoding, each processed page proxy is cleaned before the next render, and any preflight-only proxies are swept before the loading task is destroyed. A primary load/page/render/text error remains authoritative over cleanup failures; cleanup-only failure still fails closed. Generated one- and three-page regressions prove successful order, Canvas release, preflight cleanup, and error precedence. This changes no PDF bytes, pixels, page naming, extracted text, canonical state, parser allowlist, resource ceiling, broad compatibility, packaged/native behavior, Level 3, RC, or stable-v1 claim.
- Bound retained pixel-animation export work without changing animation fidelity. Pixel APNG now preflights scaled frame width × height × selected sequence length against the existing 64-megapixel expanded-frame posture before rendering any frame. Exact GIF first probes the complete selected sequence one frame at a time, then recomputes and writes one eligible frame at a time; an ineligible frame still sends the whole animation through the established sequential quantizer. The APNG limit is warranted because its encoder requires all raw RGBA frames in memory. GIF receives no new aggregate-frame rejection because its uncompressed intermediates are sequential and the encoded artifact is necessarily retained; adding a broader output limit remains a separate product policy. Boundary arithmetic, a production pre-render APNG rejection, unchanged exact/fallback GIF regressions, and a 16-frame/16-million-pixel production tripwire cover the contract. This changes no schema, coordinates, frames, tags, timing, loop/disposal, palette, pixels, encoder choice, GIF eligibility, import limit, output dimensions, or compatibility claim and adds no disk streaming, external-producer, packaged/native, Level 3, RC, or stable-v1 evidence.
- Preserve exact color in GIF when an ordinary multi-layer normal composite is already representable without quantization. The exporter first retains the established canonical-index path for an opaque ordinary single layer; another eligible frame shares the exact normal compositor, requires every final pixel to have binary alpha and at most 255 visible RGB colors, and assigns local indexes deterministically in first-visible raster order. If any frame has partial final alpha, palette overflow, fractional effective opacity, a non-normal blend, or ambiguous multi-layer transparent RGB, the complete animation retains the established rendered quantizer instead of mixing fidelity modes. A generated two-frame production round trip preserves exact `[127,0,128,255]`, red, and transparent pixels plus local-table order and timing. This changes no canonical state, layer order, frame palette, tag/delay/loop/disposal, dimensions, imported single-layer index contract, or GIF transparency model and adds no partial-alpha GIF, arbitrary-composite, external-producer, packaged, broad-format, or release-readiness claim.
- Preserve partial-alpha color through ordinary multi-layer pixel PNG, APNG, and sprite-sheet output without a premultiplied Canvas readback. The export path shares canonical visible-layer order, accepts only visible pixel leaves at full effective opacity with normal blend, visits their stored chunks, and performs deterministic unpremultiplied eight-bit source-over into one output plane. Nested normal groups are supported; one visible leaf retains its existing raw transparent RGB. At this checkpoint fractional opacity, non-normal blends, and multi-layer use of a fully transparent nonzero palette color retained the established Canvas compositor; the newer entry above supersedes only fractional normal opacity. GIF kept its narrower opaque single-layer direct-index contract. This changes no canonical state, layer order, palette/frame override, sheet geometry/metadata, animation timing, file schema, or color-profile policy and adds no arbitrary-composite, partial-alpha GIF, packaged, broad-format, or release-readiness claim.
- Bound the interactive sprite/tilemap checker and freeform grid lasso without changing canonical cells. The transparency checker now keeps its global phase but plans only the visible viewport plus 64 pixels of shadow overscan, clips partial edge cells, and applies the existing 24-pixel blur/8-pixel offset once instead of shadowing every checker square across the complete scaled surface. Lasso pointer samples remove only provably same-direction collinear middle points, retain at most 4,096 exact path vertices, and rasterize once on release under the existing one-million candidate-cell ceiling plus a four-million boundary-step ceiling. Any breach cancels the gesture and leaves the prior selection unchanged. This changes no stored coordinates, selection combination, map bounds/origin, interchange bytes, headless raster output, or Tiled claim, and adds no packaged pointer/frame-pacing or release-readiness evidence.
- Require every completed editable SVG import to pass the established canonical illustration validator before it can reach a caller. Standard Canvas-recognized CSS colors up to 256 characters—including hex shorthand/alpha, named and functional colors, `currentColor`, and `transparent`—are reduced to canonical lowercase sRGB hex; invalid colors warn and become black. One-stop gradients duplicate their exact color at both endpoints, reverse-direction lines retain their appearance through positive dimensions plus an affine sign, and overlong object/font-family names are warning-truncated to the existing 200-character boundary. Out-of-model transforms, geometry, strokes, blur, gradient-stop counts, or text now reject the complete import with one exact error, including production file import, instead of surviving direct import until a later generic utility rejection. This does not add a new schema, broad CSS/SVG grammar, color-profile promise, recovery heuristic, external corpus, packaged behavior, or release-readiness claim.
- Reject SVG transform arithmetic that cannot remain finite in the canonical illustration model. Non-finite numeric tokens, overflow while composing transform lists or nested visual paths, non-finite matrix decomposition, positioned nested/use overflow, mask traversal, and referenced gradient-coordinate overflow now fail the complete editable import with one exact error instead of returning `Infinity`/`NaN` transforms or paint geometry. Validation precedes recursive child construction on groups, nested SVG, use, and referenced symbols; transforms on ignored metadata or unsupported elements remain ignored. Exact canonical-scale-boundary local and nested compositions stay accepted without flattening stored transforms. This does not alter the existing canonical magnitude policy, define malformed-transform grammar, add CSS transforms, prove an external corpus or packaged behavior, claim broad SVG compatibility, or establish release readiness.
- Apply nested SVG viewport geometry instead of leaving nested content at raw source size. Unitless, `px`, and percentage x/y/width/height now establish nested `<svg>` and `<use>`-instantiated `<symbol>` viewports; each valid `viewBox` reuses the standard meet/slice/none matrix while child coordinates remain editable. Unreferenced symbols stay definition-only, zero/invalid dimensions or non-finite viewport matrices fail before their children enter the document, and unsupported nested overflow clipping produces an explicit warning rather than silently discarding editable content. This does not add CSS `calc()`/font/physical length resolution, symbol `refX`/`refY`, external references, nested overflow clipping, external-corpus evidence, packaged behavior, broad SVG compatibility, or release readiness.
- Apply the outer SVG `viewBox` as a real viewport transform on editable import. Default `xMidYMid meet`, explicit nine-way meet/slice alignment, and `preserveAspectRatio="none"` now scale and place canonical content inside the integer AIDraw artboard instead of reducing every viewBox to a min-X/min-Y translation; malformed alignment warns and uses the standard default. Interactive and headless Canvas traversal also draw transform-only groups directly under their affine transform, preventing the former artboard-sized pre-transform buffer from clipping source content that scales or translates into view. Groups with opacity, blend, filters, masks, blur, or shadow remain isolated to preserve one composite effect. This outer-root checkpoint did not establish nested viewport sizing or clipping, external corpora, packaged behavior, broad SVG compatibility, or release readiness.
- Preserve canonical image-crop controls across AIDraw SVG export and re-import without changing standard SVG rendering. Cropped images still use an ordinary clip rectangle plus positioned/scaled embedded image; a strict version-1 AIDraw `data-*` envelope carries the finite bounded display, source, and crop rectangles. Re-import collapses that wrapper back to one cropped image object only when every child and computed geometry still matches. Invalid metadata warns and the existing generic editable group/image/mask import remains, so private data cannot override changed visible SVG. This adds no schema, crop inference for foreign SVG, external-editor metadata-retention promise, packaged evidence, broad SVG compatibility, or release readiness.
- Correct centered and right-aligned illustration text placement in SVG export and preserve its editable AIDraw text box on re-import. Standard `text-anchor` now uses the canonical local box midpoint or right edge instead of anchoring both at zero, and all text ranges continue from the parent text position as one anchored chunk. A strict version-1 AIDraw `data-*` envelope carries finite bounded width, height, and line height; malformed, partial, or unknown-version metadata is ignored with an explicit warning. This changes no canonical schema and does not promise SVG wrapping, justified layout, font embedding/shaping, external-editor retention of AIDraw metadata, packaged behavior, broad SVG compatibility, or release readiness.
- Preserve authored paint-layer appearance in SVG raster fallbacks. Visible nonempty paint layers now reuse the non-mutating layer-source renderer, so their supported adjustment filters and vector layer masks are baked into the embedded transparent PNG while root opacity and blend remain on the SVG group exactly once. Hidden, unreachable, or empty paint layers emit no fallback or fidelity reason; temporary native surfaces are released after encoding. The export report names each embedded paint layer with the existing closed `raster-fallback` code. Editable vector structure, group/vector filter and mask warnings, canonical state, and interchange schemas remain unchanged; this does not encode those effects as SVG primitives or prove viewer blend behavior, external-producer fidelity, broad SVG compatibility, packaged behavior, or release readiness.
- Add bounded machine-readable fidelity reasons to durable interchange reports without replacing their established warning and rasterized-name arrays. A closed six-code contract now identifies the exact document, layer, or object responsible for PDF raster fallback, font substitution, retained or omitted searchable text, PSD raster fallback/flattening, and omitted later PSD animation frames. Human, approved-agent, and batch exports retain those entries through the supervised utility lane, restart-persistent version-one store, activity inspector, and exported report JSON; older stored reports load with an empty structured list. Producers cap the additive list at 4,096 entries; an exact finalizer keeps the largest reason prefix that fits with a truncation warning in the complete 1 MiB worker-report envelope, or omits the optional field when even that warning cannot fit beside an already-valid legacy report. This is initial PDF/PSD adapter instrumentation, not comprehensive loss accounting, imported-object reason coverage, packaged workflow evidence, redaction, broad format compatibility, or release readiness.
- Serialize illustration PSD fallback rendering without deep-cloning the document per layer. A dedicated renderer path neutralizes only the requested layer's visibility, opacity, and blend at draw time while preserving masks, filters, child composites, source ordering, and canonical state; export awaits each fallback in canonical order and releases every temporary fallback Canvas plus the final composite Canvas immediately after copying retained image data. A hidden 25%-opacity multiply layer now proves full-alpha source pixels plus separate metadata and an unchanged input document. This is peak-work containment, not a change to PSD pixels, layer semantics, or compatibility scope.
- Bound PSD output structure before writer recursion or layer rendering. Export now walks the exact record tree it will emit—including group records plus generated raster-fallback and AIDraw editable-text companion children—and rejects above the shared import/export ceilings of 2,048 records or depth 64. The walk is cycle-aware and stops at the first overflow; broad empty pixel groups, a depth-65 chain, and a companion-expanded illustration prove production failure before Canvas or writer work. These are AIDraw resource limits, not Photoshop maxima or broader PSD compatibility evidence.
- Bound PSD export's retained full-canvas layer rasters before allocation. A recursive preflight counts the actual illustration fallbacks or pixel leaves that the writer will retain and rejects when their combined area exceeds 64 megapixels; structural groups and metadata-only editable-text companions do not count, while the final composite remains separately subject to the established 64-megapixel static-raster limit. Exact arithmetic boundaries and production 8192×8192 pixel/illustration rejections prove the check runs before a large layer canvas is created. This changes no document coordinates, PSD layer semantics, import behavior, external-producer fidelity, or broad/lossless compatibility claim.
- Preserve the canonical pixel layer tree in PSD export and stop baking layer composite metadata into its raster twice. The current frame now emits root/nested/empty groups and pixel children in exact order with visibility, normalized opacity, blend, and lock on their PSD records. Each pixel child is rasterized from a non-mutating visible/normal/1 view, so a hidden 25%-opacity layer still carries its full-alpha source pixel beside separate hidden/0.25 metadata instead of exporting blank or compounding opacity on display. Later animation frames remain explicitly omitted. This export checkpoint itself changed no import behavior; the later pixel PSD hierarchy entry above owns the now-supported one-sprite re-import mapping. No animation, external Photoshop visual, or broad/lossless PSD claim is added.
- Preserve PSD lock-all and the editable companion's canonical editing properties. Supported illustration groups/layers and exported pixel layers now encode AIDraw's binary lock with the pinned adapter's documented PSD lock-all fields; illustration import maps only that full lock back to AIDraw. Generated partial transparency-lock evidence stays unlocked and reports the unsupported partial mode instead of silently becoming a broader lock. The private exact-suffix text companion also returns its original object name, normalized opacity, blend, and lock to the canonical text object while its still-hidden synthetic wrapper resets to unlocked normal/1. The visible raster fallback remains authoritative. This checkpoint itself changed no pixel-import mapping; the later hierarchy entry applies the same lock-all/partial-lock policy to supported pixel layers. This adds no partial-lock model, unmarked-text reinterpretation, external Photoshop visual proof, or broad/lossless PSD claim.
- Preserve empty PSD layer groups instead of reclassifying them as vector leaves. The pinned decoder represents a folder with a `children` array even when it has zero children, so import and pixel-flatten planning now use array presence as group authority rather than a nonzero child count. Generated AIDraw export→decode→import retains an empty group's type, root order, 0.2 opacity, and difference blend alongside the populated hierarchy. Empty groups still count toward existing PSD layer/nesting limits and produce no independent pixel-mode sprite. No name heuristic, schema change, new group semantics, external-producer visual claim, or broad PSD fidelity claim is added.
- Correct the PSD adapter's opacity units. Pinned `ag-psd` reads and writes normalized `0…1` layer opacity, so AIDraw now passes canonical opacity directly on illustration groups/layers, hidden text companions, and pixel layers, and imports decoded illustration opacity without dividing it again by 255. Generated nested 0.8/0.6/0.4 values survive export→decode→AIDraw import, while a 0.25 pixel layer is stored at PSD's 8-bit 64/255 value. No dual-scale heuristic is accepted: older AIDraw PSDs whose former export already clamped every nonzero value to full opacity cannot recover their lost intended fractions. Pixel-mode PSD import semantics, blend mapping, raster fallbacks, external-producer visual fidelity, and broad/lossless PSD claims remain unchanged.
- Repair AIDraw's own PSD editable-text companion round trip. Exported nonempty companions now carry the complete object affine matrix and line leading alongside their styled text box; re-import recognizes only the established AIDraw ` · editable text` convention, restores matrix-equivalent geometry and box dimensions, and keeps the text object visible inside its still-hidden companion layer so revealing that layer takes one visibility action. Empty text remains safely fallback-only instead of aborting export, and older translation-only AIDraw companions recover their stored box and position. The visible raster fallback remains authoritative, PSD tracking stays integer-quantized, and unmarked/external text, font shaping, effects, container bytes, and broad or lossless PSD fidelity are not newly claimed.
- Bound transient sprite/tilemap overlay drawing to the outward-rounded visible raster region. Flood/replace previews, reusable stamp previews, point selections, and pixel/tile replay cells now discard projected cell bounds that cannot reach the viewport; compact horizontal preview/replay runs are clipped algebraically before Canvas work instead of expanding or drawing their offscreen cells. Isometric runs use a conservative diamond-bounds range, selection adds scale-aware stroke padding, and order/colors/dashes/operation data remain unchanged. A 1,048,576-cell/65,536-run production filter passes in 2.06 ms under 100 ms. Point selections still scan their canonical array once, and this adds no spatial index, operation/schema change, packaged frame-pacing evidence, or RC claim.
- Bound targeted illustration observation for the exact translation-stable subset instead of always allocating the complete artboard. Visible integer-positioned, integer-sized solid rectangles inside otherwise empty identity structural groups/layers render into a four-pixel-expanded requested backing and raw-crop byte-exactly; a one-pixel request at the far edge of an 8,192×8,192 artboard now avoids the full 64-megapixel surface. Curves, paths, text, images, gradients, strokes, rounded/fractional/transformed geometry, masks, paint content/cache, opacity/blend isolation, and effects retain the established full-artboard render plus raw crop. A new 100 ms production tripwire passes locally at 0.29 ms. This changes no canonical model, artboard, observation/output budget, save/export, interchange, or generic regional-rendering promise and adds no packaged/native, Level 3, RC, or stable-v1 evidence.
- Make shared illustration path booleans honor the canonical fill rule of both inputs and retain Paper.js's result rule. Even-odd compound holes now survive union/subtract/intersect/exclude instead of being interpreted as solid nonzero paths; a result with no filled geometry fails before the UI or MCP can submit the delete/add transaction, so the selected inputs remain. The established first-input layer, composite, fill, and stroke policy is unchanged, with result styles copied independently. Exact four-mode geometry tests and a maintained transparent 64×64 ring-and-island raw-RGBA golden (`e0309ac1…6083`) cover the bounded contract. This does not add compound-hole node editing, multi-subpath authoring, utility isolation, packaged interaction, third-party interchange parity, or release readiness.
- Honor the complete canonical shape/path stroke style in both interactive and headless Canvas rendering. Width, opacity, cap, join, dash, and an explicit miter limit now share one applicator; opacity multiplies the existing composite alpha, and zero-width paths no longer draw with an ambient Canvas width. A maintained 80×48 raw-RGBA golden (`28318fc1…b284`) covers styled paths and shapes. Canonical values and SVG/PDF vector serialization are unchanged; native previews and raster outputs intentionally reflect the corrected style. Custom miter authoring, masks/effects, packaged antialiasing/visual acceptance, broad interchange parity, and release readiness remain unclaimed.
- Make supported illustration click geometry authoritative instead of reselecting every exact miss through expanded world bounds. Filled paths now honor even-odd holes, ellipse/concave empty corners reject, the existing scale-adjusted edge affordance expands each side of the actual boundary or configured stroke, and arrowheads use the same three rendered segments. Topmost order, Alt cycling, locked/hidden exclusion, and deliberate bounds fallback for groups, malformed paths, non-invertible transforms, paintless objects, or unavailable Canvas context remain. A 5,000-object production hit-test takes 19.7 ms under a new 500 ms tripwire. This does not redefine masks/effects/group-child silhouettes, exact skewed screen-distance, packaged pointer behavior, or release readiness.
- Add a bounded square-tile transform chooser for map painting. Eight 32×32 thumbnails share the exact selected-tile crop and Tiled diagonal-first H/V matrix already used by rendering. Permission gating now interprets the stored flip and 90°-rotation capabilities geometrically: rotation alone permits four rotations, either reflection plus rotation generates all eight square symmetries, and a stale disallowed state falls back to identity for new paint instead of changing into a different transform. Existing painted GIDs, canonical schema, and interchange bytes are unchanged. Rectangular diagonal preview/fitting, packaged interaction, isometric-specific visuals, broader Tiled fidelity, and release readiness remain unclaimed.
- Replace illustration lasso bounds guesses with deterministic canonical geometry evaluated at gesture completion. Filled shapes/paths use transformed Paper.js silhouettes with fill-rule holes, vector strokes use their `perfect-freehand` outline, text/images use their transformed object frame, and stroke-only geometry uses exact path centerlines; unsupported groups or malformed paths retain the old bounds fallback. Existing selection combination and containment modes are unchanged. A new formal tripwire selects 5,000 vector objects in 187.19 ms under a 1,000 ms budget. This does not change canonical state, object storage, clicking, transforms, masks, effects, outer stroke-width semantics, or claim packaged pointer acceptance or release readiness.
- Add bounded document-owned custom brush-library interchange. The brush toolbar can copy or load strict `{ format: "aidraw-brush-library", version: 1 }` JSON, preview it without mutation, then add fresh-ID/case-insensitively renamed copies by default or explicitly replace only the custom preset array in one transaction. The parser reuses the canonical complete dab-recipe schema, reserves built-in IDs, and caps input at 1 MiB/256 presets. Existing strokes retain their embedded recipes and seeds; the committed imported preset becomes active with its exact size/opacity. This does not add built-in/stroke/tip-image transfer, folders/tags, application-global libraries, third-party brush compatibility, native file authority, packaged acceptance, or release-readiness evidence.
- Preserve exact partial-alpha palette channels in ordinary PNG and sprite-sheet output for the guarded single-pixel-layer indexed shape. Static first-frame PNG and every sprite-sheet frame now assemble from canonical indexes/frame palettes, apply integer nearest-neighbor scaling in raw RGBA, and encode losslessly with palette rewriting disabled; this also avoids the maintained encoder's tiny-image PLTE allocation defect. If the layer shape is nontrivial, export retains the established Canvas compositor. A generated two-frame APNG import proves exact alpha-128/192 RGB through 2×1 PNG and 4×1 sheet decoding plus unchanged sheet metadata. No complex-composite, byte-identical PNG, color-profile, external-producer, packaged, or broad interchange claim is added.
- Re-export ordinary opaque single-layer indexed animations to GIF without requantizing their canonical frame palettes. The exporter now scales exact index planes directly and writes the base palette as the GIF global table plus later frame palettes as local tables, preserving generated imported-GIF colors through production import → export → re-import. Eligibility fails closed when a used color has partial alpha or the sprite has nontrivial layer composition; those cases retain the existing rendered RGBA quantizer. Exact pixels and palettes are claimed, not byte-identical GIF containers, transparent hidden RGB, complex-composite/partial-alpha fidelity, external producers, packaged behavior, or release readiness.
- Preserve bounded frame-local colors when importing pixel GIF/APNG animations. When every composited frame contains at most 255 visible RGBA colors after the established alpha threshold, matching document colors keep their indexes, novel colors receive deterministic free slots, and later frames use ID-aligned palette overrides instead of all frames being quantized to the default palette. If any frame exceeds that limit, the complete animation retains the prior quantizer and now warns explicitly; original bytes remain embedded in both cases. A narrowly gated raw-indexed APNG export path preserves stored partial-alpha channels for the simple single-pixel-layer shape produced by import, while complex composites retain the established Canvas renderer. Generated two-frame APNG/GIF controls and a 256-color APNG boundary cover only those contracts; no broad producer, color-management, complex-composite, GIF re-export, packaged, or release-readiness claim is added.
- Add explicit numeric source-pixel cropping for illustration images. The inspector now exposes X, Y, width, and height drafts with one Apply action; finite in-bounds fractional coordinates preserve the current displayed source-pixel scale and shift the transformed object origin so the selected source top-left remains at the same world position through scale, skew, and rotation. Entering the full source rectangle clears the crop. The change uses the existing revision-checked object replacement and does not rewrite image bytes, relink assets, add format fields, or redefine canvas drag cropping. Explicit transform reset remains unimplemented because identity-at-origin, position-preserving reset, and geometry baking are materially different product choices.
- Fail closed on APNG chunk types whose semantics cannot be safely ignored. The editable-frame parser now requires four ASCII letters, rejects the reserved lowercase third type bit, and rejects any unknown uppercase critical chunk before pixel conversion. Well-formed unknown ancillary chunks remain ignored by decoding and retained byte-for-byte in the embedded source. Exact generated variants prove one private ancillary control remains visually/import equivalent while unknown critical, reserved-bit, and non-letter type codes reject. Static PNG routing, known APNG/PNG chunks, resource limits, canonical state, export, and broad compatibility claims remain unchanged.
- Distinguish APNG's two standard default-image layouts explicitly. When `fcTL` precedes the PNG `IDAT` stream, that first animation frame must cover the full canvas and uses only consecutive `IDAT` data. When `IDAT` precedes the first `fcTL`, the separate fallback image is inflate/filter-validated but is not composited into the animation; the first editable frame may be partial and must use `fdAT`. Cross-mode `IDAT`/`fdAT`, interrupted `IDAT`, partial included-default controls, and malformed fallback rows now reject before document creation. A generated 2×1 fixture proves a two-`IDAT` fallback remains outside a 1×1 offset animation frame while timing and exact source retention survive. Existing resource limits, canvas origin, frame model, export, and broad compatibility claims remain unchanged.
- Harden the bounded editable-frame APNG decoder's palette and transparency contract. `PLTE` and `tRNS` chunks must now be unique, correctly ordered, legal for the PNG color type and bit depth, and internally consistent; indexed samples outside the declared palette reject instead of becoming synthetic black/opaque pixels. Sixteen-bit grayscale and truecolor transparency keys are compared at full sample precision before the established high-byte display conversion, so two output-identical colors can still differ correctly by their low byte. Generated 2×1 grayscale/truecolor fixtures and exact malformed variants cover this behavior. Existing resource limits, 8-bit conversion, canonical sprite/schema state, export, source retention, and static-PNG fallback remain unchanged; this adds no external-producer, color-management, packaged, or broad APNG compatibility claim.
- Import bounded Adam7-interlaced APNG frames into editable pixel sprites instead of rejecting them before color conversion. The existing decoder now computes the exact seven-pass inflated byte count, unfilters every pass independently, and reconstructs the same packed rows used by its established RGBA/indexed conversion. It also validates each chunk CRC, the monotonic animation sequence, declared frame count, and terminal empty IEND; row-layout, CRC, sequence, count, missing-end, and trailing-data contradictions fail before document creation. Spec-derived 8×8 fixtures prove exact two-frame RGBA partial-rectangle pixels, timing, blend/disposal metadata, source retention, and packed one-bit indexed transparency. Existing 8,192-side, 16MP canvas, 10,000-frame, and 256-million-frame-pixel limits remain unchanged. This adds no codec dependency, canonical/schema change, external-producer fixture, packaged evidence, or broad APNG compatibility claim.
- Bound visible map-object path rendering to the requested headless region or interactive viewport. One shared affine-bounds contract expands canonical rectangle/ellipse/polygon/polyline bounds for the explicit Canvas miter limit plus selected point/resize handles, transforms all four corners through the exact orthogonal or isometric object matrix, and filters while preserving object order. Editor bounds include fractional per-layer parallax and use gesture-preview geometry; headless bounds use canonical pan zero. A production regional regression proves a distant object's path is never rendered, and 100,000 objects filter to one candidate in 35.98 ms under 500 ms. Canonical objects, z-order, styling, selection/hit testing, Tiled bytes, and map bounds/origin remain unchanged; this is linear metadata culling rather than a spatial index, and packaged drag/visual/frame-pacing acceptance plus RC readiness remain open.
- Bound editor sprite, onion-skin, and wrap-preview payload traversal to the visible viewport. The editor now routes all three through the shared layered sprite-region compositor after converting its CSS view to an outward-rounded canonical pixel region; wrap copies shift that source region by the exact sprite offset. New compositor hooks preserve cumulative layer opacity/blend, linked cels, frame palette behavior, live palette cycling or onion tint, and replay masks without restoring the old full-cel loop. A production tripwire composites one pixel from 4,096 stored cel chunks in 0.31 ms under 100 ms. This remains an O(total stored chunks) key-count decision, not a spatial index. Checker-pattern traversal is deliberately unchanged because its individually shadowed cells make a finite offscreen cutoff visually ambiguous across Canvas implementations; packaged frame pacing, broad visual acceptance, and RC readiness remain open.
- Bound editor tilemap payload and orthogonal-grid traversal to the visible viewport. Fractional layout, wheel pan, zoom, and per-layer parallax are converted back through the editor's uniform map projection and rounded outward to the smallest covering canonical-pixel region. Both orientations feed only conservatively intersecting chunks into their established decode/order loops; the orthogonal grid emits viewport-adjacent line indexes rather than iterating the full nominal dimensions. This preserves Canvas clipping, signed storage, record/right-down order, GIDs/transforms, map origin, and Tiled bytes. It remains an O(total chunks) metadata scan, not a spatial index; checker-pattern cells, objects, selections/replay overlays, packaged frame pacing, and RC readiness remain open.
- Make bounded tilemap observation avoid decoding and scanning every stored cell. Before decoding a 32×32 GID payload, the headless renderer now intersects that chunk's conservative projected cell envelope with the requested backing region. The envelope covers orthogonal and isometric projection and pads by the larger tile axis so any rectangular cell transformed by stored Tiled flags remains a candidate. Retained chunks preserve record order before the established isometric right-down iterator applies its own deterministic ordering. A production far-edge observation proves a nonintersecting chunk is never decoded, and the formal gate renders one pixel from 4,096 stored chunks in 1.46 ms under a new 100 ms tripwire. This is payload/cell traversal pruning, not a spatial index: every stored chunk's geometry is still inspected once. Visible pixels, nominal bounds, signed chunk storage, GIDs/transforms, map origin, Tiled bytes, and infinite-map policy are unchanged; object-layer traversal, interactive viewport performance, packaged behavior, and RC readiness remain open.
- Bound nominal tilemap raster work before native canvas allocation. `canvas_observe` now renders only the requested nonnegative region of an orthogonal or isometric map, including canonical object overlays, instead of first constructing the complete nominal raster; a four-pixel allocation-aware backing margin plus a raw crop preserves current full-raster antialiasing at ordinary bounds. Observation outputs retain the 4,194,304-pixel budget and now return a distinct structured per-side rejection above 65,535 pixels. Whole-map rendering and raster-backed PNG/JPEG/WebP/SVG/PDF export reject dimensions above 65,535 per side or 64 megapixels before rendering. Valid oversized maps remain saveable as `.aidraw`: because `preview.png` is noncanonical, the writer uses its established transparent 1×1 fallback when a full nominal preview is unsafe rather than blocking the document save. Static export is rejected rather than silently downsampled. Nominal bounds, nonnegative observation coordinates, canonical chunks/GIDs, map origin, Tiled bytes, and the unresolved infinite-map expansion policy do not change; adaptive checkpoint/clipboard/generation previews, packaged behavior, broad Tiled fidelity, and RC readiness remain open.
- Bound placed-tile source compositing independently of the nominal source-sheet size. Interactive and headless map renderers now share one DOM-neutral layered sprite-region compositor, render only the smallest integer pixel envelope intersecting the addressed tile crop, and retain the original fractional source rectangle for exact nearest-neighbor sampling before the stored GID transform. A per-render 64 MiB / 1,024-entry disposable LRU reuses repeated crops and immediately releases transient oversize crops and all retained canvases after rendering. A production regression addresses the final 16×16 tile in a sparse 8,192×8,192 sprite without allocating that full sheet, while fractional edge crops remain byte-equal to the former full-source path. The formal 65,536-tile gate now renders real addressed cells from an 8,192×8,192 sparse sheet instead of unresolved fallback colors. Nominal sprite bounds remain the clipping contract; canonical storage, animation timing, GIDs/transforms, and Tiled bytes are unchanged. A maximum-size tile can still require a maximum-size transient crop, and packaged pacing, broad Tiled fidelity, infinite-map expansion, and RC readiness remain open.
- Render authored per-tile animations on placed map cells instead of leaving them on the static base tile. One exact integer-millisecond sampler now selects the local frame tile for both interactive and headless renderers before the stored GID transform is applied. The editor gives every placed tile one session-local phase, schedules no faster than 16 ms, and freezes on the first authored frame when reduced motion is requested; ordinary PNG, headless observation, and other static raster exports deliberately use canonical time zero for deterministic output. Exact boundary/loop tests cover unsafe aggregate duration sums, and a production 2×2 fixture proves first/second-frame crops, horizontal-flip retention, and time-zero PNG parity. Canonical tileset metadata, map GIDs, and Tiled bytes are unchanged. Time-addressable MCP observation, animated tilemap GIF/APNG export, worst-case packaged frame pacing, broad Tiled visual equivalence, and RC readiness remain open.
- Preserve rectangular orthogonal map cells at their authored aspect across editor and headless surfaces. One shared projection contract now defines finite extent, cell rectangles, continuous pointer/parallax inversion, and map-object scaling. The editor derives row height from `tileHeight / tileWidth` instead of forcing every cell square, so artwork, object overlays and hit testing, grid lines, selection/lasso/stamp/tool/replay feedback, CSS-scaled pointer input, and headless/PNG dimensions agree. Exact 3×5 geometry, fractional pointer/object coordinates, source wiring, and a byte-exact 3×5 production tile cover the correction. Canonical storage and Tiled bytes are unchanged; native-size tall-tile overhang, explicit tileset offsets/alignment, non-square diagonal-transform policy, packaged visuals, and RC readiness remain open.
- Preserve visible map-object layers across editor, headless observation, and raster export. `renderTilemap` no longer skips every object layer: rectangles, ellipses, polygons, and polylines now follow canonical nested z-order, visibility, and multiplied layer opacity at pan zero, using the same deterministic overlay path/style implementation as the editor and the shared isometric projection matrix when needed. Exact regressions prove an object above a tile changes the raster, the same object below or hidden does not, object-only rendering works, exported PNG pixels match headless output, and a 1×1 8×4 isometric object respects aspect and 50% layer opacity. Selection handles remain editor-only advisory state; parallax has no displacement at canonical pan zero. Tiled metadata/output bytes are unchanged, and this does not claim Tiled-native object styling, packaged drag/visual acceptance, or RC readiness.
- Correct shared isometric projection geometry without changing map storage or Tiled bytes. Finite headless renders now anchor the leftmost and rightmost cells inside the declared canvas instead of shifting every tile half a cell and clipping an edge. The editor fits rectangular maps by their projected `(columns + rows)` extent, derives vertical scale from the authored tile-width/tile-height ratio, and uses the same cell rectangles for tile artwork, object layers, pointer inversion, parallax correction, stamp/tool/replay previews, lasso paths, and selection outlines. Exact asymmetric 3×5 geometry plus a one-cell 4×2 tile prove bounds, inverse coordinates, and edge-to-edge pixels; the maintained isometric raw-RGBA golden changes to `fb2f38d…d268`. This advances MAP-08 projection parity but does not implement native-size tall-tile overhang/offset, define non-square diagonal-transform geometry, change infinite-map viewport bounds, or claim packaged/Tiled/RC acceptance.
- Make isometric sparse-tile compositing independent of chunk insertion order. Interactive and headless renderers now share one lazy right-down iterator matching AIDraw's existing Tiled export convention: chunk bands sort by Y, chunks within a band sort by X, and each band streams complete rows left-to-right before the next row. An exact reverse-inserted 31/32 chunk-boundary fixture proves the lower overlapping cell remains in front, while the ordinary rendering golden corpus stays unchanged. The iterator decodes only one chunk band at a time instead of materializing every sparse cell, and orthogonal maps retain their established streaming loop. This advances MAP-08 determinism but does not add native-size tall-tile overhang/offset geometry, change map storage or Tiled files, establish non-square transform policy, or claim packaged isometric visuals/RC readiness.
- Add bounded reusable stamp-library JSON transfer for pixel sprites and tilemaps. The editor copies the current document-owned library and can load or paste a strict version-1 bundle, preview the exact result without mutation, then either add fresh-ID/unique-name copies or explicitly replace the library in one transaction. Pixel import matches referenced colors exactly by value, appends only missing colors, and fails when the 256-color palette cannot fit them; it never approximates colors. Tile import preserves exact transformed 32-bit GIDs only when the active map has the exported tileset name/identity/revision/GID range/geometry and source-sprite identity/revision/size; it fails closed instead of silently rebasing GIDs, and does not bundle tileset pixels. JSON is limited to 16 MiB and 262,144 aggregate cells while existing 1,024-stamp and 65,536-cell-per-stamp limits remain. This closes PIX-06's bounded source-level library interchange gap and adds an exact-compatible subset of MAP-11; arbitrary tile asset bundling/rebasing, packaged pointer/file/clipboard acceptance, broad interchange, and RC readiness remain open.
- Add bounded session-local onion-skin controls beside the existing on/off preview. Previous and next sides independently select zero through four frames, tint, and 0–100% base opacity; defaults preserve the former one-neighbor teal/pink preview, and Reset restores them. One validated planner returns at most eight layers in global far-to-near order and attenuates each side as base opacity divided by distance. Opening settings does not silently enable Onion, the settings and cel-exposure overlays close each other, and no document transaction, save/export field, or MCP mutation contract is added. Persisting visual-aid preferences, unbounded frame fan-out, maximum-size multi-frame pacing, and packaged visual/pointer acceptance remain open, so PIX-10 stays Working and no RC claim is added.
- Advance PIX-10 with a bounded cel-exposure editor and correct selected-layer routing. The sprite timeline opens a paged layer×frame grid with at most twelve frame columns and six pixel-layer rows; every cell identifies blank, painted, linked, or missing state, and layer/frame headers select the exact exposure. One selected exposure can link to the previous resolved cel (clearing its own chunks) or unlink by snapshotting the currently resolved chunks through one revision-checked asset replacement. Selecting a visible unlocked pixel layer now directs drawing, selection transforms, fill, text, and paste to that layer; a selected locked or hidden layer fails closed instead of silently modifying another topmost layer. Existing whole-frame link/unlink remains available. Unbounded DOM grids, implicit destructive relinking, edits through locks/visibility, and a new cel operation language were rejected. The bounded session-local onion follow-up above closes that adjacent source gap; packaged grid/settings/replay/pointer acceptance remains open, so PIX-10 stays Working and no RC claim is added.
- Complete MAP-04's unambiguous source/editor multi-selection gap. Tileset collision rows now expose checkboxes plus Select all, Clear, and one revision-checked bulk delete; Shift-click adds or removes shapes on the canvas, and dragging any selected shape moves the exact selection together under one tileset human lock and one asset replacement. Group movement applies one rounded integer delta without changing order, local polygon/polyline points, dimensions, types, or typed properties. Point handles, bounded-shape resize, and property editing remain single-selection actions because group resize/property-merge semantics are not established. The first-frame collision preview now reuses the bounded layered crop sampler and never exceeds 240×200 CSS pixels instead of iterating or allocating by a potentially 8,192² tile. Marquee selection, group scaling, format changes, and packaged pointer/Tiled-fixture acceptance remain outside this checkpoint, so MAP-04 stays Working and no RC claim is added.
- Complete MAP-10's unambiguous source/editor convenience surface. Named weighted tile groups now show up to twelve clickable first-frame crop thumbnails with exact tile IDs and probabilities; each thumbnail and the existing animation preview is sampled into a 32×32/40×40 bound instead of allocating a potentially 8,192² tile canvas. The paint toolbar adds one-click **New stroke** reseeding through a deterministic signed-32-bit successor stored in `aidraw:variantSeed`; existing GIDs do not change, while subsequent strokes remain coordinate-stable under the new seed. `aidraw:variantGroup` and `aidraw:variantSeed` remain ordinary AIDraw-namespaced Tiled custom properties—Tiled may preserve them but is not claimed to interpret AIDraw's random-brush convention. Ambient randomness, retroactive repaint, and full-tile preview allocations were rejected. Packaged pointer/visual acceptance and broad Tiled behavior remain open.
- Expose the existing exact counterclockwise quarter-turn alongside clockwise rotation for sprite selections, finite/infinite tile selections, reusable pixel stamps, and reusable 32-bit-GID tile stamps. The UI now labels both directions explicitly, while all surfaces continue through the established shared transform kernels and ordinary revision-checked cel/tilemap or library replacement transactions. Exact non-square anchor/cell tests prove both stamp families preserve palette indices and unsigned Tiled flags; grid-selection coverage proves counterclockwise GID placement. Adding a second transform implementation or a direction-toggling button was rejected because explicit actions are easier to predict and keep UI/headless geometry identical. This closes PIX-13's source-level counterclockwise exposure gap and improves PIX-06/MAP-11; packaged pointer acceptance, cross-document stamp libraries, and release readiness remain open.
- Replace tileset geometry's commit-on-blur re-slicing with an explicit preview/review/apply workflow. A bounded first-frame source-sheet thumbnail overlays current, draft, and selected crop rectangles; users choose whether metadata follows matching source origins or keeps tile IDs. One immutable kernel remaps tile definitions, animation targets, Wang representatives, and Wang assignments together, reports every reframe/renumber/drop category, fails ambiguous source-position mappings closed, and requires acknowledgment before affected metadata can commit through one revision-checked asset replacement. Blank slices remain sparse even at the established 1,048,576-tile ceiling. Following source positions is the default because re-slicing usually means preserving source artwork association; keeping ordinal Tiled IDs remains an explicit alternative. Automatically discarding after a warning, silently inventing a new Wang representative, and materializing every blank tile were rejected. This is headless/source MAP-02 evidence; packaged pointer/visual acceptance and broad Tiled compatibility are not claimed.
- Enforce the documented MCP concurrency boundary instead of merely demonstrating 32 clients. AIDraw now reserves capacity during initialization, admits at most 32 authenticated transport sessions, returns a retryable HTTP 429 to a simultaneous 33rd initializer, and retires presence, subscriptions, and session-scoped folder trust when an authenticated transport DELETE closes a session. Each retained session also enforces the existing 128-distinct-resource subscription ceiling; duplicate subscriptions do not consume capacity and unsubscribe releases it. Explicit client termination was chosen over silent idle eviction because no timeout policy has been established, and `session_manage leave` intentionally remains a presence action rather than an implicit transport close. This is bounded headless QA-04 lifecycle evidence, not exhaustive MCP action/limit coverage, packaged networking evidence, Level 3, or release readiness.
- Complete the automated MAP-03 selected-tile metadata workflow. Tile-animation rows now reorder through drag-and-drop or equivalent keyboard move controls as one revision-checked asset replacement, while a pausable duration-aware inspector preview composites the exact first sprite frame and starts paused when reduced motion is requested. Shared immutable ordering/source-rectangle helpers and a crop-only renderer avoid allocating an entire large sprite for the preview. The maintained external TMJ/TSJ and TMX/TSX fixtures now carry ordered multi-frame animations and prove import → production export with PNG companion → re-import preserves exact frame order/durations, probability, typed properties, and collision metadata. This evidence is limited to those fixture surfaces; packaged pointer/visual behavior, image-collection tilesets, arbitrary Tiled extensions, and broad Tiled compatibility are not claimed.
- Complete the automated PIX-04 pixel-tool contract. Pencil cleanup now removes doubled staircase corners without globally deduplicating later revisits; exact symmetric integer ellipses replace the renderer-local parametric approximation. Fill, magic wand, replacement, and semantic fill/replacement share cached indexed readers and compact row-run kernels under the existing one-million-cell and 65,536-run operation limits. Flood fill budgets its actual connected result, permitting a one-pixel island inside a larger sparse sprite, while replacement budgets every inspected target cell; overflow warns or returns an exact error and applies no partial edit. UI fill/replacement remain one-shot actions, and pointer cancellation or Escape now clears transient work and best-effort releases any eventual lock instead of committing. Visible-composite UI sampling and addressed-cel semantic sampling are deliberately preserved; broad packaged pointer behavior, a larger operation budget, partial fills, and release readiness are not claimed.
- Correct square-tile Tiled transformation rendering. Diagonal now means the Tiled anti-diagonal transform and is applied before horizontal and vertical flags through one shared core matrix used by both the live canvas and headless/export renderer; the former duplicated Canvas composition swapped diagonal-only with diagonal-plus-both-flips. Exact labeled-pixel coverage, a maintained raw-RGBA golden, and a separate production TMJ+PNG round trip prove all eight unsigned GID combinations. Transform thumbnails, packaged interaction, isometric-specific visual evidence, non-square tile policy, and broad Tiled fidelity remain outside this checkpoint.
- Close the adjacent embedded-image gap at utility-import and profile-recovery ingress. Every utility-imported document now passes its complete generic-asset metadata through the main-owned migration/envelope gate; each embedded payload must then satisfy the established canonical base64, length/SHA, PNG/APNG/JPEG/WebP/GIF MIME/header, 128 MiB, display-geometry, 8,192-side, and 16MP checks and complete a supervised codec decode before the import can resolve. A malformed envelope retires its producer, and decoder-process exit returns no document. Profile recovery applies the same payload checks before recovered bytes become canonical: invalid bytes are omitted while the document, dirty/file/tab state, and asset metadata remain available, and an attached editor receives one aggregate startup warning. Whole-import rejection was retained because a worker result is not yet user state; metadata-preserving omission was chosen for recovery because rejecting the document would discard otherwise recoverable unsaved work. Silent recovery was rejected because missing artwork must remain visible to the user. Metadata-only recovery records, exact packaged warning presentation, arbitrary codecs, and broad PDF/PSD/image compatibility remain outside this checkpoint.
- Contain generic raster admission behind the existing supervised utility lane instead of invoking the native canvas decoder on arbitrary bytes in the main process. Live `asset.add`, production native open/save, and packaged CLI native reads first enforce canonical base64, declared length, SHA-256, PNG/APNG/JPEG/WebP/GIF MIME/header agreement, display geometry, the existing 8,192-side/16MP limit, and their established 1.5 MB inline or 128 MiB native-entry byte ceiling; only then does a typed `validate-image` request perform the real codec decode in the worker. A decoder-process exit rejects the active request and queued work resumes in a fresh worker. Live admission rejects without mutation; native open and CLI retain metadata but omit invalid data with an explicit warning; save rejects before preview rendering, temporary creation, or destination replacement. Warning-and-omission was retained over whole-document rejection to preserve the established partial-recovery contract, while the unsafe in-process-decoder alternative was rejected because malformed header-valid input can terminate its host process. This is source-level crash containment for those ingress paths, not recovery-journal or utility-import-result asset reconciliation, arbitrary codec compatibility, packaged/native acceptance, Level 3, or RC evidence.
- Reject canonical pixel palette references that cannot name an authored color. Persisted native, recovery, and utility-imported sprites and reusable stamps now require every indexed value to be below the current palette length; live cel/region, frame, asset, and stamp ingress enforces the same rule at its sequential reducer position. Per-frame palette overrides must exactly match the current palette length and entry-ID order, while their colors may differ; deleting a frame removes its override and undo restores the prior sprite. Rejection was chosen over silently remapping to transparent or the renderer's magenta missing-color diagnostic because either changes authored color meaning. Missing linked/group/asset/cache targets retain their established recovery behavior. This is exact indexed-color integrity, not general graph reconciliation, packaged/UI, Level 3, or RC evidence.
- Validate the normalized canonical state of pixel documents before native, recovery, or utility-imported content reaches rendering/export. The established recovery normalizer still repairs missing legacy frame/cel metadata, canonical record-key aliases, absent reusable-library/link/conversion preferences, and missing references; duplicate entity identities that would overwrite recovered content now reject instead. The normalized result and newly supplied pixel assets share strict schemas for palettes, reusable libraries, links, conversion settings, sprites, frames/cels, canonical 32×32 pixel chunks, tilesets/tile metadata/collisions/Wang terrain, tilemaps/layers/map objects, and canonical 32×32 GID chunks. Chunk keys must agree with aligned origins and payloads must be canonical base64 with exact decoded geometry. Native save validation preserves an existing destination and removes its temporary sibling on failure, while recovery isolates one malformed snapshot from valid siblings. Tileset count limits reuse the existing Tiled importer ceilings rather than adding a new resource policy. Full missing-reference reconciliation remains open; the separate palette-reference checkpoint now closes only the stored-index-to-palette-length and frame-override-order gap. It does not authenticate linked content, claim arbitrary repair, packaged behavior, Level 3, or RC evidence.
- Apply the established live-operation schemas to persisted illustration state before native, recovery, or utility-imported documents reach renderer/export consumers. Artboards, root layer order, every group/vector/paint layer and stroke, every vector-stroke/path/shape/text/image/group object, brush presets, guides, snap settings, and the existing animation contract now share exact finite/bounded field validation. Root order and membership IDs must be unique, and layer/object record keys must match their entity IDs. A malformed canonical record rejects the document with a field-family error; a corrupt recovery snapshot remains isolated from valid sibling journals, and failed native save validation preserves the destination and cleans the temporary sibling. Missing references and unreferenced valid records remain recoverable, while existing legacy defaults for absent brush/guide/snap/animation state remain. That earlier schema layer did not itself establish asset image-decoder validation; the later supervised-admission checkpoint above closes only the named live/native/CLI boundary. This does not claim graph completeness, strict dangling-reference rejection, pixel-family completeness, packaged behavior, Level 3, or RC evidence.
- Reject malformed or duplicate persisted activity and provenance before they can reach document-open activity accounting, renderer attribution controls, or asset-reference traversal. Activity records now require the exact writer envelope, a schema-valid actor/status, and a finite safe operation count; provenance records use the existing exact provider/model/prompt/source/mask/conversion contract. Both ID sequences must be unique. The shared migration boundary protects native open, recovery snapshots, and utility-imported documents, while native save validation leaves an existing destination byte-exact on failure. Missing referenced assets remain recoverable by design, and structurally valid self-authored metadata is not authentication, signature, provider, packaged-UI, Level 3, or RC evidence.
- Confine crash-recovery state and preserve the last contiguous committed prefix. New per-document journals use SHA-256 filenames so imported IDs—including traversal strings and case-distinct values—cannot escape or collide inside the recovery root; direct-child legacy journals still reopen and migrate on the next compaction. Recovery now requires one exact snapshot followed by same-document canonical transactions, stops at the first torn, malformed, cross-document, or non-applicable record, and never skips across that gap to later edits. The workspace manifest is trusted only when it exactly matches the writer's unique ordered-ID/active-ID contract. Opening a native document now queues its snapshot before an immediate edit can append, closing the prior sub-60-second crash-loss window. Invalid direct writes fail without changing the last good journal, and atomic replacements use exclusive unique temporary files. This is headless recovery integrity, not an fsync/power-loss guarantee, a new journal-size ceiling, packaged crash evidence, Level 3, or RC evidence.
- Reconcile native checkpoint identity before attribution, authorization, or restore. Each selected `checkpoints/index.json` summary and payload now requires the exact writer metadata envelope, a safe entry ID, same-document identity, canonical timestamp, valid actor and kind, and a nonnegative safe source revision; all metadata must agree exactly and the migrated checkpoint document must carry that same identity and revision. Duplicate selected IDs are ignored, while a malformed payload excludes only that checkpoint so the main artwork and valid siblings still open. Native save rejects invalid, inconsistent, or duplicate supplied checkpoints before destination replacement. Stored names remain nonblank but are not capped at the manual-entry limit because restore-authored safety names can legitimately grow. This is headless native checkpoint integrity, not arbitrary corruption repair, packaged restore/authorization behavior, Level 3, or RC evidence.
- Harden durable transaction traces before inspection or replay. Native and local JSONL records now require the exact version-1 writer envelope, canonical timestamp/outcome/revision metadata, same-document identity, the complete canonical transaction schema, and the established 2 MiB transaction ceiling. Canonical admission now also returns an explicit conflict for schema-shaped values that cannot be JSON-serialized instead of throwing outside the response contract. The line scanner skips tiny-line floods without per-line JSON parsing and reports all malformed native records in one bounded warning; valid records still import in order, now with one batched append instead of one filesystem write per record. Native open isolates bad trace records so artwork remains recoverable, while native save rejects an invalid supplied trace before destination replacement. This is headless trace-ingestion/replay-input integrity, not persistent-trace truncation, packaged replay, renderer behavior, Level 3, or RC evidence.
- Reject malformed or self-contradictory native manifests instead of returning a document alongside untrusted competing metadata. The reader now validates the writer-defined manifest shape and requires its schema, document identity/kind/name, revision, timestamps, and generic asset count to match `document.json` exactly; this same check protects temporary-save validation before atomic replacement. Native asset metadata must also match the writer contract, inline `document.json` bytes are never trusted, and `assets/<sha256>` data is hydrated only after exact declared-length and SHA-256 verification. A corrupt or missing payload preserves editable metadata but remains unavailable with a warning; invalid metadata or a contradictory manifest rejects the container. Saving also rejects non-canonical or hash/length-contradictory supplied asset bytes before replacing an existing destination. Matched schema-1 pairs and checkpoint isolation remain supported. That earlier container-integrity layer did not itself establish image-decoder validity; the later supervised-admission checkpoint above adds only the named MIME/header/geometry/decode contract. This is not broad corruption recovery, packaged-open behavior, or release readiness.
- Verify the complete canonical transaction/inverse surface. All 41 operation kinds now have bounded runtime payload schemas and an exhaustive forward/undo/redo corpus; deterministic illustration and pixel histories exercise eight fixed 48-step seeds plus atomic multi-operation failures. Ordered provenance, active pixel-asset restoration, and zero-only sparse chunk removal are exact, while layer/object replacement can no longer rewrite hierarchy membership behind the ordered move operations. Per-actor history now rebases monotonic revisions only after internal logical-target lineage proves the top entry safe: disjoint actors remain independently undoable, an overlapping foreign or explicitly unrecorded edit creates a non-crossable undo/redo barrier, document-revision guards retain their global foreign-edit barrier, and every committed undo/redo appears in the canonical change feed. Whole-asset history targets contain nested frame/cel/layer edits, and object-group children are validated and dependency-ordered during checkpoint merge. Blind revision rebasing was rejected because it could rewind another actor; this is deterministic headless core evidence, not persistent history across restart, CRDT merging, packaged UI, native interaction, Level 3, or RC evidence.
- Honor bounded EXIF orientation on standalone JPEG import instead of rejecting a valid file when encoded and decoder-applied display dimensions are swapped. The original JPEG bytes remain embedded, illustration geometry and pixel quantization use the oriented display, native save/reopen preserves the source, and import reports the exact encoded-to-display geometry. A frozen 948-byte orientation-6 fixture proves 24×16 encoded pixels become a 16×24 display whose production PNG re-export is pixel-identical to the decoder reference; the shared parser covers both TIFF byte orders and safely ignores a hostile IFD offset while retaining encoded geometry. This does not preserve arbitrary JPEG metadata on export or claim camera/vendor color fidelity, malformed-EXIF rejection, sprite-sheet/Tiled coordinate conventions, packaged UI behavior, or broad JPEG compatibility.
- Complete the source-level hosted-provider credential lifecycle. OpenAI and Stability keys now rotate through serialized atomic replacement, have an explicit two-step Remove action, retain the other provider during rotation/removal, and can be deleted even when the operating-system credential backend is unavailable. The renderer receives only configured/stored/availability status and never a provider secret or ciphertext; new credentials are capped at 16 KiB and fail closed without DPAPI/Keychain/libsecret. Deterministic tests cover concurrent updates, failed encryption/replacement preservation, unavailable Windows DPAPI, removal without decryption, and the write/status-only preload surface. This does not prove native DPAPI/Keychain/libsecret behavior, live providers, packaged UI interaction, or release readiness.
- Bound every utility-process result family under one main-owned resource policy. Export results are limited to 512 MiB of aggregate decoded data and 4,097 primary/companion members, and must match the requested format's exact MIME, extension, and companion shape before any permissive base64 decode; this prevents a malformed worker from adding adjacent output files. Imported documents are measured without whole-result serialization against a 512 MiB JSON, 16,777,216-node, and 128-level budget before migration. Reports/warnings are capped at 4,096 entries and 1 MiB serialized, generic observation results at the existing 4 MiB PNG base64 allowance plus 64 KiB, provider metadata at 64 KiB, generation progress at 4 KiB, and terminal errors at 16 KiB. Invalid terminal results retire the worker and queued work recovers fresh; oversized advisory progress is ignored. These post-IPC source gates do not establish a pre-deserialization or process-RSS ceiling, provider behavior, packaged/native acceptance, broader-platform evidence, or release readiness.
- Make repeated native saves of sparse paint layers update only tiles touched by strokes appended after the last valid cache prefix. Successful atomic replacement adopts the newly materialized content-addressed cache into the live document for the next save; write, flush, validation, or replace failure leaves the prior live cache and destination unchanged. Native open now also rejects a cache whose tile index does not exactly cover its bound editable strokes. This is deterministic headless storage evidence, not packaged reopen/erase/edit acceptance or a claim that every paint operation is incremental.
- Warn PDF exports that retain importer-authored invisible searchable text that visible covering artwork is not redaction. A deterministic full-artboard black-overlay regression proves the 241×151 page can look fully covered while pdf.js still extracts the exact three LibreOffice phrases; production re-import preserves an exact opaque-black one-pixel-inset interior. This adds a reviewable warning, not intent inference, content removal, redaction tooling or guarantees, or broader PDF evidence.
- Preserve supported text from PDF raster-fallback imports through PDF re-export. Import-authored hidden text layers now carry a typed interchange role; the hybrid exporter writes each eligible run once as invisible searchable standard-font content, leaves ordinary hidden layers omitted, and reports exact retention, substitution, unsupported-glyph, or transform/effect loss. The frozen LibreOffice clipped-transparency fixture proves the three exact phrases plus opaque 241×151 visual fallback survive import/re-export/re-import within the existing Poppler bounds; this does not claim arbitrary Unicode/fonts, editable operators, broader producers/pages, packaged behavior, or broad PDF compatibility.
- Add a migration-readiness gate for macOS development: deterministic tracked/prospective-path collision and forbidden-name checks, explicit LF/binary attributes without renormalizing the dirty tree, broader local secret/generated-artifact ignores, target-architecture-aware Darwin fuse handling, and a fail-closed native release-host identity check. Document the clean macOS bootstrap and keep Keychain, native dependency loading, DMG/ZIP, signing/notarization, Electron/UI, and Computer Use acceptance explicitly unclaimed.
- Make the authenticated MCP contract self-documenting for cold external agents: initialize instructions, strict action-specific schemas, described inputs/outputs, structured results, progressive `aidraw_help`, optional `aidraw://guide`, and explicit privacy-safe job/retry next steps now remain learnable from tools alone. Add a tools-only cold workflow and exact wire-schema regressions without weakening human approvals, file authority, attribution, revisions, locks, save behavior, or owner-private job summaries.
- Certify that contract in one exact packaged headless raw-MCP acceptance: a repository-free tools-only client discovers join/list/observe/mutate and human-approval wait/cancel before optionally reading `aidraw://guide`, while outsider job access and private path/result/request/prompt data remain unavailable, the denied target stays absent, cleanup is graceful and credential-redacted, and external/provider/paid/non-loopback requests remain zero.
- Add a launch-free AGT-14 installed-OpenCode discovery safety harness that pins exact client/package identities, confines configuration/home/AppData/user-data/temp artifacts to one fresh retained root, excludes ambient provider credentials, requires independent offline enforcement, records the later exact PID, and redacts only its disposable authorization after a normal exit; installed-client acceptance remains pending.
- Harden the sandboxed renderer's privileged IPC origin boundary: every registered handler and main-window navigation now share one parsed exact-origin policy that accepts only `aidraw://app` or the configured HTTP loopback development origin, rejecting arbitrary local services, port-prefix lookalikes, credential/host confusion, wrong web contents, null frames, and subframes before privileged work begins. This is headless policy evidence only; packaged sandbox/fuse/permission behavior remains a separate acceptance boundary.
- Freeze the actual preload bridge as a headless security contract: `window.aidraw` is one frozen function-only 56-member allowlist, all 54 commands bind to their named IPC channels regardless of renderer-controlled arguments, the two event subscriptions strip raw Electron event objects, and preload may import only Electron's bridge/renderer endpoint plus the shared typed contracts. No generic IPC, Node/Electron object, side-effect import, or dynamic import is exposed; packaged contextBridge serialization remains a separate boundary.
- Add a reusable packaged-security verifier for the executable fuse wire and bundled BrowserWindow, origin/IPC, permission/window/navigation, CSP, and preload contracts. Freeze one fresh exact package plus a uniquely selecting, unlaunched runtime observer for later independent verification of live contextBridge serialization and denial behavior; no native packaged acceptance is claimed yet.
- Certify that frozen package in one exact independent `FND-02-PACKAGED-SECURITY` run: the live sandboxed renderer exposes only the frozen 56-function typed bridge, no Node/generic IPC primitive, enforces CSP and permission/window/navigation/webview denials, preserves a working bootstrap, observes zero non-loopback/provider/paid requests, and exits gracefully with credential redaction while every unrelated process remains untouched.
- Add a launch-free FND-09 utility-containment contract and exact-package checkpoint. A fixed test-only worker probe crashes one raster utility and hangs another until cancellation; queued real PNG exports must recover byte-identically through three distinct workers without changing canonical state or starting generation/provider work. Package verification now inspects Vite's hashed worker implementation chunk, and one tools-only headless observer is frozen for later independent runtime acceptance with authenticated MCP agreement, empty public jobs, zero network/provider/paid activity, and graceful credential redaction. Runtime and resource-pressure acceptance remain unclaimed.
- Certify the frozen FND-09 package in one exact independent headless run: a real raster worker crash and an abortable hung task each fail honestly, queued production PNG exports recover byte-identically through three distinct workers, canonical state and authenticated MCP observation agree, public jobs remain empty, generation stays untouched, network/provider/paid activity remains zero, and cleanup is graceful and credential-redacted. Resource-pressure and broader platform acceptance remain open.
- Bound utility admission under headless resource pressure: each independently supervised lane retains at most 32 waiting tasks, the 33rd receives an explicit retryable `utility_queue_full` error, queued cancellation frees capacity without worker replacement, and admitted work preserves FIFO order. Quantization now rejects source input above the existing 1,500,000-byte / 2,000,000-base64-character boundary before encoding or worker allocation. This is deterministic source-level containment, not packaged pressure, provider behavior, an exact RSS ceiling, or broader-platform acceptance.
- Prepare one immutable exact-package FND-09 pressure checkpoint with a fixed test-only real-worker gate and a single listed future observer. The bundled source and static verifier require the exact 32-waiter/33rd-retry/cancel-refill/FIFO/same-worker contract plus a pre-base64 oversize trap, while the fresh retained profile/output remain absent pending independent native authorization. No process-RSS, provider/network, or broader-platform acceptance is claimed.
- Certify that pressure contract in one exact Windows packaged run: the 1,500,001st source byte rejects before base64 conversion or worker creation; one real raster worker holds while 32 requests wait; the 33rd returns retryable `utility_queue_full`; cancelling `queued-10` admits a replacement; and all 32 remaining requests drain FIFO through the original worker. Canonical and authenticated-MCP state stay exact, public jobs and raw utility payloads remain private, generation stays untouched, guarded non-loopback/provider/paid activity remains zero, and cleanup is graceful and credential-redacted. Exact process-RSS and broader-platform claims remain open.
- Fail closed on compromised quantization child results before caller or canonical use. Successful output must match the active request, contain no more than its width×height pixel budget, use unique in-bounds integer coordinates and valid palette indexes, and carry a valid response envelope; malformed error envelopes are also rejected immediately. Invalid workers are retired and queued work restarts cleanly in a fresh worker. This headless slice does not invent a global serialized-byte cap or claim export/import/observation/generation result budgets, exact RSS, providers, native packaging, or broader platforms.
- Complete package/static/list acceptance for the quantization-result boundary without native launch. The immutable EXE `1CF937B4…DCCE` / ASAR `CABAC58E…C815` contains the fixed private post-quantizer faults and main-owned count/geometry gates; the exact tools-only observer lists once at `utility-containment.spec.ts:715:5`, keeps invalid payloads private, requires canonical/authenticated-MCP and generation-lane invariance, and is statically force-free. Freeze a separate fresh runtime guard with exact package/source/observer, retained-tree and protected-process oracles; native Windows behavior remains independently audited and unclaimed.
- Certify that fixed quantization-result boundary in one exact Windows packaged run: the two-change result from PID 8248 failed against its one-pixel budget and recovered through PID 23932; that worker's x=1 contradictory result failed and recovered through PID 24288. Corrupt payloads stayed private, canonical and generation state stayed exact, public jobs/non-loopback/provider/paid counts remained zero, and cleanup was graceful and credential-redacted. This is one immutable fixture against EXE `1CF937B4…DCCE` / ASAR `CABAC58E…C815`, not generic result-pressure, provider, broader-platform, or release certification.
- Validate `export-document` child artifacts before any permissive base64 decode. Primary and companion envelopes now require canonical base64, syntactically valid MIME/extension values, safe companion leaf names, and string-only report arrays; malformed workers are retired and queued valid exports restart through a fresh worker. This first boundary added no total encoded-byte cap; the later common policy above adds an aggregate decoded-data cap without claiming the base64 IPC itself has that size.
- Certify the separate export-result boundary in one exact Windows packaged run: immutable EXE `2F745784…8CEA2` / ASAR `DDE43A0F…E562D` ran the line-931 observer 1/1 in 2.9 seconds; malformed primary worker 13124 recovered through 9500, malformed companion worker 9500 recovered through 15680, valid PNG+JSON hashes agreed, invalid results stayed private, canonical/generation state remained unchanged, public jobs/targets/non-loopback/provider/paid counts stayed zero, and cleanup was graceful and credential-redacted. This is the fixed sprite-sheet result fixture only, not a total serialization/RSS, other-format, broader-platform, provider, release, or pipeline certificate.
- Validate `import-document` child results before caller/workspace use. The existing 256-page PDF ceiling now defines the shared 1–256 document result boundary, every returned document passes through the established migration/schema gate, and warnings must remain strings; malformed workers retire and queued valid imports restart fresh. This first boundary added no serialized-document or warning-size ceiling; the later common policy above establishes both as source-owned post-IPC limits.
- Accept the exact import-result package/static/list checkpoint without native launch: source manifest `E3D404A1…4BEA` produced immutable EXE `EB2DA9F9…90AD` / ASAR `4BF8947F…C705`, passed all private main/worker and package-security markers, and listed only the line-1166 observer. A separately frozen graceful-only runtime guard binds the exact package/source/observer, one profile-owned SVG input, zero output targets, privacy/canonical/generation invariance, prior evidence trees and protected processes; native acceptance remains unexecuted and unclaimed.
- Certify that import-result boundary in one exact Windows packaged run: malformed-document worker 21900 recovered through 3436, malformed warnings on 3436 recovered through 27588, valid SVG semantic hashes agreed, invalid results stayed private, canonical/generation state stayed unchanged, outputs/public jobs/non-loopback/provider/paid counts stayed zero, and cleanup was graceful and credential-redacted. This remains the fixed SVG result fixture only, not a serialized-document/warning-size, other-format, RSS, provider, broader-platform, release, or pipeline certificate.
- Accept the exact provider-free general-generation package/static/list checkpoint without native launch: source manifest `7F411C91…AB16` produced immutable EXE `13AF1B6A…1637` / ASAR `82B01525…6F9E`, all private-fixture and package-security markers passed, and only the line-1423 observer listed. That preparation froze a separate graceful-only runtime guard over then-absent `...generation-result-native-20260807T184500` profile/output roots; the independently certified execution is recorded immediately below. No provider behavior, aggregate serialization/RSS, retry, progress-message, UX, broader-platform, or release claim is added.
- Certify that exact provider-free general-generation result boundary in one independently audited Windows packaged run: five malformed results stayed private while fault workers 21272, 27580, 22916, 10052, and 3188 each retired and queued valid work recovered through a six-worker chain ending at 14316. Canonical and raster-lane state stayed unchanged; input/output targets, public jobs, non-loopback/provider/paid counts stayed zero; and shutdown was graceful, credential-redacted, and survivor-free. This remains the fixed local 1×1 fixture and current request-derived count/image/header/dimension/seed/metadata contracts only, not a provider, aggregate serialization/RSS, retry, broader-platform, release, or pipeline certificate.
- Validate `capture-observation` child results before caller/MCP use. Only established error discriminants or an exact request-derived static PNG may pass: target, region, scale, dimensions, `maxPixels`, canonical base64, the existing 4 MiB PNG limit, bounded chunk CRCs, static identity, image data/end markers, and IHDR dimensions must agree. Invalid workers retire and queued captures recover fresh. This first boundary added no generic result budget; the later common policy above adds one without adding an RSS, packaged, or generation-result claim.
- Validate `generation-run` child results before preview/job/canonical use. Output count cannot exceed the originating request; each result requires a unique ID, canonical base64, exact PNG/JPEG/WebP MIME/header/dimensions, the existing per-image 32 MiB / 8,192-side / 32 MP limits, request-consistent seed attribution, and record-shaped metadata. Invalid workers retire and queued valid results recover fresh while empty/partial no-retry semantics and main-owned canonical provenance remain intact. This first boundary added no provider-metadata limit; the later common policy above adds one without an RSS or packaged claim, while oversized acceptance handling remains separate and is described next.
- Make `generation-progress` an explicit advisory boundary: only a matching active generation request with finite 0–1 progress and a string message reaches the caller. Malformed values are ignored without coercion, cross-lane progress stays invisible, and terminal work continues without worker replacement. The later common policy above adds a 4 KiB message limit while terminal response, timeout, and cancellation remain authoritative; no RSS or packaged/provider claim is inferred.
- Normalize oversized generated previews only at explicit acceptance without mutating the provider result. The raster lane keeps dimensions fixed, tries lossless PNG then a fixed descending alpha-preserving WebP quality ladder, requires the accepted derivative to meet the existing 1,500,000-byte and full inline-image contract before quantization/commit, and reports source/accepted hashes, bytes, codecs and quality in private job/provenance data. If no accepted copy fits, the preview remains available with guidance and canonical state unchanged. This is launch-free source evidence only; no older package inherits it.
- Certify that generated-preview policy in one exact Windows packaged lifecycle: a real utility worker produces the bounded derivative while both private provider outputs remain byte-exact, visible preview-only and success notices agree with canonical/authenticated-MCP evidence, five screenshots preserve the human-facing path, provider/non-loopback/paid activity stays zero, and cleanup is graceful and credential-redacted. This certificate covers only the deterministic normalization scenario and preserves both earlier observer failures as immutable history.
- Fully decode accepted `capture-observation` PNG results before caller/MCP use. After the existing canonical-base64, 4 MiB, request-geometry, chunk-bound, CRC, IHDR, static-image and IEND checks, the existing UPNG codec must produce exactly one matching RGBA plane. A CRC-repaired but zlib-corrupt IDAT fixture now fails closed, retires that worker, and lets queued valid observation work recover fresh. The later common policy adds a generic serialized-result limit; no RSS or packaged observation claim is added.
- Prepare a launch-free exact-package observation-codec checkpoint without inheriting any earlier certificate. A fixed, profile-gated main-only request makes the real utility child return one CRC-valid but undecodable IDAT stream; the supervisor must reject it at the full decoder, retire that worker, and complete already-queued ordinary observation work in a fresh child. The tools-only observer requires canonical/authenticated-MCP agreement, an empty private-job surface, no renderer/preload discovery of the hook, zero provider/non-loopback/paid activity, and force-free graceful cleanup. Package build/list and native execution remain separate independent audit boundaries.
- Freeze the independently built observation-codec package at EXE SHA-256 `A1FB98E3BB482367F17D011B83005F1410180BBD270E0A78329C7FA05CF1E1C5` and ASAR SHA-256 `530DF2761572F554F46B0C14BAB5B4455E19439CDC7D43E68B5CADEEBF932DC9`. Static verification passes; the first list guard failed safely because Windows Playwright emitted the stable basename form. A separate list-only guard now accepts only that form or the exact `tests/e2e/`-prefixed equivalent and resolves exactly one test without launching the package. Native acceptance remains pending.
- Freeze a separate observation-codec runtime guard without changing that package: the exact observer disables Playwright's timeout-driven worker termination, retains bounded rejection-only graceful-exit waits, and is statically rejected if force-control returns. The future headless run must preserve four exact unrelated process identities, prove corrupt-IDAT rejection plus distinct-worker queued recovery and canonical/MCP/privacy invariance, retain encrypted/redacted evidence, and stop without cleanup or signaling on any survivor or mismatch.
- Certify that observation-codec boundary in one exact Windows packaged run against EXE `A1FB98E3BB482367F17D011B83005F1410180BBD270E0A78329C7FA05CF1E1C5` / ASAR `530DF2761572F554F46B0C14BAB5B4455E19439CDC7D43E68B5CADEEBF932DC9`: a CRC-valid but undecodable result stayed private and failed main-owned full decode, its worker retired, queued valid observation recovered in a distinct child, canonical state and generation stayed unchanged, renderer/public jobs remained absent, and cleanup was graceful and credential-redacted with zero provider/non-loopback/paid activity. Preserve the earlier controller-only `NO_COLOR`/`FORCE_COLOR` failure as immutable evidence; this certifies only the fixed scenario, not wider result/RSS/platform behavior.
- Make stale revision recovery explicit on the tools-first MCP surface. `canvas_apply` discovery now describes entity ID, expected revision, actual revision and retryability; an authenticated regression proves a rejected stale replacement cannot mutate canonical state, directs the caller to `canvas_observe`, and succeeds only after rebuilding intent from the observed revision with a fresh operation ID.
- Fix OpenCode onboarding to write the stable `mcp.aidraw` remote-server shape with `enabled: true` instead of the incompatible V2 `mcp.servers.aidraw` wrapper, with an isolated packaged-UI regression that cannot change a developer's global config or login startup.
- Verify that the packaged editor publishes live advisory tool, zoom, and viewport state through MCP and clears it when the editor detaches.
- Add an opt-in QA-session force-on-timeout fallback that refuses termination unless the recorded executable, hash, PID, exact profile, and loopback MCP identity all match, with post-exit bearer redaction.
- Verify the exact packaged native File → Open mouse path against an isolated saved document, with visible reopen evidence and an authenticated MCP identity/state assertion.
- Verify packaged renderer failure containment with a fixed sanitized malformed event, a test-only path-contained diagnostic export, secret-exclusion assertions, and same-process canonical-state recovery after renderer reload.
- Verify packaged pointer-down human priority for illustration-object and pixel-region locks, including visible held previews, honest retryable authenticated-agent conflicts, canonical human commits, graceful shutdown, and post-stop credential redaction.
- Verify two-document background collaboration in the exact package: actor-colored live/unread tab badges carry named cursor state without stealing focus, a simultaneous foreground human gesture remains responsive and intact, and authenticated MCP observation agrees for both canonical documents through graceful credential-safe cleanup.
- Verify an exact no-charge packaged file-approval lifecycle: the authenticated actor, disposable path, and four authority choices are visible; denial preserves filesystem and canonical state; allow-once writes only its approved target; a same-folder probe proves no implicit trust; and graceful cleanup removes retained credentials.
- Verify an exact isolated packaged animated-sprite lifecycle: four timed frames cover independent and linked cels, onion skin, and a named ping-pong tag; authenticated MCP and renderer state agree; one approved no-network APNG decodes to the exact six-frame sequence; and cleanup is graceful and credential-redacted. Preserve the initial failed observer-contract run without exposing raw job results through the public MCP summary.
- Verify an exact isolated packaged finite-map Wang lifecycle: one named mixed terrain set and color drive deterministic 3×3 paint/erase/repaint repair on a 12×10 orthogonal map; authenticated MCP, renderer state, a bounded allow-once TMJ export, and its sole PNG companion agree; and cleanup is graceful, zero-charge, and credential-redacted. Preserve both strict-locator observer failures as failed history and keep public MCP job summaries privacy-redacted; that finite-map run did not claim infinite maps or full Tiled round-trip.
- Verify an exact isolated packaged generated-fill lifecycle with a deterministic keyless local runner: full authenticated approval, denial, real expiry, cancellation, separate allow-once, visible comparison, non-destructive human acceptance, canonical Stability provenance, renderer/MCP/mock agreement, zero external or paid requests, privacy-redacted public job summaries, and graceful credential cleanup. Preserve the two earlier observer failures distinguishing requesting-agent versus accepting-human attribution and approval display labels versus canonical model identifiers.
- Verify exact packaged multi-document Save All, collision-safe batch export, and Close All: cancellation preserves focus and canonical state, bounded export reports two successes plus one sanitized failure, final saves parse as distinct revision-2 native documents, renderer and authenticated MCP agree, writes stay inside declared profile children, and cleanup is graceful, credential-redacted, and zero-cost.
- Verify exact packaged single-frame pixel palette conversion with a deterministic keyless local runner: the fixed 16×16 indexed contract preserves original layers/cels and palette, adds one human-owned layer/cel while retaining agent job ownership and Stability provenance, matches renderer/authenticated MCP/fixture state, blocks external and paid requests, preserves public-summary privacy, and cleans up gracefully with redacted credentials. Preserve the earlier single-container text-observer failure as immutable history; multi-frame policy and palette-file interchange remain unclaimed.
- Verify an exact-package orthogonal infinite-map lifecycle: exact UI controls resize the governed 64×64 default to 32×32 before infinite mode, fourteen signed-coordinate cells persist across seven sparse chunks, negative/positive navigation and renderer/authenticated-MCP/export state agree, one actor-bound allow-once export yields only the independently parsed TMJ and PNG, and cleanup is graceful, credential-redacted, and zero-cost. Preserve the first clean setup-observer failure without changing product geometry semantics; isometric infinite maps and full Tiled round-trip remain unclaimed.
- Add bounded headless Tiled JSON coverage that first preserves signed orthogonal chunks through external import/current export, then proves a no-overwrite TMJ+PNG export/import/re-export lifecycle across two documents with equal supported map/tileset/property/chunk/cell semantics, exact indexed-pixel recovery, and identical decoded RGBA. Packaged/UI execution, isometric behavior, unsupported modern fields, and broad/full-fidelity claims remain open.
- Verify the exact packaged supported orthogonal `QA-06-TILED-RT` lifecycle after preserving two failed runs: first fix the real utility-process `nativeImage` decoder defect, then remove only the source-only map-name observer overclaim. Three actor-bound allow-once approvals drive first TMJ+PNG export, exact-pair import into a distinct document, and distinct re-export; renderer/authenticated-MCP state, typed metadata, seven signed chunks, fourteen cells, and indexed pixels agree, both persisted pairs are byte/pixel equal, public jobs stay private, writes stay exclusive, and cleanup is graceful, credential-redacted, and zero-provider. Source editor map-name preservation, isometric/unsupported/image-collection/broad-fidelity behavior, broader conflicts, palette interchange, and multi-frame policy remain unclaimed.
- Validate every expanded entry in a native `.aidraw` ZIP against its central-directory CRC-32 after bounded decompression. A stored-checksum-only tamper fixture changes both local and central CRC fields while leaving `document.json` intact and now fails closed instead of being accepted by the decompression library.
- Harden native atomic save with a narrow injectable open/replace/remove boundary and one owned-root regression covering partial-write, post-sync, and pre-replace failures. Each failure preserves the existing destination byte-for-byte, exposes no partial destination, removes only its exact temporary sibling, and permits a later normal Windows save/open; an OS-locked temp is never promoted or recovered implicitly.
- Harden static PNG import with a tiny spec-derived Adam7 fixture through the production utility-safe decoder, an explicit header/decoded-dimension mismatch guard regression, and interlaced over-axis/over-pixel headers that prove the existing limits fire before decoding. Metadata sprite-sheet geometry and its existing 64MP expanded-output budget now also run before companion read/decode. This headless slice adds no corpus, dependency, APNG-interlace claim, or packaged/UI claim.
- Harden PSD utility import with deterministic top-level section-length preflight, a decoder image-data budget derived from existing 64MP-layer/16MP-composite limits, and exact header/canvas/RGBA agreement checks. A locally generated 52-byte RGB fixture covers standalone utility decode plus six truncated/hostile section variants without adding a corpus, dependency, broad parser, or compatibility claim.
- Harden PDF utility import so pdf.js loading tasks are destroyed even when malformed input rejects during load, and preflight every page viewport against the existing 256-page, 8,192-axis/16MP per-page, and 64MP cumulative budgets before any render begins. A locally generated one-page PDF, an eight-byte truncation, a hostile unterminated stream, and narrow decoder-boundary limit fixtures add headless evidence without a new parser, dependency, strictness policy, or packaged/UI claim.
- Extend malformed-PDF evidence with fixed-size missing-root, missing-page-kid, and missing-`endstream` mutations through real pdf.js load/page/render stages. Loading-task destruction is still always awaited, but a simultaneous cleanup rejection can no longer mask the primary malformed-input error; destruction-only failure still fails closed. pdf.js recovery choices, existing page/pixel budgets, and supported compatibility remain unchanged.
- Add a true Flate `/ObjStm` regression generated locally with `pdf-lib`: the valid control imports, while one-byte unknown-filter and invalid-zlib-header twins both fail during real pdf.js document load before page/render work, destroy their loading tasks, and return no document. Production and pdf.js recovery policy remain unchanged; broader filter chains, predictors, and codecs are not claimed.
- Add one real Flate `/DecodeParms` predictor family: a valid 3×2 RGB image control imports, one-byte predictor `15→16` corruption fails during pdf.js render with no result, and one-byte columns `3→0` corruption follows pdf.js's bounded render recovery. Every loading task is destroyed; production and recovery policy remain unchanged.
- Add one real two-filter PDF image family using `/ASCIIHexDecode` followed by `/FlateDecode`: the valid control imports, a same-size reversed-order twin follows bounded pdf.js render recovery, and a same-size early ASCIIHex end-marker twin fails during render with no result. All loading tasks are destroyed; no filter allowlist, decompressor policy, or production change is added.
- Add one real `/ASCII85Decode` followed by `/FlateDecode` image family: the valid control imports, while a same-size four-byte twin moves the ASCII85 `~>` marker after the first two encoded characters and fails during pdf.js render with no result. Both loading tasks are destroyed; production and pdf.js syntax policy remain unchanged.
- Add a writer-produced one-layer PSD regression: the valid 2×2 RGB control imports, while a same-size one-byte mutation makes the inner layer-record extra-data length extend exactly one byte past EOF. The existing top-level preflight delegates this inner structure to `ag-psd`, which rejects it before channel decode or canonical construction under the unchanged 320 MiB cap; production remains unchanged.
- Add a related PSD layer-channel regression: the valid first channel declares 12 bytes, while a same-size three-byte mutation declares 100 MiB plus three bytes. The pinned decoder rejects its 100 MiB-plus-one post-compression read before fallback allocation, channel decode, or canonical construction; the unchanged 320 MiB budget continues to govern decoded image data, and no new AIDraw serialized-byte policy is added.
- Add a memory-proportionate short PSD channel-overrun regression: changing the first declaration from 12 to 75 bytes ends it exactly one byte past EOF. The pinned decoder pads the missing serialized byte, then rejects the next channel header before image decode or canonical construction; no result escapes and production policy remains unchanged.
- Add a same-size in-file PSD channel-overlap regression: changing the first declaration from 12 to 13 consumes the next channel's first compression byte. The pinned decoder's realignment reaches bounded ZIP-with-prediction decode and propagates `incorrect header check`; no canonical result escapes, and production policy remains unchanged.
- Fix the transaction-policy test lifecycle race exposed by the full gate: every live document service now flushes scheduled recovery compaction before its owned temporary journal root is removed, and teardown aggregates rather than hides flush or cleanup failures. The bounded direct-constructor audit found five additional owned-root fixtures and applied the same barrier to all eighteen of their services; MCP fixtures were already protected by `McpHost.stop()`. A deterministic `RecoveryJournal.flush()` regression, independently repeated 8/8 pair, exact 38/38 lifecycle suite, and clean 108-file / 584-test replay preserve production journal scheduling, ordering, durability, and atomic-write semantics unchanged.
- Prove the existing Tiled 64-level recursive-group limit at its TMJ utility boundary: a boundary-valid 64-group map retains its exact canonical parent/child hierarchy and tile, while a depth-65 leaf rejects before its hostile geometry/data or canonical layer construction. Accepted Tiled semantics and production ordering were already correct, so this slice changes only regression and release-truth coverage.
- Prove the same established depth convention through the separate inline-TMX `xmlLayer` path: a boundary-valid 64-group XML map retains the exact hierarchy and tile, while depth 65 wins over invalid compressed leaf data before layer-data expansion, generic normalization, or canonical construction. External references and broader XML policy remain outside this test/documentation-only slice.
- Prove the established global 4,096-layer Tiled budget with a shallow TMJ that canonically retains 4,095 empty object layers plus one identifiable tile marker. Sibling 4,097 wins over its hostile geometry/base64 data before canonical construction; the existing normalization order was already correct, so this slice is regression/documentation-only.
- Add a deterministic property-style TMJ hierarchy family across mixed root/group/object/tile layouts at depths 0, 1, 2, 7, 16, 32, and 64. Canonical ordering, parent links, and marker GIDs remain exact; three generated depth-65 variants reject before hostile leaf parsing; and a nested/broad 4,096-layer map remains valid while its hostile 4,097th child rejects before expansion or canonical construction. Existing limits and production ordering were already correct, so this adds regression evidence without new parser semantics.
- Add the separate inline-TMX property family for the same established limits. Mixed XML hierarchies at depths 0, 5, 31, and 64 retain the parser's exact tile/object/group canonical category order, parent links, and GIDs; three depth-65 variants reject before invalid CSV parsing; and a 64-level spine with 63 leaves per level proves the combined 4,096-layer boundary while its deepest 4,097th hostile layer fails early. Production XML behavior remains unchanged.
- Audit exact fast-suite coverage of the existing 16,777,216-cell aggregate Tiled budget and retain the current policy unchanged. An exact accepted case requires four 4,194,304-cell decoded arrays and canonical construction maps each maximum layer into 4,194,304 change objects; a pinned-Node-24 one-million-cell measurement projects more than 350 MiB heap and 450 MiB RSS before parser/document overhead. Defer the exact corpus to an isolated opt-in high-memory child-process gate rather than adding a private seam or destabilizing the fast suite.
- Preserve the first opt-in QA-08 aggregate-cell run as a failed startup attempt: Vitest 4 rejected the obsolete `--reporter=basic` override and removed `test.poolOptions`, so the controller exited before loading the acceptance test or allocating its corpus. The fixed v1 root stayed absent, no source/evidence was produced, and no QA-08 Node process survived; the v1 guard and identities remain immutable and must not be retried or counted as acceptance.
- Certify the separately versioned Vitest-4-compatible aggregate-cell gate in one independently reviewed high-memory run. The exact production importer accepted four 4,194,304-cell layers as four canonical layers in 2,957.4787 ms at maxRSS 1,248,976 KiB, then rejected 16,777,217 total cells with the established aggregate-budget error, zero canonical layers, and maxRSS 466,216 KiB in 361.9904 ms. Source identity stayed exact, no QA child survived, and the 179,583-byte retained tree is frozen at SHA-256 `A50BFB38…C2E3`. This earns only the current Tiled aggregate boundary on that audited Node/host run; the v1 startup failure remains separate history.
- Reject cyclic illustration group-layer children during native document migration with an iterative input-linear graph check. A writer-produced acyclic `.aidraw` archive still opens, while a CRC-valid re-zipped twin differing only by one inner-to-outer back-edge fails before native open returns renderer-facing state; existing ZIP, manifest, reference, and recovery policies are unchanged.
- Extend that same iterative migration-time check to illustration object groups inside vector layers. A writer-produced acyclic two-group object hierarchy reopens with exact layer membership, while a CRC-valid twin whose only canonical mutation is one child back-edge rejects before native open returns it to renderer or export traversal; missing-reference, DAG/shared-child, parent, ZIP, manifest, and recovery policies remain unchanged.
- Apply the iterative migration-time group-cycle check to native pixel-sprite layers before normalization. A writer-produced acyclic sprite now reopens with exact child-derived root membership and unchanged frame/cel links, while a CRC-valid twin differing only by one group back-edge rejects before persistence return; sprite normalization still uses every ordered layer for cel completion, and tilemap/recovery policy remains separate.
- Extend the same pre-normalization cycle rejection and child-derived root contract to native pixel tilemaps after an independent acyclic proof exposed nested tile/object layers being promoted to roots. One orthogonal writer archive now reopens with its exact single root, GID-7 chunk, and rectangle object; its CRC-valid one-back-edge twin rejects before persistence return, while isometric and Tiled interchange semantics remain unchanged.
- Reject native pixel-sprite cel-link cycles after normalization with the same internal iterative reference-graph kernel used by group hierarchies. A writer-produced second cel linked to the first reopens with exact frame/layer/chunk/link state, while a CRC-valid twin whose only canonical mutation adds the first-to-second back-edge rejects before persistence return; absent link targets retain native recovery behavior.
- Prove the existing native checkpoint isolation boundary for that cel-link validation. A writer-produced archive with an acyclic main sprite and valid checkpoint loads both; a CRC-valid twin differing only by a two-cel back-edge inside the checkpoint returns the exact main document, excludes only that checkpoint, and emits the established exact corrupt-checkpoint warning while preserving checkpoint index metadata and every other archive entry byte-for-byte.
- Prove the established newest-32 native checkpoint input budget without changing production. A writer archive reopens exactly 32 checkpoints in original order with exact summary/document metadata and an unchanged main sprite; a CRC-valid twin prepends two older parse/migration traps, yet native open returns only the same newest 32 with no warnings or main-document change because index slicing precedes checkpoint entry parsing and migration.
- Prove native checkpoint-index corruption isolation for invalid JSON and non-array JSON without changing production. Two CRC-valid twins modify only `checkpoints/index.json`; each retains a byte-identical valid checkpoint payload but returns the exact main sprite, no checkpoints, and only the established corrupt-index warning because index validation fails before payload read or migration.
- Prove per-entry native checkpoint metadata-mismatch isolation without changing production. A two-checkpoint writer archive reopens exactly; a CRC-valid twin changes only the selected payload's `documentId`, so that entry migrates then fails the existing identity invariant, is excluded with its exact corrupt-checkpoint warning, and leaves the ordered sibling plus main sprite exact.
- Add an actor-colored, progress-bearing four-lane playback rail to both canvas modes and verify it with an exact four-session authenticated MCP regression while a real held human gesture remains responsive and canonically intact. A controlled comparison proved the prior GPU-child `0xC0000135` failure came from the Codex execution sandbox, so packaged Playwright now fails closed until its Windows launch is explicitly declared unsandboxed.
- No stable-v1 compatibility promise applies yet; native format changes still require explicit migrations.

## [0.1.0-alpha.1] - 2026-08-04

First local prerelease baseline of the agent-native Windows editor. It includes the editor-independent document engine, authenticated localhost MCP, durable attributed traces and replay, illustration and indexed-pixel workflows, animated export, file/generation approval jobs, and native `.aidraw` persistence.

This version is not a v1 release. Advanced illustration, animation/map authoring, interchange fidelity, provider mock coverage, performance/accessibility gates, and public release evidence remain tracked work.
