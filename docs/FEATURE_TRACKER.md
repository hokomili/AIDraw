# AIDraw feature tracker

Last audited: 2026-08-14

This is the source of truth for implementation status. The README and design documents describe the intended v1 product; an item is not complete merely because its data type, dependency, or menu entry exists.

## Status rules

| Status | Meaning |
| --- | --- |
| ✅ Verified | The workflow is implemented and covered by an automated acceptance, contract, or regression test. |
| 🟢 Working | The workflow is usable in the current editor, but its acceptance coverage is incomplete. |
| 🟡 Partial | A useful subset works, but a material part of the promised workflow or fidelity is absent. |
| 🟠 Scaffolded | The model or interchange path exists, but human authoring is too thin to call the feature usable. |
| ⬜ Missing | No meaningful implementation exists yet. |
| ⏸ Deferred | Explicitly outside the selected v1 scope. |

Priorities are `P0` release blocker, `P1` core v1 workflow, `P2` completeness/fidelity, and `P3` polish. These values express release significance, not a standalone execution queue: the numbered **Recommended critical path** is the binding work-selection order, a later-step P0 remains release-blocking without preempting an earlier numbered P1 product step, and unrelated same-tier work does not supersede the selected path. A feature may move to **Verified** only when its exit criteria have an automated test.

## Current release truth

- A source/headless PIX-08 seamless-sprite checkpoint (2026-08-15) upgrades the existing repeated-copy preview into an explicit **Wrap edit** mode. Repeated-copy coordinates map through one exact modulo kernel before the existing indexed cel mutation: freehand/eraser/shape/dither/lighten/darken gestures, reusable stamps, and bitmap text wrap across both axes; eyedropper, wand, fill, and replace accept a repeated-copy seed. Raw gesture order survives until the ordinary writer's deterministic last-write behavior, while the existing full-sprite human lock, expected cel revision, one transaction, actor undo, palette indices, linked-cel resolution, replay, native persistence, and exporters remain authoritative. Rectangle/lasso selections and flood connectivity stay bounded rather than toroidal, and the session-local control adds no canonical schema, map GID, Tiled field, or AGT-11 operation. Pure and renderer-source coverage proves negative/multi-period coordinates, payload/order retention, one exact commit/inverse, stamp/text/dither/sample wiring, lock scope, and the named control. Focused pinned-Node-24 typecheck/ESLint and **5 files / 36 tests** pass. Authored symmetry axes, mode persistence, animated-cycle export, packaged pointer/visual evidence, Aseprite round-trip, broad parity, Level 3, RC, and stable-v1 remain unclaimed; PIX-08 stays Working/P2.
- A source/headless IO-08/QA-08 Tiled-reference-integrity checkpoint (2026-08-13) fails whole before body or PNG rendering when recoverable canonical state cannot produce one unambiguous artifact. One shared plan runs after the established hierarchy/cell resource preflight, caps maps at the importer’s existing **1,024 tilesets**, requires each declared tileset and source sprite, rejects duplicate references plus overlapping or 28-bit-overflowing base-GID ranges, and resolves every nonzero value the writer emits. Finite validation follows the writer's last-write nominal plane and therefore ignores already-clipped out-of-plane storage; infinite validation covers every stored signed chunk cell without deriving bounds or origin. JSON/XML consume those admitted arrays directly rather than decoding/rebuilding them. Pure and production JSON/XML coverage proves missing-source/reference, duplicate, exact count, range, transformed-GID, unresolved signed-coordinate, finite-clipping, and early-before-render behavior. Focused pinned-Node-24 typecheck/ESLint and **4 files / 36 tests** pass; the maintained integrity/object-ID/resource/artifact/export/import/utility/Tiled corpus passes **8 files / 115 tests**. The complete safe gate passes portability for **369 tracked + 148 untracked paths**, TypeScript, full ESLint, and **188 files / 1,109 tests**. The unchanged formal performance gate passes **1/1** at **945.22 MiB** RSS, with dense regional illustration at **121.81 ms**, native save at **129.33 ms**, and PNG export at **42.89 ms**; no Tiled-integrity timing or exact utility-process peak is claimed. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and **275 dirty paths (127 modified + 148 untracked)**. Failure never repairs, renumbers, rebases, drops, or mutates canonical recovery state. This adds no new bounds/origin, arbitrary-field/external-producer or broad Tiled compatibility, packaged/native, Level 3, RC, or stable-v1 evidence.
- A source/headless IO-08/QA-08 Tiled-object-ID checkpoint (2026-08-13) replaces JSON digit concatenation and XML per-group index resets with one deterministic positive-uint32 namespace across the exact inline tileset collision records and map object layers emitted by an artifact. An exact decimal canonical ID survives only when in range and unique; opaque, out-of-range, and duplicate occurrences receive the lowest unreserved values in canonical tileset-then-layer order. Pure reservation/duplicate/boundary coverage plus production JSON/XML parity and TMJ production-utility import/re-export pass. Focused pinned-Node-24 typecheck/ESLint and **3 files / 30 tests** pass; the maintained object-ID/resource/artifact/export/import/utility/Tiled corpus passes **7 files / 109 tests**. The complete safe gate passes portability for **369 tracked + 146 untracked paths**, TypeScript, full ESLint, and **187 files / 1,103 tests**. The unchanged formal performance gate passes **1/1** at **951.28 MiB** RSS, with dense regional illustration at **112.34 ms**, native save at **126.83 ms**, and PNG export at **41.62 ms**; no object-ID timing is claimed. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and **273 dirty paths (127 modified + 146 untracked)**. The output ID is an artifact identity; arbitrary canonical string IDs are not claimed as losslessly portable, and canonical state is never rewritten. This adds no foreign object-reference/template semantics, external-tileset or broad Tiled compatibility, packaged/native, Level 3, RC, or stable-v1 evidence.
- A source/headless IO-08/QA-08 Tiled-companion planning checkpoint (2026-08-13) plans the exact PNG member set before JSON/XML source rendering. Canonical tileset order retains the first sanitized leaf and gives later filename-, case-, or Unicode-normalization collisions deterministic ` (2)`, ` (3)`, … suffixes. Export bodies, emitted companions, utility-result admission, and approval-time destination previews share that plan. The **4,097-member** worker-result envelope retains its at-most-**4,096 PNG companion** defense, but Layer 118 supersedes that map-capacity interpretation: a map now reaches the narrower existing **1,024-tileset import boundary** first. Collision-family coverage, production JSON/XML name/reference and early-rejection regressions, and one TMJ write/re-import with two distinct source pixels pass. Focused pinned-Node-24 typecheck/ESLint and **3 files / 29 tests** passed at that checkpoint; the maintained resource/policy/export/import/utility/Tiled corpus passed **6 files / 106 tests**. Its complete safe gate passed portability for **369 tracked + 144 untracked paths**, TypeScript, full ESLint, and **186 files / 1,100 tests**. The unchanged formal performance gate passed **1/1** at **963.89 MiB** RSS, with dense regional illustration at **99.18 ms**, native save at **126.13 ms**, and PNG export at **41.4 ms**; no Tiled-companion timing is claimed. The audit remained Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and **271 dirty paths (127 modified + 144 untracked)**. Disambiguation was chosen over rejecting valid artwork because each resolved image leaf is explicit in Tiled output; canonical tileset names/IDs/order/pixels/GIDs, map coordinates/mode/bounds/origin, file authority, and supported fields remain unchanged. This is exact AIDraw companion-set evidence, not universal filesystem equivalence, broad/external Tiled compatibility, packaged/native, Level 3, RC, or stable-v1 evidence.
- A source/headless IO-08/QA-08 Tiled-export resource-preflight checkpoint (2026-08-13) makes JSON and XML writers walk the exact emitted map-layer tree before tileset companion rendering, chunk decode, or finite-plane allocation. One shared policy now supplies import and export with the existing **4,096-layer**, depth-**64**, **100,000-object**, **4,194,304-cell-per-layer**, and **16,777,216-cell aggregate** ceilings. Finite tile layers count their complete width×height output; infinite layers count every stored chunk rectangle they serialize, including an all-zero record, without decoding occupancy or deriving visual bounds. Exact boundary/one-over, broad/deep/cycle/object, finite/infinite payload, and production early-rejection coverage pass. Focused pinned-Node-24 typecheck/ESLint and **2 files / 19 tests** pass; the maintained policy/export/import/Tiled corpus passes **4 files / 70 tests**. The complete safe gate passes portability for **369 tracked + 143 untracked paths**, TypeScript, full ESLint, and **185 files / 1,095 tests**. The unchanged formal performance gate passes **1/1** at **969.28 MiB** RSS, with dense regional illustration at **97.06 ms**, native save at **124.11 ms**, and PNG export at **41.4 ms**; no new Tiled-export timing is claimed. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 270 dirty paths (127 modified + 143 untracked). Rejection emits no partial artifact and does not silently omit, flatten, split, change map mode, or raise AIDraw's established resource posture. Canonical coordinates/GIDs/layers/visibility, nominal dimensions, infinite-map bounds/origin, companion pixels/names, file authority, and supported fields remain unchanged. These are AIDraw round-trip/resource limits, not Tiled maxima, broad compatibility, packaged/native, Level 3, RC, or stable-v1 evidence.
- A source/headless FND-10/FND-12/QA-07 regional-illustration object-culling checkpoint (2026-08-13) keeps the established exact renderable subset and four-pixel backing, but stops submitting a shape/image leaf to Canvas or decoding its PNG when the leaf's conservative integer destination frame—plus already-admitted structural-group translation—is disjoint from that backing. The renderer still scans all leaves in canonical order; unsupported leaves or unsafe accumulated arithmetic remain included, and half-open bounds exclude only empty edge contact. Pure bounds cover boundary contact, parent translation, and conservative unsafe fallback. A production maximum-artboard regression returns the far-edge target pixel while proving a distant valid PNG payload is inspected once for eligibility but never decoded. The formal gate builds 100,000 eligible rectangles, runs the complete eligibility/graph/frame scan, renders only the final requested leaf, and passes at **95.33 ms** under **500 ms**. Focused pinned-Node-24 typecheck/ESLint and **3 files / 30 tests** pass. The complete safe gate passes portability for **369 tracked + 141 untracked paths**, TypeScript, full ESLint, and **184 files / 1,091 tests**; the formal gate passes **1/1** at **978.16 MiB** RSS, with straight-primitive, materialized-paint, embedded-PNG, and dense-object regions at **0.39 / 0.47 / 0.10 / 95.33 ms**, native save at **129.79 ms**, and PNG export at **41.06 ms**. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 268 dirty paths (127 modified + 141 untracked). This is an O(total leaves) work filter, not a spatial index or new renderer; canonical objects/order/coordinates, pixels, asset/decode authority, observation limits/results, save/export/interchange, and the exact eligibility subset remain unchanged. No editor culling, richer leaf, packaged/native, Level 3, RC, or stable-v1 claim is added.
- A source/headless FND-10/FND-12/QA-07 integer-phase embedded-PNG regional-observation checkpoint (2026-08-13) admits only embedded `image/png` objects with retained data, positive safe-integer source and displayed dimensions, optional safe-integer in-bounds source crop, integer placement or structural-group translation, and the established full-opacity/normal/no-effect layer contract. The production renderer remains the single Canvas authority on the four-pixel-expanded backing. A generated partial-alpha matrix proves native-size, upsampled, downsampled, cropped, nonuniformly scaled, and integer-group-translated results byte-for-byte against full-artboard raw crops across two backing offsets and opaque/transparent backgrounds; a maximum-artboard production observation and separate formal tripwire address a far-edge 16×16 PNG without allocating the 64-megapixel artboard. Fractional or incomplete geometry, out-of-bounds crops, JPEG/WebP/GIF/APNG, masks, filters, effects, partial/non-normal composites, and every previously excluded vector/paint case retain full-artboard render plus raw crop. Focused pinned-Node-24 typecheck/ESLint and **3 files / 28 tests** pass. The complete safe gate passes portability for **369 tracked + 141 untracked paths**, TypeScript, full ESLint, and **184 files / 1,089 tests**; the formal gate passes **1/1** at **869.88 MiB** RSS, with straight-primitive, materialized-paint, and embedded-PNG regions at **1.28 / 1.19 / 0.10 ms**, native save at **134.4 ms**, and PNG export at **42.2 ms**. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 268 dirty paths (127 modified + 141 untracked). Asset admission/decode authority, source bytes, canonical image/crop model, save/export/interchange, observation limits/results, and richer fallbacks remain exact. This proves only the named generated PNG cases; it adds no broad PNG/raster compatibility, external-producer, packaged/native, Level 3, RC, or stable-v1 claim.
- A source/headless FND-10/ILL-18/FND-12/QA-07 complete-materialized-paint regional-observation checkpoint (2026-08-13) admits a visible paint layer to the established exact bounded illustration renderer only when its structurally valid materialized tile cache covers every editable stroke inside default full-opacity/normal layers. The production renderer scans the cache index but decodes and copies only 256×256 tiles intersecting the four-pixel-expanded backing. Two regional offsets under transparent/opaque backgrounds match full-artboard raw crops byte-for-byte; a second maximum-artboard production observation and formal tripwire address one cached far-edge paint pixel without allocating the 64-megapixel artboard. This reuses the save/open cache contract because it already preserves canonical editable strokes and exact rendered tiles; regional replay of live brush geometry or an invented second cache/origin contract was rejected. An appended live stroke, missing/incomplete cache, unsafe tile arithmetic, opacity/blend/effect/mask, and every previously excluded vector case retain full-artboard render plus raw crop. Focused pinned-Node-24 typecheck/ESLint and **3 files / 25 tests** pass. The complete safe gate passes portability for **369 tracked + 141 prospective paths**, TypeScript, full ESLint, and **184 files / 1,086 tests**; the formal gate passes **1/1** at **874.63 MiB** RSS, with the straight-primitive region at **0.36 ms**, materialized-paint region at **0.52 ms**, native save at **125.48 ms**, and PNG export at **40.21 ms**. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 268 dirty paths (127 modified + 141 untracked). Cache validation/materialization, canonical strokes, save/export/interchange, observation limits/results, and the straight-primitive subset remain exact. No live-stroke regional renderer, generic paint/vector clipper, spatial index, packaged/native proof, Level 3, RC, or stable-v1 readiness is added.
- A source/headless FND-10/FND-12/QA-07 straight-primitive regional-illustration checkpoint (2026-08-13) expands the established four-pixel-backed exact observation path from square-corner rectangles to integer-sized/positioned polygons, stars, and structural groups that add only integer translation. Solid or empty fill, no visible stroke, full opacity, normal compositing, and default nonisolating layers remain required. A deterministic matrix varies positions, sizes, side counts, backing offsets, opaque/transparent backgrounds, and grouped translucent overlap while matching the full-artboard raw crop byte-for-byte; an 8,192×8,192 production observation now addresses a filled star without allocating the 64-megapixel artboard. Candidate ellipse and rounded-rectangle curves produced actual channel differences and remain on the full-artboard fallback with fractional geometry, gradients, strokes, paths, text/images, paint, masks, effects, isolation, rotation/scale/skew, and every unproved curve. Focused pinned-Node-24 typecheck/ESLint and **3 files / 23 tests** pass. The complete safe gate passes portability for **369 tracked + 141 prospective paths**, TypeScript, full ESLint, and **184 files / 1,084 tests**; the formal gate passes **1/1** at **869.23 MiB** RSS, with the filled-star region at **0.35 ms**, native save at **122.11 ms**, and PNG export at **40.3 ms**. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 268 dirty paths (127 modified + 141 untracked). Canonical model, artboard, observation budget/result, save/export/interchange behavior, and richer fallback remain exact. No generic regional renderer, ellipse/rounded-curve/stroke/gradient claim, packaged/native proof, Level 3, RC, or stable-v1 readiness is added.
- A source/headless FND-07/QA-08 graceful-shutdown integrity checkpoint (2026-08-13) makes ordinary Electron/OS quit wait for the established engine stop contract instead of launching it in an unobserved `void` promise. Repeated `before-quit` events prevent exit and share one stop; only successful MCP shutdown, utility cancellation, and final recovery compaction mark the coordinator complete and reissue `app.quit()`, whose next event passes through. A failed stop keeps quit cancelled, reports once to stderr per coalesced OS request, clears the coordinator for retry, and leaves `EngineRuntime.started` true until a later complete stop. Deterministic barriers prove no early/duplicate quit, repeated-event coalescing, failure cancellation/reporting/retry, explicit/OS production routing, concurrent runtime-stop sharing, and retry after injected final-compaction failure. Focused pinned-Node-24 typecheck/ESLint and **4 files / 39 tests** pass. The complete safe gate passes portability for **369 tracked + 141 prospective paths**, TypeScript, full ESLint, and **184 files / 1,084 tests**; the formal gate passes **1/1** at **878.36 MiB** RSS, with native save at **127.07 ms** and PNG export at **41.64 ms**. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 268 dirty paths (127 modified + 141 untracked). Existing quit confirmation, CLI/no-runtime exit, stop order, recovery formats, headless lifecycle, and live-state/journal failure boundary remain exact. No timeout/force-quit UX, crash/power-loss/directory-fsync guarantee, packaged OS-quit acceptance, Level 3, RC, or stable-v1 claim is added.
- A source/headless FND-13/AGT-14/QA-08 start-at-login integrity checkpoint (2026-08-13) serializes enable/disable mutations in invocation order within one process. Windows/macOS retain their existing native Electron calls; Linux replaces its reused PID temporary with the shared unique private same-directory writer, complete write, file sync, close, and atomic replacement. A blocked enable followed by disable now returns exact enabled-then-disabled results and leaves the later intent authoritative. Injected replacement failure preserves an existing XDG entry byte-for-byte, removes temporary residue, and leaves the queue usable for successful retry; the exact desktop command and POSIX mode `0600` remain covered. Focused pinned-Node-24 typecheck/ESLint and **6 files / 37 tests** pass. The complete safe gate passes portability for **369 tracked + 138 prospective paths**, TypeScript, full ESLint, and **182 files / 1,079 tests**; the formal gate passes **1/1** at **876.27 MiB** RSS, with native save at **126.15 ms** and PNG export at **40.97 ms**. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 265 dirty paths (127 modified + 138 untracked). Existing platform support/status, executable/arguments, XDG path/entry, macOS launch suppression, consent/client setup, and unsupported-development behavior remain exact. No cross-process/multiple-instance coordination, desktop-environment compatibility expansion, directory-fsync/power-loss durability, packaged login/reboot interaction, Level 3, RC, or stable-v1 claim is added.
- A source/headless AGT-14/IO-10/QA-08 agent-client-config integrity checkpoint (2026-08-13) serializes every automatic Codex, Claude Code, OpenCode, and Antigravity config update within one process. Each request re-reads current bytes after its predecessor, preserves that exact existing version in a distinct timestamp-based private backup, then publishes one complete mode-0600 config through the shared flushed atomic writer. Deterministic barriers prove same-path invocation order, same-timestamp backup uniqueness, exact intermediate-version retention, final second-token authority, private POSIX modes, replacement-failure byte preservation and temporary cleanup, and queue recovery. A separate tested completion boundary retains `configured` status plus config/backup paths when start-at-login enablement throws and tells the user to start AIDraw manually; isolated packaged validation still does not invoke startup changes. Focused pinned-Node-24 typecheck/ESLint and **5 files / 29 tests** pass. The complete safe gate passes portability for **369 tracked + 138 prospective paths**, TypeScript, full ESLint, and **182 files / 1,077 tests**; the formal gate passes **1/1** at **884.02 MiB** RSS, with native save at **126.4 ms** and PNG export at **40.61 ms**. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 263 dirty paths (125 modified + 138 untracked). Existing consent, client schemas/paths, unrelated settings/comments, generic manual setup, restart instructions, and login-start mechanisms remain exact. No config size ceiling, cross-process/multiple-instance lock or compare-and-swap guarantee, directory-fsync/power-loss durability, live client restart/discovery, packaged interaction, Level 3, RC, or stable-v1 claim is added.
- A source/headless AGT-01/AGT-04/IO-10/QA-08 MCP-connection-handoff checkpoint (2026-08-13) publishes the explicitly requested plaintext bootstrap token through the shared private atomic writer and enforces the documented nonempty absolute output path instead of resolving a relative destination. A unique same-directory mode-0600 temporary is completely written, flushed, and closed before it replaces the named destination, so readers see the prior artifact or one complete version-one handoff and a formerly permissive POSIX destination becomes private. Injected replacement failure preserves the exact prior bytes and leaves no temporary; production source excludes its former direct write. Focused pinned-Node-24 typecheck/ESLint and **2 files / 9 tests** pass. The complete safe gate passes portability for **369 tracked + 138 prospective paths**, TypeScript, full ESLint, and **182 files / 1,073 tests**; the formal gate passes **1/1** at **869.41 MiB** RSS, with native save at **133.71 ms** and PNG export at **42.87 ms**. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 261 dirty paths (123 modified + 138 untracked). Version-one fields, absolute path/overwrite authority, token/server values, process-lifetime trusted folders, import/redaction responsibility, and network behavior remain exact. The final named entry is replaced, not updated through its old inode; no parent-path canonicalization, cross-process writer lock, directory-fsync/power-loss, packaged launcher, Level 3, RC, or stable-v1 claim is added.
- A source/headless AGT-01/GEN-05/QA-08 MCP-token-integrity checkpoint (2026-08-13) makes one local bearer-token store process-serial and atomically persistent. Concurrent missing-token requests now converge on the first stored canonical 32-byte base64url credential. Version-one input requires exactly `version`, `encryption`, and canonical nonempty base64 `value`; successful decryption must recover that canonical token shape. Missing, over-ceiling, malformed, unknown-field, noncanonical, or decrypt-failed state retains the established automatic rotation, but the encrypted replacement uses the shared unique private mode-0600 same-directory temporary, full write, file flush, close, and atomic replacement before returning the token. Empty encryption or injected replacement failure leaves prior bytes exact, cleans the temporary, returns no token, and does not poison retry ordering. Focused pinned-Node-24 typecheck/ESLint and **3 files / 15 tests** pass. The complete safe gate passes portability for **369 tracked + 136 prospective paths**, TypeScript, full ESLint, and **181 files / 1,069 tests**; the formal gate passes **1/1** at **875.22 MiB** RSS, with native save at **131.45 ms** and PNG export at **42.06 ms**. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 259 dirty paths (123 modified + 136 untracked). Token bytes/format, encrypted-file ceiling, `safeStorage` backend requirement, corrupt-secret rotation, connection authority, and renderer exposure remain exact. No cross-process/multiple-instance lock, directory-fsync/power-loss guarantee, native backend/package proof, Level 3, RC, or stable-v1 claim is added.
- A source/headless FND-06/FND-07/FND-08/QA-08 advisory native-trace import checkpoint (2026-08-13) keeps validated artwork open when its separate local trace-store append fails. Native parsing warnings retain their order; the failure emits `trace-error`, adds one addressed transaction-history warning, and still returns the already-installed document as opened and active. A generated revision-one artwork/trace pair plus one malformed trace record proves exact filtered import input, warning/error order, immediate revision-two editing, and fresh journal recovery. Focused pinned-Node-24 typecheck/ESLint and 2 files / 35 tests pass. The complete safe gate passes portability for 369 tracked + 136 prospective paths, TypeScript, full ESLint, and 181 files / 1,067 tests; the formal gate passes 1/1 at 882.7 MiB RSS, with native save at 133.72 ms and PNG export at 42.32 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 259 dirty paths (123 modified + 136 untracked). Canonical artwork remains authoritative over auxiliary history; no retry/completeness/atomicity, archive/schema, broad native, packaged, Level 3, RC, or stable-v1 claim is added. Live-state-versus-recovery-journal write failure remains a separate durability decision.
- A source/headless FND-04/FND-07/AGT-06/QA-08 same-document mutation-ordering checkpoint (2026-08-13) serializes canonical apply, undo, redo, and checkpoint-restore requests in invocation order for each document while leaving different documents independent. Async transaction admission rechecks both the original document incarnation and human lock state before commit, so a delayed image validation cannot let a later rename/history command overtake it or mutate a same-ID replacement after close. Deterministic barriers prove apply → rename → undo → redo reaches revisions 1–4 in order while another document commits independently, checkpoint restore preserves a pending committed branch before restoring, both outcomes survive journal recovery, and a stale old-incarnation transaction returns an explicit conflict without touching replacement state. Focused pinned-Node-24 typecheck/ESLint and 2 files / 34 tests pass. The complete safe gate passes portability for 369 tracked + 136 prospective paths, TypeScript, full ESLint, and 181 files / 1,066 tests; the formal gate passes 1/1 at 885.55 MiB RSS, with native save at 128.02 ms and PNG export at 40.8 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 259 dirty paths (123 modified + 136 untracked). This preserves reducer conflicts, per-actor history, journal/trace formats, visible playback, revision-bound save semantics, and cross-document concurrency; it adds no global ordering, cross-process lock, cancellation/coalescing, persistent undo stack, packaged interaction proof, Level 3, RC, or stable-v1 claim.
- A source/headless FND-06/FND-07/QA-08 document-identity-admission checkpoint (2026-08-13) makes the canonical document ID a fail-closed unique workspace key. Reopening the exact already-open path stays idempotent and preserves live dirty state; a different native file with the same ID emits an explicit warning and cannot replace the existing document, histories, checkpoints, trace association, or recovery snapshot. Utility imports now preflight the complete returned document set against open and within-set IDs, clone every document before mutation, and publish the successful set once with its final document active; any collision rejects that set before a sibling is installed. Deterministic coverage proves dirty same-path reopen, copied-file refusal while a noncolliding sibling still opens, exact recovery preservation, and atomic imported-set rejection without a workspace publication. Focused pinned-Node-24 typecheck/ESLint and 2 files / 26 tests pass. The complete safe gate passes portability for 369 tracked + 136 prospective paths, TypeScript, full ESLint, and 181 files / 1,063 tests; the formal gate passes 1/1 at 897.69 MiB RSS, with native save at 137.72 ms and PNG export at 48.75 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 259 dirty paths (123 modified + 136 untracked). Closing the existing document before opening its copy is the explicit path; no re-key, clone-as-new, merge, alias, realpath/symlink identity, schema/ID rewrite, broad native compatibility, packaged warning acceptance, Level 3, RC, or stable-v1 claim is added.
- A source/headless FND-06/FND-07/FND-09/ILL-18/QA-08 revision-bound-native-save checkpoint (2026-08-13) captures one immutable document/checkpoint/preview revision at save invocation, admits only trace entries no newer than that revision, and serializes same-document saves in request order through a rejection-recovering queue. A later canonical edit remains outside the older archive, keeps the live document dirty after that destination becomes its file association, safely reuses any persisted paint-cache prefix, and is compacted into recovery instead of being mislabeled saved. An unchanged revision becomes clean normally. Incarnation identity prevents a completion from a closed/replaced document from mutating or resurrecting its replacement. Deterministic coverage proves the archive/preview/trace revision, later dirty live and recovered state, same-document queue blocking and recovery after a failed first preview, and stale-close/replacement isolation. Focused pinned-Node-24 typecheck/ESLint and 1 file / 20 tests pass. The complete safe gate passes portability for 369 tracked + 136 prospective paths, TypeScript, full ESLint, and 181 files / 1,061 tests; the formal gate passes 1/1 at 803.42 MiB RSS, with native save at 127.7 ms and PNG export at 40.7 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 259 dirty paths (123 modified + 136 untracked). Existing archive schema, atomic replacement, overwrite behavior, trace/checkpoint formats, oversized preview fallback, save paths, and cross-document concurrency remain unchanged. No save cancellation/coalescing, cross-process locking, directory-fsync/power-loss guarantee, packaged interaction evidence, Level 3, RC, or stable-v1 readiness is added.
- A source/headless ILL-01/IO-09/QA-08 application-store-integrity checkpoint (2026-08-13) serializes every reusable-preset and interchange-report list/get/save/delete/record request within one runtime. Each mutation builds copied state, writes the complete JSON through a unique exclusive private temporary, flushes and closes it, atomically replaces the destination, and publishes the copy only after success. Deterministic barriers prove concurrent preset saves and report records survive restart in invocation order; injected update/delete/report replacement failures leave prior file bytes and live state exact, best-effort remove temporaries, and do not poison later reads. Focused pinned-Node-24 typecheck/ESLint and 2 files / 7 tests pass. The complete safe gate passes portability for 369 tracked + 136 prospective paths, TypeScript, full ESLint, and 181 files / 1,058 tests; the formal gate passes 1/1 at 899.92 MiB RSS, with native save at 126.51 ms and PNG export at 45.77 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 259 dirty paths (123 modified + 136 untracked). Existing version-one shapes, normalization/recovery behavior, 64-preset and 200-report caps, structured fidelity reasons, and UI/API contracts remain unchanged. No cross-process or multi-instance locking, store byte ceiling, directory-fsync/power-loss guarantee, packaged persistence evidence, broader interchange claim, Level 3, RC, or stable-v1 readiness is added; ILL-01, IO-09, and QA-08 remain Working.
- A source/headless PAR-09/QA-08 durable-batch-integrity checkpoint (2026-08-13) prevents a committed batch operation ID from advancing another sequence, while preserving exact retry-as-duplicate behavior at the original sequence. The version-one ledger now admits only exact writer-shaped roots and records, ignores invalid records, and replaces stale in-memory resumability with empty state when the root is invalid. Every start/resume/prepare/finish/cancel transition is copied, flushed through a unique private temporary, atomically replaced, and installed in memory only after success; injected replacement failures leave prior file/live state byte- and progress-exact, with prepare and finish independently retryable. Focused pinned-Node-24 typecheck/ESLint and 2 files / 42 tests pass. The complete safe gate passes portability for 369 tracked + 135 prospective paths, TypeScript, full ESLint, and 181 files / 1,056 tests; the formal gate passes 1/1 at 886.67 MiB RSS, with native save at 132.93 ms and PNG export at 41.3 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 256 dirty paths (121 modified + 135 untracked). Resume tokens remain hashed; canonical drawing/journal/trace content, valid restart recovery, batch count/transaction limits, and cancellation stay unchanged. No ledger-size cap, directory-fsync/power-loss guarantee, cross-batch key policy, packaged restart, Level 3, RC, or stable-v1 claim is added.
- A source/headless IO-10/QA-04/QA-08 MCP-control-state checkpoint (2026-08-13) admits persisted folder trust only from the exact version-one writer shape: no extra fields, one unique array of absolute NUL-free paths, and all canonical identities staged before any authority is installed. Every start clears and replaces the in-memory persistent set, so malformed scalar state cannot be interpreted as path fragments and a corrupt restart cannot retain stale authority. Preferred-port state similarly requires the exact version-one object and an integer from 48200 through 48231; invalid state returns to the ordinary first-available numeric scan. Both files now use private flushed temporary files and atomic replacement; persistent grants serialize, enter memory only after replacement, and drain during stop. Focused pinned-Node-24 typecheck/ESLint and 1 file / 36 tests pass. The complete safe gate passes portability for 369 tracked + 135 prospective paths, TypeScript, full ESLint, and 181 files / 1,054 tests; the formal gate passes 1/1 at 875.58 MiB RSS, with native save at 127.99 ms and PNG export at 42.28 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 254 dirty paths (119 modified + 135 untracked). Valid persistent/session/launch trust, overwrite approval, canonical path behavior, port range, settings locations, and file-operation authority are unchanged. No settings byte/count limit, local-file authentication, packaged restart, Level 3, RC, or stable-v1 claim is added; IO-10, QA-04, and QA-08 remain Working.
- A source/headless GEN-05/AGT-01/QA-08 encrypted-credential-admission checkpoint (2026-08-13) applies one shared 256 KiB handle-bound ceiling before reading either hosted-provider ciphertext or the localhost MCP bearer-token file. Oversized or observably changing files never reach JSON or `safeStorage`; provider status retains the established corrupt-file recovery and reports no stored keys until explicit rotation, while the MCP token retains its established corrupt-secret recovery and rotates to a new encrypted token. Focused pinned-Node-24 typecheck/ESLint and 5 files / 29 tests pass. The complete safe gate passes portability for 369 tracked + 135 prospective paths, TypeScript, full ESLint, and 181 files / 1,052 tests; the unchanged formal gate passes 1/1 at 886.45 MiB RSS, with native save at 143.55 ms and PNG export at 42.01 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 254 dirty paths (119 modified + 135 untracked). This changes no secret/ciphertext exposure, native backend, secure-storage availability, provider rotation/removal, MCP connection authority, unrelated user-data store, packaged behavior, exact peak-RSS, Level 3, RC, or stable-v1 claim.
- A source/headless FND-09/IO-01/IO-07/QA-08 bounded-file-read checkpoint (2026-08-13) makes established local-source ceilings effective during reads for root/companion interchange inputs, direct sprite sheets, native archives, human/approved-agent palettes, and project-link relinks. One shared reader performs preliminary and handle-bound regular-file/limit checks, uses a nonblocking open, allocates only the handle size plus one sentinel, and rejects observable growth or shrinkage before parser/decode/canonical work. Exact boundary bytes and every existing 1 MiB/1.5 MB/16 MiB/256 MiB/512 MiB ceiling remain unchanged. Focused pinned-Node-24 typecheck/ESLint and 6 files / 86 tests pass. The complete safe gate passes portability for 369 tracked + 134 prospective paths, TypeScript, full ESLint, and 180 files / 1,048 tests; the unchanged formal gate passes 1/1 at 863.11 MiB RSS, with native save at 129.81 ms and PNG export at 41.76 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 251 dirty paths (117 modified + 134 untracked). This is bounded whole-file admission, not streaming, and adds no format/authority/compatibility, path-identity or arbitrary same-size mutation snapshot, exact peak-RSS, packaged UI, Level 3, RC, or stable-v1 claim. FND-09, IO-01, IO-07, and QA-08 remain Working.
- A source/headless FND-09/GEN-06/QA-08 generation-approval-preview checkpoint (2026-08-13) moves MCP approval source/mask decode and thumbnail rendering from the Electron browser process to the supervised raster lane, separate from potentially long provider work. Main admits the canonical asset's exact bytes/hash/MIME/header/display geometry under the existing 1.5 MB, 8,192-side, and 16MP limits; the worker returns a no-enlarge 180×120-fit static PNG under 256 KiB; main checks canonical base64, chunk/CRC structure, exact preview geometry, and full RGBA decode. A malformed producer retires and queued work recovers fresh. Preview failure remains advisory: exact asset IDs and paid-request fields still reach approval without silently granting or denying it. Focused pinned-Node-24 typecheck/ESLint and 4 files / 80 tests pass. The complete safe gate passes portability for 369 tracked + 132 prospective paths, TypeScript, full ESLint, and 179 files / 1,044 tests; the unchanged formal gate passes 1/1 at 863.5 MiB RSS, with native save at 126.29 ms and PNG export at 40.51 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 249 dirty paths (117 modified + 132 untracked). Provider requests/calls, approval options/expiry, canonical state, source bytes, network/file authority, format semantics, packaged UI, exact worker peak RSS, Level 3, RC, and stable-v1 readiness are unchanged or unclaimed. FND-09, GEN-06, and QA-08 remain Working.
- A source/headless FND-09/IO-01/QA-03/QA-08 sprite-sheet selection checkpoint (2026-08-13) moves picker/approved-agent source reads, codec decode, and picker preview generation into the supervised raster lane. Worker input is one explicit absolute path; the unchanged 1,500,000-byte retained-source ceiling applies before decode, header truth selects PNG/JPEG/WebP MIME, and header display geometry must agree with the decoder. Picker output is a no-enlarge static PNG at most 640 wide or 480 high, admitted by main through canonical base64, 4 MiB, chunk/CRC, exact geometry, and full decode checks. Human import rechecks the selected SHA-256/MIME; approved-agent slicing derives both in its import worker without a browser-process file read. The frozen EXIF-orientation-6 JPEG previews at 16×24, slices into four 8×12 frames, and remains byte-exact in the document. Focused pinned-Node-24 typecheck/ESLint and 5 files / 85 tests pass. The complete safe gate passes portability for 369 tracked + 132 prospective paths, TypeScript, full ESLint, and 179 files / 1,043 tests; the unchanged formal gate passes 1/1 at 884.63 MiB RSS, with native save at 126.17 ms and PNG export at 41.14 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 249 dirty paths (117 modified + 132 untracked). Display-oriented slicing was chosen so visible preview/grid pixels agree; exact source retention, picker extensions, slicing/quantization, canonical schema, selection TTL/cap, and authority remain unchanged. Animated-source semantics, arbitrary producers, packaged UI, exact worker peak RSS, broad compatibility, Level 3, RC, and stable-v1 readiness are not claimed. FND-09, IO-01, QA-03, and QA-08 remain Working.
- A source/headless FND-09/GEN-06/QA-08 generation-comparison integrity checkpoint (2026-08-13) separates retained comparison sources from provider outputs. The first selected canonical PNG/APNG/JPEG/WebP/GIF preserves its exact bytes, truthful MIME, and validated header/display geometry without an Electron `nativeImage` re-decode; an unselected canvas source instead renders one isolated document snapshot through supervised PNG export after the established full-size static-raster preflight. Main-side admission copies the result and requires a static PNG whose header dimensions exactly match that snapshot under the existing utility-result ceiling. Malformed or contradictory rendering creates no job and dispatches no provider request. Focused pinned-Node-24 typecheck/ESLint and 4 files / 48 tests pass. The complete safe gate passes portability for 369 tracked + 131 prospective paths, TypeScript, full ESLint, and 179 files / 1,038 tests; the unchanged formal gate passes 1/1 at 868.86 MiB RSS, with native save at 125.24 ms and PNG export at 40.53 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 245 dirty paths (114 modified + 131 untracked). Exact source animation/bytes are retained rather than frozen or transcoded; provider inputs/calls, canonical state, acceptance, schema, limits, adaptive previews, and network authority are unchanged. Packaged comparison behavior, real providers, arbitrary producers, exact utility peak RSS, Level 3, RC, and stable-v1 readiness are not claimed. FND-09, GEN-06, and QA-08 remain Working.
- A source/headless FND-09/QA-08 native-save-preview isolation checkpoint (2026-08-13) moves production `.aidraw` preview generation from direct Electron browser-process Canvas rendering to the established supervised PNG export lane. Save uses an isolated document snapshot, retains the exact nominal full-raster guard and valid-oversized transparent 1×1 fallback, then admits only a copied static PNG whose header width/height match the snapshot before persistence can create or replace a file. Canonical asset/checkpoint validation still runs first. Malformed, contradictory, timed-out, or crashed ordinary preview work fails without direct-render or concealed-1×1 fallback and before destination replacement. Focused pinned-Node-24 typecheck/ESLint and 4 files / 76 tests pass. The complete safe gate passes portability for 369 tracked + 131 prospective paths, TypeScript, full ESLint, and 179 files / 1,035 tests; the unchanged formal gate passes 1/1 at 885.52 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 244 dirty paths (113 modified + 131 untracked). Canonical native content/schema, save target, limits, oversized behavior, adaptive-preview and infinite-map policies remain unchanged; exact preview/archive bytes, preview-specific utility peak RSS, packaged save/error behavior, Level 3, RC, and stable-v1 readiness are not claimed. FND-09 and QA-08 remain Working.
- A source/headless FND-09/QA-08 checkpoint-comparison isolation checkpoint (2026-08-13) replaces the browser-process `Promise.all` of two native renders with one explicit current-then-saved sequence through the supervised PNG export lane. Both documents are immutable snapshots; returned bytes must be a static PNG whose header geometry exactly matches each canonical render dimension before the existing revision/width/height/data-URL result can reach the renderer. Metadata inconsistency or malformed/contradictory output fails before display. The ordinary export lane preserves the prior full-size comparison allowance instead of imposing observation's separate 4 MP ceiling. Focused pinned-Node-24 typecheck/ESLint and 4 files / 49 tests pass. The complete safe gate passes portability for 369 tracked + 131 prospective paths, TypeScript, full ESLint, and 179 files / 1,032 tests; the unchanged formal gate passes 1/1 at 874.13 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 244 dirty paths (113 modified + 131 untracked). This changes no checkpoint/merge/document/UI shape or limit, does not isolate native-save preview rendering, add adaptive previews, measure comparison-specific utility peak RSS, prove packaged dialog behavior, or claim Level 3, RC, or stable-v1 readiness. FND-09 and QA-08 remain Working.
- A source/headless FND-09/IO-06/QA-08 clipboard-admission checkpoint (2026-08-13) extracts copy/paste into an injected workflow boundary. Current private HTML carries version 1, exact fragment-byte SHA-256, canonical base64, the established 2 MiB decoded-fragment limit, and a 16 MiB complete-envelope limit; the exact legacy `data-aidraw` form stays readable. Any malformed claimed marker now returns an explicit conflict without consulting PNG, while marker-absent content may use a PNG only after native/header geometry, hash/bytes, 8,192-side/16MP, and 1,500,000-byte editable-asset checks agree. Copy PNG rendering and pixel bitmap-paste quantization use the supervised raster utilities; Electron native images remain only at OS clipboard ingress/egress. Pure/workflow/source coverage proves versioned/legacy round trips, checksum/duplicate/noncanonical/amplification failure, fail-closed fallback isolation, illustration asset paste, pixel quantization, and production wiring. Focused pinned-Node-24 typecheck/ESLint and 4 files / 47 tests pass. The complete safe gate passes portability for 369 tracked + 129 prospective paths, TypeScript, full ESLint, and 178 files / 1,028 tests; the unchanged formal gate passes 1/1 at 862.44 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 242 dirty paths (113 modified + 129 untracked). The private attributes are not a registered MIME/third-party standard; no real cross-application or packaged clipboard preservation, native UI behavior, broader interchange compatibility, Level 3, RC, or stable-v1 readiness is claimed. FND-09, IO-06, and QA-08 remain Working.
- A source/headless UX-08 keyboard discoverability checkpoint (2026-08-13) adds a searchable current-mode shortcut guide on F1, ?, and a named top-bar button. Active tool shortcuts come from the existing mode-specific tool catalog rather than a parallel list; fixed file/history/selection/navigation commands are explicitly bounded. The vertical ARIA toolbar keeps one roving tab stop with wrapping Up/Down and edge Home/End focus, every shortcut-bearing tool exposes `aria-keyshortcuts`, and a focus-visible Skip to canvas control targets either named drawing surface. Global New/tool/editing/clipboard/history commands fail closed while a modal is open, and tool letters ignore input, textarea, select, and editable-content targets. Focused pinned-Node-24 typecheck/ESLint and 3 files / 9 tests pass. The complete safe gate passes portability for 369 tracked + 123 prospective paths, TypeScript, full ESLint, and 175 files / 1,015 tests; the formal gate passes 1/1 at 885.75 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 235 dirty paths. Fixed commands remain non-remappable; there is no new keyboard drawing model, complete focus-order proof, packaged/native key-layout/focus/screen-reader acceptance, 200% layout evidence, Level 3, RC, or stable-v1 claim. UX-08 remains Partial.
- A source/headless ILL-13 in-canvas text-content checkpoint (2026-08-13) opens one multiline editor through Select-tool double-click or selected-object Enter only for text under visible unlocked object/layer ancestry. One pure geometry adapter composes ancestor-group and object affine matrices with pan/zoom and applies the first-character range style; its memoized result does not rescan document hierarchy per draft keystroke, and Canvas remains the mixed-style visual authority. Blur or Ctrl/Cmd+Enter rechecks that hierarchy before one canonical `replaceStyledText` replacement at the captured revision, Escape cancels, and either a conflict or newly ineligible hierarchy retains/refocuses the draft. The canonical 100,000-code-unit ceiling is exposed in the control, all existing range styles survive replacement, and selection-only clicks no longer create zero-distance move history. Focused pinned-Node-24 typecheck/ESLint and 4 files / 14 tests pass. The complete safe gate passes portability for 369 tracked + 120 prospective paths, TypeScript, full ESLint, and 174 files / 1,010 tests; the formal gate passes 1/1 at 894.81 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 232 dirty paths. The overlay intentionally uses one range style instead of introducing rich-text DOM layout; range typography remains inspector-owned. This adds no schema/layout/render/interchange change, broader transformed-group hit-test guarantee, packaged pointer/keyboard evidence, Level 3, RC, or stable-v1 readiness; ILL-13 remains Working.
- A source/headless QA-02/REL-07 deterministic missing-font checkpoint (2026-08-13) keeps every authored/installed Canvas font first, then appends one private Liberation Sans fallback family with hash-pinned regular/bold/italic faces. Browser bootstrap loads all four inlined assets before React mounts; headless illustration rendering registers the same bytes before drawing. The real release-license generator inventories all four hashes and the complete OFL 1.1 notice; Forge copies that exact tracked notice into every desktop resource layout and package verification rejects missing or changed bytes. Pure asset/shorthand/browser/native tests, isolated license generation, two production Vite graphs, and a maintained 180×96 missing-family Latin raw-RGBA golden (`09e38eae…dce8`) bind the scope. Focused pinned-Node-24 typecheck/ESLint and 7 files / 11 tests pass; the neighboring release-artifact suite brings the executed focused collection to 8 files / 18 tests. Direct production Vite graphs transform 131 main and 1,985 renderer modules with four inline font assets in each and no emitted TTF. The complete safe gate passes portability for 369 tracked + 118 prospective paths, TypeScript, full ESLint, and 173 files / 1,008 tests; the unchanged formal gate passes 1/1 at 905.33 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 230 dirty paths. This changes no canonical text/style/schema or authored font name, does not replace an installed requested font, embed fonts into saved/interchange documents, guarantee glyphs outside the bundled faces, prove advanced shaping/browser-native pixel equivalence, or establish packaged/native, Level 3, RC, stable-v1, or broad format compatibility; QA-02 and REL-07 remain Working/Partial.
- A source/headless IO-04/IO-09 PSD import blend-substitution reporting checkpoint (2026-08-12) makes the existing unsupported-mode-to-`normal` conversion inspectable without widening canonical blend semantics. Illustration and pixel imports emit one aggregate warning plus deterministic pre-order `blend-mode-substitution` reasons tied to exact generated layer IDs/names and original decoded modes. The optional list crosses the supervised lane under the existing closed-entry/count contract and 1 MiB aggregate report ceiling; human and approved-agent Activity reports retain it, while an invented worker code retires that producer before caller use. A generated 1×1 `pass through` folder / `color` leaf proves identical ordered illustration/pixel outcomes. Focused pinned-Node-24 typecheck/ESLint and 4 files / 46 tests pass. The complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 1,000 tests; the unchanged formal gate passes 1/1 at 877.39 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 213 dirty paths. This adds no blend implementation, comprehensive import-loss ledger, external-producer/Photoshop visual evidence, packaged Activity/JSON-export acceptance, broad PSD compatibility, Level 3, RC, or stable-v1 readiness; IO-04 remains Partial and IO-09 remains Working.
- A source/headless PIX-12/PIX-14/IO-01/IO-07/QA-03 fractional-opacity exact normal-composite checkpoint (2026-08-12) extends deterministic unpremultiplied PNG/APNG/sprite-sheet output to normal pixel leaves with bounded path-multiplied group/layer opacity. The resolver reproduces the pinned renderer's opacity-byte floor before combining palette alpha, retains source RGB, and keeps ambiguous multi-layer fully transparent colors plus every non-normal blend on Canvas. Exact GIF independently requires full effective opacity, preserving its prior whole-animation quantizer. Pure controls cover a half-opacity `#4080c080` leaf, leaf/group opacity composition, normal overlap, non-normal fallback, and explicit GIF decline; a two-frame production fixture proves exact PNG/APNG/sheet bytes. Focused pinned-Node-24 typecheck/ESLint and 3 files / 47 tests pass. The complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 998 tests; the unchanged formal gate passes 1/1 at 876.16 MiB RSS, with its one-million-pixel exact normal composite at 25.31 ms under 500 ms. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 213 dirty paths. This adds no schema/opacity/blend/GIF/profile policy, external-producer or broad compatibility, packaged/native behavior, Level 3, RC, or stable-v1 readiness; all rows remain Working.
- A source/headless IO-04/QA-03 exact shared-palette pixel-PSD checkpoint (2026-08-12) preserves raw raster-leaf RGBA exactly when the complete PSD hierarchy fits the canonical 255-visible-color indexed model. One streaming planner retains matching reference indexes and assigns novel colors deterministically in tree/raster discovery order; every leaf uses the same palette because structural layers have no override contract. At 256 union colors the whole import retains the established document-palette quantizer with an explicit warning rather than mixing fidelity modes. Pure shared-source controls and an AIDraw custom-color PSD export → one-sprite import → re-export preserve `#123456` / `#abcdef`; a generated 256-color PSD proves production fallback. Focused pinned-Node-24 typecheck/ESLint and 4 files / 75 tests pass. The complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 996 tests; the unchanged formal gate passes 1/1 at 860.98 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 213 dirty paths. This adds no PSD palette/index metadata recovery, animation behavior, external-producer/Photoshop visual equivalence, broad PSD compatibility, packaged/native behavior, Level 3, RC, or stable-v1 readiness; IO-04 remains Partial and QA-03 remains Working.
- A source/headless IO-04/QA-03 pixel-PSD-hierarchy checkpoint (2026-08-12) replaces the former one-sprite-per-raster-leaf pixel import with one decoded-canvas-sized, one-frame sprite. Decoder-authoritative folders, including empty folders, and raster leaves retain exact order plus supported visibility, normalized opacity, blend, and binary lock-all; raster pixels use independent per-leaf indexing and land at decoded signed `left`/`top` coordinates through existing sparse cels. Layer 83 above supersedes the representable color-selection step with one shared exact palette while retaining the established fallback quantizer. An AIDraw pixel PSD export → pixel import → re-export lifecycle preserves one group/empty-group/hidden-leaf tree and its raw indexed pixel, while a generated crop proves `(-1,2)` / `(0,2)` placement, explicit non-raster omission, blank editable fallback when no raster leaf exists, and pixel-mode partial-lock warning. Focused pinned-Node-24 typecheck/ESLint and 3 files / 64 tests pass; the complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 992 tests. The unchanged formal gate passes 1/1 at 854.14 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 213 dirty paths. No animation reconstruction, adjustment/effect editability, partial-lock preservation, external-producer visual equivalence, broad PSD compatibility, packaged/native behavior, Level 3, RC, or stable-v1 readiness is claimed; IO-04 remains Partial and QA-03 remains Working.
- A source/headless IO-01/IO-07/PIX-14/QA-08 retained-raster admission checkpoint (2026-08-12) reuses the existing 1,500,000-byte canonical inline-asset ceiling before import-created rasters can be retained. Top-level raster/animation sources, metadata sprite-sheet companions, and direct sliced sheets reject before parser/native decode; one shared constructor backstop rejects rendered PDF pages, PSD fallbacks, or successfully decoded Tiled companions before hash/base64 construction. Exact arithmetic accepts the ceiling and rejects its next byte; production routing covers ordinary PNG, pixel APNG/GIF, metadata sheets, and direct slicing with zero native image decode for the guarded cases. Focused pinned-Node-24 typecheck/ESLint and 7 files / 84 tests pass; the complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 985 tests; the unchanged formal gate passes 1/1 at 862.72 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 211 dirty paths. Oversized retained sources are not resized, recompressed, or silently omitted; corrupt Tiled-companion warning behavior, all existing codec/MIME/geometry limits, infinite-map bounds/origin, external-format scope, packaged/native behavior, Level 3, RC, and stable-v1 remain unchanged/unclaimed. All rows remain Working.
- A source/headless IO-04/QA-08 PSD canonical-admission checkpoint (2026-08-12) applies the established migration and normalized-pixel validator to completed illustration and pixel-mode PSD documents before direct return. Editable decoded text additionally shares the canonical 100,000-code-unit ceiling and exact lower-bound accounting against the unchanged 512 MiB imported-document envelope before text-object construction. One writer-produced control accepts the exact text boundary, then injected decoder results reject the next code unit, non-finite font size/opacity, and an overlong pixel-layer name with one PSD-specific integrity error; maintained PSD interchange stays green. Focused pinned-Node-24 typecheck/ESLint and 5 files / 81 tests pass; the complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 982 tests; the unchanged formal gate passes 1/1 at 876 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 210 dirty paths. Out-of-model decoded content is not clamped, renamed, split, or partially omitted; parser authority, PSD bytes/semantics, canonical/resource limits, external-format scope, packaged/native behavior, Level 3, RC, and stable-v1 remain unchanged/unclaimed. IO-04 stays Partial and QA-08 stays Working.
- A source/headless IO-05/QA-08 PDF canonical/pre-transfer checkpoint (2026-08-12) reuses the established 100,000-code-unit illustration-text ceiling and 512 MiB imported-document byte/node/depth envelope. Every completed PDF page document now passes the canonical validator before direct return; out-of-model extracted strings or geometry fail with one PDF-specific error, and exact incremental JSON-string accounting rejects once text alone proves the transfer envelope cannot fit. The utility worker checks the complete existing envelope before IPC while the main process independently repeats it and remains trust authority. Boundary arithmetic, canonical/overlong/non-finite injected text, cleanup-error precedence, cycle/depth envelope cases, and the maintained PDF fixture cover the contract without a ceiling-sized allocation. Focused pinned-Node-24 typecheck/ESLint and 4 files / 76 tests pass; the complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 981 tests; the unchanged formal gate passes 1/1 at 880.13 MiB RSS. The audit remains Level 3 blocked 2 / stable v1 blocked 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 210 dirty paths. No text is clamped, split, or silently omitted; PDF pixels/searchability, pdf.js recovery, canonical/resource limits, external-format scope, packaged/native behavior, Level 3, RC, and stable-v1 remain unchanged/unclaimed. IO-05 stays Partial and QA-08 stays Working.
- The preceding source/headless IO-05/QA-08 PDF page-resource checkpoint (2026-08-12) preserves the existing complete geometry preflight, then bounds sequential native work: each encoded page Canvas resets to 1×1 before raster admission/text extraction continues, each processed `PDFPageProxy` cleans before the next render, preflight-only pages are swept on failure, and the loading task is always destroyed. Import/page/render/text errors win over cleanup errors, while cleanup-only failure remains fail-closed. A three-page production import proves render/cleanup alternation and three returned documents; direct success/failure coverage proves Canvas release; existing malformed and dimension corpora prove cleanup and error precedence. Focused pinned-Node-24 typecheck/ESLint and 2 files / 41 tests pass. The complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 978 tests; the unchanged formal performance gate passes 1/1. PDF pixels, names, text, page/count/dimension budgets, pdf.js syntax/recovery authority, canonical/interchange state, and export stay unchanged. Exact peak RSS, broader producers/parsers, packaged/native behavior, Level 3, RC, and stable-v1 remain unclaimed; IO-05 stays Partial and QA-08 stays Working.
- A source/headless PIX-14/IO-07/QA-08/FND-12/QA-07 animation-export-resource checkpoint (2026-08-12) closes the mismatch between retained raw APNG frames and the existing expanded-animation safety posture. Pixel APNG now rejects before rendering when scaled width × height × selected sequence length exceeds 64 megapixels. Exact GIF preserves its existing capability and all-or-nothing fidelity contract, but probes eligibility without retaining returned planes and then recomputes/writes one exact frame at a time; the established fallback was already sequential. Pure boundary arithmetic and one 1,024×1,024/two-frame/8× production rejection prove APNG preflight, while exact/fallback regressions remain green. Focused pinned-Node-24 typecheck/ESLint and 3 files / 43 tests pass. The complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 975 tests. The final formal gate passes at 860.3 MiB RSS; a 16-frame production exact GIF covering 16 million final pixels takes 1,138.52 ms under 5,000 ms. This changes no schema, frames/tags/timing/loop/disposal, pixels, palette, encoder, GIF eligibility, dimension ceiling, import policy, or compatibility claim. GIF compressed output remains retained and has no newly invented aggregate-frame limit; exact peak RSS, disk streaming, external producers, packaged behavior, Level 3, RC, and stable-v1 remain unclaimed; all rows stay Working.
- A source/headless PIX-12/PIX-14/IO-07/QA-03/FND-12/QA-07 exact-GIF normal-composite checkpoint (2026-08-12) avoids whole-frame quantization when an ordinary multi-layer normal composite already fits GIF's real binary-transparency/color model. The established opaque ordinary single-layer path still preserves canonical index slots. Other eligible frames share the exact normal compositor, require every final pixel to have alpha zero or 255 and at most 255 visible RGB colors, reserve transparent index zero, and assign local colors in deterministic first-visible raster order. One ineligible frame keeps the complete animation on the established rendered quantizer. Pure guards cover direct-index preservation, exact blended/local indexes, partial-final-alpha decline, and the 255/256-visible-color boundary; a generated two-frame production export/re-import preserves `[127,0,128,255]`, red, transparent pixels, local-table order, and timing. Focused pinned-Node-24 typecheck/ESLint and 3 files / 32 tests pass. The complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 974 tests. The final formal gate passes at 862.2 MiB RSS; one million final binary-alpha normal-composite pixels receive exact GIF indexes in 35.07 ms under 750 ms. This changes no schema, canonical layers/pixels/frame palettes, imported single-layer index identity, animation/tag/delay/loop/disposal, dimensions, or GIF transparency semantics and adds no partial-alpha GIF, arbitrary composite/blend, external-producer, packaged, Level 3, RC, or stable-v1 claim; all rows remain Working.
- A source/headless PIX-12/PIX-14/IO-01/IO-07/QA-03 exact-normal pixel-composite checkpoint (2026-08-12) extends lossless PNG/APNG/sprite-sheet output beyond one imported layer without pretending to emulate arbitrary Canvas composites. One shared visible-layer traversal resolves linked cels/frame palettes, visits stored chunks, and emits one exact RGBA plane for nested normal pixel leaves. At this layer boundary it required full effective opacity; layer 84 above later adds renderer-equivalent fractional opacity. A single leaf retains its raw stored channels; multiple leaves use deterministic unpremultiplied eight-bit source-over in canonical order. Non-normal blend or multi-layer use of a fully transparent nonzero palette entry falls back to the established Canvas renderer, and GIF retains its separate direct-index/exact-composite eligibility. Generated pure and production controls cover a two-layer alpha-128 overlap whose exact output is `[85,0,170,192]`, nested order, fallback guards, transparent-RGB single-layer preservation, and matching two-frame PNG/APNG/sprite-sheet bytes. Focused pinned-Node-24 typecheck/ESLint and 3 files / 29 tests pass. The complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 971 tests. The formal gate passes at 873.59 MiB RSS; two one-million-pixel normal layers composite in 32.63 ms under 500 ms. This changes no schema, layer order, frame/palette, animation timing, sheet metadata, color-profile, or GIF quantization policy and adds no broad composite/format, packaged, Level 3, RC, or stable-v1 claim; all rows remain Working.
- A source/headless PIX-05/MAP-07/MAP-08/FND-10/FND-12/QA-07 bounded-grid-surface checkpoint (2026-08-12) removes two unbounded interactive paths without changing canonical cells. The globally phased transparency checker now intersects the scaled surface with the viewport, carries 64 pixels of overscan for the existing 24-pixel blur/8-pixel offset, clips partial edge cells, and shadows exactly one bounded source rectangle before drawing dark squares without shadow. Freeform grid lasso samples collapse only a provably same-direction collinear middle point, retain at most 4,096 vertices, incrementally validate the exact work envelope, show the reduced outline during drag, and rasterize once on release through shorter-axis even-odd scanlines. Safe-integer, one-million candidate-cell, and four-million boundary-step limits fail closed with the prior selection unchanged. Focused pinned-Node-24 typecheck/ESLint and 3 files / 13 tests pass. The complete safe gate passes portability for 369 tracked + 107 prospective paths, TypeScript, full ESLint, and 168 files / 968 tests. The final formal gate passes at 864.63 MiB RSS; its new 1,280×720 viewport/524,288-square-surface, 1,000,001 straight samples, and incremental 4,096-vertex path take 51.26 ms under 500 ms and retain exactly 57,600 checker candidates, 1,408×848 shadow source, two straight endpoints, and the full bounded complex path. Canonical selection combinations, stored chunks/GIDs, map bounds/origin, headless/export/interchange pixels/bytes, and Tiled semantics are unchanged. Canvas blur appearance, pointer dispatch, frame pacing, display scaling, packaged behavior, Level 3, RC, and stable-v1 remain unclaimed; all rows stay Working.
- A source/headless IO-02/QA-08 canonical-SVG-admission checkpoint (2026-08-12) requires every completed editable SVG document to pass the established canonical illustration validator before return. Standard pinned-Canvas CSS colors up to 256 characters—including shorthand/alpha hex, named/functional tokens, inherited `currentColor`, and transparent—reduce deterministically to lowercase sRGB hex; invalid tokens warn and become black. A one-stop gradient duplicates that exact color at offsets zero/one, reverse-direction lines retain their endpoints through positive dimensions plus an affine sign, and overlong object/font-family names warning-truncate to the existing 200-character boundary. Six generated out-of-model cases cover scale, geometry, stroke width, blur, 33 gradient stops, and text size; all reject the complete import with exact `SVG import produced content outside AIDraw's canonical illustration limits.`, and the scale case repeats through production file import. Focused pinned-Node-24 typecheck/ESLint and 1 file / 8 tests pass. The complete safe gate passes portability for 369 tracked + 105 prospective paths, TypeScript, full ESLint, and 167 files / 961 tests. The formal performance gate passes at 887.23 MiB RSS growth with every established metric below budget. The existing schema and utility-result gate are reused; this adds no broad CSS/color-profile/foreign-corpus or packaged behavior, broad SVG compatibility, Level 3, RC, or stable-v1 claim. IO-02 and QA-08 remain Working.
- A source/headless IO-02/QA-08 SVG-transform-integrity checkpoint (2026-08-12) prevents transform arithmetic from escaping editable import as non-finite canonical state. Recognized numeric tokens, every intermediate transform-list composition, and a validation-only cumulative source visual matrix must remain finite; matrix decomposition, nested/use positioning, mask-prefix traversal, and referenced linear/radial gradient endpoints receive the same invariant without flattening stored transforms. Groups, nested SVG, use, and referenced symbols validate before recursive child construction, and any violation rejects the complete import with exact `SVG transform produces non-finite canonical geometry.` rather than returning a partial document. Nine hostile generated variants cover a non-finite token, finite-token list overflow before an invalid image can decode, two individually finite nested transforms whose visual product overflows, finite matrix decomposition overflow, referenced-symbol failure before child decode, positioned nested overflow, mask-prefix overflow, and both gradient kinds' endpoint overflow. Production file import replays the transform-list failure. Exact local-list and nested `100 × 100` paths at the canonical scale boundary remain accepted, while transforms on ignored metadata/unsupported content and unknown functions retain their prior omission/ignore behavior. Focused pinned-Node-24 typecheck/ESLint and 3 files / 60 tests pass. The complete safe gate passes portability for 369 tracked + 105 prospective paths, TypeScript, full ESLint, and 167 files / 960 tests. The formal performance gate passes at 867.95 MiB RSS growth with every established metric below budget. The existing canonical magnitude policy is unchanged; malformed-transform grammar, CSS transforms, external corpus, packaged behavior, broad SVG compatibility, Level 3, RC, and stable-v1 remain unclaimed. IO-02 and QA-08 remain Working.
- A source/headless IO-02/QA-03 nested-SVG-viewport checkpoint (2026-08-12) gives nested `<svg>` and `<use>`-instantiated `<symbol>` content its actual viewport instead of leaving source coordinates at raw size. Missing/`auto` dimensions, missing coordinates, and unitless/px/percentage geometry resolve against the current viewport; a valid viewBox reuses standard alignment/meet/slice/none inside a neutral transform-only group, while the positioned/styled wrapper retains element semantics. Direct symbols stay definition-only. Zero/invalid dimensions and a non-finite matrix reject before child objects enter the document, while a non-finite outer matrix rejects before document construction. One generated 240×120 control proves exact production-import pixels against a separately decoded standards-equivalent expansion, 50% symbol width, 60% nested x, default symbol meet, nested none scaling, hidden unreferenced symbols, and four fail-closed mutations. Focused pinned-Node-24 typecheck/ESLint and 3 files / 59 tests pass. The complete safe gate passes portability for 369 tracked + 105 prospective paths, TypeScript, full ESLint, and 167 files / 959 tests. The formal performance gate passes at 880.42 MiB RSS growth with every established metric below budget. Nested overflow remains editable with an explicit warning instead of synthetic clipping. CSS `calc()`/font/physical units, symbol `refX`/`refY`, external references, overflow clipping, external corpora, packaged behavior, broad SVG compatibility, Level 3, RC, and stable-v1 remain unclaimed; both rows stay Working.
- A source/headless FND-10/ILL-14/IO-02/QA-03 outer-SVG-viewport checkpoint (2026-08-12) applies the outer `viewBox` through one canonical affine group instead of reducing it to a min-X/min-Y translation. Standard default `xMidYMid meet`, explicit alignment with `meet`/`slice`, and `preserveAspectRatio="none"` determine scale and placement inside the existing integer artboard; a malformed value warns and falls back to the standard default. Interactive and headless Canvas traversal now shares one pure transform/isolation classification: transform-only groups draw their children directly under the affine matrix, while opacity, blend, filter, mask, blur, or shadow groups remain isolated so the effect applies once. Exact generated pixels prove default horizontal letterboxing, nonuniform stretching, one bottom-aligned slice, and malformed fallback; pure/source wiring proves the shared renderer branch. Focused pinned-Node-24 typecheck/ESLint and 5 files / 61 tests pass. The complete safe gate passes portability for 369 tracked + 105 prospective paths, TypeScript, full ESLint, and 167 files / 958 tests. The unchanged formal performance gate passes at 870.83 MiB RSS growth with every established metric below budget. Canonical coordinates/schema and existing effect-composite behavior remain unchanged. Nested `<svg>`/`symbol` sizing, `use` viewport sizing, percentage/CSS viewport resolution, effect-bearing transformed-group overhang, external corpora, packaged behavior, broad SVG compatibility, Level 3, RC, and stable-v1 remain unclaimed; all four rows stay Working.
- A source/headless ILL-16/QA-02 path-boolean checkpoint (2026-08-12) corrects the shared editor/MCP kernel where stored even-odd paths were silently handed to Paper.js as nonzero and every result was relabeled nonzero. Both canonical input rules now participate, the Paper result rule is retained, transforms still flatten into one identity result, and the existing first-input layer/composite/fill/stroke policy is preserved with independent style copies. Empty filled results reject before either caller can submit its atomic delete/delete/add operations. Exact unit coverage binds all four modes, transformed bounds, an even-odd hole with a union island, style/source immutability, and the empty-result failure. A maintained transparent 64×64 raw-RGBA ring-and-island golden is `e0309ac1…6083`. Focused pinned-Node-24 typecheck/ESLint and 2 files / 4 tests pass. The complete safe gate passes portability for 369 tracked + 97 prospective paths, TypeScript, full ESLint, and 163 files / 926 tests. The formal gate passes at 909.64 MiB RSS with every metric below budget. Canonical schema/operation, first-input style policy, callers, and serialization remain; compound-hole node editing, general multi-subpath authoring, style blending, utility isolation, packaged interaction, foreign-format parity, Level 3, RC, and stable-v1 remain unclaimed; ILL-16 and QA-02 stay Working.
- A source/headless ILL-11/QA-02 illustration-stroke checkpoint (2026-08-12) removes a renderer split despite the existing canonical/export contract: interactive editable paths previously ignored opacity/cap/join/dash and could draw width zero with ambient state, while headless paths ignored opacity/cap/join and headless shapes ignored stroke opacity. One DOM-neutral applicator now assigns canonical width, opacity, cap, join, dash, and explicit miter limit 10 in both Canvas renderers; stroke opacity multiplies the already composed object/group/layer alpha, and nonpainted/zero-width strokes issue no draw. The click kernel shares only the miter geometry, not visual opacity. Pure state tests, renderer-source wiring, and a maintained 80×48 raw-RGBA path/shape golden (`28318fc1…b284`) pass. Focused pinned-Node-24 typecheck/ESLint and 3 files / 4 tests pass. The complete safe gate passes portability for 369 tracked + 96 prospective paths, TypeScript, full ESLint, and 162 files / 923 tests. The formal gate passes at 900.55 MiB RSS with every metric below budget. Canonical schema/values and SVG/PDF vector serialization remain unchanged; native previews and raster outputs intentionally correct the drift. Paint resolution, object order, custom miter authoring, masks/effects, packaged antialiasing/visual acceptance, broad interchange parity, Level 3, RC, and stable-v1 remain unclaimed; ILL-11 and QA-02 stay Working.
- A source/headless ILL-02/ILL-10/FND-12/QA-07 illustration-click checkpoint (2026-08-12) makes a supported painted Canvas path authoritative instead of falling every exact miss through expanded world bounds. Filled paths honor stored even-odd/nonzero rules; filled silhouettes, pressure outlines, text/image frames, configured shape/path strokes, and all three rendered arrow segments receive the established object/view-scale-adjusted edge affordance. Groups, malformed paths, non-invertible transforms, paintless objects, or an unavailable Canvas context deliberately retain bounds fallback. Topmost order/Alt cycling, locked/hidden exclusion, canonical state, Canvas state, and the established stem-only arrow mask path remain exact. Direct production regressions prove ellipse-corner and even-odd-hole rejection, affine mapping, arrowhead/stroke reach, mask-scope isolation, fallback cases, order, exclusion, and state restoration. Focused pinned-Node-24 typecheck/ESLint and 4 files / 17 tests pass. The complete safe gate passes portability for 369 tracked + 93 prospective paths, TypeScript, full ESLint, and 160 files / 920 tests. The formal gate adds a 5,000-object click hit test at 19.7 ms under 500 ms and passes at 872.75 MiB RSS with every metric below budget. Mask/effect/group-child silhouettes, exact skewed screen-distance, packaged pointer interaction, Level 3, RC, and stable-v1 remain unclaimed; ILL-02, ILL-10, FND-12, and QA-07 stay Working.
- A source/headless MAP-12/QA-02/QA-03 square-transform-picker checkpoint (2026-08-12) adds eight bounded 32×32 selected-tile previews that share the exact crop and diagonal-first core matrix already used by editor/headless rendering. One pure permission kernel interprets tileset H/V flips and 90° rotation as geometric generators rather than independent stored bits: rotation-only permits identity plus the three nonidentity rotations, either reflection plus rotation generates all eight square symmetries, and H+V without rotation permits the four axis/half-turn states. An exact allowed state is retained; a stale disallowed state becomes identity for new paint instead of a different transform. The same constraint protects current-tile stamps, ordinary painting, the picker, and quick toggles; existing GIDs and canonical/interchange state remain untouched. Pure/SSR/source coverage proves all choices, capability closures, fail-safe identity, disabled/active UI state, bounded crop/matrix wiring, scratch release, rectangular refusal, and both encoding paths. Focused pinned-Node-24 typecheck/ESLint and 4 files / 18 tests pass. The complete safe gate passes portability for 369 tracked + 91 prospective paths, TypeScript, full ESLint, and 159 files / 913 tests. The unchanged formal performance gate passes at 855.81 MiB RSS with every established metric below budget. Rectangular diagonal fitting/preview, packaged layout/pointer acceptance, isometric-specific visuals, broader Tiled fidelity, Level 3, RC, and stable-v1 remain unclaimed; MAP-12 and QA-02/QA-03 stay Working.
- A source/headless ILL-02/FND-12/QA-07 illustration-lasso checkpoint (2026-08-12) replaces rotated-bounds selection with one lazily loaded canonical geometry kernel at pointer completion. Filled shapes and paths use transformed Paper.js silhouettes with stored fill rules, vector strokes use their `perfect-freehand` outline, text/images use their transformed frame, and stroke-only shapes/paths use exact centerlines. Unsupported groups or malformed paths retain the transformed-bounds fallback; replace/add/subtract/intersect, Ctrl/Cmd containment, canonical state, and object order remain unchanged. Pure regressions cover ellipse bounds-corner rejection and curve intersection, nonrectangular containment, transformed even-odd holes, pressure-outline reach, Bézier centerlines, image frames, fallback groups, order, and immutability; static source coverage binds the editor path. Focused pinned-Node-24 typecheck/ESLint and 3 files / 9 tests pass. The complete safe gate passes portability for 369 tracked + 87 prospective paths, TypeScript, full ESLint, and 157 files / 907 tests. The formal performance gate adds a 5,000-object lasso at 187.19 ms under 1,000 ms and passes at 895.97 MiB RSS with every metric below budget. Outer stroke width, masks/clipping, effects/shadows/blur, malformed-path precision, packaged pointer interaction, Level 3, RC, and stable-v1 remain unclaimed; ILL-02, FND-12, and QA-07 stay Working.
- A source/headless ILL-08/QA-03/QA-08 custom brush-library checkpoint (2026-08-12) exposes strict document-owned AIDraw JSON copy/load/paste with non-mutating preview. The version-1 parser reuses the complete canonical dab-recipe schema, rejects unknown fields, duplicate/reserved built-in IDs, empty or over-256 libraries, and UTF-8 input above 1 MiB. **Add copies** is the default and allocates fresh IDs plus case-insensitively unique names; explicit replacement preserves imported IDs and changes only the custom preset array in one ordinary transaction. Any document revision change expires the preview. Existing strokes keep embedded recipes/seeds, and a successful import selects the committed preset with its exact size/opacity. Focused pinned-Node-24 typecheck/ESLint and 4 files / 14 tests pass. The complete safe gate passes portability for 369 tracked + 84 prospective paths, TypeScript, full ESLint, and 155 files / 901 tests. The formal performance gate passes at 856.86 MiB RSS with every established metric below budget. Built-ins, strokes, bitmap tips, folders/tags, global libraries, third-party formats, native directory authority, packaged interaction, Level 3, RC, and stable-v1 remain unclaimed; ILL-08 and QA-03/QA-08 stay Working.
- A source/headless PIX-12/PIX-14/IO-01/IO-07/QA-03 exact indexed-PNG checkpoint (2026-08-12) bypasses Canvas premultiplication for ordinary single-pixel-layer sprite PNG and sprite-sheet output. Canonical indexes/frame palettes become raw RGBA, integer scaling repeats exact pixels, sheet frames are copied into one bounded plane, and lossless no-PLTE encoding retains partial-alpha RGB while avoiding the tiny-image palette allocation defect. A generated two-frame APNG import proves exact alpha-128/192 colors in a 2×1 first-frame PNG and 4×1 frame-local sheet plus exact frame order/size metadata. Complex layer shapes preflight out before raw-sheet allocation and retain the established Canvas compositor. Focused pinned-Node-24 typecheck/ESLint and 3 files / 32 tests pass. The complete safe gate passes portability for 369 tracked + 80 prospective paths, TypeScript, full ESLint, and 153 files / 895 tests. The formal performance gate passes at 883.36 MiB RSS with every established metric below budget. Canonical state, dimensions, sheet layout/metadata, APNG/GIF, JPEG/WebP/PDF, illustration/tilemap rendering, and general compositing remain unchanged. Byte-identical PNGs, profiles/metadata, complex composites, external producers, packaged behavior, broad compatibility, Level 3, RC, and stable-v1 remain unclaimed; all five rows stay Working.
- A source/headless PIX-12/PIX-14/QA-03 exact GIF re-export checkpoint (2026-08-12) extends the guarded indexed-frame resolver to opaque GIF output. An eligible ordinary single-pixel-layer sprite retains exact canonical indexes through nearest-neighbor scaling; frame zero supplies the global RGB table and later override palettes become GIF local tables. One generated two-frame local-table fixture now passes production import → export → production re-import with exact decoded RGBA, base/override colors, timing, and exported-source retention. Pure guards prove a used partial-alpha color or layer opacity makes the whole animation retain the established rendered quantizer instead of mixing modes. Focused pinned-Node-24 typecheck/ESLint and 3 files / 31 tests pass. The complete safe gate passes portability for 369 tracked + 80 prospective paths, TypeScript, full ESLint, and 153 files / 894 tests. The formal performance gate passes at 869.42 MiB RSS with every established metric below budget. Canonical state, import/container limits, frame/tag/delay/loop semantics, dimensions, and complex-composite rendering remain unchanged. Byte-identical GIF containers, transparent hidden RGB, partial-alpha GIF, external producers/color management, packaged behavior, broad compatibility, Level 3, RC, and stable-v1 remain unclaimed; PIX-12, PIX-14, and QA-03 stay Working.
- A source/headless PIX-12/PIX-14/QA-03 frame-local animation-palette checkpoint (2026-08-12) preserves deterministic indexed colors instead of forcing every imported GIF/APNG frame through the default document palette. Each composited frame with at most 255 colors visible after the existing alpha threshold retains matching reference slots and assigns novel RGBA values to the lowest free indexes; aligned canonical overrides carry later-frame colors. One generated two-frame APNG proves exact stored partial-alpha palettes, indexed cels, source retention, and exact APNG re-export through the narrowly gated single-pixel-layer raw path. A generated two-frame GIF proves exact local-table colors and overrides. One generated 256-color APNG proves whole-animation default-palette quantization, no partial override plan, an explicit warning, and exact source retention. Focused pinned-Node-24 typecheck/ESLint and 3 files / 29 tests pass. The complete safe gate passes portability for 369 tracked + 80 prospective paths, TypeScript, full ESLint, and 153 files / 892 tests. The formal performance gate passes at 859.09 MiB RSS with every established metric below budget. Canonical schema, decoder/container/resource limits, threshold/dithering, and source bytes remain unchanged. Transparent/below-threshold RGB, complex-composite byte identity, GIF re-export, external producers, color management, packaged import/export, broad compatibility, Level 3, RC, and stable-v1 remain unclaimed; PIX-12, PIX-14, and QA-03 stay Working.
- A source/headless ILL-12 numeric image-crop checkpoint (2026-08-12) adds explicit source-pixel X/Y/width/height drafts and one Apply action to the image inspector. One pure helper rejects non-finite, nonpositive, negative, or out-of-source rectangles; preserves fractional coordinates and current display-units-per-source-pixel; and shifts the local origin through scale, skew, and rotation before atomically replacing crop and displayed dimensions. The full-source rectangle clears the crop through the established reset. Source coordinates were chosen because they are the durable stored/interchange geometry and the canvas already supplies display-space drag cropping; commit-on-blur and asset-byte rewriting were rejected. Focused pinned-Node-24 typecheck/ESLint and 3 files / 23 tests pass. The complete safe gate passes portability for 369 tracked + 78 prospective paths, TypeScript, full ESLint, and 152 files / 887 tests. The formal performance gate passes at 918.19 MiB RSS with every established metric below budget. The existing revision-checked object replacement, schema, image bytes/identity, export, canvas drag crop, and aspect presets remain unchanged. Replace/relink, missing-asset UX, packaged layout/focus/keyboard/pointer acceptance, Level 3, RC, and stable-v1 remain open, so ILL-12 stays Working. The adjacent ILL-03 explicit transform-reset gap is decision-ready rather than implemented: identity-at-origin, position-preserving reset, and geometry baking have materially different results and no product contract selects one.
- A source/headless PIX-14/IO-07/QA-03/QA-08 APNG chunk-type checkpoint (2026-08-12) validates every animated-container type as four ASCII letters, enforces the uppercase reserved third bit, and rejects unknown uppercase critical chunks before pixel conversion. A generated private ancillary `vpAg` chunk remains ignored by decoding while exact RGBA, editable dimensions/timing, and byte-for-byte source retention stay unchanged. Three matched variants reject unknown critical `ABCD`, reserved-bit `abcz`, and non-letter `a1Cd`. Focused pinned-Node-24 typecheck/ESLint and 3 files / 59 tests pass. The complete safe gate passes portability for 369 tracked + 76 prospective paths, TypeScript, full ESLint, and 151 files / 883 tests. The formal performance gate passes at 857.75 MiB RSS, with object filtering at 42.4 ms, map/sprite regions at 1.46/0.35 ms, 65,536 tiles at 147.3 ms, native save at 125.49 ms, and flood at 164.73 ms. Known chunk behavior, static-PNG routing, resource limits, canonical state, export, MIME, and source retention remain unchanged. No exhaustive ancillary-extension, external-producer, color-management, packaged import, broad APNG/PNG, Level 3, RC, or stable-v1 claim is added; all four rows stay Working.
- A source/headless PIX-14/IO-07/QA-03/QA-08 APNG default-image checkpoint (2026-08-12) binds the container's two standard image-data layouts. A first `fcTL` before `IDAT` now requires one full-canvas animation frame carried only by consecutive `IDAT` chunks. An `IDAT` stream before the first `fcTL` is instead a separate fallback image: its inflated row/filter geometry is validated under the existing ceiling, it is not composited into the animation, and the first partial animation frame must use `fdAT`. One generated 2×1 RGBA control splits the fallback across two valid `IDAT` chunks, then proves only a 1×1 frame at offset (1,0) becomes the 90 ms editable cel with transparent space and exact source retention. Five malformed variants reject short fallback rows, post-fallback frame `IDAT`, a partial first frame carried by `IDAT`, `fdAT` appended to an included first frame, and interrupted/nonconsecutive `IDAT`. Focused pinned-Node-24 typecheck/ESLint and 3 files / 57 tests pass. The complete safe gate passes portability for 369 tracked + 76 prospective paths, TypeScript, full ESLint, and 151 files / 881 tests. The formal performance gate passes at 888.17 MiB RSS, with object filtering at 32.78 ms, map/sprite regions at 1.34/0.34 ms, 65,536 tiles at 157.41 ms, native save at 122.41 ms, and flood at 173.34 ms. Existing APNG limits, canonical frame/canvas/palette state, export, MIME, source retention, and static-PNG fallback remain unchanged. No external producer, color-management/ancillary corpus, packaged import, broad APNG, Level 3, RC, or stable-v1 claim is added; all four rows stay Working.
- A source/headless PIX-14/IO-07/QA-03/QA-08 APNG color-metadata checkpoint (2026-08-12) validates `PLTE` and `tRNS` uniqueness, ordering, color-type legality, palette/depth capacity, transparency payload shape/range, and indexed sample membership before editable-frame construction. Sixteen-bit grayscale and truecolor transparency keys are compared as complete encoded samples before the existing high-byte display conversion, so a low-byte-only difference remains opaque even when both pixels have the same displayed color. Exact generated 2×1 fixtures prove that distinction, editable import, timing, and source retention; ten deterministic malformed variants cover a non-triplet palette, a one-bit palette with three entries, excess indexed transparency, an out-of-range palette index, duplicate palette/transparency chunks, a forbidden grayscale palette, invalid 8-bit transparency ranges, and `tRNS` on RGBA. Focused pinned-Node-24 typecheck/ESLint and 3 files / 55 tests pass. The complete safe gate passes portability for 369 tracked + 76 prospective paths, TypeScript, full ESLint, and 151 files / 879 tests. The formal performance gate passes at 902.97 MiB RSS, with object filtering at 35.07 ms, map/sprite regions at 1.38/0.37 ms, 65,536 tiles at 157.52 ms, native save at 131.29 ms, and flood at 178.07 ms. Existing resource limits, noninterlaced/Adam7 row conversion, 8-bit output, canonical state, export, MIME, source retention, and static-PNG fallback remain unchanged. No external producer, color-profile/ancillary corpus, packaged import, broad APNG, Level 3, RC, or stable-v1 claim is added; all four rows stay Working.
- A source/headless PIX-14/IO-07/QA-03/QA-08 interlaced-APNG checkpoint (2026-08-12) replaces the custom decoder's blanket Adam7 rejection with exact bounded seven-pass reconstruction. Each pass is independently unfiltered after one exact inflated-byte calculation, then byte-aligned or packed sub-byte samples are placed into the established row layout before unchanged color conversion and frame compositing. The APNG path now also verifies every chunk CRC, the one monotonic `fcTL`/`fdAT` sequence, declared-versus-observed frame count, and one empty terminal IEND. Generated 8×8 fixtures prove every Adam7 pass, a two-frame RGBA partial rectangle with 50/70 ms timing and exact blend/disposal metadata, one-bit indexed transparency, editable import pixels, and exact source retention; header/data, CRC, sequence, count, missing-IEND, and trailing-data contradictions reject before document creation. Focused pinned-Node-24 typecheck/ESLint and 3 files / 53 tests pass. The complete safe gate passes portability for 369 tracked + 76 prospective paths, TypeScript, full ESLint, and 151 files / 877 tests. The unchanged formal performance gate passes at 885.28 MiB RSS, with object filtering at 32.25 ms, map/sprite regions at 1.20/0.29 ms, 65,536 tiles at 156.57 ms, native save at 123.25 ms, and flood at 166.65 ms. Existing APNG byte/dimension/frame/aggregate limits, canonical sprite/schema state, static-PNG fallback, and noninterlaced behavior remain unchanged. External producer/color-profile/ancillary-chunk/filter corpora, packaged import behavior, broad APNG compatibility, Level 3, RC, and stable-v1 readiness remain unclaimed; all four rows stay Working.
- A source/headless MAP-09/FND-10/FND-12 map-object viewport checkpoint (2026-08-12) filters visible object layers before path rendering. One pure contract expands canonical bounds for the renderer's explicit miter limit and selected point/resize handles, transforms the four corners through the shared orthogonal/isometric affine matrix, and intersects the requested raster region while preserving input order. The editor includes fractional per-layer parallax, selected state, and gesture-preview geometry; headless observation/export uses canonical pan zero. Exact affine/handle/validation/order tests, source wiring, and a production regional getter prove a distant object's draw path is never reached. Focused pinned-Node-24 typecheck/ESLint and 4 files / 33 tests pass; the complete safe gate passes portability for 369 tracked + 76 prospective paths, TypeScript, full ESLint, and 151 files / 873 tests. The formal gate filters 100,000 objects to one in 35.98 ms under 500 ms and passes at 868.39 MiB RSS; the map/sprite sparse regions are 1.36/0.33 ms, 65,536 addressed tiles 145.91 ms, native save 126.86 ms, and flood 179.24 ms. This is linear projected-metadata culling, not a spatial index. Object data/z-order/appearance, selection and hit testing, map bounds/origin, Tiled bytes, packaged drag/visual/pacing behavior, Level 3, RC, and stable-v1 readiness remain unchanged or unclaimed; MAP-09/FND-10/FND-12 stay Working.
- A source/headless PIX-10/FND-10/FND-12 sprite-viewport checkpoint (2026-08-12) replaces the editor's full-cel base/onion/wrap loop with the established shared bounded sprite-region compositor. One generic pure viewport transform now covers both maps and sprites. Base frames and up to eight planned onion neighbors use its outward-rounded canonical pixel region; each wrap copy subtracts its exact sprite offset before candidate lookup. New compositor options preserve nested layer opacity/blend, linked-cel resolution, frame palettes, live palette cycling or onion tint, and replay masks. Exact tests prove option behavior, candidate-only payload access, generic viewport validation, and editor source wiring for base/onion/wrap. Focused pinned-Node-24 typecheck/ESLint and 6 files / 17 tests pass; the complete safe gate passes portability for 369 tracked + 76 prospective paths, TypeScript, full ESLint, and 151 files / 869 tests. The formal gate adds one pixel across 4,096 stored cel chunks at 0.31 ms under 100 ms and passes at 895.41 MiB RSS; the map counterpart is 1.23 ms, 65,536 addressed tiles 155.82 ms, native save 132.44 ms, and flood 168.39 ms. This stays an O(total chunks) key-count decision and is source/headless evidence, not a spatial index or packaged frame-pacing proof. Canonical pixels, cel links/chunks, palettes, onion settings, wrap semantics, export/MCP output, and file bytes are unchanged; PIX-10/FND-10/FND-12 stay Working and Level 3/RC/stable-v1 remain unclaimed.
- The adjacent checker-pattern audit is decision-ready but intentionally unimplemented. The editor currently draws the base rectangle and every dark checker cell while a 24-blur/8-pixel-offset map shadow is active. Canvas does not provide a cross-implementation finite influence radius for those individual shadows, so viewport-dropping offscreen cells cannot be claimed pixel-exact headlessly. A future choice must either preserve this appearance with native visual evidence or explicitly refactor shadow ownership to the whole canvas rectangle. No silent pattern/shadow change was introduced.
- A source/headless MAP-07/MAP-08/FND-12 editor-viewport checkpoint (2026-08-12) reuses the conservative pre-decode chunk filter on every interactive tilemap redraw. One pure transform maps fractional CSS viewport size, rounded fit/zoom offset, wheel pan, and per-layer parallax back through the uniform projection scale, then rounds outward to canonical map pixels. Both orientations retain only intersecting payloads before the existing orthogonal record loop or isometric right-down iterator. At grid-visible zoom, a second pure range emits only viewport-adjacent orthogonal line indexes with one neighboring line for clipped stroke coverage. Pure geometry/validation tests plus source wiring cover both decode paths, parallax, projection scale, deterministic order, and grid bounds. Focused pinned-Node-24 typecheck/ESLint and 4 files / 10 tests pass; the complete safe gate passes portability for 369 tracked + 75 prospective paths, TypeScript, full ESLint, and 150 files / 866 tests. The unchanged formal gate passes at 861.42 MiB RSS, with the shared 4,096-chunk regional filter/render at 1.45 ms, 65,536 addressed tiles at 147.82 ms, native save at 137.38 ms, and flood at 169.68 ms. This is source-level viewport traversal evidence, not packaged frame-pacing proof or a spatial index. Pixels, nominal/signed storage, origin, chunks/GIDs/transforms, record/right-down order, Tiled bytes, checker-pattern/object/selection/replay traversal, and infinite-view policy are unchanged; MAP-07, MAP-08, and FND-12 stay Working, and Level 3/RC/stable-v1 readiness remain unclaimed.
- A headless MAP-07/MAP-08/FND-12 regional-traversal checkpoint (2026-08-12) filters stored tile chunks before decoding them for a bounded nominal observation. One pure conservative envelope covers orthogonal/isometric projection and the larger tile axis, retaining any chunk whose rectangular cell could reach the backing region under a stored transform. Retained input order remains exact before the established isometric right-down iterator; no occupied-cell scan or chunk rewrite is introduced. Every chunk's small geometry record is still inspected once, so this is payload/cell pruning rather than a spatial index. Pure tests cover boundary-only exclusion, rectangular-transform padding, signed/isometric candidates, order preservation, and invalid geometry. A production million-column far-edge observation includes a nonintersecting payload that throws if decoded, proving the renderer prunes it. Focused pinned-Node-24 typecheck/ESLint and 5 files / 22 tests pass. The complete safe gate passes portability for 369 tracked + 75 prospective paths, TypeScript, full ESLint, and 150 files / 863 tests. The formal gate adds a one-pixel render across 4,096 stored chunks at 1.46 ms under a 100 ms tripwire and passes at 874.91 MiB RSS; the 65,536 addressed maximum-sheet render takes 149.35 ms, native save 133.97 ms, PNG export 42.02 ms, and flood 166.56 ms. Visible output, nominal bounds, signed storage, GIDs/transforms, origin, Tiled bytes, object traversal, and infinite-view policy are unchanged. The source-level editor follow-up above now shares the filter, while packaged pacing/behavior, Level 3, RC, and stable-v1 readiness remain unclaimed; MAP-07, MAP-08, and FND-12 stay Working.
- A headless MAP-07/MAP-08/FND-09 allocation checkpoint (2026-08-12) renders a requested nominal tilemap observation region without first allocating the complete projected map. Orthogonal and isometric region output is byte-exact with the corresponding full-raster crop for current tiles and visible object overlays; a four-pixel backing margin is contracted at the shared 65,535-side / 64-megapixel raster limits before raw-pixel extraction. The unchanged observation budget remains 4,194,304 output pixels, while a distinct validated `observation_dimensions_too_large` result reports the per-side limit. Static PNG/JPEG/WebP/pixel-SVG/pixel-PDF export rejects oversized whole rasters before rendering. Native `.aidraw` save deliberately remains available and writes the established transparent 1×1 auxiliary preview when the full nominal preview is unsafe; silently downsampling canonical export or blocking an otherwise valid save was rejected. Focused pinned-Node-24 typecheck/ESLint and 9 files / 131 tests pass; the complete safe gate passes portability for 369 tracked + 73 prospective paths, TypeScript, full ESLint, and 149 files / 860 tests. The formal performance gate passes at 872.47 MiB RSS growth, with the 65,536 addressed maximum-sheet tile render at 148.58 ms, native save at 126.01 ms, and one-million-cell flood at 186.71 ms. Nominal bounds, nonnegative observation coordinates, coordinate/chunk/GID/schema/Tiled bytes, signed-chunk visibility outside nominal bounds, and the unresolved infinite-map origin policy are unchanged. Adaptive checkpoint/clipboard/generation preview policy, packaged behavior, broad Tiled fidelity, Level 3, RC, and stable-v1 readiness remain unclaimed; MAP-07, MAP-08, and FND-09 stay Working.
- A headless MAP-03 placed-animation checkpoint (2026-08-12) makes authored tileset animation visible on placed map cells. One exact integer-millisecond sampler selects the local frame tile in both interactive and headless renderers before the original GID transform is applied; BigInt cycle arithmetic remains exact when aggregate safe frame durations exceed JavaScript's safe-number sum. The editor uses one session-local epoch for map-wide phase alignment, schedules no faster than 16 ms, and freezes at the first authored frame for reduced motion. Headless observation and static raster export deliberately sample time zero, avoiding wall-clock-dependent evidence. Exact boundary/loop validation, shared-source wiring, and a production 2×2 horizontally flipped two-frame tile plus PNG parity cover the contract. Focused pinned-Node-24 typecheck/ESLint and 9 files / 40 tests pass; the complete safe gate passes portability for 369 tracked plus 65 prospective paths, TypeScript, full ESLint, and 144 files / 848 tests. The formal performance gate passes at 814.69 MiB RSS growth, with the 65,536-tile static render at 39.11 ms, native save at 125.14 ms, and one-million-cell flood at 179.5 ms. Canonical animation metadata, map GIDs/transforms, source-sprite frames, and Tiled bytes are unchanged; time-addressable tilemap observation, animated tilemap GIF/APNG export, worst-case animated-map frame pacing, packaged visuals, broad Tiled equivalence, Level 3, RC, and stable-v1 readiness remain unclaimed; MAP-03 stays Verified for its exact automated workflow.
- The adjacent signed infinite-map clipping audit found enough canonical information to enumerate stored 32×32 chunk origins, but not enough established product contract to change initial fit or raster bounds safely. A complete decision must choose nominal-versus-stored-versus-occupied/visible/object bounds, define which output pixel represents canonical cell `(0,0)`, and specify crop/reject/viewport behavior for sparse spans too large to allocate. `canvas_observe` currently exposes only nonnegative raster regions and no map origin, while static export owns the whole raster. No implicit offset, coordinate rewrite, or broadened Tiled promise was introduced; MAP-07/MAP-08 infinite-view expansion remains open.
- A headless MAP-07 orthogonal projection-parity checkpoint (2026-08-12) removes the editor's square-cell assumption for rectangular authored map cells. One pure contract now defines finite extent, cell rectangles, continuous pointer and parallax inversion, and object scaling for both interactive and headless surfaces. The editor derives row height from `tileHeight / tileWidth`, so tile artwork, object overlays/hit testing, CSS-scaled pointer input, grid lines, selection/lasso/stamp/tool/replay feedback, and PNG dimensions agree without independent X/Y object distortion. Exact 3×5 geometry, fractional pointer/object coordinates, source wiring, and a byte-exact 3×5 production tile cover the correction. Focused pinned-Node-24 typecheck/ESLint and 8 files / 36 tests pass; the complete safe gate passes portability for 369 tracked plus 65 prospective paths, TypeScript, full ESLint, and 144 files / 844 tests. The formal performance gate passes at 823.89 MiB RSS growth, with the 65,536-tile render at 40.01 ms, native save at 125.37 ms, and one-million-cell flood at 169.12 ms. Canonical storage and Tiled bytes are unchanged; native-size tall-tile overhang, explicit Tiled tile offsets/alignment, non-square diagonal-transform policy, infinite-view expansion, packaged visuals, Level 3, RC, and stable-v1 readiness remain unclaimed; MAP-07 stays Working.
- A headless MAP-09 raster-parity checkpoint (2026-08-12) makes canonical visible map-object layers observable outside the editor. Rectangle, ellipse, polygon, and polyline overlays now use one shared deterministic path/style renderer in the interactive canvas, headless observation, and raster export; nested flattened z-order, visibility, multiplied layer opacity, and the shared orientation-specific object matrix stay exact. Headless rendering uses canonical pan zero, so parallax contributes no displacement, and it never adds advisory selection handles. Exact regressions prove above/below/hidden ordering, object-only output, production PNG/headless pixel equality, and an 8×4 isometric object at 50% opacity. Focused pinned-Node-24 typecheck/ESLint and 5 files / 23 tests pass; the complete safe gate passes portability for 369 tracked plus 62 prospective paths, TypeScript, full ESLint, and 142 files / 835 tests. The formal performance gate passes at 817.33 MiB RSS growth, with the 65,536-tile orthogonal render at 38.51 ms, native save at 127.82 ms, and one-million-cell flood at 172.37 ms. Tiled metadata/output bytes are unchanged; Tiled-native object styling, viewport-specific headless parallax, packaged drag/visual behavior, broad Tiled fidelity, Level 3, RC, and stable-v1 readiness remain unclaimed; MAP-09 stays Working.
- A headless MAP-08 projection-parity checkpoint (2026-08-12) replaces three drifting isometric formulas with one pure geometry contract. Headless tiles now begin at the true left boundary instead of a half-cell offset; the editor derives rectangular-map fit, vertical aspect, artwork, object matrices, pointer inversion, parallax deltas, stamps/tools/replay feedback, lasso paths, and selection outlines from the same extent/cell geometry. Exact asymmetric 3×5 tests and a one-cell 4×2 source tile prove all edge pixels, continuous inverse coordinates, object projection, and parallax deltas; sparse right-down depth tests remain exact, and the corrected maintained isometric raw-RGBA golden is `fb2f38d…d268`. Focused pinned-Node-24 typecheck/ESLint and 6 files / 22 tests pass; the complete safe gate passes portability for 369 tracked plus 59 prospective paths, TypeScript, full ESLint, and 140 files / 827 tests. The formal performance gate passes at 817.88 MiB RSS growth with the 65,536-tile orthogonal render at 38.98 ms. Native-size tall-tile offsets/overhang, non-square diagonal-transform geometry, infinite-view expansion, packaged interaction, broad Tiled fidelity, Level 3, RC, and stable-v1 readiness remain unclaimed; MAP-08 stays Working.
- A headless MAP-08 checkpoint (2026-08-12) makes isometric sparse-layer compositing independent of chunk record insertion. Interactive and headless paths share a lazy right-down iterator: chunk bands sort by Y, chunks within a band sort by X, and complete rows stream left-to-right while only one band is decoded. A reverse-inserted two-tile fixture straddles rows 31/32 and proves the lower overlapping cell remains in front; the maintained raw-RGBA golden corpus stays unchanged. Orthogonal rendering keeps its established streaming loop, and canonical chunks/GIDs/Tiled output do not change. Focused pinned-Node-24 typecheck/ESLint and 4 files / 11 tests pass; the complete safe gate passes portability for 369 tracked plus 56 prospective paths, TypeScript, full ESLint, and 138 files / 819 tests. The formal performance gate passes at 820.83 MiB RSS growth with every metric under budget. Native-size tall-tile overhang/offset and non-square transform geometry, projection/navigation polish, packaged isometric visuals, broad Tiled fidelity, Level 3, RC, and stable-v1 readiness remain unclaimed; MAP-08 stays Working.
- A headless PIX-06/MAP-11 checkpoint (2026-08-12) adds strict version-1 reusable stamp-library JSON copy/load/paste with an explicit non-mutating preview and one atomic append-or-replace transaction. Append is the non-destructive default and assigns fresh IDs plus collision-free names. Pixel transfer maps referenced colors exactly, appends exact missing colors, and fails rather than approximating when the 256-color palette is full. Tile transfer preserves transformed 32-bit GIDs only when the active map exactly matches every exported tileset name/identity/revision/GID range/geometry and source-sprite identity/revision/size; assets are not bundled or rebased. Input is bounded to 16 MiB and 262,144 aggregate cells in addition to canonical library/stamp limits. Focused pinned-Node-24 typecheck/ESLint and 4 files / 15 tests pass; the complete safe gate passes portability for 369 tracked plus 53 prospective paths, TypeScript, full ESLint, and 136 files / 815 tests. The formal performance gate passes at 823.14 MiB RSS growth with every metric under budget, while the non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 133 dirty paths. Arbitrary tile asset bundling/GID rebasing, packaged pointer/file/clipboard acceptance, broad interchange, Level 3, RC, and stable-v1 readiness remain unclaimed; PIX-06 and MAP-11 stay Working.
- A headless PIX-10 follow-up (2026-08-12) adds bounded session-local onion settings without changing canonical documents. Previous/next sides independently choose zero through four frames, six-digit tint, and 0–100% base opacity; Reset restores the former one-previous teal 22% / one-next pink 18% behavior. A pure validated planner yields at most eight layers in global far-to-near order and fades each side by base opacity ÷ distance. Opening settings does not silently enable Onion, and the settings/cel-exposure overlays close each other. Focused pinned-Node-24 typecheck/ESLint and 3 files / 7 tests pass; the complete safe gate passes portability for 369 tracked plus 49 prospective paths, TypeScript, full ESLint, and 134 files / 808 tests. The formal performance gate passes at 813.23 MiB RSS growth, while the non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 129 dirty paths. Settings persistence, an export/MCP mutation contract, maximum-size multi-frame pacing, packaged visual/pointer behavior, Level 3, RC, and stable-v1 readiness remain unclaimed; PIX-10 stays Working.
- A headless PIX-10 checkpoint (2026-08-12) adds a paged cel-exposure grid bounded to twelve frames × six pixel layers, exact blank/painted/linked/missing states, explicit layer/frame selection, and one-exposure link/unlink through the existing revision-checked asset replacement. Link follows the previous resolved cel and clears the exposure's own chunks; unlink snapshots current resolved pixels. Pixel tools now honor the selected visible unlocked layer, while a selected locked/hidden layer fails closed rather than editing another layer. Focused typecheck/ESLint and 4 files / 15 tests pass; the complete safe Node-24 gate passes portability for 369 tracked plus 45 prospective paths, TypeScript, full ESLint, and 132 files / 803 tests. The performance gate passes at 826.36 MiB RSS growth with every metric under budget, while the non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 125 dirty paths. Existing whole-frame linking remains; the bounded session-local onion follow-up above closes the adjacent settings source gap, while packaged grid/settings/replay/pointer acceptance remains open. PIX-10 stays Working and no Level 3, RC, or stable-v1 claim is added.
- A headless MAP-04 checkpoint (2026-08-12) adds explicit collision checkboxes, Select all/Clear/bulk-delete controls, Shift-additive canvas selection, and exact locked group dragging through one shared integer-translation kernel and one revision-checked tileset replacement. Group movement preserves collision order, local points, dimensions, types, and typed properties. Point/resize/property edits intentionally require one selected shape because group scaling/property merge is not defined. The first-frame collision preview now samples into at most 240×200 CSS pixels instead of iterating or allocating by source-tile size. Focused typecheck/ESLint and 3 files / 13 tests pass; the complete safe Node-24 gate passes portability for 369 tracked plus 43 prospective paths, TypeScript, full ESLint, and 131 files / 799 tests. The performance gate passes at 820.97 MiB RSS growth with every metric under budget, while the non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 121 dirty paths. Packaged pointer and Tiled-fixture acceptance remain open, so MAP-04 stays Working; no marquee/group-scale, broad Tiled, Level 3, RC, or stable-v1 claim is added.
- A headless MAP-10 checkpoint (2026-08-12) adds up to twelve clickable, probability-labelled crop thumbnails for each named non-Wang variant group and a one-click deterministic signed-32-bit **New stroke** seed persisted through the existing revision-checked map replacement. Existing painted GIDs remain unchanged; later UI/MCP choices stay coordinate-stable under the new seed. Crop sampling now bounds variant thumbnails to 32×32 and tile-animation preview to 40×40 even when a valid source tile is much larger. Format/MCP docs state literally that `aidraw:variantGroup` and `aidraw:variantSeed` are AIDraw-namespaced custom Tiled properties, not a native Tiled random-brush promise. Focused typecheck/ESLint and 4 files / 13 tests pass; the complete safe Node-24 gate passes portability for 369 tracked plus 42 prospective paths, TypeScript, full ESLint, and 130 files / 796 tests. The performance gate passes at 823.28 MiB RSS growth with every metric under budget, while the non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 117 dirty paths. Packaged pointer/visual acceptance remains open, so MAP-10 stays Working; no broad Tiled, Level 3, RC, or stable-v1 claim is added.
- A headless PIX-06/PIX-13/MAP-11 checkpoint (2026-08-12) exposes both clockwise and counterclockwise quarter-turn actions for sprite selections, tile selections, reusable pixel stamps, and reusable 32-bit-GID tile stamps. The UI calls the existing exact shared kernels and ordinary revision-checked mutation surfaces; non-square stamp tests preserve anchors, indexed colors, and Tiled transform flags in both directions, while grid coverage proves counterclockwise GID placement. Focused typecheck/ESLint and 5 files / 21 tests pass. The complete safe Node-24 gate passes portability for 369 tracked plus 41 prospective paths, TypeScript, full ESLint, and 130 files / 792 tests; the performance gate passes at 815.88 MiB RSS growth with every metric under budget. The non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 114 dirty paths. PIX-13's source exposure gap is closed, but it and the stamp rows remain Working pending their named packaged/cross-document exit criteria; no Level 3, RC, or stable-v1 claim is added.
- A headless MAP-02 checkpoint (2026-08-12) replaces destructive commit-on-blur tileset re-slicing with controlled draft fields, a bounded source-sheet thumbnail, and current/draft/selected crop overlays. Users explicitly choose source-position or tile-ID metadata mapping; one pure kernel remaps tile properties/probability/collisions, animation targets, Wang representatives, and Wang assignments, reports exact reframe/renumber/loss counts, fails ambiguous source-position mappings closed, and requires acknowledgment before affected metadata commits as one revision-checked asset replacement. Blank maximum-size slices remain sparse. Focused typecheck/ESLint and 3 files / 8 tests pass. The complete safe Node-24 gate passes portability for 369 tracked plus 41 prospective paths, TypeScript, full ESLint, and 130 files / 791 tests; the performance gate passes at 829.23 MiB RSS growth with every metric under budget. The non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, 57 unclosed selected-v1 P1, and 111 dirty paths. Packaged pointer/visual acceptance remains open, so MAP-02 stays Working; no broad Tiled, Level 3, RC, or stable-v1 claim is added.
- A headless MAP-03 checkpoint (2026-08-12) completes the selected-tile metadata workflow's automated exit criteria. Animation frames now reorder through native drag-and-drop or equivalent keyboard move buttons using one immutable kernel and one revision-checked tileset replacement. A pausable duration-aware preview uses Tiled tile-list semantics, the source sprite's first frame, and a crop-only layered compositor; it starts paused when reduced motion is requested and does not allocate the complete source sprite. The maintained hand-authored TMJ/TSJ and TMX/TSX fixture pairs now contain ordered multi-frame animations and prove import, production export with the ordinary PNG companion, and re-import preserve exact frame order/durations, probability, typed properties, and collision metadata. Focused typecheck/ESLint and 4 files / 14 tests pass. The complete safe Node-24 gate passes portability for 369 tracked plus 37 prospective paths, TypeScript, full ESLint, and 128 files / 783 tests; the unchanged performance gate passes at 822.14 MiB RSS growth with every timing under budget. The non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, now 57 unclosed selected-v1 P1, and 105 dirty paths. This satisfies MAP-03's automated exit criteria, so it is Verified; packaged pointer/visual behavior, image-collection tilesets, arbitrary Tiled extensions, broad compatibility, Level 3, and RC readiness remain unearned.
- A headless PIX-04 checkpoint (2026-08-12) completes the pixel-tool automated exit criteria without inferring packaged interaction. One shared core surface now owns ordered pixel-perfect corner cleanup, exact symmetric integer ellipses, cached chunk reads, and compact flood/replacement row runs. Flood and wand cap the actual connected result at one million cells; replacement caps the entire inspected target at one million cells; all cap output at 65,536 runs and fail without partial edits. A live semantic flood now edits a one-pixel island inside a 1,001,000-cell sparse sprite instead of rejecting the canvas wholesale, while an over-limit component leaves the document exact. UI fill/replacement remain one-shot, and pointer cancellation or Escape clears drafts before best-effort lock release rather than committing. Focused typecheck/ESLint and 4 files / 46 tests pass. The complete safe Node-24 gate passes portability for 369 tracked plus 30 prospective paths, TypeScript, full ESLint, and 125 files / 775 tests. The formal performance gate passes with a production-cel one-million-cell fill compacted into 1,000 runs in 172.1 ms under its new 2,000 ms tripwire and 818.31 MiB total RSS growth. The non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, now 58 unclosed selected-v1 P1, and 96 dirty paths. This satisfies PIX-04's automated exit criteria, so it is Verified; packaged pointer execution, Level 3, RC, and broader format/platform claims remain unearned.
- A headless MAP-12/QA-02/QA-03 checkpoint (2026-08-12) corrects the duplicated Tiled tile-transform composition that swapped diagonal-only with diagonal-plus-both-flips on a labeled square tile. One core affine matrix now applies Tiled's anti-diagonal first and horizontal/vertical flags afterward in both the interactive and headless/export renderers. Pure corner mapping and exact raw pixels cover all eight combinations; the maintained golden adds SHA-256 `6bc27075…44934d`; and a separate production orthogonal TMJ+PNG export/import preserves the eight unsigned GIDs with pixel-identical rendering. Focused coverage passes 4 files / 21 tests, and the complete safe Node-24 gate passes portability for 369 tracked plus 25 prospective paths, TypeScript, full ESLint, and 122 files / 761 tests. The unchanged performance gate passes at 741.47 MiB RSS growth with the 65,536-tile render at 38.88 ms. The non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, 59 unclosed selected-v1 P1, and 85 dirty paths. Transform thumbnails, packaged interaction, isometric-specific visual evidence, non-square tile policy, broader Tiled fidelity, Level 3, and RC readiness remain unclaimed; MAP-12 and QA-02/QA-03 stay Working.
- A headless FND-07/FND-09/QA-08 follow-up (2026-08-12) closes the adjacent data-bearing recovery and utility-import-result image gap without broadening codec claims. Every utility-returned generic asset now passes metadata validation; each embedded payload must also pass the established canonical base64, length/SHA, PNG/APNG/JPEG/WebP/GIF MIME/header, 128 MiB, display-geometry, 8,192-side, and 16MP checks plus a supervised decode before the import resolves. Malformed envelopes retire their producer and decoder exit returns no document. Profile recovery applies the same payload contract, but preserves the recovered document/tab/dirty/file state and asset metadata, omits only invalid `data`, and exposes one aggregate renderer-bootstrap warning. Whole-import rejection was retained because utility output is not yet user state; warning-and-omission was chosen for recovery to avoid discarding otherwise recoverable unsaved work. Focused typecheck/ESLint and 3 files / 44 tests pass, the broader producer/consumer selection passes 10 files / 86 tests, and the complete safe Node-24 gate passes portability for 369 tracked plus 25 prospective paths, TypeScript, full ESLint, and 122 files / 758 tests. The performance gate passes at 739.88 MiB RSS growth and 128.1 ms native save. The non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, 59 unclosed selected-v1 P1, and 81 dirty paths. Metadata-only recovery records, immediate journal rewriting, arbitrary codec compatibility, packaged warning presentation, broader PDF/PSD/image compatibility, Level 3, and RC readiness remain unclaimed; FND-09 and QA-08 stay Working.
- A headless FND-09/SEC-05/QA-08 checkpoint (2026-08-12) moves generic image admission decode out of the main process. Live `asset.add`, production native open/save, and packaged CLI native reads first enforce canonical base64, declared length/SHA, PNG/APNG/JPEG/WebP/GIF MIME/header, display geometry, 8,192-side/16MP, and their established 1.5 MB inline or 128 MiB native-entry ceiling; a typed worker request then performs the real codec decode. Decoder exit rejects the active task and queued work restarts fresh. Live rejection preserves the document; native open/CLI preserve metadata but omit invalid data with an explicit warning; save rejects before preview rendering or filesystem mutation. Focused typecheck/ESLint and 7 files / 81 tests pass, a broader producer/consumer selection passes 17 files / 124 tests, and the complete safe Node-24 gate passes portability for 369 tracked plus 25 prospective paths, TypeScript, full ESLint, and 122 files / 753 tests. The performance gate passes at 757.44 MiB RSS growth and 129.26 ms native save. The non-certifying audit remains Level 3 `BLOCKED` 2 / stable v1 `BLOCKED` 5 with 25 unfinished P0, 59 unclosed selected-v1 P1, and 79 dirty paths. That checkpoint by itself did not establish recovery-journal or utility-import-result asset reconciliation; the follow-up above closes only the data-bearing recovery and embedded-result payload boundaries. It still does not establish arbitrary codec compatibility, packaged/native proof, Level 3, or RC readiness; FND-09 and QA-08 stay Working while SEC-05 remains Verified on its now-supervised source contract.
- A deterministic release-readiness preflight (2026-08-11) now separates the literal Level 3 RC gate from the stricter stable-v1 checklist. `rc:audit` non-certifyingly reports the current checkout as blocked by missing fresh Level 3 evidence, 25 non-Verified P0 rows, absent stable-v1 native/signing/reproducibility/prerequisite evidence, 59 selected-v1 P1 rows that are neither Verified nor Deferred, and the current uncommitted checkpoint. `rc:verify` fails closed on candidate commit/version/cleanliness, exact independent report identity/hash/sections, automation/MCP/Computer Use/cross-surface PASS, defect disposition, documentation reconciliation, native results, and the checksummed release artifact/license matrix; `--stable-v1` additionally enforces tracker closure and remaining stable evidence. The original focused verifier/release-metadata run passed 11/11; the latest complete launch-free Node-24 gate passes portability for 369 tracked plus 18 prospective paths, TypeScript, full ESLint, and 120 Vitest files / 716 tests. This does not create native evidence, establish a release candidate, sign, tag, publish, or replace independent review.
- A headless REL-07 follow-up (2026-08-12) makes every native release job retain clean-commit source/toolchain/checksum/artifact provenance and adds an exact two-manifest comparator. Stable-v1 verification now rejects the former bare reproducibility PASS and requires hashed zero-difference comparison reports for Windows x64, macOS arm64, and Linux x64 plus separate dependency/license review. The focused provenance/RC/release selection passes 4 files / 26 tests; the complete Node-24 gate passes portability for 369 tracked plus seven prospective paths, TypeScript, full ESLint, and 114 Vitest files / 632 tests. No independent native build pair ran, so no reproducibility, hosted, packaged, platform, or release claim is added and REL-07 remains Partial.
- A headless ILL-18 follow-up (2026-08-12) makes repeated native saves reuse validated untouched paint tiles and rerender only keys touched by appended strokes. The newly materialized cache reaches the live document only after atomic replacement succeeds, while failed saves retain the prior cache and destination; native open now rejects incomplete tile indexes instead of trusting partial stroke coverage. The focused persistence/render/reducer/cache selection passes 4 files / 39 tests, the complete Node-24 gate passes portability for 369 tracked plus seven prospective paths, TypeScript, full ESLint, and 114 Vitest files / 634 tests, and the unchanged performance gate passes 1/1 at 736.14 MiB RSS growth with every timing below budget. Packaged reopen/erase/edit acceptance remains missing, so ILL-18 stays Working and no RC, platform, or native-UI evidence is inferred.
- A headless FND-09 follow-up (2026-08-12) closes the previously open common utility-result policies without broadening native claims. Export now requires the request's exact primary and companion identities, rejects unexpected adjacent files, and caps 4,097 members at 512 MiB aggregate decoded data before base64 decode. Import documents are measured without a second whole serialization against 512 MiB / 16,777,216 nodes / 128 levels before migration; reports and warnings share a 4,096-entry / 1-MiB serialized ceiling; generic observation results derive their allowance from the existing 4 MiB PNG plus 64 KiB; provider metadata is bounded JSON at 64 KiB; progress is capped at 4 KiB; and terminal errors at 16 KiB. Invalid terminal results retire the worker and queued work recovers fresh, while oversized advisory progress is ignored. The focused utility/export/Tiled/provider selection passes 20 files / 170 tests; the complete Node-24 gate passes portability for 369 tracked plus ten prospective paths, TypeScript, full ESLint, and 115 Vitest files / 638 tests; and the unchanged non-GUI performance gate passes 1/1 at 734.41 MiB RSS growth with every timing under budget. These are post-IPC source contracts, not pre-deserialization or exact utility-process RSS, provider, packaged/native, broader-platform, Level 3, or RC evidence; FND-09 stays Working.
- A headless GEN-05 follow-up (2026-08-12) completes the source-level protected provider-credential lifecycle. Concurrent OpenAI/Stability updates serialize, rotations use a flushed uniquely created temporary (requested mode `0600` on POSIX) plus atomic replacement, failed encryption/replacement and over-16-KiB input preserve the prior file, per-provider removal retains the other key, and removing the last key deletes the file. When DPAPI is unavailable, status distinguishes encrypted material from a usable credential, refuses new plaintext, does not decrypt, and still permits explicit deletion. The renderer offers Replace and two-step Remove controls but receives only configured/stored/availability status through the frozen write/status-only preload surface. The focused security/generation selection passes 7 files / 60 tests; the complete safe Node-24 gate passes portability for 369 tracked plus 11 prospective paths, TypeScript, full ESLint, and 116 Vitest files / 642 tests; and the unchanged performance gate passes 1/1 at 753.19 MiB RSS growth with every timing under budget. This satisfies GEN-05's automated source exit criteria, so it is Verified; it is not native DPAPI/Keychain/libsecret, real-provider, packaged-interaction, broader-platform, Level 3, or RC evidence.
- A headless QA-03/IO-01 follow-up (2026-08-12) fixes standalone JPEG EXIF orientation instead of accepting only encoded geometry. One frozen 948-byte orientation-6 fixture retains its exact source SHA-256, imports 24×16 encoded pixels as a 16×24 illustration and pixel sprite, survives native save/reopen, and re-exports a PNG pixel-identical to the decoder-applied blue/red/yellow/green display. The bounded APP1/IFD0 parser covers big- and little-endian TIFF, recognizes only valid SHORT/count-one orientations 1–8, and ignores a hostile out-of-range IFD offset; inline admission shares the same display-geometry check. The focused raster/import/security selection passes 6 files / 65 tests; the complete safe Node-24 gate passes portability for 369 tracked plus 14 prospective paths, TypeScript, full ESLint, and 117 Vitest files / 645 tests; and the unchanged performance gate passes 1/1 at 746.67 MiB RSS growth with every timing under budget. IO-01 and QA-03 remain Working because color/alpha/vendor corpora, JPEG metadata export, malformed-EXIF policy, other external raster producers, and packaged/native interaction remain open; this is not broad JPEG, platform, Level 3, or RC evidence.
- A headless QA-01 checkpoint (2026-08-12) verifies all 41 canonical operation/inverse pairs with bounded payload schemas, exact one-step forward/undo/redo restoration, eight deterministic 48-step histories per document family, and 32 seeded atomic rollback cases. It fixes ordered provenance, active pixel-asset restoration, sparse-zero chunk cleanup, structural replacement corruption, object-group dependency validation, and multi-step monotonic-revision undo. Per-actor logical-target lineage permits disjoint undo, blocks overlapping foreign or explicitly unrecorded work, preserves the global barrier for document-revision guards, invalidates unsafe redo, and reports undo/redo in the canonical revision change feed. The focused selection passes 11 files / 155 tests; the complete safe Node-24 gate passes portability for 369 tracked plus 18 prospective paths, TypeScript, full ESLint, and 120 Vitest files / 716 tests; and the unchanged performance gate passes 1/1. This satisfies QA-01's automated exit criteria, so it is Verified. It does not claim persistent history across restart, CRDT merging, packaged/native UI, broader platform, Level 3, or RC evidence.
- The newest macOS-compatibility baseline (2026-08-09) supersedes the earlier 598-test migration checkpoint below: Node 24.14.0 passes portability for 359 tracked + 5 prospective implementation paths, TypeScript, ESLint, and 111 Vitest files / 615 tests. The unchanged performance gate's final run passes at 744.58 MiB RSS growth; exact local app/DMG/import/Keychain/Level 2 evidence and remaining release blockers are itemized in `MACOS_DEVELOPMENT.md`. Hosted and Level 3 release evidence are not inferred.
- A bounded PDF safety follow-up (2026-08-11) makes every export that actually retains importer-authored invisible text warn that visible covering artwork is not redaction. An exact full-artboard black overlay leaves the 241×151 production re-import visually black with an exact opaque-black one-pixel-inset interior while pdf.js still extracts all three original phrases. The focused regression passes 3/3 under Node 24.14.0; portability passes for 369 tracked paths, TypeScript and full ESLint are clean, and the complete launch-free collection passes 112 files / 618 tests. This is a persistent-report warning and one exact overlay proof, not redaction-intent detection, content removal, a secure-redaction guarantee, packaged/native evidence, or broader PDF compatibility.
- A subsequent headless third-party PDF checkpoint (2026-08-11) freezes one LibreOffice Draw clipped-transparency PDF and an independent Poppler golden, then preserves its three supported text runs as invisible searchable content through AIDraw PDF re-export without exposing ordinary hidden layers or changing the raster fallback. Its focused production import/re-export regression passes 2/2 under Node 24.14.0 with clean TypeScript, full ESLint, and portability across 364 tracked + 5 prospective paths. The complete launch-free collection passes 112 files / 617 tests, including all 32 MCP contract cases. This fixture earns only the exact one-page compatibility evidence recorded under IO-05/QA-03; it is not a broader PDF, packaged/native, MCP, platform, or release certificate.
- Three independent macOS Level 2 certificates are retained. The first honestly failed 0/37 before product assertions because the harness selected a Windows EXE and undeclared retained profiles. The second correctly failed on active identity/transient bleed and all-dirty recovery. After monotonic workspace snapshots, active-only advisory updates, document-keyed/cancelled transients/locks, and ordered per-document recovery were added, final run `20260809T154905Z-macos-level2` strictly passed automation, exact native identity, two-actor MCP/Computer Use, exact eight-document three-dirty/five-clean restart, saved path/object, UI→MCP undo/redo, preview isolation, blank-original restoration, and graceful redacted cleanup. Its report SHA-256 is `fa75d068…0783`; launcher `8E1523B8…1BDE` / ASAR `1B2C8E40…909D`. Level 2 is earned for that immutable Apple Silicon subject; Level 3 and hosted release gates remain.
- The last complete launch-free source baseline passed **100 Vitest files / 512 tests**, plus clean TypeScript and ESLint, and the packaged Playwright matrix declares **32 scenarios**. The subsequent advisory generation-progress slice is verified separately at 1 focused file / 21 tests with clean typecheck and targeted ESLint; no newer aggregate result or package certificate is inferred.
- The exact FND-09 quantization-result Windows observer passed 1/1 in 2.9 seconds against source manifest `A642C883…77B9`, EXE `1CF937B4…DCCE`, and ASAR `CABAC58E…C815`. A real worker's two-change result against a one-pixel request was rejected before caller use and recovered from PID 8248 through queued replacement PID 23932; that replacement's x=1 contradictory result was likewise rejected and recovered through PID 24288. Both corrupt payloads stayed private, canonical and generation state stayed unchanged, public jobs plus non-loopback/provider/paid counts stayed zero, and graceful exit redacted credentials. The immutable profile is 18 directories / 26 files / 90,709 bytes / tree SHA-256 `FF6223E47EEA6A6C18A813D1C751B1109C31E70D11FB643367CD0A3C2DE4994D`; the output is one 45-byte file / tree SHA-256 `E9C8773F9C896351AEBEBCF13A1A6958D8D3AC1A65EF1053D885244EBAA3B22C`. This is the fixed quantization-result fixture only, not a generic result-pressure, provider, broader-platform, or release certificate.
- AIDraw is a capable **pre-v1 prototype**, not a completed v1 release. Workspace metadata now honestly identifies `0.1.0-alpha.1`; stable `1.0.0` remains gated by this tracker and the release checklist.
- The strongest areas are the application-owned document/reducer model, native ZIP persistence, indexed sprite storage, headless authenticated MCP, attributed trace/replay, basic illustration drawing, basic pixel animation, and presentation-scaled pixel export.
- The largest product gaps are packaged pointer/frame-pacing and hardware proof, deeper natural-media simulation, advanced third-party SVG/PDF/PSD fidelity, live provider compatibility, broader external interchange fixtures, and release evidence.
- The current launch-free verification baseline passes **109 Vitest files / 598 tests**, plus the new 245-tracked/114-prospective-path portability preflight, clean TypeScript, and ESLint. The packaged Playwright matrix declares **31 scenarios**. MCP cold-client discovery now has exact wire coverage for initialize instructions, ten described tools, strict conditional action branches, structured outputs, progressive `aidraw_help`, the optional `aidraw://guide`, safe retry/job next steps, and owner-private approval summaries. The exact `MCP-COLD-DISCOVERY` package then passed 1/1 in 2.3 seconds against EXE SHA-256 `3824676578BB15E1AA94D79D333DAA2D65B71FEC8111CC2D27F9956F8B40E0CB` and ASAR SHA-256 `C7C43234CB18322A221492AA44F3C4F60DDB2B20F60D9FE621DA28DE20F8349E`: a repository-free raw-MCP client learned the core workflow before reading the optional guide, kept private job/path/result data owner-scoped, cancelled the human-only approval without writing its target, made zero external/provider/paid/non-loopback requests, and exited gracefully with redacted credentials. This is packaged headless protocol evidence, not renderer/UI or exhaustive-action acceptance. The third exact `QA-06-TILED-RT` run passed 1/1 in 6.5 seconds against SHA-256 `931E5FF3CBA3EB6B62787534B8C1661DFFCD1E2D12C0EAAC5E358792F2D97B9D`: the production utility process decoded the exact TMJ+PNG pair, actor-bound allow-once first export/import/distinct re-export stayed within exclusive targets, renderer/authenticated-MCP state agreed at every stage, both persisted pairs were semantically and pixel-identical, public jobs stayed redacted, and cleanup was graceful and credential-safe with zero provider/paid activity. The initial utility-process `nativeImage` failure and the corrected-decoder source-only map-name observer overclaim remain immutable failed evidence; imported map naming is filename-derived and not claimed as portable Tiled semantics. The exact `UX-11-BATCH` selector passed 1/1 against SHA-256 `565EBFA09AFAE2285ECD2F1D405AFD1A947A138FF73F6292E9ACCDE45D8BDA3D`: three-document Save All, bounded batch export, and Close All each proved cancellation invariance and their allowed branch, including collision-safe paths, one sanitized per-document export failure, stable focus, renderer/authenticated-MCP agreement, independent revision-2 native-container parsing, exact profile-child targets, zero external/provider/paid requests, and graceful credential-redacted cleanup. The corrected exact `QA-06-PALETTE` selector passed 1/1 in 6.0 seconds against SHA-256 `58E2A194D2F4D06F704E187DE444BA6BF4DBE9211C5DEFB478F4FCD9DBE33FFF`: one deterministic generated result became a new human-owned indexed layer/cel while the original one-frame sprite, layers, palette, provenance, and agent-owned job remained intact; the exact area/OKLab/Bayer/0.6 conversion, renderer/authenticated-MCP/fixture agreement, public-summary redaction, zero external/provider/paid requests, and graceful credential-redacted cleanup all passed. Its earlier single-container text-observer failure remains immutable history. The corrected exact `QA-06-INFINITE` selector passed 1/1 in 6.0 seconds against that same package: exact UI geometry setup, fourteen signed-coordinate cells across seven 32×32 chunks, negative/positive navigation, renderer/authenticated-MCP/export equality, one actor-bound allow-once TMJ export with its sole PNG companion, zero network/provider/paid requests, and graceful credential-redacted cleanup all passed. Its earlier 64×64-default-versus-32×32-intent setup failure remains immutable history; only orthogonal sparse infinite maps are claimed, while isometric infinite maps and broad/full-fidelity Tiled interchange remain open. The third fresh exact `QA-06-GEN` run passed 1/1 in 27.2 seconds and proves the bounded no-network generated-fill lifecycle. Renderer recovery, held-human locks, the four-lane scheduler, two-document background collaboration, the no-charge file-approval lifecycle, animated-sprite lifecycle, and finite-map Wang-terrain lifecycle also pass against their exact Windows packages. Earlier observer failures remain preserved without weakening the public MCP job-summary privacy boundary. A controlled same-command comparison proved the earlier four-lane renderer failure was caused by launching Electron inside the Codex filesystem sandbox; packaged Playwright now refuses an unacknowledged Windows launch boundary. A native Computer Use smoke verified the OpenCode selector and consent dialog without approving global configuration or login-startup changes.
- The exact `FND-09-UTILITY-CONTAINMENT` package passed 1/1 in 3.5 seconds against EXE SHA-256 `9B0B1759215286F5C9ADB09A5AF38339E46A81911017BE36A97A4343845D1423` and ASAR SHA-256 `E2184D3689DE10B6EBBD3CA232366836927D1420849FD08FBEB415022B13859C`: one real raster worker crashed, its replacement rejected an abortable hang with `AbortError`, and a third worker completed the next task. Queued production PNG exports were byte-identical, canonical revision/hash stayed unchanged, authenticated MCP returned a post-restart render with an empty public job list, generation remained untouched, network/provider/paid counters stayed zero, and cleanup was graceful and credential-redacted. The separate exact `FND-09-UTILITY-PRESSURE` package then passed 1/1 in 3.7 seconds at EXE `0B10F6046267FDF3C981B7097A104439821C894A3DFDE900FCCDBF72D5C5BA65` / ASAR `EEF235C256892290060866C82829680481CD056DE1E896AD7ACC5A68939B07D9`: the 1,500,001st source byte rejected before base64/worker allocation, a real worker admitted 32 waiters and rejected the 33rd with retryable `utility_queue_full`, queued-10 cancellation/refill preserved exact FIFO through the same PID, canonical/authenticated-MCP state stayed unchanged, public jobs stayed empty, generation remained untouched, and cleanup was graceful and credential-redacted with zero guarded non-loopback/provider/paid activity. FND-09 remains Working; the later source checkpoint closes the common utility-result policies, while exact RSS, broader platforms, and more general provider pressure stay open.
- A subsequent headless FND-09 result-boundary slice closes the direct quantization-output gap: same-request successful results are capped at width×height entries, require unique in-bounds integer coordinates and valid palette indexes, and never resolve the caller when the child returns an invalid kind, payload, response envelope, or error envelope. Each invalid worker is retired and queued normal work restarts fresh. The fixed exact Windows result fixture now passes as described above; generic serialized/RSS pressure, providers, broader platforms, and packaged proof of the general-generation gate remain open.
- A separate headless FND-09 export-result slice now rejects malformed primary/companion serialization before `Buffer.from`: canonical base64, MIME/extension syntax, safe companion leaf names, and string-only reports are enforced, invalid workers retire, and queued valid exports recover through a fresh worker. That slice added no total ceiling; the later source resource checkpoint adds a 512 MiB aggregate decoded-data limit and exact request-derived companion shape without claiming a 512 MiB base64 IPC, packaged, or native boundary.
- The export-result package/static/list guard passed without launching AIDraw at source/controller manifest 283 files / 3,488,952 bytes / SHA-256 `6251F84C…B0A5`, immutable EXE `2F745784…8CEA2`, ASAR `DDE43A0F…E562D`, observer `19523978…2FCA`, and exactly `utility-containment.spec.ts:931:5` / one test in one file. The separately audited exact Windows observer then passed 1/1 in 2.9 seconds: malformed-primary worker 13124 recovered through 9500, malformed-companion worker 9500 recovered through 15680, both valid PNG+JSON recoveries agreed by hash, invalid results stayed private, canonical/generation state and public jobs stayed unchanged/empty, export targets and non-loopback/provider/paid counts stayed zero, and cleanup was graceful and credential-redacted. Its immutable profile is 18 directories / 26 files / 92,431 bytes / tree SHA-256 `A60186401FD6CD236B2B5A6D2761CB6F5F025877197DB2D7EE67EAF5FBDAD068`; output is one 45-byte file / tree SHA-256 `E9C8773F9C896351AEBEBCF13A1A6958D8D3AC1A65EF1053D885244EBAA3B22C`. This certifies only the fixed sprite-sheet PNG+JSON result boundary, not total serialization/RSS, other formats, providers, broader platforms, release readiness, or the deferred pipeline.
- A separate headless FND-09 import-result slice shares the current PDF 256-page ceiling as the 1–256 result-document boundary, runs every document through the established migration/schema gate, and requires dense string-only warnings before caller/workspace use. A fixed private real-SVG fixture mutates only completed worker output into a newer schema or numeric warning. Its exact Windows package at EXE `EB2DA9F9…90AD` / ASAR `4BF8947F…C705` passed 1/1 in 2.9 seconds: malformed-document worker 21900 recovered through 3436, malformed warnings on 3436 recovered through 27588, valid supported semantic hashes agreed, invalid results stayed private, canonical/generation state stayed exact, outputs/public jobs/non-loopback/provider/paid counts stayed zero, and cleanup was graceful and credential-redacted. Preserve the accepted 18-directory / 27-file / 92,066-byte profile tree `9FB88ACA…FAAC` and one-file output tree `E9C8773F…B22C`. That slice added no serialized-document or warning-size ceiling; the later source checkpoint meters imported JSON at 512 MiB / 16,777,216 nodes / 128 levels and warning arrays at 1 MiB serialized without changing the retained package claim.
- A separate headless FND-09 observation-result slice accepts only established observation errors or an exact request-derived static PNG, including canonical base64, the existing 4 MiB ceiling, bounded chunk/CRC structure, request geometry and one full matching RGBA decode. Exact Windows package `A1FB98E3BB482367F17D011B83005F1410180BBD270E0A78329C7FA05CF1E1C5` / ASAR `530DF2761572F554F46B0C14BAB5B4455E19439CDC7D43E68B5CADEEBF932DC9` then passed the fixed corrupt-IDAT lifecycle: the bad result stayed private, its worker retired, queued capture recovered in a distinct worker, canonical/generation state stayed exact, and cleanup was graceful and credential-redacted with zero network/provider/paid activity. The later source checkpoint adds a generic serialized allowance derived from the 4 MiB PNG's maximum base64 plus 64 KiB; the retained package remains one exact Windows fixture, not an RSS, broader-platform, renderer, or wider observation certificate.
- A separate headless FND-09 generation-result slice now bounds successful child output by the originating `resultCount` and existing per-image 32 MiB / 8,192-side / 32 MP producer limits. Outputs require unique IDs, exact PNG/JPEG/WebP MIME/header/dimensions, canonical base64, request-consistent seed attribution and record-shaped provider metadata before preview/job use; malformed workers retire and queued valid results recover fresh. Main-owned canonical provenance still derives from the approved request, not provider metadata. Empty/partial results retain the existing no-automatic-retry manager behavior. The later source checkpoint bounds provider metadata at 64 KiB / 4,096 nodes / 32 levels; no exact RSS or new packaged/native claim is added, and the separate acceptance policy below handles previews above the later 1.5 MB boundary.
- The provider-free general-generation package/static/list checkpoint passes against source/controller manifest 295 files / 3,600,834 bytes / `7F411C91…AB16`, immutable EXE `13AF1B6A…1637`, ASAR `82B01525…6F9E`, and observer `32B7A8FF…CB0` exactly at `utility-containment.spec.ts:1423:5` / one test in one file. Its independently audited exact Windows run then passed 1/1 in 4.0 seconds: over-count, MIME/header, declared/header dimension, request-derived seed, and metadata-shape results stayed private across five retired fault workers; every queued valid request recovered through the six-worker chain ending at PID 14316. Canonical/authenticated-MCP and raster-lane state stayed unchanged; input/output targets, public jobs, non-loopback/provider/paid counts stayed zero; and cleanup was graceful, credential-redacted, and survivor-free. This is the fixed provider-free local 1×1 fixture only, not a provider, aggregate serialization/RSS, retry/progress, broader-platform, release, or pipeline certificate.
- Generation progress is now an explicit advisory boundary: only a finite 0–1 value and string message for the currently active `generation-run` request reaches its callback. Malformed values are not coerced, and progress-shaped messages on raster work stay invisible; neither case aborts terminal work or replaces the worker. The later source checkpoint adds a 4 KiB message limit on both worker post and main relay; terminal result, timeout, and cancellation remain authoritative, and no RSS or packaged/provider policy is inferred.
- A bounded headless generation-acceptance slice closes that stage-policy gap without changing previews. At the human acceptance boundary, oversized but otherwise valid previews are copied through the raster lane at the same dimensions: deterministic lossless PNG re-encoding first, then the first fitting quality from a fixed descending alpha-preserving WebP ladder. The accepted copy must be at most 1,500,000 bytes and pass the full inline-asset decode/hash/MIME/geometry contract before quantization or commit. Source/accepted hashes, sizes, codecs and quality are returned and recorded in provenance/job state; the original provider output remains byte-exact. If same-geometry normalization cannot satisfy policy, acceptance is unavailable with explicit guidance and canonical state stays unchanged. This is source-only, not packaged/native evidence.
- AGT-14 now has a launch-free installed-OpenCode safety checkpoint for exact version/hash identity, a fresh direct-child retained run root, disposable XDG/home/AppData/Electron user-data paths, an allowlisted child environment that excludes provider and unrelated secrets, the exact product `mcp.aidraw` shape, immutable PID evidence, and post-exit config redaction. Native client discovery is still unearned and requires a separately enforced offline boundary plus exact process/window audit.
- QA-08 newest-32 checkpoint budget evidence now proves a writer-produced 32-checkpoint archive reopens with exact ordered summary/document metadata and an exact main sprite. A CRC-valid untrusted twin prepending two older parse/migration traps still returns only those newest 32 with no warnings or main-document change because surplus entries are sliced away before checkpoint parsing or migration; production and the established policy are unchanged.
- QA-08 checkpoint-index corruption evidence now proves invalid JSON and a non-array JSON value are isolated before checkpoint payload processing. Each CRC-valid twin changes only the index, returns the exact main sprite, no checkpoints, and only the established corrupt-index warning while retaining the valid checkpoint payload byte-for-byte; production is unchanged.
- QA-08 checkpoint metadata-mismatch evidence now proves a selected payload `documentId` inconsistency is isolated after that checkpoint migrates: only the selected entry is excluded with its exact per-entry warning, while the other ordered checkpoint and exact main sprite remain unchanged. The CRC-valid twin changes no index or other entry, and production is unchanged.
- The recovered 283-path Layers 1–119 checkout and subsequent product-focus, FND-05 recovery/acceptance, UX-01 density, truth, package-generation, and window-chrome preparation/correction checkpoints are synchronized through base `0f42b227e6fe88c27e7bd1cdaccf3f4456f62984`. This bounded **ten-path** source/headless UX-01 correction records consumed window-chrome r4 and separates exact-app activation/readiness, trusted renderer event delivery, and product menu outcome. R1/r2 retain the established origin/decorated-frame findings; r3 proves a stale expected-bounds/evidence seam but not why the native window moved; r4 proves only that one safe exact-PID CoreGraphics post call completed before the menu stayed absent, because it did not record frontmost/focus readiness or renderer delivery. R1–r4 remain immutable controller/evidence results, not product failure or native PASS. Alive-hang r1/r2/r3/r4 remain separately attributed immutable failures, while r5 is the distinct narrow macOS/arm64 current-package PASS. The retained `codex-pre-pull-macos-compatibility` stash stays separate. The clean synchronized base's non-certifying audit remains Level 3 blocked **1** / stable v1 blocked **4**; the unstaged candidate adds only the exact `source_not_clean` issue, producing **2 / 5** until ordinary review and commit. Final verification totals are recorded in `docs/TESTING.md`; no controller correction or narrow native result earns Level 3 RC or stable-v1 evidence.
- A completed competitiveness assessment observed one older long-running AIDraw instance remaining as a blank window with no renderer helper, while a fresh isolated launch of the current package succeeded. Source diagnosis found one directly matching current-code contract: a live single-instance owner treated any non-destroyed native shell as attachable, so a later launch merely showed/focused it even after its renderer process was gone; a renderer lost mid-gesture could also strand its non-expiring human lock. The current owner now retires a gone/failed/stale renderer shell before admitting one bounded replacement against the same canonical engine. Confirmed detach clears that renderer's transient gesture locks without cancelling already admitted canonical work; tentative/cancelled close, concurrent show, stalled-load, exception, and failed-shutdown races are contained. Pinned Electron 43 responsiveness events add bounded confirmation for an alive but persistently unresponsive shell while retaining transient stalls; immutable r5 now supplies one narrow macOS/arm64 current-package acceptance for that exact input-ACK-hang path. Read-only forensics now prove the older owner/default-profile/no-renderer shape and its mapped unlinked old bundle generation after pathname replacement; the supported package path separately refuses a same-root replacement. This still does not prove the initiating renderer-loss trigger, cryptographic identity of the inaccessible old bytes, renderer-local unsubmitted work, cache/profile repair, customer updater semantics, or broader platforms. FND-05 owns those remaining gaps without erasing its narrower exact-package attach/show/reattach, crash-replacement, or alive-hang evidence; UX-05 and FND-07 retain their distinct renderer-event and persisted-document recovery evidence.
- Product focus is now ordered: close the observed lifecycle and control-density defects; establish the professional input/workspace baseline in UX-01/08/09/10; deepen the local, auditable sprite/animation/palette/cel/onion/tag/tileset/terrain/map and agent workflows to compete with the combined Aseprite + Tiled workflow; measure those tasks with external technical artists in QA-12; keep the release gates authoritative; then build the multi-asset game-production moat in AGT-17. This is workflow focus, not Aseprite file-format parity, broad Tiled compatibility, or a Photoshop/Illustrator replacement claim.
- UX-01 now has a source/headless professional-density baseline rather than whole-UI scaling. One shared contract requests a 1,520×940 default renderer content viewport and enforces a 980×640 minimum renderer content viewport; Electron content-size semantics keep native frame insets outside that renderer floor. macOS uses `hiddenInset` traffic lights within the existing 56 px app top bar so the content contract does not add a second vertical title-bar band; traffic-light space is reserved and top-bar controls are explicitly no-drag. Other platforms retain their ordinary native title bars. The same contract adds 12/11/10 px primary text tiers, 38/32 px primary/secondary pointer targets, 19–21 px primary icons, and selectively enlarges the top bar, tool rail, contextual controls, inspector navigation/layer-object actions, status/zoom controls, pixel floating controls, and timeline. PixelCanvas measurement plus every CSS timeline/panel offset consume the same 138 px timeline token. At the supported minimum the compact 62 px rail and 286 px inspector retain a 632×498 base canvas floor, or 360 px with the timeline. Crowded regions scroll on their own axis and compact mode removes only redundant named visual text. This is deterministic source/CSS contract evidence, not packaged visual, 200%-text, high-DPI, assistive-technology, hardware, or customizable-workspace acceptance, so UX-01 remains Working.
- UX-01 density r1 exited 1 with 1 failed in 3.8 seconds before density assertions because remote CDP lacked `Browser.getWindowForTarget`; owner/quit PIDs 26736/26752 exited 0 with zero survivors. Its user-only trace/HTML containing the now-inactive local bearer is immutable and non-reusable. Credential-safe r2 exited 1 with 1 failed in 3.7 seconds because 1,520×940 requested content measured 1,520×928; owner/quit PIDs 29908/29926 exited 0 with zero survivors, no later assertion/screenshot ran, and its private evidence is immutable. Root/work-area evidence led to the `hiddenInset` correction. Secure r3 then exited 1 with 1 failed in 11.4 seconds after exact default geometry and initial long-document checks passed; owner/quit PIDs 45883/45903 exited 0 with zero survivors. It failed because the controller raced deferred active-item focus, and headless evidence corrected the wait/Home/wait/ArrowDown/wait route without changing production focus behavior. R4 was consumed before package output at Forge's missing-`npm` system check. Fresh secure r5 subsequently ran exactly once and passed the narrow supported-viewport/disclosure/reachability/timeline/screenshot/network/redaction/cleanup contract. R1–r3 and r5 remain immutable and non-reusable.
- Separate UX-01 native traffic-light/top-bar r1 `20260814t092808z-7ff773c-r1` failed **1/1 in 3.2 seconds** before input because it equated Quartz absolute `x` with Blink `screenX`. Fresh r2 `20260814t102354z-f7e6d0c-r2` failed **1/1 in 2.6 seconds** before input after permission and exact owner 14422 / renderer 14431 / layer-zero window 7029 admission: Quartz `203,32 1514×936` did not equal trusted Blink `200,30 1520×940` at DPR 1 with no overflow. Fresh r3 `20260814t112230z-ff44905-r3` admitted owner 22702, one renderer, and layer-zero window 7030 with the accepted dynamic relation, then exited **1** before input because target observation was followed by a driver reinspection against an older native-bounds snapshot; the missing action-time bounds prove a controller TOCTOU/evidence gap, not the cause of the native move. Fresh r4 `20260814t121343z-0f42b22-r4` admitted owner 30153, renderer 30168, and layer-zero window 7031 with stable/action-ready Quartz and Blink both `200,30 1520×940`, then completed one exact-PID CoreGraphics post while the menu stayed absent for 10 seconds. Because r4 recorded neither exact-app frontmost/focus readiness nor renderer delivery, it earns posted-input evidence only and is not a product-defect finding. Every run cleaned up gracefully with zero survivors. The corrected source route retains raw measurements, dynamic decorated-frame containment, complete bounded hit neighborhoods, paired stability/action-ready snapshots, exact current-bounds admission, native AppKit traffic-light metrics, and independent per-space movement proof without a fixed conversion or widened tolerance. It now activates only the exact run-owned regular AppKit PID with default options, requires exact frontmost PID plus visible focused renderer readiness, rechecks readiness before every post, and requires an ordered trusted primary pointer/mouse/click sequence on the exact button before the separate menu-visible product assertion. No production `acceptFirstMouse`, deprecated ignore-other-app activation, blind second click, global input, Accessibility/private API, or production test hook is introduced. R1–r4 remain immutable controller/evidence results, not privacy completion or a native traffic-light/top-bar PASS. A wholly fresh package and separate native authority remain required. The route uses Option-click standard green-button zoom rather than fullscreen. The narrow density r5 PASS and this correction do not complete UX-01's native window-chrome, ordinary-green fullscreen, accessibility, hardware/display, customizable-workspace, broader-polish, RC, or stable-v1 exits.

### Audit snapshot

| Measure | Count |
| --- | ---: |
| Tracked items | 146 |
| Verified | 50 |
| Working | 82 |
| Partial | 7 |
| Scaffolded | 2 |
| Missing | 4 |
| Deferred | 1 |
| P0 / P1 / P2 | 59 / 74 / 13 |

Requirements added during hands-on testing and competitiveness review are included explicitly: autonomous headless work and trace replay (FND-04, AGT-10), model/effort attribution (AGT-16), the observed stale-instance and control-density defects (FND-05, UX-01), professional input/workspace acceptance (UX-08/09/10), external technical-artist task benchmarking (QA-12), the game-production agent moat (AGT-17), natural-media and custom brushes (ILL-07/08), polished material construction (ILL-19), illustration animation (ILL-21), presentation upscaling and CLI export (PIX-15), large-run save/export/close UX (UX-11), and independent three-level Luna/high MCP plus Computer Use QA (QA-09/10).

## 1. Foundation, process model, and storage

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| FND-01 | Node 24 npm workspace, Electron/React/TypeScript shell | 🟢 Working | P0 | Workspace and Forge/Vite build exist. Packaging/formal-QA runtime preflight now rejects non-24 Node and post-package verification rejects a missing executable after a misleading Forge exit. Verify clean-clone `npm ci`, package, and launch in CI. |
| FND-02 | Sandboxed renderer and narrow IPC boundary | ✅ Verified | P0 | A shared parsed policy requires the exact main-window web contents/frame and packaged or configured-loopback origin before navigation or IPC. Headless contracts freeze the 56-function named preload allowlist, and the exact Windows package passes nine-fuse plus bundled BrowserWindow/permission/window/navigation/webview/CSP/source verification. The independent `FND-02-PACKAGED-SECURITY` run passed 1/1 against EXE SHA-256 `31338B835FEC7852D4556015F4703A088E5344359954DE1B6511AE1D6CA7829D`: live `aidraw://app` contextBridge serialization exposed exactly those 56 frozen functions, no Node/generic IPC primitive, effective inline-script rejection, denied notifications/windows/navigation, no webview primitive, and a working typed bootstrap, followed by graceful credential-redacted cleanup and zero provider/paid/non-loopback observations. |
| FND-03 | Application-owned schemas, migrations, reducer, inverses | ✅ Verified | P0 | Schema rejection, migration, atomic reducer inverses, stale revisions, layer grouping, and revision-checked cross-layer/object-order moves are tested. Persisted illustration artboard, root order, all group/vector/paint layers and strokes, all vector-stroke/path/shape/text/image/group objects, brush presets, guides, snap settings, and animation reuse the established live-operation schemas before canonical use. Pixel migration now normalizes its established incomplete frame/cel, key-alias, absent-preference, and missing-reference recovery surface, then validates exact palette/library/link/conversion, sprite/frame/cel/chunk, tileset/tile/collision/Wang, and tilemap/layer/object/chunk state; new pixel assets/libraries share those nested schemas. Canonical sprite-cel bytes and reusable-stamp cells must reference the current palette, while frame overrides must match its length and entry-ID order. Duplicate raw entity identities reject before normalization can overwrite content. Persisted activity likewise requires exact schema-valid attribution records and unique IDs, while provenance reuses the strict live-operation provider/model/prompt/source/mask/conversion contract and also requires unique IDs. Missing references and valid unreferenced records remain recoverable, and structural attribution is not authentication. |
| FND-04 | Canonical editor-independent engine | ✅ Verified | P0 | Packaged E2E covers authenticated drawing and durable replay with no editor window. Source/headless service coverage additionally serializes apply, undo, redo, and checkpoint restore per document, keeps other documents independent, and rejects delayed work from a closed/replaced incarnation before canonical mutation. |
| FND-05 | Single-instance attach/show and stale-instance recovery | 🟢 Working | P0 | The narrower packaged ownership → show/attach → close while health/MCP remain live → show/reattach → explicit engine-quit regression remains valid. The customer observation stays literal: one older long-running default-profile instance showed a blank window without a renderer helper, while a fresh isolated current package launched; this is not proof every launch fails or artwork was lost. Source recovery now treats only a loaded main frame as attached and retires one failed/destroyed/crashed shell—never the canonical engine—across initial/later load failure, renderer exit, show/load/close/reload races, bounded retry, activation, and shutdown. Full-document reload temporarily detaches playback and clears departed-renderer locks; tentative close preserves identity/locks until confirmed; confirmed detach clears transient human occupancy without cancelling canonical work already admitted in the browser process. Pinned Electron 43 `unresponsive`/`responsive` events use one non-extending **10,000 ms** grace: a transiently recovered renderer retains its shell, while a persistent confirmation retires only that shell through the existing one-window/one-retry boundary. No polling, production test hook, cache/profile clearing, engine restart, canonical document/history mutation, or durable lock migration occurs. The immutable crash route retains its first controller failure and narrow fresh-profile r2 PASS (**1/1 in 4.0 seconds**): owner/MCP PID 21649, one renderer/target replacement 21658 / `9D5F…CEB3` → 21665 / `BCC6…7F7F`, revision 1→2 only after retry, preserved history/activity, cleared occupancy, close/show renderer 21668, same-profile relaunch 21676/21680, graceful zero-survivor cleanup, credential-safe evidence, zero observed external renderer requests, and evidence SHA-256 `5faa8bf41c94574db8f1f868d273af6650dbd8e0ad16436aec673b845791716c`. The separate alive-hang route preserves four consumed failures before its narrow r5 PASS. Immutable r1 `20260814t023118z-eab3063-r1` failed before launch during Playwright callback discovery. Immutable r2 `20260814t030554z-d082ab7-r2` proved a responsive renderer, two-second pending stall, same owner/MCP/canonical revision/object/activity/undo and intact lock, but no replacement or product marker; cleanup was graceful. Immutable r3 `20260814t040447z-b111d9b-r3` added one exact-target F24 input that became ACK-pending while the same pre-stall state remained intact, but exited 1 with **1/1 failed in 40.7 seconds** because no exact product marker appeared within 35,000 ms and no replacement was admitted; cleanup again left zero survivors. Chromium 150 source explains the controller seam: `WebContentsImpl::ShouldIgnoreUnresponsiveRenderer` suppresses input-ACK unresponsive reporting while `DevToolsAgentHost` is attached, and pinned Playwright 1.62.1 kept root auto-attach plus the explicit session alive through r3's one-shot timeout. Immutable r4 `20260814t051235z-6d7a896-r4` proved F24 ACK-pending admission and complete public transport detach at **3,030.6915 ms** with `browser.isConnected() === false` plus the same owner/MCP/lock responsive, but exited 1 with **1/1 failed in 7.7 seconds** before the debugger-free floor because the admitted child `CDPSession.send` promise remained pending and the controller awaited it; graceful cleanup left zero survivors. Pinned Playwright 1.62.1 source shows transport close disposes its root Chromium session but does not dispose the target child session's separate callback map, while the dispatcher/client await that child send without a timeout. The corrected controller closes only the public `connectOverCDP` transport within a derived **5,000 ms** pre-ACK deadline, independently proves full detach plus owner/MCP/lock continuity, and observes the input and stall commands concurrently for one bounded **2,000 ms** interval. A still-pending child command becomes an explicit `pending-after-debugger-detach` disposition instead of an indefinite await; every resolved/rejected/invalid result retains its exact fail-closed classifier. It then stays fully debugger-free through the **10,000 ms** no-replacement floor and exact product marker before reconnecting to the same loopback owner and requiring exactly one different trusted renderer/process. Pinned installed-code coverage binds transport close to `closeAndWait`, binds the root-versus-child callback behavior, forbids protocol `Browser.close` and Playwright private fields, and retains exact-target/owner, canonical continuity, occupancy/retry, privacy, network-sentinel, and graceful-cleanup assertions. The derived stall remains **65,000 ms**; private progress distinguishes command settlement/post-detach disposition, input admission, full debugger detach, marker, replacement process, transport reconnect, replacement target, and later assertions. Immutable secure macOS/arm64 r5 `20260814t062017z-41a3b0c-r5` then ran exactly once and passed **1/1 in 31.2 seconds**. Complete debugger detach occurred at **3,030.199709 ms**; the marker stayed absent through the full **10,000 ms** floor and appeared at **28,176.619584 ms**; owner/MCP PID 91335 retained the canonical engine while renderer PID 91345 was replaced exactly once by PID 91362 before reconnect. Canonical document/object/history/activity/undo state survived, transient occupancy cleared only after confirmed detach, the same-agent retry alone advanced revision 1→2, and graceful cleanup left zero survivors. Zero external renderer requests were directly observed only during attached intervals; the debugger-free interval was bounded by stripped provider environment, dead proxies, disabled background networking, loopback-only transport, and an absent forbidden-network sentinel, not directly observed. R1/r2/r3/r4/r5 and their package/profile/evidence roots are immutable and non-reusable. Renderer-local work never submitted to the canonical engine is not promised. Read-only forensics now prove that the protected older owner used the default profile, had no renderer descendant, and kept unlinked old launcher/ASAR/framework vnodes mapped after its ordinary bundle pathname was replaced; the supported package workflow now refuses same-root replacement. A main-process **View → Recover Editor Window…** command now provides a user-confirmed fallback when a shell is visibly blank or unusable but no detectable failure event arrived: it binds confirmation to the exact current shell so an automatic successor is never retired by stale consent, states the renderer-local-work boundary, coalesces repeated requests, requires confirmed retirement before one replacement, and reuses the explicit-show single-retry limit without restarting the canonical engine. This is deterministic source/headless behavior, not packaged/native proof or a diagnosis of the historical owner. The initiating renderer-loss trigger, cryptographic identity of those inaccessible unlinked bytes, cache/profile repair, customer updater/restart semantics, broader platforms, Level 3, RC, and stable-v1 remain open, so FND-05 stays Working/P0. |
| FND-06 | Native `.aidraw` ZIP, assets, preview, atomic save | ✅ Verified | P0 | Temporary ZIP validation, editable named-checkpoint persistence, content-addressed checkpoint assets, schema-1 container migration, unsafe-path rejection, per-entry and total expansion budgets, central-directory bounds, and per-entry CRC-32 integrity are tested. Deterministic atomic-save injection now covers a partial write, post-sync failure, and pre-replace failure against an existing valid destination: its bytes and openable state remain unchanged, no partial destination or extra entry appears, only the exact exclusive-create temporary sibling is removed, and a later real Windows replacement saves and reopens correctly. Cleanup is best-effort and never implicitly promotes or recovers a temp if the OS refuses its removal. Source saves additionally capture one invocation revision, serialize per document, keep later edits dirty/recoverable, and ignore stale close/replacement completions. Exact-path reopen preserves the existing live document, while a different native file carrying an in-use canonical document ID fails with a warning before workspace replacement. A valid document whose nominal static raster exceeds 65,535 pixels per side or 64 megapixels remains saveable with the established transparent 1×1 noncanonical preview fallback; this does not alter document geometry. |
| FND-07 | Recovery journal, compaction, discard semantics | ✅ Verified | P0 | Snapshot replay, explicit-discard cleanup, and flush-before-owned-root teardown are tested. New journals hash opaque document IDs, keeping traversal and case-distinct imports inside separate recovery-root files; safe legacy direct-child names reopen and migrate on successful compaction. Recovery requires one exact snapshot and a contiguous same-document canonical transaction prefix, stopping at the first torn/invalid/cross-document/non-applicable record instead of losing prior work or skipping across a gap. Native open queues its snapshot before immediate edits. Apply, undo, redo, and checkpoint restore now enter one per-document mutation order, and deterministic delayed-admission controls recover the exact resulting revision sequence and restored branch. Data-bearing recovered generic assets must pass their metadata/envelope contract and a production supervised decode; invalid payloads are omitted without losing document/tab/dirty/file state or asset metadata, and one aggregate startup warning exposes the loss. Metadata-only recovery entries retain their existing boundary. The exact workspace manifest preserves unique tab order and an included active ID while each compact document preserves its own dirty bit and saved path; corrupt workspace metadata falls back without discarding valid journals. Open/import admission now rejects an in-use or within-result canonical document ID before replacing or partially installing workspace/recovery state. Native-save completion compacts the current live revision: edits newer than the saved snapshot remain dirty, while a stale closed/replaced incarnation cannot recreate recovery state. A deterministic eight-document mixed clean/dirty restart, packaged regression, and final independent Level 2 same-profile restart all pass. Ordinary Electron/OS quit now prevents exit and coalesces repeated requests until MCP/workers and final document/workspace compaction complete; failed stop remains cancelled, diagnosed, and retryable instead of being marked complete. This Verified row covers persisted document/workspace recovery and orderly stop; FND-05 separately owns its narrow crash and alive-hang replacement passes plus the still-open historical renderer-loss trigger, unsent renderer-local work, cache/profile repair, and broader-platform policy evidence. |
| FND-08 | Durable attributed transaction trace | ✅ Verified | P0 | Trace survives compaction and drives non-mutating replay. Native/local JSONL ingestion now requires the exact version-1 writer envelope, canonical metadata, same-document canonical transaction schema, and existing 2 MiB ceiling; malformed records cannot reach inspection/replay, while valid history remains untruncated. A local trace-import storage failure is advisory: valid native artwork still opens and enters recovery, while the addressed result warns that imported history may be incomplete. No automatic retry or trace-completeness guarantee is claimed. |
| FND-09 | Utility-process isolation for conversion/render/generation | 🟢 Working | P0 | Bounded local-file admission, image quantization, import/conversion, document export, MCP observation compositing, generation approval thumbnails, generation providers, comparison rendering, generated-preview acceptance normalization, accepted-result quantization, and generic-image admission decode run through supervised single-lane Electron utility workers with timeout, AbortSignal cancellation, crash rejection/restart, progress relay, and clean shutdown. Generic-image requests reuse the established 1.5 MB inline / 128 MiB native-entry and 8,192-side/16MP policies, reject oversize input before base64 amplification, require main-validated MIME/header/display geometry, and return only matching decoded geometry; production live/native, recovery, utility-import-result, and CLI ingress inject or own this lane. Each independent lane retains at most 32 waiting requests and returns retryable backpressure for the next; queued cancellation frees capacity without changing FIFO. Quantization enforces its 1,500,000-byte source / 2,000,000-character worker boundary before base64 amplification, then validates child output against request-derived width×height, coordinate, uniqueness, and palette limits before caller/canonical use. Export accepts only the request-derived primary MIME/extension and companion form, canonical base64 and safe unique leaf names, with at most 4,097 total members and 512 MiB aggregate decoded data before decode; reports/warnings are limited to 4,096 entries, 64 KiB per string and 1 MiB serialized. Import accepts 1–256 documents under the existing PDF ceiling, measures exact JSON without a second whole serialization against 512 MiB / 16,777,216 nodes / 128 levels, then applies the canonical migration/schema gate. Every returned generic asset must have valid metadata; each embedded payload must additionally pass the established 128 MiB envelope and a supervised decode before the import promise resolves. Pure malformed results retire their producer, and decoder exit returns no document. Observation accepts only established errors or a request-derived static PNG whose canonical base64, 4 MiB PNG ceiling, chunk/CRC structure, dimensions, region, scale, 4,194,304-pixel and 65,535-side budgets, and full RGBA decode agree, within a generic serialized allowance equal to maximum 4 MiB PNG base64 plus 64 KiB. The aggregate and per-side rejections are distinct validated results, and tilemap regions render without a full nominal map allocation. Generation output cannot exceed the requested count; every result requires a unique ID, exact PNG/JPEG/WebP MIME/header/dimensions, canonical base64, the existing 32 MiB / 8,192-side / 32 MP limits, request-consistent seed, and JSON-safe provider metadata within 64 KiB / 4,096 nodes / 32 levels. Progress is advisory and relayed only for the active generation request with finite 0–1 progress and a message at most 4 KiB; malformed, oversized, or cross-lane telemetry stays invisible without aborting terminal work. Terminal errors are capped at 256-byte codes and 16-KiB messages; malformed terminal envelopes/payloads retire their worker and queued work restarts fresh. At explicit acceptance, a same-geometry raster-lane derivative tries lossless PNG and then a fixed descending alpha-preserving WebP quality ladder; only a fully validated result at or below 1,500,000 bytes can reach quantization/commit, while source and accepted identity remain explicit and failures stay preview-only without canonical mutation. Generation remains a separate lane; cancellation first lets the provider remove/interrupt remote work, then replaces the worker before queued work starts. Exact Windows packages pass fixed crash/cancel/restart containment, queue/input pressure, quantization-result rejection/recovery, generated-preview normalization, observation-codec corruption, sprite-sheet PNG+JSON export-result, SVG import-result, and provider-free general-generation fixtures. The quantization package at EXE SHA-256 `1CF937B4CCD8C23128975691BFD1E436426C713811CA6DEF57B5D2CE8BE5DCCE` / ASAR SHA-256 `CABAC58E83F5A46692A49C921FE05480124BCA13DD46DEB7A3C8B0919AC2C815` passed 1/1; the observation package at EXE `A1FB98E3BB482367F17D011B83005F1410180BBD270E0A78329C7FA05CF1E1C5` / ASAR `530DF2761572F554F46B0C14BAB5B4455E19439CDC7D43E68B5CADEEBF932DC9`, export package at EXE `2F74578467C53DDDFB43F13BB69B53F9B10E4C7F96E3A8E53C09413B7848CEA2` / ASAR `DDE43A0F54647D56B78BADFDBCDF3BF80A0712F3CEF3A83700AE3C30E32E562D`, and import package at EXE `EB2DA9F9F38D33B0BFB9BF151FE4F05A7152FB583D7FE3246CADC2DA5F8790AD` / ASAR `4BF8947F76EBD072064674E5B058CD3A896BC7585BA6365DFB339B0BA922C705` passed their recorded narrow fixtures likewise. The exact provider-free general-generation Windows package passed its five-fault/six-worker fixture with invalid results private, fresh-worker recovery, invariant canonical/MCP and raster-lane state, zero targets/jobs/network/provider/paid activity, and graceful credential-redacted cleanup. The aggregate meters are post-IPC source contracts, not pre-deserialization or process-RSS ceilings. Native save additionally binds archive/checkpoint/trace/preview inputs to one invocation revision and serializes same-document requests so later edits stay dirty instead of leaking into or being cleared by an older save. Exact RSS, packaged/native proof of the new generic-image and aggregate policies, broader platforms, and general real-provider pressure remain open. |
| FND-10 | Application-owned rendering adapters | 🟢 Working | P1 | v1 deliberately uses shared custom Canvas2D renderers over the application-owned model; Paper.js remains a narrow geometry kernel and `perfect-freehand` a stroke-outline kernel. Unused Fabric.js/PixiJS/tilemap runtime dependencies were removed. Tilemap observation can render a bounded nominal region with byte-exact ordinary full-crop parity, while whole static rasters reject the shared 65,535-side / 64-megapixel limits before allocation. Illustration observation additionally renders a bounded backing for integer-translated, integer-sized solid/empty square-corner rectangles, polygons, and stars; integer-source/destination/crop embedded PNGs; complete materialized paint caches; and integer-translated structural groups inside default layers. It scans eligible leaves in canonical order but skips conservative integer frames disjoint from the backing before Canvas work or PNG decode; unsupported/unsafe leaves remain included, so this is not a spatial index or second renderer. The paint renderer similarly scans cache metadata and decodes/copies only backing-intersecting tiles. Ellipses, rounded rectangles, other raster formats, and every fractional, stroked, gradient, path, text, live/incompletely cached paint, mask, rotated/scaled/skewed, isolated, or effect-bearing case retain the full-artboard render and raw crop. The editor and headless renderer share conservative pre-decode chunk filtering; the editor derives one outward-rounded canonical viewport for tilemap parallax/grid and sprite base/onion/wrap work, while the shared sprite compositor retains exact color/opacity/replay hooks. Map-object paths additionally share conservative affine bounds including stroke miters/selected handles before render. Transient fill/stamp/selection/replay cells use one projection-aware visible-region filter, and compact horizontal runs clip algebraically before Canvas work. Interactive and headless illustration renderers also share transform detection and group-isolation policy: transform-only groups traverse their children directly under the affine matrix, while opacity, blend, filter, mask, blur, or shadow groups retain isolated compositing. Deterministic cross-mode goldens and the non-GUI vector/4K-paint/large-map/object/overlay/grid-surface scale gate pass. The interactive checker is viewport/overscan-bounded with one shadowed source, and grid lasso rasterizes once on release under explicit work ceilings; live-stroke/generic illustration regional rendering, broad raster admission, effect-bearing transformed-group overhang, adaptive previews, and packaged pointer/frame-pacing evidence remain. |
| FND-11 | Versioned format migrations beyond schema 1 | ✅ Verified | P1 | Canonical schema 2 adds illustration animation. Unversioned/schema-1 illustration and pixel fixtures migrate to schema 2, native schema-1 ZIP containers reopen, and newer unsupported versions fail explicitly. |
| FND-12 | Performance instrumentation and budgets | 🟢 Working | P0 | `npm run test:performance` records machine-readable Windows evidence and gates 5,000 vectors; separate requested pixels from a filled star, a complete materialized paint cache, and an integer-phase embedded PNG on 8,192×8,192 illustrations under 100 ms each; a one-pixel regional render across 100,000 exact-subset illustration objects under 500 ms; exact canonical lasso selection across those 5,000 objects under 1,000 ms; four populated editable and materialized-sparse 4K paint layers; 65,536 visible map tiles; one-pixel regional renders across 4,096 stored map chunks and 4,096 stored sprite-cel chunks; a one-region projected-bounds filter at the 100,000-map-object ceiling; viewport filtering across 1,048,576 overlay cells in 65,536 compact runs under 100 ms; viewport checker planning plus exact straight-path reduction across 1,000,001 lasso samples under 500 ms; exact normal compositing of two one-million-pixel sprite layers under 500 ms; exact GIF indexing of one million final binary-alpha normal-composite pixels under 750 ms; complete sequential exact-GIF export across 16 million final pixels under 5,000 ms; native save, PNG export, one-million-sample playback accounting, a production-cel one-million-cell compact flood fill, and RSS growth. The exact package now proves four concurrently advancing visible lanes plus a responsive held human gesture; broader packaged pointer latency, frame pacing, 200% scaling, and tablet latency remain for Level 3 Computer Use. |
| FND-13 | Cross-OS desktop runtime services | 🟢 Working | P0 | Platform-neutral engine status, Windows/macOS/Linux secure-storage policy, macOS login-launch suppression, Windows login items, and XDG headless autostart are implemented and unit-tested. Start-at-login mutations serialize in process invocation order; Linux enablement publishes one complete private flushed atomic entry, replacement failure preserves prior state and queue retryability, and a later disable deterministically wins. Linux refuses Electron `basic_text`. Static migration readiness adds deterministic case/Unicode/path/artifact checks, LF source attributes, a target-architecture-aware Darwin fuse hook, and a release-host identity guard; the lockfile declares both Darwin canvas architectures. Windows and local Apple Silicon packages now have interactive/headless evidence, including Keychain restart and native Playwright launch/attach. Cross-process coordination, Developer ID/notarization, login/reboot, Intel, hosted macOS, and independent formal proof remain required before promotion. |

Evidence: [core model](../packages/core/src/model.ts), [operations](../packages/core/src/operations.ts), [document service](../src/main/document-service.ts), [engine runtime](../src/main/engine-runtime.ts), [persistence](../src/main/persistence.ts), [journal tests](../tests/main/journal.test.ts), [packaged E2E](../tests/e2e/editor.spec.ts).

## 2. Agent-native MCP and collaboration

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| AGT-01 | Authenticated stateful localhost MCP | ✅ Verified | P0 | Bearer auth, stateful sessions, health endpoint, persistent port range, and 32 concurrent raw clients are contract-tested. Wire tests request and assert the installed SDK's exact legacy `2025-11-25` initialize result, send that negotiated version on later requests, prove modern-only initialize fallback explicitly, and reject a later mismatched version header. Session IDs remain process-lifetime and a 404 requires fresh initialize plus `session_manage` join. The encrypted local bearer store serializes same-instance operations, retains exact version-one active records, and writes exact version-two active/revoked state only through private flushed atomic replacement. Source/headless lifecycle tests prove rotation invalidates the prior bearer and retires transports only after persistence, while revocation leaves no ciphertext, rejects every bearer, survives reload, and can be explicitly re-enabled. Startup admits runtime authority only after loopback binding and private preferred-port publication; post-bind bookkeeping failure closes and forgets the listener/token, and a later retry remains usable. The explicit plaintext headless bootstrap handoff remains separately caller-authorized and atomically published. This raw/source evidence does not prove packaged secure-storage interaction, installed-client refresh, or historical incident cleanup tracked in AGT-14. |
| AGT-02 | Public resources and subscriptions | ✅ Verified | P1 | The optional complete `aidraw://guide` plus documents, manifest, snapshot, changes, and trace resources are contract-read; a negotiated SSE subscription receives `notifications/resources/updated` after commit and the matching changes resource returns the committed transaction. Tools-only correctness does not depend on resource support. |
| AGT-03 | Public tool surface | ✅ Verified | P0 | All ten named tools, including model-callable progressive `aidraw_help`, are registered and schema-discoverable. For `session_manage`, `document_manage`, `history_manage`, and `job_manage`, discovery now emits one composition-free flat object with a required action enum and action-labelled optional superset fields; server-side strict discriminated validation still rejects cross-action fields and requires each selected action's conditional inputs. Exact initialize/version assertions, protocol-visible parameter/output descriptions, structured results, safe incomplete-job/retry next steps, and negative calls across all four action tools are wire-tested. Exact-package raw-MCP acceptance proves only that a repository-free raw tools-only client can use the representative workflow before optionally reading `aidraw://guide`, while owner-private approval summaries remain redacted. This does not certify OpenCode/Antigravity, installed-client schema conversion, complete semantics of every action, or renderer/UI behavior. |
| AGT-04 | Headless bootstrap and launch-time folder trust | ✅ Verified | P0 | Explicit connection handoff and process-lifetime trusted folders are exercised in packaged E2E; roots are realpath-canonicalized so macOS aliases work without allowing symlink escape. Its password-bearing version-one connection handoff rejects an empty/relative destination and appears only as a complete private atomic replacement; a permissive prior POSIX entry becomes mode 0600 and replacement failure preserves prior bytes. |
| AGT-05 | Presence, named cursors, activity, background-tab badges | ✅ Verified | P1 | Exact-package Playwright holds a real foreground human stroke while an independently authenticated agent works visibly in a second document. The actor-colored background tab exposes live working state plus named pen coordinates without changing focus, the explicit background view renders the named cursor, and completion remains unread until that document is viewed. Authenticated observation matches the 600-point background agent stroke and intact foreground human stroke; presence returns idle, both document revisions agree, shutdown is graceful, and retained credentials are redacted. |
| AGT-06 | Fair four-lane playback and backpressure | ✅ Verified | P0 | Scheduler integration tests prove all four lane IDs, next-lane cross-actor fairness, queue cleanup, and the one-million-sample busy gate. Both canvases expose four concurrently distinguishable actor-colored rows with progress. Exact-package Playwright opens four independently authenticated MCP sessions, proves every visible lane advances while authenticated presence reports all four actors working, keeps a simultaneous held human stroke responsive and canonically untouched until release, then matches four fair commits, renderer state, authenticated observation, idle presence, graceful exit, and credential redaction. Final canonical commits, including instant work and history commands outside visible playback, now serialize per document without imposing a global cross-document queue. |
| AGT-07 | Human locks and optimistic human priority | ✅ Verified | P0 | Automated service tests cover same-object replace/delete, overlapping pixel regions, unrelated work, and human bypass priority. Exact-package acceptance now holds a real illustration-object move and a real pixel-pencil gesture at pointer-down, observes the exact object/full-sprite region occupancy through authenticated MCP, and proves overlapping agent delete/write requests return the honest human-lock message with `retryable: true`. Canvas hashes change for both held previews while canonical state remains untouched; pointer release commits only the human move/pixels, authenticated observation matches, and both locks clear. |
| AGT-08 | Partial cancellation and per-actor undo | ✅ Verified | P0 | Unit integration retains a visible pixel prefix as one undoable/redoable actor transaction, and packaged Playwright covers Stop All Agents plus Activity-panel agent undo/redo without removing the human stroke. |
| AGT-09 | Idempotency and revision conflicts | ✅ Verified | P0 | Duplicate operation IDs and stale entity revisions are covered. MCP discovery now documents the structured entity/expected/actual/retryable conflict envelope, and an authenticated contract proves a stale replacement leaves canonical state exact, returns an explicit `canvas_observe` next step, and commits only after re-observation with the current entity revision and a fresh operation ID. |
| AGT-10 | Durable autonomous trace inspection and human-triggered replay | ✅ Verified | P0 | Agents can inspect complete traces, and headless transactions remain replayable from Activity after an editor attaches. Agent-triggered replay is separately tracked as PAR-05. |
| AGT-11 | Rich semantic canvas operation families | 🟢 Working | P1 | MCP expands validated pixel fill/replace/adjust/dither/quantize/selection-scale, animation/stamp/tilemap/map-object/per-tile-collision, Wang-authoring, deterministic tile variants, and project-link embed/pack operations plus illustration align/distribute/material/gradient/styled-text/image-crop/object/layer-filter/mask/boolean, document fragments, and Bézier inspect/move-handle/insert/delete/convert/open-close/arc-convert/split/join into canonical revision-checked operations. Approved file tools also relink/extract project sources. Richer cross-asset graph cascades remain. |
| AGT-12 | Approval jobs and folder authority | ✅ Verified | P0 | Structured review shows actor, exact path/provider/prompt, scale/count, paid-request exposure, overwrite targets, and expiry. Packaged UI exposes Deny/once/session/persistent choices; contract tests prove session trust, persistent trust across host restart, and overwrite re-approval. |
| AGT-13 | Job inspect/wait/cancel/approval dependency | ✅ Verified | P1 | Owner-scoped list, inspect, bounded wait, and cancel are contract-tested across sessions without leaking private payloads. `approve-dependent` reports the human-only boundary; durable batch start/resume/progress/cancel also rebinds only with an opaque resume token. |
| AGT-14 | First-class Codex/Claude Code/OpenCode/Antigravity onboarding | 🟢 Working | P0 | Consent-driven **configuration profiles** back up configuration and replace only the AIDraw entry using each client's documented Streamable HTTP shape; a profile is not installed-client support. Automatic updates serialize in invocation order within one process, retain distinct private backups, and publish one complete flushed atomic config while preserving unrelated settings and later retryability. OpenCode uses `mcp.aidraw` with `enabled: true`, and the launch-free 1.18.13 harness remains safety/preparation evidence only. The exact Windows incident remains open: OpenCode 1.18.18 reduced the former composition-heavy native action inputs to `{}` although simple help and explicit raw JSON-RPC worked; Antigravity 2.8.1 retained a prior-process session and did not recover after the correct 404; a fallback copied the bearer into temporary helper/history state and rejoined as split `opencode`/`Agent 13` identities; no installed-client acceptance has run. Flat discovery, strict validation, exact version assertions, and the now-supported Activity rotation/revocation source path remain remediation—not installed-client or incident-cleanup proof. The report's source/headless P2 diagnostic exit is now explicit: no-store 401 authentication, 400 initialization/version, and 404 stale-session responses name the `stateful-legacy` profile, exact `2025-11-25` version, credential-neutral class, and safe next action; guide/help distinguishes strict tool-level `Invalid arguments`. This does not prove an installed client displays or follows that guidance. Rotation atomically persists a new encrypted bearer before invalidating the old one and ending active transports; from revoked state its Activity label and main confirmation explicitly say that the endpoint and local agent access will be re-enabled. Revocation persists a no-ciphertext marker, ends transports, and stops MCP while leaving the editor/canonical engine, start-at-login choice, persistent folder approvals, and third-party config files intact. Already admitted or pending approval work retains its existing review/cancel path rather than being silently rolled back. A post-persistence cleanup error leaves revocation authoritative, reports the partial state, clears renderer credential/setup disclosure, and requires restart before re-enable. The new security card reuses UX-01's 32 px secondary-action and 11/10 px label/caption tokens. Current-package acceptance consumers require the exact v2 active encrypted envelope and return only a credential-free shape/error; v1 remains read-compatible only for intentional migration evidence. Every intended client must be explicitly reconnected and freshly joined. Binding order remains **P0**: preserve affected artwork, confirm the historical session ended, execute and verify rotation/revocation on the affected runtime, refresh intended profiles, make separately authorized cleanup/history decisions, and require fresh joins; **P1**: exact-version OpenCode and Antigravity native help/session/document/observe/apply, AIDraw-restart/404 recovery, simultaneous stable attribution, bearer-boundary scan, and graceful owned-process cleanup; **P2**: preserve the completed source/wire diagnostic contract during those later exact-client runs. Keep Working/P0 until the P0/P1 runtime exits pass. |
| AGT-15 | Generic MCP-client onboarding | 🟢 Working | P2 | The UI exposes/copies a client-neutral Streamable HTTP JSON profile with bearer header, and docs explain the shared contract. Add one independent exact-version third-party installed-client acceptance. The supported source/headless token lifecycle is owned by AGT-14 as a P0 incident/release boundary; it deliberately leaves generic and generated client configs stale until the user reconnects them and is not installed-client proof. |
| AGT-16 | Agent model/effort/task provenance | 🟢 Working | P2 | A joined session may declare validated model, reasoning effort, and task ID metadata; it persists into actors/activity and the UI formats missing fields without `undefined`. This is client-declared provenance, not platform-attested model identity or proof that an external Codex task has completed. |
| AGT-17 | Multi-asset game-production briefs, validation, and repair | ⬜ Missing | P1 | Existing semantic sprite/tilemap operations, durable jobs, project links, and exports are useful primitives, but no coordinated production workflow yet turns one local brief into a coherent engine-ready asset set. Exit requires auditable multi-asset briefs; explicit character/style source and propagation provenance across related sprites, animations, tilesets, and maps; deterministic sprite/map package validators; named engine-ready export profiles; and agent repair plus revalidation of failed production checks without silently changing accepted artwork or file authority. Reuse AGT-11, PIX-16, PIX/MAP authoring, IO reports, and release gates rather than duplicating their low-level contracts. P1 keeps this selected moat in core scope rather than postponing it behind P2 completeness, while the numbered critical path deliberately schedules it after release trust. No provider dependency, autonomous publication, broad art-suite parity, or guarantee of subjective style quality is implied. |

Evidence: [MCP host](../src/main/mcp-host.ts), [scheduler](../src/main/playback-scheduler.ts), [MCP contract tests](../tests/mcp/contract.test.ts), [trace tests](../tests/main/trace-store.test.ts), [replay tests](../tests/renderer/replay.test.ts), [human/agent parity audit](PARITY_AUDIT.md).

### 2A. Human/agent parity and autonomous iteration

The shared reducer gives agents access to every low-level operation kind, but low-level write access is not the same as practical parity. These items reconcile the dedicated [human/agent parity audit](PARITY_AUDIT.md) into the release backlog.

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| PAR-01 | New-document option parity | ✅ Verified | P1 | Human IPC and MCP use one strict creation schema for kind/name/dimensions/background and tilemap orientation, finite/infinite mode, and tile geometry. A contract test compares the resulting isometric documents. |
| PAR-02 | Safe document-fragment exchange for agents | ✅ Verified | P2 | Clipboard and MCP share a versioned, self-contained 2 MiB fragment contract. Illustration relation/asset closure, pixel sprite→tileset→map dependencies, legacy clipboard upgrade, ID/reference remapping, palette-preserving import, server attribution, malformed input rejection, and live cross-document MCP exchange are tested without exposing the ambient clipboard to agents. |
| PAR-03 | Agent-callable image-to-indexed quantization | ✅ Verified | P1 | `pixel.image.quantize` converts an embedded raster asset into compact cel runs using document area/OKLab/dither/alpha defaults and records reproducible conversion metadata. Exact pixels, trace metadata, and actor undo are contract-tested. |
| PAR-04 | Human-presence and lock awareness | ✅ Verified | P1 | Session inspection and canvas observation expose coarse human object/region occupancy, advisory editor state, and retry guidance. A contract test proves an authenticated agent sees a direct human lock and receives a retryable conflict without gaining human-priority lock authority. |
| PAR-05 | Agent-callable trace replay | ✅ Verified | P2 | `history_manage:replay` starts one bounded, non-mutating transaction replay and reports missing/busy/already-replaying/too-large states. Contract coverage verifies document revision remains unchanged. |
| PAR-06 | Multi-granularity visual observation | ✅ Verified | P1 | `canvas_observe` targets current or named-checkpoint state plus asset/frame/layer/region with integer scale and document/transparent/solid background, validates exact targets, and enforces pre-render pixel and PNG-byte caps. It can return a race-free before/after pair for the most recent transaction under one combined pixel budget; exact cropped, paired RGBA, and checkpoint output are contract-tested. |
| PAR-07 | Explicit workspace and job observation | ✅ Verified | P1 | Document listing returns `activeDocumentId`; owner-scoped jobs can be listed/inspected/waited/cancelled through sanitized summaries that omit human prompts, results, and approval review data. Cross-session isolation is contract-tested. |
| PAR-08 | Advisory attached-editor state | ✅ Verified | P2 | While attached, the renderer publishes explicitly advisory document/tool/selection/zoom, visible world viewport, active pixel asset/frame/tag, onion-skin, playback, and direction through narrow IPC; it clears on detach and cannot feed back into canonical state. Sanitization/clamping and MCP exposure are contract-tested, and packaged E2E proves real tool/zoom/viewport publication plus detach clearing through MCP. |
| PAR-09 | Bulk region operations and long-run throughput | ✅ Verified | P1 | Canonical RLE pixel/tile writes support one million cells, cross chunks, progressive reveal/cancel, locks, inverse/undo, and sample backpressure. `job_manage` starts/resumes/cancels opaque-token durable batches up to 10,000 numbered `canvas_apply` transactions with progress, cross-session ownership rebinding, strict version-one ledger recovery, and crash-window reconciliation against the durable trace. Each new sequence requires a fresh batch-local operation ID; the original sequence/ID pair remains duplicate-safe. Copied state becomes live only after a private flushed atomic replacement, and deterministic replacement-failure tests prove prepare/finish remain retryable without phantom progress; unit and live host-restart MCP contracts pass. |
| PAR-10 | Non-destructive branches, variants, and comparison | 🟢 Working | P2 | Humans and agents can create attributed editable document checkpoints, observe them headlessly, compare them side-by-side or overlaid, and restore safely. Checkpoint review can selectively merge up to 32 top-level illustration layer trees or pixel assets with dependency closure as one canonical undoable transaction; native/service/MCP contracts cover persistence and merge. Cheap in-place layer variants, recovery-journal checkpoint state, and packaged comparison acceptance remain. |
| SEC-01 | Server-authoritative entity attribution | ✅ Verified | P0 | Commit policy recursively normalizes new/replaced entity creator, timestamps, and revisions from the authenticated actor, renderer IPC cannot impersonate agents, and undo/redo restores exact normalized metadata. Service and MCP regressions cover the boundary. |
| SEC-02 | Asset deletion referential integrity | ✅ Verified | P0 | The reducer reports image, paint-tile, linked-preview, and provenance references; deletion is rejected until references are removed. An explicit ordered object/asset cascade and its exact inverse are tested. |
| SEC-03 | Trusted provenance creation | ✅ Verified | P0 | Generic human/agent canvas transactions cannot add or remove verified provenance. Generation acceptance uses the engine-only path, timestamps are server-issued, referenced assets must exist, and MCP forgery is contract-tested. |
| SEC-04 | Strict conversion-settings validation | ✅ Verified | P0 | Nested conversion settings are strict: area/OKLab, three supported dither modes, finite 0–1 alpha, and no unknown keys. Malformed enums, NaN, ranges, and extra fields are rejected by schema tests. |
| SEC-05 | Inline asset ingestion policy | ✅ Verified | P0 | Inline assets require canonical base64 safe-raster data with declared length/SHA/MIME agreement, header plus supervised codec decode, 1.5 MB, 8192 px, and 16 MP limits; dangerous formats and forged generated origin are rejected/normalized. Decoder exit rejects the transaction without mutation and queued utility work restarts fresh. The trust boundary is documented and tested. |

## 3. Illustration editor

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| ILL-01 | Bounded custom artboard, background, sRGB defaults | 🟢 Working | P1 | Dedicated creation and Artboard inspector UIs support dimensions, DPI, and solid/transparent background after creation. Reusable mode-specific document presets serialize reads/writes per runtime, persist copied state before publishing it live, and retain concurrent saves or prior state across deterministic replacement failures under the existing 64-entry cap. Revision-checked replace and translate/expand operations have strict bounds and exact editable-state undo; accepted outpaint uses the atomic form to shift content, guides, paint, and animation consistently. Add packaged preset and outpaint acceptance. |
| ILL-02 | Selection and lasso | 🟢 Working | P1 | Freeform pointer lassos support replace/add/subtract/intersect and containment modes. Completed lassos evaluate transformed canonical fill silhouettes (including even-odd holes), pressure outlines, text/image frames, or stroke-only curve/shape centerlines in document order. Clicks now treat supported painted fill/stroke paths as authoritative, honor fill rules and rendered arrowheads, retain a scale-adjusted edge affordance, and Alt-cycle topmost overlaps; groups, malformed/paintless geometry, non-invertible transforms, or unavailable Canvas context retain bounds fallback. Mask/effect/group-child silhouettes, exact skewed tolerance, and packaged pointer acceptance remain. |
| ILL-03 | Transform workflow | 🟢 Working | P1 | Single/multi-selection world bounds, drag translation, four scale handles, rotation handle, Shift-uniform scale, Shift-15° rotation, live previews, and one lock-safe revision-checked commit are implemented. Skew/pivot handles remain missing. Explicit reset is decision-ready rather than silently defined: identity-at-origin, position-preserving reset, and geometry baking have materially different results. Pure geometry is covered but that product choice and packaged pointer acceptance remain. |
| ILL-04 | Snapping and guides | 🟢 Working | P1 | Persistent colored/lockable guides can be added, edited, deleted, and dragged; shared snapping targets artboard, objects, guides, configurable grid, and integer pixels with a document-owned tolerance. Rulers/origin control and packaged pointer acceptance remain. |
| ILL-05 | Pressure pen and vector pencil | 🟢 Working | P1 | Pressure samples and `perfect-freehand` outlines render. Add real stylus-device acceptance for pressure, tilt policy, latency, and long strokes. |
| ILL-06 | Six essential raster brushes and eraser | 🟢 Working | P1 | Hard/soft round, pencil, marker, airbrush, and eraser use a deterministic pressure-aware dab engine with spacing, stabilization, hardness, flow, scatter, size/opacity jitter, angle, and roundness. Shared live/headless/save-tile rendering, destructive erasing, and a maintained hard-round golden are tested; add tablet latency and a broader preset corpus. |
| ILL-07 | Watercolor/natural-media painting | 🟢 Working | P1 | A bounded watercolor tip implements translucent accumulation, wet edge pooling, deterministic granulation, scatter, pressure, and stabilization through the shared renderer, with a deterministic rendering golden. Paper textures, pigment mixing/bloom simulation, and wet-canvas interaction remain. |
| ILL-08 | Custom brush engine and preset management | 🟢 Working | P1 | Named document-owned presets can be created, edited, selected, tuned across all dab dynamics, deleted, and transferred through strict version-1 AIDraw brush-library JSON. Copy/load/paste previews before one append-or-replace transaction; append makes fresh-ID/unique-name copies by default, while explicit replacement preserves imported IDs and leaves existing embedded stroke recipes exact. Input is capped at 1 MiB/256 presets and reserves built-in IDs. Folders/tags, tip images, application-global libraries, third-party formats, and packaged file/clipboard acceptance remain. |
| ILL-09 | Bézier construction and node editing | 🟢 Working | P1 | Node mode distinguishes anchors from in/out handles, draws tangent controls, drags anchors/handles, mirrors handles with Shift, Alt-click subdivides the actual cubic, Ctrl-click deletes, double-click converts smooth/corner, and the inspector opens/closes paths. Exact SVG arcs convert to editable cubics; open paths split into styled objects, closed paths cut open without losing the closing segment, and transformed paths join by nearest world-space endpoints through shared UI/MCP kernels. Multi-subpaths and packaged pointer acceptance remain. |
| ILL-10 | Lines, arrows, rectangles, ellipses, polygons, stars | 🟢 Working | P1 | Core creation tools and editable model objects exist. World-space transformed bounds drive handles and deliberate fallback, completed lassos use canonical filled silhouettes or stroke-only centerlines, and click hit testing now rejects supported silhouette misses, honors fill rules, and includes all rendered arrow segments plus configured strokes. Packaged transform coverage and richer per-shape parameter editing remain. |
| ILL-11 | Fill/stroke styles, gradients, eyedropper | 🟢 Working | P1 | Solid/linear/radial styles, interactive placement, and a full 2–32-stop editor support ordered positions, color, opacity, geometry, preview, and semantic agent editing. Shape/path strokes now apply canonical width, opacity, cap, join, dash, and explicit default miter geometry through one interactive/headless Canvas contract, with a maintained raw-RGBA golden. Reusable swatches, custom miter editing, gradient transforms beyond endpoints, robust composite sampling, and packaged visual acceptance remain. |
| ILL-12 | Images and crop | 🟢 Working | P1 | Image objects, clipboard images, filters, rotated visual crop handles, centered aspect presets, Shift-constrained free crop, and exact non-stretching/resettable source crops work through shared UI/headless geometry. Explicit source-pixel X/Y/width/height fields preserve fractional coordinates, displayed pixel scale, and the transformed world origin through one Apply transaction; a full-source rectangle clears the crop. AIDraw SVG export/re-import now preserves that exact canonical crop when its bounded private metadata and standard clip/image geometry still agree; ordinary foreign or edited wrappers retain the generic editable group/image/mask import. Replace/relink, missing-asset UX, and packaged interaction remain. |
| ILL-13 | Styled text ranges and typography | 🟢 Working | P1 | The inspector tracks exact character selections and applies font, size, weight, italic, underline, color, and tracking to only that range; shared live/headless layout supports multiline wrapping, alignment, box dimensions, and line height. Select-tool double-click or selected-object Enter now opens a bounded multiline content editor for visible unlocked text, aligned through its ancestor/object affine transform; blur or Ctrl/Cmd+Enter rechecks eligibility then commits one revision-checked styled replacement, while Escape cancels and failures retain the draft. The overlay intentionally uses the first-character style while range typography stays in the inspector. Font embedding, vertical text, text-on-path, broader transformed-group hit testing, and packaged interaction remain. |
| ILL-14 | Layer groups, object groups, masks, clipping masks | 🟢 Working | P1 | Layer/object grouping and ungrouping are authorable; one-click editable layer masks and top-object masks render and enforce reference-safe move/delete behavior. Transform-only object groups now traverse directly in both interactive and headless Canvas rendering, so source geometry can transform into the artboard without a premature artboard-sized isolation clip; opacity, blend, filter, mask, blur, and shadow groups remain isolated so their composite applies once. Effect-bearing transformed-group overhang, a dedicated isolation/edit mode, clipping-stack ordering UX, feathering, and semantic agent mask helpers remain. |
| ILL-15 | Alignment and distribution | 🟢 Working | P1 | UI and MCP share rotated-world-bounds geometry for selection, key-object, or artboard alignment and center or true equal-gap distribution on either axis. Packaged multi-selection acceptance remains. |
| ILL-16 | Union/subtract/intersect/exclude | 🟢 Working | P1 | UI and headless MCP share exact Paper.js union/subtract/intersect/exclude for transformed rectangles, ellipses, polygons, stars, and paths with one atomic delete/add transaction. Canonical even-odd/nonzero input and result rules are retained; an empty filled result rejects before deletion. Exact four-mode geometry plus a raw-RGBA compound-hole/island golden are gated. Compound-hole node-edit UX, general multi-subpath authoring, utility-process isolation, and packaged acceptance remain. |
| ILL-17 | Blend modes, opacity, shadows, non-destructive filters | 🟢 Working | P1 | Common blends, opacity, object blur/shadow, and ordered duplicate-capable brightness/contrast/saturation/hue/blur stacks render identically for objects plus isolated object groups and layer/group composites in live and headless paths. Shared UI/MCP editing and overlap/filter regressions are present; filter masks, SVG/PSD/PDF parity, effect-buffer performance proof, and packaged visual acceptance remain. |
| ILL-18 | Sparse tiled paint-layer storage | 🟢 Working | P0 | Save materializes content-addressed 256×256 PNG tiles with a SHA-256-bound editable-stroke prefix; a valid prefix now reuses untouched tiles and rerenders only keys touched by appended strokes. The cache is adopted into the live document only after atomic replacement succeeds and only while its stroke prefix still matches; this permits safe reuse when edits land during a revision-bound save, keeps later saves incremental, and avoids runtime mutation on failure. Native open validates exact tile coverage, dimensions, payload hashes, artboard bounds, and the stroke digest before enabling the cache. Live and headless renderers consume valid tiles then composite newly appended strokes, fall back to source strokes while browser tiles load or replay hides a cached stroke, and generic layer mutations cannot inject stale cache data. A bounded headless observation may decode/copy only cache tiles intersecting its backing when the materialized prefix covers every stroke; any live tail or incomplete cache retains full rendering. Multi-tile incremental equivalence, incomplete/stale-open invalidation, successful/failed-save cache adoption, reducer trust, native round-trip, exact regional crops, maximum-artboard cached observation, and editable/materialized 4K performance are gated. Add packaged reopen/erase/edit acceptance before Verified. |
| ILL-19 | Editable material presets and polished gold | 🟢 Working | P2 | The polished-gold preset builds editable gradient/blur/mask layers and exports to SVG. General preset management and more materials are not implemented. |
| ILL-20 | Multiple artboards | ⏸ Deferred | P2 | v1 intentionally has one bounded artboard per illustration document; multiple pages/artboards use separate document tabs. Multi-artboard documents are a post-v1 format change. |
| ILL-21 | Illustration animation timeline | 🟢 Working | P2 | Canonical schema-2 object-pose keyframes cover transform, opacity, visibility, three easing modes, duration/FPS, loop/once/ping-pong playback, scrub/play UI, transform-at-playhead authoring, exact-time MCP observation, and bounded GIF/APNG plus batch export. Reducer inverse, interpolation, migration, export, MCP, and rendering-golden tests pass; add richer property tracks, dope-sheet editing, and packaged playback/export acceptance. |
| ILL-22 | Object list / scene outliner | 🟢 Working | P1 | The Layers panel now has a searchable hierarchical per-layer object tree with type icons, canvas-synchronized range/additive selection, inline rename, visibility/lock controls, revision-checked drag/button ordering, layer/group moves, group creation, duplicate/delete, and reveal-on-canvas centering. It derives directly from canonical snapshots, so agent additions appear live without clearing human selection. Packaged E2E covers live agent insertion, selection preservation, rename, order, grouping, and filtering; add explicit drag/drop, visibility/lock, duplicate/delete, deep-cycle, and reveal-position acceptance before marking Verified. |

Evidence: [illustration canvas](../src/renderer/canvas/IllustrationCanvas.tsx), [geometry](../src/renderer/canvas/geometry.ts), [path booleans](../src/common/path-boolean.ts), [object inspector](../src/renderer/App.tsx), [material tests](../tests/common/material-presets.test.ts).

## 4. Pixel sprites and animation

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| PIX-01 | Pixel Sprite/Tilemap/Project creation and custom sizing | ✅ Verified | P0 | Dedicated creation UI, exact sprite dimensions, resize/crop, undo, and isometric/infinite map options have regressions. |
| PIX-02 | Indexed palette up to 256 entries | 🟢 Working | P1 | Indexed storage, protected transparent index 0, editing, adding, exact slot moves, and atomic replace-and-delete work across every cel, reusable stamp, palette cycle, and per-frame override with exact undo. Live cel/region/frame/asset/stamp ingress rejects indices outside the current palette, persisted state enforces the same rule plus exact frame-override palette length and entry-ID order, and frame deletion removes its override with exact inverse restoration. Bounded portable AIDraw JSON and GIMP GPL import/export preserve order/names, offer replace-by-index or append-unique behavior, never delete indexed artwork implicitly, and create durable reports; core and live MCP regressions cover replacement. Add packaged palette UX acceptance. |
| PIX-03 | Integer coordinates and nearest-neighbor rendering | ✅ Verified | P0 | Low-zoom pointer mapping and CSS-stretched canvas coordinates have regressions. Add high-DPI/multi-monitor/stylus tests. |
| PIX-04 | Pixel-perfect pencil, eraser, fill, replace, line, shape tools | ✅ Verified | P1 | Shared core kernels now preserve ordered pencil revisits while removing doubled staircase corners, produce exact symmetric integer ellipse outlines, and return fill/replace results as compact row runs under the canonical one-million-cell and 65,536-run limits. Flood fill budgets the actual connected result, so a small island remains editable inside a larger sparse sprite; replacement budgets the entire inspected target. UI fill/wand use the visible composite while semantic fill uses its addressed cel, preserving each surface's established source semantics. Overflow warns and applies nothing. Fill/replace are one-shot gestures, and pointer cancellation or Escape clears every transient preview before releasing pending locks without committing. Pure goldens, source wiring, live MCP, cancellation, large-canvas, and the formal million-cell performance gate cover the exit criteria; packaged pointer behavior remains Level 3 evidence rather than a PIX-04 blocker. |
| PIX-05 | Selection and magic wand workflow | 🟢 Working | P1 | Rectangle, true freeform lasso, and connected-color wand selections render marching ants and support replace/add/subtract/intersect modifiers. Drag move, copy/cut/paste at cursor, Select All, Escape cancel, delete, flips, rotation, and scaling use exact indexed/GID cells with union locks, clipping feedback, palette-preserving cross-sprite paste, and actor undo. Marching-ant Canvas work is bounded to scale-padded visible cell candidates after one honest O(selection-size) metadata scan. Freeform lasso keeps an exact same-direction-collinear reduced outline during drag, rasterizes once on release with shorter-axis even-odd scanlines, and leaves selection unchanged above 4,096 vertices, one million candidate cells, four million boundary steps, or safe coordinates. Cross-application clipboard publication and packaged pointer acceptance remain. |
| PIX-06 | Stamps | 🟢 Working | P1 | Exact indexed irregular selections can be copied locally or captured into a persistent named library, previewed, placed, flipped, rotated clockwise/counterclockwise, and deleted; non-square anchor/index tests and semantic MCP placement share the tested kernel. Bounded version-1 JSON copy/load/paste previews before one append-or-replace transaction; pixel imports reuse or append exact colors and fail rather than approximate a missing color in a full palette. Tilemap selections use a separate exact 32-bit-GID stamp library with the stricter compatibility boundary in MAP-11. Cross-application indexed-selection clipboard interchange and packaged pointer/file/clipboard acceptance remain. |
| PIX-07 | Ordered dithering and lighten/darken | 🟢 Working | P2 | UI and semantic MCP share deterministic 2×2/4×4/8×8 Bayer kernels, exact foreground coverage, base/mix palette indices, phase-aware region filling, and selection-clipped previews. Lighten/darken steps to the nearest visual-luminance palette entry instead of assuming palette slot order. Add stroke-phase controls, preset persistence, and packaged visual acceptance. |
| PIX-08 | Symmetry, wrap editing, palette cycling | 🟢 Working | P2 | Horizontal/vertical symmetry, repeated preview, and named validated palette-cycle ranges with direction and timing are authorable and preview exactly without rotating unrelated entries. Session-local **Wrap edit** makes repeated sprite copies actionable: one exact modulo kernel maps freehand/eraser/shapes/dither/lighten/darken, reusable stamps, bitmap text, and one-shot sample/region seeds into canonical indexed coordinates before the existing full-sprite lock and one revision-checked undoable cel transaction. Rectangle/lasso geometry and flood connectivity remain bounded rather than toroidal; no schema, GID, Tiled field, exporter, or AGT-11 operation changed. Authored symmetry axes, mode persistence, animated-cycle export behavior, and packaged visual/pointer acceptance remain. |
| PIX-09 | Bitmap-font text | 🟢 Working | P2 | Pixel documents own strict reusable bitmap-font assets. A shared deterministic glyph kernel supports multiline text, scale, letter/line spacing, left/center/right anchors, exact locked painting, SVG glyph preview, JSON copy/import, and authenticated semantic MCP placement. Add a glyph/sheet editor, mapping UI, broader bundled character coverage, and packaged acceptance. |
| PIX-10 | Layered cels, linked cels, durations, onion skin | 🟢 Working | P1 | Layers/cels, duplicate/delete/reorder, exact duration editing, whole-frame or per-exposure link/unlink, a bounded layer×frame exposure grid, and playback are authorable through UI and semantic MCP kernels. Session-local onion settings independently bound previous/next frames to zero–four, tint, and base opacity; a shared planner renders at most eight frames far-to-near with distance fading and never mutates the document. Base, onion, and wrap redraws now composite only their outward-rounded viewport through one shared region renderer while retaining linked-cel, nested opacity/blend, palette/tint, and replay-mask semantics. Pixel tools target the explicitly selected visible unlocked layer and fail closed for a selected hidden/locked layer. Exact-package acceptance proves four frames at 80/120/160/200 ms, three independent cels plus a fourth linked to frame three, visibly distinct onion on/off states, and canonical renderer/MCP agreement. Settings persistence, maximum-size multi-frame packaged pacing, and broader packaged grid/settings/replay permutations remain. |
| PIX-11 | Animation tags and loop/ping-pong preview | 🟢 Working | P1 | Named ranges can be created, edited, selected, deleted, and previewed forward/reverse/ping-pong through validated UI and MCP operations; UI, MCP, batch, and CLI GIF/APNG/sprite-sheet export resolve the chosen tag sequence exactly. Exact-package acceptance now proves a named four-frame ping-pong tag and its six-frame export sequence. Nested/overlap policy and broader packaged tag permutations remain. |
| PIX-12 | Per-frame palette overrides | 🟢 Working | P1 | Enable/edit/clear authoring, validation, rendering, frame reordering, and semantic MCP replacement work. Bounded GIF/APNG import assigns exact frame-local RGBA palettes for up to 255 visible colors per composited frame with deterministic aligned overrides. PNG, APNG, and sprite-sheet output preserves partial alpha without Canvas readback for visible nested normal pixel leaves, using exact source channels, renderer-equivalent path-multiplied opacity, and deterministic eight-bit source-over. GIF preserves exact canonical slots for an eligible opaque ordinary single layer; another full-effective-opacity normal composite uses deterministic local slots when its final pixels have binary alpha and at most 255 visible colors. Whole-animation quantization fallback covers over-limit import, partial final alpha, palette overflow, fractional opacity, non-normal blend, or another ineligible GIF frame. Partial-alpha/fractional-opacity GIF policy, broader non-normal composite fidelity, external producers/color management, and packaged acceptance remain. |
| PIX-13 | Integer selection transforms and 90-degree rotation | 🟢 Working | P1 | Pure kernels preserve exact palette indices/GIDs for move, flips, clockwise/counterclockwise 90° rotation, and independent 1×–64× nearest-neighbor X/Y scaling of rectangular or irregular masks while clearing source and clipping finite destinations. Sprite and tile selection UI now exposes both labelled quarter-turn directions through those kernels; compact-run MCP scaling shares the same tested geometry. Packaged acceptance remains. |
| PIX-14 | GIF/APNG/sprite-sheet import and animation export | 🟢 Working | P1 | GIF/APNG/sprite-sheet export is tested with exact durations, transparency, presentation scale, disposal modes, and UI/MCP/batch/CLI named forward/reverse/ping-pong sequences. Retained pixel APNG/GIF sources and direct or metadata-driven sprite sheets must fit the existing 1,500,000-byte canonical inline-asset ceiling before parser/native decode. Pixel APNG output now rejects before rendering when its scaled selected raw frames exceed 64 megapixels; exact GIF probes all-frame eligibility and writes eligible frames sequentially without retaining every uncompressed index plane. Bounded APNG and GIF parsers reject unsafe dimensions, frame rectangles/counts, truncation, and expanded-pixel budgets. Editable GIF patches additionally require exact LZW pixel count plus end-of-information, valid dictionary codes, an active color table, and in-range color/transparency indexes instead of accepting decoder-padded zero or black pixels. Pixel GIF import replays that strict decoder for palette planning and cel writing, referencing one at-most-64-MiB patch at a time instead of an aggregate patch array. Generated controls prove exact frame-local indexed palettes within 255 visible RGBA colors, warned whole-animation quantization above that limit, exact partial-alpha APNG/PNG/sprite-sheet output for simple imported, full-opacity normal-composite, and fractional-opacity normal-composite frames, exact opaque single-layer GIF local-palette re-export, and exact GIF output for a full-effective-opacity two-layer normal composite whose final pixels have binary alpha and at most 255 visible colors. The APNG parser additionally reconstructs Adam7 passes into its established byte-aligned or packed-sample rows; generated RGBA and one-bit indexed fixtures preserve exact editable pixels, timing, partial rectangles, disposal/blend, transparency, and source bytes. Its palette/transparency validator rejects contradictory `PLTE`/`tRNS` metadata and out-of-range indexed samples, while exact 16-bit grayscale/truecolor keys are compared before high-byte display conversion. A separate generated control proves that a validated fallback `IDAT` image stays outside the transparent animation canvas and that the later partial frame uses `fdAT`; contradictory cross-mode or interrupted image data rejects. Unknown critical or malformed/reserved chunk types now fail closed, while a well-formed unknown ancillary chunk remains ignored and source-retained. The sprite-sheet image wizard and approved agent import share exact grid/margin/spacing/order/count/duration settings, optional empty-frame removal, alignment-preserving shared trim, embedded source retention, companion containment, and expanded-frame limits. Exact-package acceptance now exports one approved 2× APNG to a new isolated target and independently decodes 32×32, six frames, and 80/120/160/200/160/120 ms while matching the authenticated canonical sequence. Representative third-party fixtures, partial-final-alpha/over-255/non-normal/fractional-opacity GIF color round trips, rotated/trimmed metadata conventions, exact GIF peak-memory proof, disk streaming, and packaged GIF/import/sprite-sheet coverage remain. |
| PIX-15 | Presentation upscale in UI and headless CLI | ✅ Verified | P1 | Integer 1×–64× nearest-neighbor export and overwrite-safe batch CLI are tested. Add batch APNG/sprite-sheet/Tiled coverage and a documented exit-code contract. |
| PIX-16 | Standalone/project links, embed/extract, Pack Project | 🟢 Working | P1 | Imported project sources retain portable relative paths, SHA-256 pins, and embedded cached content. The Assets panel supports per-link Embed, Extract, and Relink plus whole-project packing; MCP has semantic embed/pack and approval-backed relink/extract paths. Stale caches are refused, replacement cache deletion is reference-safe, and document-revision guards prevent long-open approvals/dialogs from overwriting concurrent edits. P1 reflects that the selected local multi-asset game studio and AGT-17 depend on these project contracts; pure transitions, file preparation, schemas, reducer conflict/undo, and live MCP contracts are tested, while packaged native-dialog and approved-job external-file round-trip acceptance remain. |

Evidence: [pixel canvas](../src/renderer/canvas/PixelCanvas.tsx), [pixel storage](../packages/core/src/pixel.ts), [pixel tests](../tests/core/pixel.test.ts), [coordinate tests](../tests/renderer/pixel-coordinates.test.ts), [CLI tests](../tests/main/cli.test.ts), [export tests](../tests/main/export-document.test.ts).

## 5. Tilesets, Wang terrain, and tilemaps

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| MAP-01 | Tileset/map/Wang/collision data model | ✅ Verified | P1 | Sparse chunks and deterministic Wang matching are unit-tested; the complete model is schema-validated. |
| MAP-02 | Tileset slicing | 🟢 Working | P1 | Tile width/height, margin, spacing, rows, columns, exact source coordinates, and selected-tile preview are authorable. Re-slicing now drafts all geometry together over a bounded first-frame source-sheet thumbnail with current/draft/selected crop overlays. Source-position and tile-ID policies remap tile metadata, animation targets, Wang representatives, and assignments through one immutable kernel; exact impact categories and acknowledgment precede any affected commit, ambiguous source origins fail closed, and maximum blank slices stay sparse. Add packaged pointer/visual acceptance before Verified. |
| MAP-03 | Per-tile properties, probability, and animation editor | ✅ Verified | P1 | The selected-tile editor authors probability, typed custom properties, and ordered frame/timing lists. Drag-and-drop plus keyboard move controls share one immutable reorder kernel; a pausable duration-aware first-sprite-frame preview starts paused for reduced motion. Placed map cells resolve those authored frames through one exact session clock before retaining the stored GID transform; reduced motion and every static headless/export surface use the first authored frame. Interactive and headless maps share crop-only layered source compositing plus a per-render 64 MiB / 1,024-entry disposable LRU, so a small tile in an 8,192² sparse sheet no longer materializes the sheet. Fractional source offsets and nominal clipping remain exact. External TMJ/TSJ and TMX/TSX fixture round trips preserve exact order/durations, probability, property types, and collision metadata. Automated pure/renderer/headless/export/interchange coverage satisfies this exact workflow; maximum-size tiles, time-addressable observation, animated map export, worst-case packaged pacing, broad Tiled visual equivalence, and RC readiness are not inferred. |
| MAP-04 | Collision-shape editor | 🟢 Working | P1 | A bounded nearest-neighbor tile preview renders rectangle, ellipse, polygon, and polyline collisions; checkboxes/Shift-click select multiple shapes for exact group movement or bulk deletion, while bounded shapes resize from a corner, polygon/polyline points drag independently, Alt-click inserts a projected edge point, Ctrl-click deletes while enforcing topology minimums, numeric fields stay synchronized, and each collision has typed custom properties. Authenticated agents share revision-checked upsert/delete semantics. Packaged pointer/Tiled-fixture acceptance remains. |
| MAP-05 | Edge/corner/mixed Wang set editor | 🟢 Working | P1 | Multiple Wang sets/colors, type, probability, deletion, and all eight edge/corner tile slots are authorable through one validated UI/MCP kernel. Exact-package finite-map acceptance now proves one named mixed set, its named color/probability, ten mapped tiles, and all eight full-terrain slots through visible UI and authenticated MCP agreement. Multiple-set/color/deletion packaged coverage, visual edge diagrams, and external fixtures remain. |
| MAP-06 | Terrain painting | 🟢 Working | P1 | Terrain/set/color and paint/erase mode are selectable; a tested 3×3 repair kernel enforces shared edge/corner constraints, reports unmapped transitions, and chooses weighted variants. Exact-package finite-map acceptance visibly proves the intended nine-GID paint pattern, all-nine empty-terrain repair on erase, deterministic repaint hash restoration, and renderer/MCP agreement. Drag diagnostics, larger repair propagation where tilesets require it, and packaged infinite-map acceptance remain. |
| MAP-07 | Finite/infinite orthogonal maps | 🟢 Working | P1 | Exact tile painting and 32×32 sparse chunks work. Exact-package acceptance proves a finite 12×10 orthogonal bounds resize with visible terrain paint/erase/repaint, plus a separate orthogonal infinite-map lifecycle that visibly resizes the governed 64×64 default to 32×32, navigates signed space, and preserves exactly fourteen GID-1 cells across seven canonical chunks at negative and positive origins. One shared projection contract now preserves rectangular authored cell aspect across editor/headless artwork, objects, pointer/parallax inversion, grid/feedback, and PNG dimensions; exact 3×5 geometry and production pixels cover it. Headless observation renders only the requested nominal region, and the editor maps its fractional/parallax viewport outward into canonical pixels; both decode only conservatively intersecting chunk payloads after one metadata scan, while the editor also bounds visible orthogonal grid lines, transient preview/selection/replay Canvas work, the globally phased checker plus its single shadow source, and release-time lasso work. Whole static rasters reject the 65,535-side / 64-megapixel limits before allocation and native save retains an auxiliary fallback preview. Renderer, authenticated MCP, and the allow-once TMJ export agree; independent parsing recovers the exact geometry, chunks, and cells. The first infinite-map package attempt remains a preserved setup-observer failure because it expected the mode toggle itself to change geometry. Native-size tall-tile/tileset offsets and alignment, infinite-view expansion, adaptive non-observation previews, isometric packaged acceptance, and full round-trip E2E remain. |
| MAP-08 | Finite/infinite isometric maps | 🟢 Working | P1 | Creation, rendering, exact painting, stamps, negative infinite-map coordinates, and object-layer selection/move/resize work. Sparse layers share one lazy storage-independent right-down row iterator. One projection kernel now owns finite bounds, asymmetric-map origin/aspect, tile/object coordinates, continuous pointer/parallax inversion, and projected interaction feedback across interactive and headless surfaces; exact edge, inverse, overlap, and raw-RGBA regressions cover it. Requested nominal observation regions render without a full-map allocation and remain byte-exact with ordinary full-raster tile/object crops; both headless regions and outward-rounded fractional/parallax editor viewports decode only conservatively intersecting chunk payloads after one metadata scan. Transient preview/selection/replay cells additionally use conservative projected diamond bounds, horizontal compact runs derive candidates algebraically, and the shared checker/lasso surface path is viewport/work-bounded. Native-size tall-tile overhang/offset, non-square diagonal-transform geometry, infinite-view expansion beyond nominal bounds, adaptive non-observation previews, packaged interaction, and broader external visual goldens remain. |
| MAP-09 | Tile/group/object layers, parallax, opacity | 🟢 Working | P1 | Pixel and map layer panels show true nested z-order with collapse/expand, cycle-safe group assignment, and drag reordering (Alt-drop nests); shared tree kernels are unit-tested. Authored X/Y parallax previews while panning include parallax-correct object hit testing. Orthogonal/isometric rectangle, ellipse, polygon, and polyline object layers use one shared overlay renderer in editor, headless observation, and raster export, honoring flattened z-order, visibility, multiplied opacity, and isometric projection at canonical pan zero; exact tests cover above/below/hidden order, object-only output, PNG parity, and isometric aspect/opacity. Both renderers linearly filter conservative stroke/selected-handle-expanded affine bounds against their region/viewport before path rendering, retaining order and live gesture geometry. Selection handles remain editor-only, lock constrains edits rather than visibility, and Tiled metadata serialization remains unchanged. Packaged drag/parallax/topology/maximum-object acceptance and native Tiled overlay-style equivalence remain. |
| MAP-10 | Random variants and probabilities while painting | 🟢 Working | P1 | Wang terrain and named non-Wang tile groups use authored probabilities. A persisted signed map seed and stable coordinate hash make UI and `pixel.tile-variants.paint` MCP results reproducible; pure and live-contract tests cover weighting. Named groups now show bounded clickable probability thumbnails, and **New stroke** persists a deterministic next seed without changing existing GIDs. Tiled/format/MCP docs identify both `aidraw:` values as custom properties rather than a native Tiled convention. Add packaged pointer/visual acceptance before Verified. |
| MAP-11 | Reusable tile/map stamps | 🟢 Working | P1 | Exact 32-bit transformed GIDs can be captured from tile selections into persistent named stamps, previewed, placed on finite/infinite maps, flipped, rotated clockwise/counterclockwise, deleted, and placed through authenticated semantic MCP. Non-square anchor/GID tests preserve transform flags in both directions. Bounded JSON transfer now previews and preserves exact GIDs between cloned/shared projects only when tileset name/identity/revision/range/geometry and source identity/revision/size match; it does not bundle pixels or rebase GIDs. Arbitrary cross-document asset libraries and packaged pointer/file/clipboard acceptance remain. |
| MAP-12 | Tile transformations | 🟢 Working | P2 | All eight square-tile H/V/diagonal combinations share one anti-diagonal-first core matrix across interactive and headless/export rendering. Exact corner/pixel tests, a raw-RGBA golden, and a production orthogonal TMJ+PNG round trip preserve all unsigned GIDs and pixel output. A bounded chooser now shows eight 32×32 selected-crop previews; one pure geometric-capability closure constrains the picker, quick toggles, current-tile stamps, and ordinary paint without converting stale disallowed state into another transform. Selection/stamp transforms retain full unsigned GIDs. Packaged Tiled fixture interaction, isometric-specific visuals, and explicit rectangular diagonal fitting/preview remain. |
| MAP-13 | Tiled TMJ/TMX/TSJ/TSX fidelity | 🟢 Working | P1 | JSON/XML, chunks, nested layers, transforms, Wang data, collisions, animation, and typed properties are implemented. Committed external TMJ→TSJ and TMX→TSX fixtures cover isometric/orthogonal maps, typed metadata, geometry, Wang colors, GID flags, and property-preserving current JSON export. Headless and exact-package supported-surface acceptance now export an orthogonal signed-chunk TMJ plus its PNG companion, import that exact pair through the production utility boundary into a distinct document, preserve geometry/typed properties/tileset metadata/seven signed chunks/fourteen cells/indexed pixels, and re-export to distinct exclusive-create targets with equal supported semantics and decoded RGBA. The package run uses three actor-bound allow-once approvals and independently reparses two byte-identical 110,622-byte TMJs plus two byte-identical 140-byte 32×16 PNGs. The original decoder defect and subsequent source-only map-name observer overclaim remain preserved; Tiled has no portable top-level map name, so filename-derived imported naming is excluded. Isometric infinite export acceptance, unsupported modern fields, image-collection tilesets, templates, zstd, newer blend/tint fields, broad/full fidelity, and comprehensive loss reports remain. |

Evidence: [Wang implementation](../packages/core/src/wang.ts), [Tiled importer](../src/main/import-document.ts), [Tiled exporter](../src/main/export-document.ts), [Wang tests](../tests/core/wang.test.ts), [export tests](../tests/main/export-document.test.ts).

## 6. Import, export, clipboard, and file authority

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| IO-01 | PNG/JPEG/WebP import/export | 🟢 Working | P1 | Raster import/export paths exist. A retained addressed raster must fit the established 1,500,000-byte canonical inline-asset ceiling before header/native decode; the shared constructor repeats the guard before hashing/base64 so no accepted import can construct an oversized asset. A frozen 948-byte JPEG with valid EXIF orientation 6 imports through the production utility as a 16×24 display from 24×16 encoded pixels in illustration and pixel modes, retains the exact source through native reopen, and produces a pixel-identical oriented PNG; bounded IFD0 parsing covers both byte orders and inline validation. Indexed PNG output now encodes exact canonical RGBA without Canvas premultiplication for a raw ordinary leaf or deterministic normal-layer source-over, including renderer-equivalent fractional group/layer opacity; generated partial-alpha controls prove each subset. Add non-normal composite color/alpha, ICC, large-image, other external-producer/orientation visual fixtures, metadata-export policy, and packaged acceptance. |
| IO-02 | SVG editable import | 🟢 Working | P1 | Committed fixtures cover editable shapes/paths/text/images, nested groups/transforms, defs/use, class and inline styles, linear/radial gradients, clips, and blur filters. The outermost SVG viewport converts a valid `viewBox` plus standard `preserveAspectRatio` alignment/`meet`/`slice`/`none` into one canonical transform. Nested `<svg>` and `<use>`-instantiated `<symbol>` viewports resolve missing/auto dimensions, unitless/px/percentage geometry, and the same viewBox matrix without rewriting child coordinates; direct symbols remain definition-only, and invalid/zero/non-finite viewports fail before child admission. Transform-bearing geometry/containers/references/masks and referenced gradients reject the complete import if numeric parsing, composition, decomposition, positioning, or derived paint coordinates become non-finite; irrelevant metadata/unsupported transforms remain ignored. Every completed document then crosses the established canonical illustration validator. Pinned-Canvas standard CSS colors up to 256 characters reduce to lowercase sRGB hex, invalid colors warn to black, one-stop gradients stay solid, reverse-direction lines use positive dimensions plus an affine sign, and names/font families warning-truncate at 200 characters; any remaining out-of-model result fails the whole import with an exact SVG-specific error. Generated controls prove outer letterbox/stretch/slice/fallback, exact nested and expanded-symbol-equivalent pixels, hostile/canonical-boundary transform arithmetic, canonical color/line/name recovery, and six rejected schema-limit families. Unsupported constructs produce explicit warnings; malformed-transform grammar, nested viewport overflow clipping, CSS `calc()`/font/physical lengths, CSS transforms, symbol `refX`/`refY`, external references, effect-bearing transformed-group overhang, advanced masks/filter primitives, full CSS cascade/color profiles, and broad third-party corpus fidelity remain. |
| IO-03 | SVG editable export | 🟢 Working | P1 | Vectors, nested groups, gradients, ranged text, arrows, image crops, and object blur/masks remain editable where supported. Center/right text uses the standard local box midpoint/right-edge anchor instead of zero; strict version-1 AIDraw `data-*` metadata restores finite bounded box width/height and line height on AIDraw re-import, while invalid metadata warns and falls back. Cropped images retain their standard clip/image geometry and add a separate version-1 display/source/crop envelope; AIDraw restores one canonical cropped image only when that exact wrapper still matches, otherwise warning and preserving the generic editable group/image/mask interpretation. Reachable visible paint content exports as a transparent PNG fallback rendered without document mutation; its own supported adjustment filters and vector layer mask are baked while opacity/blend remain on the SVG group exactly once, and the exact layer receives a structured raster-fallback reason. Hidden/unreachable/empty paint does not emit a fallback. Multiline wrapping/justify, exact font shaping/embedding, crop inference or metadata retention for foreign editors, complex editable layer filters/masks, blend portability, packaged behavior, and broader third-party visual goldens remain. |
| IO-04 | PSD import/export | 🟡 Partial | P1 | Nested groups, visibility/opacity/blends, full binary locks, and supported styled-text companions are retained beside visible per-layer raster fallbacks and the composite. Completed illustration and pixel-mode imports pass the established migration/canonical gate before direct return; editable strings additionally share the 100,000-code-unit canonical ceiling and exact lower-bound accounting against the existing 512 MiB imported-document envelope before text-object construction. Out-of-model decoded content fails the complete import rather than being clamped or partially omitted. Import and export share 2,048-record and depth-64 structural ceilings; export preflights its exact emitted tree, including generated fallback and private text-companion children, before writer recursion or layer rendering. It then preflights the full-canvas illustration fallbacks or pixel leaves it will retain and rejects an aggregate layer-raster area over 64 megapixels before allocation; structural groups and metadata-only text companions do not count, and the final composite retains its separate existing static-raster bound. Illustration fallback layers render sequentially through a non-mutating source view that neutralizes only the root layer's visibility/opacity/blend, preserves masks/filters/descendant composites, and releases each temporary Canvas after copying its retained pixels. The pinned decoder's `children` array is group authority even when empty. Pixel import now maps one PSD canvas to one one-frame sprite at that canvas size; folders and raster-bearing leaves retain exact order plus supported visibility, normalized opacity, blend, and binary lock-all. A bounded pre-order union preserves raw raster-leaf RGBA exactly through one shared indexed palette when at most 255 visible colors survive the alpha threshold; matching document colors keep their nonzero indexes and novel colors receive deterministic lowest-unused slots. At 256 union colors every leaf instead uses the established document-palette quantizer with one warning. Each leaf is indexed independently against the selected shared palette, then placed at decoded signed `left`/`top` coordinates through sparse cels. Empty groups remain in that sprite, non-raster leaves are omitted with an exact warning, and a source with no decoded raster leaf keeps the ordinary editable fallback layer rather than silently substituting the composite. Pixel PSD export emits the active sprite's exact current-frame group/child order, including empty groups and hidden leaf pixels; each leaf carries unbaked full-alpha source data with visibility/opacity/blend/lock stored separately, while later frames remain omitted. AIDraw-generated export → pixel import → re-export preserves that supported structure and representable custom leaf colors, while a generated signed-offset crop proves existing sparse coordinates without new origin metadata. Illustration and pixel imports map only lock-all back to AIDraw; partial transparency/position/composite/artboard locks remain unlocked with an explicit warning. AIDraw-generated nonempty hidden text companions preserve their canonical object name, normalized opacity, blend, lock, bounded box, line height, style runs, and complete visual affine matrix through AIDraw re-import; the canonical transform decomposition may differ while remaining matrix-equivalent, and the synthetic hidden wrapper is neutral. Their text object stays visible relative to that wrapper so one layer toggle reveals it without duplicating the authoritative fallback by default. Empty text remains fallback-only, older translation-only companions recover stored size/position, and formerly clamped opacity fractions are unrecoverable. The convention is exact-suffix-bound and does not reinterpret unmarked/external PSD text. Animated-frame export, original PSD palette IDs/index identity, partial-lock preservation, vector descriptors, effects/adjustments, uncommon color modes, font shaping, integer-tracking loss, byte identity, broad producer fidelity, and lossless text/effect round-trip remain unsupported and are reported explicitly. |
| IO-05 | PDF import/export | 🟡 Partial | P1 | Multi-page import preserves each page as a raster fallback plus hidden extracted text. Extracted strings share the canonical 100,000-code-unit text ceiling, exact incremental accounting rejects when text alone cannot fit the established 512 MiB imported-document envelope, and each completed page document is canonically validated before direct return; the utility worker preflights the complete existing envelope before IPC while the main process independently repeats it. The first maintained third-party fixture is one LibreOffice Draw clipped-transparency page: its fractional 240.746×150.746 pt box imports at 241×151, all three text runs remain on an importer-tagged hidden editable layer, and the fully opaque AIDraw fallback stays below a mean 8/255 channel delta with fewer than 5% of channels over delta 32 against an independent Poppler 72 dpi golden. Supported text from that tagged layer re-exports once as invisible searchable Helvetica content; an otherwise identical untagged hidden layer remains omitted, while unsupported glyphs or transforms remain omitted with explicit loss warnings. Every actual invisible-text retention now also reports that visible covering artwork is not redaction. An exact opaque full-page overlay proves visual coverage does not remove the three searchable phrases; this warning neither infers intent nor implements or guarantees content removal. The re-exported raster also stays within the same visual thresholds. Multi-page import retains its complete geometry preflight, then resets each encoded page Canvas, cleans each processed page proxy before the next render, sweeps preflight-only proxies on failure, and always destroys the loading task while preserving a primary import error over cleanup failures. Out-of-model extracted text fails the whole import rather than being clamped, split, or silently omitted. Export is otherwise hybrid: simple solid vector paths/shapes, standard-font text, and embedded images remain native PDF content while paint, effects, gradients, masks, and unsupported transforms fall back transparently with a report. Editable common-operator import and broader PDF fidelity remain missing. |
| IO-06 | Clipboard AIDraw JSON + SVG + PNG | 🟢 Working | P1 | Editable illustration relation-closed and pixel dependency-closed fragments share a published application-private HTML contract plus ordinary SVG/JSON text and PNG fallbacks. Current HTML is version 1, SHA-256-bound, canonical-base64 UTF-8 JSON under the existing 2 MiB fragment ceiling and a 16 MiB envelope ceiling; the exact legacy marker remains readable. A malformed claimed marker fails explicitly instead of silently substituting bitmap content. Marker-absent PNG paste requires exact bytes/hash/header plus matching native/decoded geometry under the canonical 8,192-side/16MP and 1,500,000-byte editable-asset limits. Copy rendering and pixel quantization use supervised raster utilities, with native images confined to OS clipboard I/O. Pure/workflow coverage proves current/legacy parsing, tamper/duplicate/amplification failure, fragment/bitmap isolation, illustration asset paste, and pixel quantization routing. Add real packaged cross-application clipboard preservation and multi-object/mask UI acceptance; the private HTML attributes are not a registered MIME or third-party standard. |
| IO-07 | Pixel animated and sprite-sheet interchange | 🟢 Working | P1 | Generated GIF/APNG files round-trip timing, transparency, disposal, bounded exact frame-local palettes, simple-shape APNG/GIF re-export, exact partial-alpha PNG/APNG/sprite-sheet output for raw ordinary leaves plus full- or fractional-opacity normal-layer composites, and exact GIF output for full-effective-opacity ordinary normal composites whose final pixels fit binary alpha and 255 visible colors. Retained pixel APNG output preflights scaled selected frames against 64 megapixels before render, while exact GIF's two-pass all-or-nothing decision references one uncompressed frame at a time and leaves the established sequential fallback unchanged. Editable GIF patch decoding now fails closed on missing/early end-of-information, short or excess pixel output, invalid dictionary codes, absent color tables, and out-of-range color/transparency indexes. Its import planner/writer replay exposes one fresh at-most-64-MiB patch at a time instead of retaining up to 1,024,000,000 aggregate patch bytes. Spec-derived Adam7 APNG controls add exact two-frame RGBA partial-rectangle and one-bit indexed-transparency import evidence through the same bounded editable-frame path. Generated 2×1 16-bit grayscale/truecolor controls prove full-sample transparent-key comparison, while exact malformed palette/transparency/index variants reject before import. A separate 2×1 control proves a validated two-chunk PNG fallback remains outside a later 1×1 `fdAT` animation frame, with exact timing and source retention. Metadata sheets import, and a dedicated image-slicing wizard plus approved MCP path covers grids, margins, spacing, order, count, timing, empty-cell skipping, shared trim, source retention, exact companion-folder authority, and bounded decoded work. Add external-tool fixtures, non-normal composite fidelity, partial-alpha/fractional-opacity GIF policy, exact peak-memory/disk-streaming evidence, and rotated/trimmed metadata conventions. |
| IO-08 | Tiled interchange | 🟢 Working | P1 | See MAP-13. External JSON/XML map→tileset fixtures round-trip the supported typed metadata surface, while headless and exact-package orthogonal TMJ+PNG lifecycles now prove first export, production-utility second-document import, exact canonical/indexed-pixel recovery, and distinct re-export with semantic/RGBA equality under exclusive-create writes. Root/companion byte limits, canonical relative containment, DTD/entity rejection, shared 1,024-tileset/layer/depth/object/cell/tile budgets, exact array lengths, bounded compressed expansion, and absence of Electron `nativeImage` from the packaged utility importer are regression-tested. JSON/XML map export preflights the exact emitted hierarchy and finite planes or sparse chunk rectangles before companion rendering, then requires every declared tileset/source sprite, disjoint 28-bit base-GID ranges, and one resolved declared tile for every emitted nonzero cell. It fails whole rather than emitting a stub, omitted XML tileset, dangling image, ambiguous range, or unresolved GID; finite clipping and all signed infinite chunks remain under their existing contracts. Its companion planner assigns deterministic case/NFC-collision-safe PNG leaves in canonical tileset order; the 4,097-member worker envelope remains defense in depth while maps stop at the narrower import ceiling. One exact two-tileset TMJ re-import preserves both authored names and rendered pixels. One artifact-wide positive-uint32 object-ID allocator also replaces JSON digit concatenation and XML per-group resets; exact unique numeric identities remain stable through TMJ import/re-export while opaque canonical strings are explicitly not claimed as foreign identity. It neither derives infinite-map bounds/origin nor advertises resource/member limits as Tiled maxima or the naming evidence as universal filesystem compatibility. Both prior packaged failures remain immutable. Unsupported modern fields, image-collection tilesets, automatic GID repair/rebasing, broad/full fidelity, portable source map-name preservation, object-reference/template semantics, and complete per-field loss reporting remain. |
| IO-09 | Explicit import/export report | 🟢 Working | P1 | Human, approved-agent, and batch exports preserve actor-attributed, restart-persistent reports with exact source/output paths, warnings, rasterized entries, failures, document associations, and JSON export. Report reads/records serialize per runtime; copied latest-200 state becomes live only after private atomic replacement, so concurrent records survive restart and a rejected replacement cannot create a phantom Activity entry. A closed bounded reason contract now identifies exact SVG paint layers embedded as raster fallback, PDF objects for raster fallback/font substitution/retained or omitted searchable text, PSD export layers/documents for raster fallback, filtered/masked-group flattening, and omitted later frames, plus exact illustration/pixel PSD import layers whose unsupported blend modes became canonical `normal`. Import reasons cross the supervised worker under closed-entry validation and the existing aggregate report ceiling before human or approved-agent Activity recording. Legacy version-one reports normalize the additive field to empty; producers cap the list at 4,096 and fit its prefix/warning without displacing valid legacy arrays; Activity exposes the reasons. Extend exact reason coverage to remaining adapters/import outcomes and add packaged workflow acceptance; current codes are not comprehensive loss accounting. |
| IO-10 | Exact-path authority and overwrite approval | 🟢 Working | P0 | Structured approval, session/persistent choices, symlink/junction canonicalization, case-normalized trust, race-aware exclusive writes, and planned companion overwrite paths are tested. Persisted authority additionally requires the exact writer-shaped version-one folder array and replaces the complete in-memory set only after every entry has canonicalized; malformed or stale settings grant nothing. Trusted folders may read existing import sources, but never auto-approve an existing save/export target or companion. Consented automatic client setup serializes within one process, privately backs up each observed existing config version, rechecks before mutation, and atomically replaces the complete private config; no cross-process compare-and-swap guarantee is inferred. Add a packaged sprite-sheet/Tiled companion overwrite scenario and broader reparse-point corpus before Verified. The explicitly addressed headless bootstrap output likewise replaces one complete private flushed temporary rather than direct-writing its plaintext token through the prior destination inode. |

Evidence: [import adapters](../src/main/import-document.ts), [export adapters](../src/main/export-document.ts), [clipboard IPC](../src/main/main.ts), [export tests](../tests/main/export-document.test.ts), [persistence tests](../tests/main/persistence.test.ts).

## 7. Image generation

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| GEN-01 | Provider-neutral request/job/provenance model | ✅ Verified | P1 | Provider, mode, prompt, negative prompt, sources, mask, size/aspect, count, seed, options, outputs, and provenance exist. A shared UI/engine/MCP capability validator tests provider modes, ignored inputs, embedded source/mask authority, counts, dimensions, seeds, and ComfyUI workflow/endpoint requirements before any paid request. |
| GEN-02 | OpenAI `gpt-image-2` adapter | 🟢 Working | P1 | The utility runner maps `gpt-image-2` create plus multipart edit/inpaint/variation requests, normalizes matching PNG source/mask inputs, and builds a same-size transparent expansion mask for true centered outpaint. Mock coverage proves decoded dimensions, invalid-mask rejection before a paid call, rate limits, cancellation, and zero automatic retries for ambiguous failures. Live API compatibility and broader moderation/timeout payload fixtures remain. |
| GEN-03 | Stability adapter | 🟢 Working | P1 | Create/inpaint/directional-outpaint v2beta endpoints, source/mask/style/seed/form mapping, real decoding, rate limits, cancellation, malformed results, no retry, and partial paid-result retention are mock-tested. Unsupported modes and invalid directional options fail before approval/request. Live-version compatibility and broader moderation/timeout payload fixtures remain. |
| GEN-04 | ComfyUI workflow adapter | 🟢 Working | P1 | API-format workflow loading, automatic or explicit node/input mapping UI, upload, `/prompt`, WebSocket progress/reconnect, bounded history polling, multi-output retrieval, malformed history, and queue removal/running-prompt interrupt on cancellation are mock-tested. Live workflow/version compatibility and broader node families remain. |
| GEN-05 | Protected provider credential lifecycle | ✅ Verified | P0 | Hosted keys use OS-protected storage and remain main-owned. Serialized atomic rotation preserves the other provider and the prior file on failure; explicit per-provider removal works without decryption when the backend is unavailable; 16-KiB input and encrypted-file schema/size limits fail closed. The shared 256 KiB encrypted-file ceiling now applies through an opened handle before JSON or `safeStorage`, so oversized/changing provider ciphertext is treated as corrupt without decryption. The MCP token applies the same bounded admission, exact writer/decrypted-token validation, same-instance ordering, and private flushed atomic replacement before corrupt-secret rotation returns a credential. The renderer has Replace and two-step Remove controls but only a write/status bridge: status distinguishes stored/configured/backend availability without returning secrets or ciphertext. Deterministic main/preload regressions satisfy this row's source exit criteria. Native DPAPI/Keychain/libsecret lifecycle and packaged interaction remain separately gated by FND-13, QA-11, and the release checklist. |
| GEN-06 | Agent paid-request approval | 🟢 Working | P0 | Agent jobs wait for a one-request approval with actor, provider/model or workflow, mode, prompt/negative prompt, source and mask thumbnails, result and potential paid-request counts, seed/size/options, expiry, and an explicit no-persistent-trust note. Headless contracts cover the full structured payload, bounded previews, normal two-minute expiry, and timer-driven invariance; source/mask thumbnails now decode in supervised raster work and enter approval only after static-PNG geometry admission, while failure remains advisory. Exact-package no-network acceptance now visibly proves the actor, every review field including provider options, deny, accelerated real expiry, originating-agent cancellation, separate allow-once, and no persistent trust or credential/provider request. Live-provider approval compatibility and broader paid-failure cases remain. |
| GEN-07 | Create/edit/inpaint/outpaint/variation workflows | 🟢 Working | P1 | Source/mask selection, provider-specific mode filtering, inline preflight, explicit Comfy workflow mappings, and non-destructive accepted-result layers/cels are shared by human and agent requests. Exact-package generated inpaint acceptance now preserves the source object/assets, adds one editable generated layer/object/asset, and records engine-owned provider/model/seed/source/mask provenance through human acceptance. Illustration outpaint expands and translates the artboard/content atomically with exact undo. Automatic canvas-source capture and selection-to-mask authoring remain. |
| GEN-08 | Result compare, before/after overlay, accept/cancel | 🟢 Working | P1 | The source asset or canonical canvas is frozen before the provider request; completed results support synchronized side-by-side/opacity-overlay comparison, shared zoom/scroll, accept-to-layer/cel, and explicit unaccepted-result cleanup. Exact-package acceptance now proves a visible deterministic comparison, non-destructive human accept, agent-job/human-transaction attribution, renderer/MCP equality, and accepted-output/mock/provenance agreement. Memory limits and multi-result comparison cases remain. |
| GEN-09 | Pixel palette conversion and reproducibility | 🟢 Working | P1 | Area resize, OKLab nearest palette, alpha threshold, optional Bayer/Floyd–Steinberg, source retention, and conversion metadata exist. Manager integration pins the exact document defaults, non-destructive indexed cel, retained source/provenance, two-actor ownership, and undo contract. Corrected exact-package `QA-06-PALETTE` acceptance now proves revision 2→3 for one frame, preservation of every original layer/cel and palette, one new human-owned indexed layer/cel, retained Stability `stable-image-core` provenance, agent-owned generation, exact area/OKLab/Bayer/0.6 conversion, renderer/authenticated-MCP/fixture agreement, and the 256-cell index distribution `{0:24,1:60,4:49,7:49,9:49,15:25}`. The first single-container text-observer failure remains immutable history. More golden quantization/dither variants and explicit multi-frame policy remain. |
| GEN-10 | No silent provider substitution or ambiguous paid retry | ✅ Verified | P0 | Shared preflight rejects unsupported modes and inputs rather than approximating them. OpenAI is configured with zero SDK retries and its ambiguous server failure is issued once; Stability first/later failures are never retried and retain completed paid outputs with partial-failure metadata. ComfyUI is never silently substituted for a hosted provider. |

Evidence: [generation types](../src/common/generation.ts), [generation manager](../src/main/generation-manager.ts), [quantization](../src/main/quantize.ts), [generation panel](../src/renderer/App.tsx).

## 8. Desktop UX and accessibility

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| UX-01 | Legible, configurable editor shell and context panels | 🟢 Working | P0 | The completed competitiveness assessment found editor icons, type, spacing, and hit targets too small and dense for comfortable sustained work. A bounded source baseline requests a 1,520×940 default renderer content viewport, enforces a 980×640 minimum renderer content viewport, and defines shared 12/11/10 px primary text, 38/32 px action, 19–21 px primary-icon, 18 px secondary-icon, and shell-dimension tokens. On macOS, native `hiddenInset` traffic lights share the existing 56 px app top bar; reserved traffic-light space and no-drag controls avoid a second vertical title band without hiding native window controls. Other platforms retain ordinary native title bars. Primary surfaces use those tiers without scaling the canvas; PixelCanvas and CSS share the exact 138 px timeline geometry. A bounded follow-up moves the sprite-animation cel exposure and onion-skin companion panels off their legacy 22–29 px controls and 6–8 px copy: paging, close, exposure-cell, link/reset, select, tint, and opacity controls now use the 32 px secondary target, supporting copy uses the 11/10 px tiers, and the bounded exposure grid scrolls locally. At 980×640, compact 62/632/286 px columns retain a 632×498 base canvas floor, or 360 px with the timeline, while crowded regions scroll locally. R1 remains an immutable controller/security-evidence failure; credential-safe r2 remains an immutable 1,520×928 product-size failure; secure r3 remains an immutable controller-sequencing failure after its narrower default-geometry/pointer checks passed. R4 was consumed before output when Forge could not spawn `npm` (`ENOENT`), produced no package/native evidence, and is not reusable. Secure r5 used the corrected controller in exactly one wrapper invocation and passed **1/1 in 5.5 seconds**. It proved exact 1,520×940 default content, exact 980×640 minimum content, an 860×550 request clamped to 980×640, zero root/body overflow, work-area containment, regular/compact disclosure with 62/632/286 px columns, the synchronized long-document route, 12 pointer and 12 keyboard workflows, 11 sampled control floors, the 138 px timeline/canvas boundary, revision 9→10 with an inert below-boundary click, four distinct screenshots, zero external renderer requests, credential-safe private evidence, graceful cleanup, and zero survivors. R5 is immutable and must never be reused. This is a narrow exact-package subclaim, not full UX-01 acceptance: UX-01 remains Working/P0. Separate window-chrome r1 `20260814t092808z-7ff773c-r1` failed **1/1 in 3.2 seconds** before input on invalid Quartz/Blink origin equality. Fresh r2 `20260814t102354z-f7e6d0c-r2` admitted exact owner 14422, renderer 14431, and layer-zero window 7029 after no-prompt permission checks, then failed **1/1 in 2.6 seconds** before input because Quartz `203,32 1514×936` and Blink `200,30 1520×940` disproved size equality too; graceful cleanup was survivor-free. Fresh r3 `20260814t112230z-ff44905-r3` admitted owner 22702, one renderer, and layer-zero window 7030 with the accepted dynamic relation, then exited **1** before input when an earlier native-bounds snapshot failed the strict action-time reinspection; missing current bounds made this a controller TOCTOU/evidence failure rather than a product finding. Fresh r4 `20260814t121343z-0f42b22-r4` admitted owner 30153, renderer 30168, and layer-zero window 7031 with stable/action-ready Quartz and Blink both `200,30 1520×940`; the mapped `1287.484375,27.5` point and complete uncertainty neighborhood stayed inside the All open documents no-drag button. Its exact-PID CoreGraphics post call returned, but the menu remained absent for 10 seconds and the DOM button remained exposed. Because r4 recorded neither exact-app frontmost/focus readiness nor renderer event delivery, it earns posted-input evidence only and is not a product-defect finding. Cleanup was graceful and survivor-free. The corrected source model preserves the dynamic decorated-frame containment relation, complete bounded hit neighborhoods, paired stability/action-ready snapshots, exact current-bounds admission, and independent coordinate-space movement. It now additionally activates only the exact run-owned regular AppKit application with default options, waits at most 3,000 ms for exact frontmost PID plus visible focused renderer readiness, and rechecks readiness before every post. A test-only capture listener must then acknowledge an ordered trusted primary pointer/mouse/click sequence on the exact button inside its safe neighborhood before the separate menu-visible assertion. It cannot satisfy that assertion and adds no production hook. R1/r2/r3/r4 are immutable controller evidence, not product failure, privacy completion, or native window-chrome PASS. A wholly fresh package and separate native authority remain required for close/minimize/Option-click standard zoom plus intended drag and interactive no-drag behavior. No production `acceptFirstMouse` change, deprecated ignore-other-app activation, blind second click, fixed inset, wider tolerance, screenshot mapping, Accessibility/private API, or global input is used. Native window-chrome PASS, ordinary-green-click fullscreen behavior, customizable workspace persistence and progressive-disclosure completion, 200% text and assistive technology, tablet/touchpad input, high-DPI/mixed-display behavior, smaller work areas, broad polish, RC, and stable-v1 remain explicit nonclaims. UX-09 owns assistive acceptance and UX-10 owns high-DPI/input/multi-monitor evidence. |
| UX-02 | Dedicated New Document configuration | ✅ Verified | P0 | Toolbar, File > New, Ctrl+N/Ctrl+Shift+N on Windows and Command+N/Command+Shift+N on macOS route into the same configuration dialog instead of mutating the document list. Packaged Playwright verifies the native primary shortcut exposes the 1920×1080 configuration and Cancel creates no document. |
| UX-03 | Reachable overflowing document tabs | ✅ Verified | P0 | Scroll controls and an all-documents menu are covered by packaged E2E. |
| UX-04 | File menu Open and keyboard Open | ✅ Verified | P0 | Both code paths call the same open workflow. Exact-build Windows acceptance proved the packaged executable SHA-256, PID, isolated profile, and unique target window before Computer Use clicked native File → Open by mouse. The saved fixture reopened visibly with its expected name and 321×123 artboard, while an authenticated MCP assertion matched the active file path, persisted document ID, kind, dimensions, background, and clean state. |
| UX-05 | Renderer failure containment | ✅ Verified | P0 | Malformed transactions are schema-rejected and an editor-only recovery boundary prevents a permanent white screen. An exact packaged regression injects one fixed ≤1 KiB sanitized malformed workspace event only under a path-contained isolated test gate, proves the recovery controls and canonical engine remain usable through renderer IPC plus authenticated MCP, saves a bounded local-only diagnostic, and reloads into the same document/revision/artwork/waiting job on the same engine PID. The diagnostic corpus assertion excludes embedded artwork bytes/metadata, prompts, provider payloads, task IDs, the live bearer token, profile/connection paths, and the malformed renderer snapshot. This narrow in-renderer event recovery remains Verified; it does not itself cover the older long-running no-renderer-helper observation. FND-05 separately retains its narrow crash/alive-hang replacement passes and owns the historical renderer-loss trigger, unsent renderer-local work, cache/profile repair, and broader-platform gaps. |
| UX-06 | Session restore and close/discard clarity | ✅ Verified | P0 | Explicitly discarded untitled documents no longer restore in unit/E2E coverage. Save All and Close All now consolidate unsaved-folder selection and save/discard decisions; add packaged multi-document save/close/crash recovery scenarios. |
| UX-07 | Replace browser prompts with editor UI | 🟢 Working | P1 | All four browser prompts are replaced by in-editor, focus-trapped, Escape/cancel-safe dialogs with validation and previews; tags also configure range/direction/color. Add packaged keyboard/focus and commit/cancel acceptance before Verified. |
| UX-08 | Keyboard-only operation, discoverability, and shortcut remapping | 🟡 Partial | P0 | F1, ?, and a named top-bar control open one searchable current-mode guide for the implemented fixed file/history/selection/navigation commands and shortcut-bearing active tools. The vertical ARIA toolbar has one roving tab stop with wrapping Up/Down and Home/End focus; the open-document tablist has one active tab stop with wrapping Left/Right, edge Home/End, automatic activation, a separate active close stop, and post-close replacement focus. All documents opens on the active item and has one wrapping Up/Down/Home/End route through document and batch commands; Export is a named non-modal dialog that focuses its first available control. Escape closes either popup without reaching background canvas handlers and returns its trigger. Tool buttons expose direct-key metadata, Tab reveals a Skip to canvas target, and global shortcuts stay inert behind modal dialogs or editable targets. Fixed shortcuts are not remappable, so the professional workspace baseline remains incomplete. Exit requires persistent shortcut remapping with conflict detection, reset/default recovery, discoverability that reflects the active mapping, complete focus order, keyboard alternatives for direct canvas drawing/tool settings/remaining menus, typeahead, 200% layout, and packaged native key-layout/focus/screen-reader acceptance. |
| UX-09 | Accessibility audit | 🟢 Working | P0 | A static regression gate covers named icon controls, canvas names, status live regions, visible focus, reduced motion, increased contrast, forced colors, roving keyboard/ARIA tab and All-documents menu semantics, a named Export dialog, non-nested document-tab close controls, and popup trigger restoration markers; actor identity also uses labels/icons rather than color alone. Screen-reader passes, complete focus order, reflow/legibility at 200% text, and packaged Windows high-contrast/color-vision acceptance remain. UX-01 owns the base control-size/density contract; this row must prove that its larger controls, customizable workspace, and progressive disclosure preserve names, order, focus, contrast, zoom, and assistive access. |
| UX-10 | Supported-window, high-DPI, tablet, multi-monitor, and touchpad QA | ⬜ Missing | P0 | The coordinate fix has unit coverage, but no professional input/display hardware matrix exists. Exit requires exact supported minimum/typical window-size behavior; 100%/200% and mixed-scale high-DPI coordinate/render checks; tablet pressure range, stroke continuity, and end-to-end latency measurements; touchpad pan/zoom/navigation behavior; and window moves across monitors with different scale/geometry without pointer drift, clipped critical UI, lost input, or corrupted canonical edits. Record devices, drivers, displays, package identity, timing method, thresholds, and failures; UX-01 owns visual density/workspace design rather than duplicating it here. |
| UX-11 | Batch save/export/close workflows | ✅ Verified | P1 | The all-tabs and native File menus expose Save All, collision-safe folder batch export, and consolidated Close All. PNG/JPEG/WebP plus sprite and keyframed-illustration GIF/APNG exports return per-document saved/exported/closed/skipped/failed reports; pixel outputs support integer presentation scale and named-tag selection. Injected orchestration tests cover cancellation, collisions, isolated save/export/close failures, and close aborts. Exact packaged acceptance passed a three-document Save All cancel/save, PNG export cancel/two-success-one-failure, and Close All cancel/save-and-close lifecycle with stable focus, authenticated MCP/renderer agreement, independently parsed revision-2 native files, exact profile-child targets, zero provider/cost exposure, and graceful credential-redacted cleanup. |

Evidence: [editor shell](../src/renderer/App.tsx), [styles](../src/renderer/styles.css), [error boundary](../src/renderer/RendererErrorBoundary.tsx), [packaged E2E](../tests/e2e/editor.spec.ts).

## 9. Testing, hardening, documentation, and release

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| QA-01 | Unit coverage for core schema/reducer/storage | ✅ Verified | P0 | Every one of the 41 canonical operation kinds has a bounded runtime payload schema and an exhaustive forward/inverse/redo scenario. Eight fixed 48-step illustration and pixel histories plus 32 seeded failing multi-operation transactions cover structural validity, sparse storage, atomic rollback, revision rebasing, and semantic restoration. Service regressions prove disjoint per-actor undo, non-crossable foreign/unrecorded overlap, safe redo invalidation, global document-revision barriers, and complete change feeds; the full persistence/migration suite remains green. |
| QA-02 | Golden-image rendering suite | 🟢 Working | P0 | A deterministic raw-RGBA SHA-256 corpus gates illustration blend/filter/mask compositing, canonical path/shape stroke opacity/cap/join/dash, an even-odd path-boolean ring/island, one hash-pinned missing-family Latin fallback across regular/bold/italic faces, hard-round and watercolor brushes, indexed sprites, orthogonal maps, isometric maps, all eight square-tile Tiled H/V/diagonal combinations, and a keyframed illustration midpoint. Expand to installed-font/shaping and broader-script behavior, all brush presets, broader boolean/compound geometry, isometric/non-square transform policy, and file round-trip goldens. |
| QA-03 | Interchange round-trip corpus | 🟢 Working | P0 | Generated GIF/APNG round trips assert timing, transparency, disposal, and source retention; animation-output resource controls additionally prove a scaled two-frame APNG rejects before rendering above 64 megapixels and a 16-frame exact GIF completes through sequential uncompressed intermediates. Generated two-frame local-palette controls prove exact bounded frame-local RGBA indexing/overrides, partial-alpha APNG/PNG/sprite-sheet output for the simple imported layer shape plus full- and fractional-opacity normal composites, exact opaque single-layer GIF colors, exact binary-alpha two-layer normal-composite GIF colors/local-table order through production re-import, and a warned default-palette fallback for one 256-color APNG frame. Spec-derived 8×8 Adam7 APNG controls add exact two-frame RGBA partial-rectangle and one-bit indexed/transparency import evidence, generated 2×1 16-bit grayscale/truecolor controls plus malformed variants prove full-sample transparent-key and fail-closed palette/index behavior, a separate 2×1 fallback/partial-frame control proves the two APNG default-image layouts and image-data ordering, and matched ancillary/critical/type-code variants prove exact ignore-versus-reject behavior, while sprite sheets, JSON/GPL palettes, and strict AIDraw brush-library JSON are covered. A frozen local JPEG orientation-6 fixture proves bounded big-/little-endian EXIF parsing, exact source retention, 24×16→16×24 illustration/pixel import, native reopen, and pixel-identical oriented PNG output; broader camera/vendor/color/metadata cases remain. Committed SVG fixtures exercise editable nested structure and fallback reports; generated AIDraw SVG controls additionally preserve centered/right edit boxes and one exact fractional image crop only when versioned metadata still matches standard geometry. The generated AIDraw PSD round trip covers an ordered empty group with 0.2 opacity/difference blend plus nested 0.8/0.6/0.4 group/layer/companion opacity, visible raster fallback, styled text, line height, exact box dimensions, and matrix-equivalent affine geometry while retaining one-toggle hidden-companion behavior. A separate pixel lifecycle preserves one current-frame group/empty-group/hidden-leaf tree through PSD export, one-sprite pixel import, and re-export, including supported visibility/opacity/blend/lock metadata and raw leaf pixels; a generated two-color crop additionally preserves decoded coordinates `(-1,2)` and `(0,2)`. Its exact shared-palette extension preserves custom `#123456` / `#abcdef` leaf RGBA through AIDraw export/import/re-export, and a generated 256-color PSD proves whole-import warned quantizer fallback. These are AIDraw/pinned-writer controls, not external-producer visual goldens or original PSD index-identity evidence. Hybrid PDF tests prove searchable text. A frozen LibreOffice Draw PDF plus independently rendered Poppler golden gates one production-utility clipped-transparency import at exact 241×151 geometry, opaque raster fallback, three importer-tagged hidden text runs, mean channel delta below 8/255, and fewer than 5% of channels over delta 32; fixture and golden identities are pinned. Its AIDraw PDF re-export preserves those exact three supported phrases as invisible searchable text, leaves same-named but untagged hidden text private, reports unsupported glyph loss, and re-imports within the same visual bounds. A separate full-artboard black-overlay case stays visibly black through production re-import while the three phrases remain extractable and the exact export report warns that coverage is not redaction. Tiled JSON/XML fixtures plus exact-package supported-surface acceptance perform first orthogonal TMJ+PNG export, production-utility second-document import, and distinct re-export while preserving map/tileset/properties, seven signed chunks, fourteen nonzero cells, the indexed source plane, and decoded RGBA under no-overwrite writes. This does not claim source editor map-name preservation, isometric behavior, unsupported modern fields, image-collection tilesets, transparent/below-threshold RGB preservation, complex-composite PNG/sheet fidelity, partial-final-alpha/over-255/non-normal/fractional-opacity or byte-identical GIF round trips, exact peak RSS, disk streaming, redaction intent/removal/security, external/general SVG/PSD private metadata semantics, external-producer opacity visuals, adjustment/effect editability, partial-lock preservation, or broad/full fidelity. Additional third-party PDF/PSD/animated/image-tileset cases and visual loss goldens remain. |
| QA-04 | MCP contract and concurrency suite | 🟢 Working | P0 | Auth, exact discovery payloads, all resources, duplicate operations, semantic project links, durable batches, checkpoints, observation, locks, cancellation, approvals, representative failure payloads, and the four-lane packaged visual scheduler are covered. Persisted trust and port controls now accept only exact writer-shaped state; a production restart regression proves malformed trust grants nothing, valid trust remains effective, and later corruption clears rather than retains authority. A concurrent 33-initialize boundary now proves exactly 32 retained sessions, retryable 429 backpressure, authenticated DELETE retirement of presence/session state, stale-ID rejection, and immediate replacement capacity; a separate boundary proves 128 distinct subscriptions, idempotent duplicates, rejection of the 129th, and unsubscribe capacity release. Exact-package raw-MCP acceptance proves a tools-only cold client learns join/list/observe/mutate plus human-approval job/wait/cancel without repository context or private job leakage, then optionally discovers and reads `aidraw://guide`. No idle-eviction contract is claimed. Systematically exercise every remaining tool action/limit combination before Verified. |
| QA-05 | Generation provider mock suite | 🟢 Working | P0 | Shared capability tests plus the actual utility runner cover OpenAI create/edit/inpaint/variation/true padded outpaint, mask validation, rate-limit/cancel, and ambiguous no-retry failure; Stability create/inpaint/outpaint/rate-limit/cancel/partial failure; Comfy workflow mapping/progress/reconnect/history/multi-output/queue cancellation; graceful worker cancellation/timeouts; and malformed images. Manager integration covers moderation/permission classification, partial completion, indexed provenance, terminal-state stability, and undoable outpaint expansion. Live compatibility and broader provider error payload matrices remain. |
| QA-06 | Complete packaged editor E2E matrix | 🟡 Partial | P0 | Core regressions and human+agent/headless scenarios now include exact two-document background-tab collaboration, a no-charge authenticated file-approval lifecycle, animated-sprite and finite-map Wang lifecycles, deterministic no-network generated fill, single-frame generated-image palette conversion, the three-document Save All / bounded export / Close All lifecycle, an orthogonal signed sparse infinite-map lifecycle, and a supported orthogonal TMJ+PNG export/import/re-export lifecycle. The Tiled run passed with three exact actor-bound allow-once approvals, two distinct documents, renderer/authenticated-MCP equality at all stages, exclusive two-pair bounds, equal supported semantics and decoded pixels, public-summary privacy, zero provider/paid requests, and graceful credential cleanup. Its utility-decoder product failure and source-only map-name observer failure remain immutable history. Broader conflicts, isometric behavior, unsupported modern fields, image-collection tilesets, broad/full-fidelity interchange, multi-frame conversion policy, and palette-file interchange remain incomplete, so QA-06 stays Partial. |
| QA-07 | Automated performance gate | 🟢 Working | P0 | The repeatable Node-24 Windows gate records build/machine identity and fails budgets for 5,000 vectors; separate requested pixels from a filled star, a complete materialized paint cache, and an integer-phase embedded PNG on 8,192×8,192 illustrations; a one-pixel regional render across 100,000 exact-subset illustration objects; exact canonical lasso selection across those 5,000 objects under 1,000 ms; four populated editable and materialized-sparse 4K paint layers; a 65,536-tile view; one requested pixel across 4,096 stored map chunks; one requested pixel across 4,096 stored sprite-cel chunks; a 100,000-map-object regional filter; viewport filtering across 1,048,576 compact-overlay cells; a 524,288-square checker viewport plus exact reduction across 1,000,001 straight samples and incremental validation to 4,096 lasso vertices; exact normal compositing across two one-million-pixel sprite layers; exact GIF indexing across one million final binary-alpha normal-composite pixels; complete sequential exact-GIF export across 16 million final pixels; native save, PNG export, one-million playback samples, a production-cel one-million-cell compact flood fill, and RSS growth. Apple Silicon reproduced 1,378.50 MiB native-allocation growth; bounded scratch/tile reuse plus scenario-output release reduced five fresh runs to 744.98, 753.14, 737.78, 736.13, and 744.58 MiB, all below the unchanged 1,200 MiB budget with every timing below threshold. The latest straight-primitive, complete-materialized-paint, embedded-PNG, and 100,000-object regional renders pass at 0.39, 0.47, 0.10, and 95.33 ms under their 100/100/100/500 ms budgets; the exact normal composite, one-frame GIF index, and 16-million-pixel GIF export pass at 25.58, 28.49, and 950.53 ms, with 978.16 MiB completed-operation RSS inside the unchanged 1,200 MiB aggregate gate. Exact instantaneous peak memory and disk streaming remain unclaimed. Exact-package acceptance covers four-lane isolation and bounded human-gesture dispatch; broader packaged input latency, frame pacing, display scaling, and tablet measurements remain. |
| QA-08 | Corrupted/untrusted-input hardening | 🟢 Working | P0 | Ordinary Electron/OS quit now prevents exit until one coalesced engine stop completes; failure remains diagnosed and retryable, including final-recovery failure, rather than allowing early exit or false stopped state. Start-at-login mutations now serialize in-process, and Linux XDG enablement preserves prior state until complete private flushed atomic replacement while failure cleans temporary state and leaves retry ordering usable. Automatic agent-client config updates now serialize in-process, preserve each observed version in a distinct private backup, publish through complete flushed atomic replacement, recover their queue after failure, and retain truthful configured state when the separate login-start action fails. The explicit plaintext MCP connection handoff now reaches its named destination only through a complete private flushed atomic replacement; failure preserves prior bytes and cleans temporary state. The MCP bearer-token store now serializes same-instance reads/rotations, admits only its exact encrypted writer/decrypted-token shape, and returns a rotated credential only after private flushed atomic replacement; failure preserves prior bytes and queue retryability. Native saves now isolate archive/checkpoint/trace/preview inputs to one invocation revision, serialize per document, retain later edits as dirty recovery state, and ignore stale close/replacement completions. Reusable-preset and interchange-report requests now serialize per runtime, install copied state only after a unique exclusive private flushed atomic replacement, and retain prior disk/live state with best-effort temporary cleanup on failure. The private durable-batch ledger now admits only exact version-one writer roots/records, discards malformed records, clears stale resumability on an invalid root, and installs copied state only after private flushed atomic replacement. Application-owned MCP trust/port state now has strict writer-shape parsing, all-or-nothing authority installation, stale-state clearing, and atomic private replacement. Native ZIP, APNG, GIF, inline raster, palette, brush-library, bitmap-font, sprite-sheet, SVG/XML, PSD-header, PDF, and Tiled paths now have explicit byte/dimension/count/expansion limits and malicious regressions. Editable GIF patches additionally require exact LZW termination/pixel count, valid dictionary codes, an active palette, and in-range color/transparency indexes instead of accepting synthetic zero/black pixels. Import-created raster assets additionally reuse the canonical 1,500,000-byte inline ceiling before base64 construction; addressed raster/animation and sprite-sheet sources enforce it before parser/native decode, while corrupt Tiled companion warning/omission remains unchanged. Pixel APNG export additionally evaluates scaled width × height × selected-frame count against a 64MP retained-frame budget before rendering. SVG transform admission additionally rejects non-finite numeric tokens, intermediate matrix overflow, non-finite decomposition/positioning/mask traversal, and referenced-gradient endpoints before returning canonical state, while ignored metadata/unsupported elements retain prior omission behavior. Completed SVG documents now reuse the strict illustration validator; six schema-limit families and a production file replay fail whole-import with one exact error, while bounded standard colors/one-stop gradients/reverse lines/names are normalized without inventing a parallel schema. Strict version-1 brush-library JSON rejects unknown fields, duplicate or built-in-reserved IDs, invalid complete dab recipes, empty/over-256 libraries, and UTF-8 input above 1 MiB before planning. Native ZIP open additionally validates central-directory structure and every expanded entry against its stored CRC-32; a stored-checksum-only tamper fixture is rejected. Native manifest parsing now fails closed on malformed format/schema/metadata and requires the writer-defined schema, document identity/kind/name, revision, timestamps, and generic asset count to match `document.json`; matched schema-1 pairs still migrate, and missing-payload/checkpoint recovery is unchanged. Generic native asset metadata is shape-checked, inline document bytes are discarded, and content-addressed entries hydrate only on exact declared-length/SHA-256 agreement; missing or corrupt payloads preserve metadata with an explicit warning, while invalid metadata rejects. Production native open/save, live inline admission, CLI native reads, utility-imported embedded images, and data-bearing recovery assets now additionally require claimed MIME/header/display geometry plus a real codec decode in the supervised raster worker; a decoder exit cannot terminate the main process, active work rejects, and queued work restarts fresh. Native open/CLI and recovery retain metadata but omit undecodable data with a warning, while save rejects before preview rendering or filesystem mutation and utility import returns no document. Metadata-only recovery records retain their existing boundary. Native and local durable-trace ingestion now rejects malformed/cross-document/over-2-MiB records before inspection or replay, aggregates tiny-line floods into one warning, preserves valid order without truncation, and batches accepted native import; save rejects an invalid supplied trace before replacement. Canonical admission also returns an explicit conflict for schema-shaped non-JSON values before size accounting or mutation. Selected checkpoint summaries and payloads now require the exact writer metadata and must agree with one another plus the migrated checkpoint document's identity/revision; duplicate IDs and malformed entries stay isolated from valid checkpoints and the main artwork, while save rejects invalid, inconsistent, or duplicate supplied checkpoints before replacement. Persisted activity and provenance now require their schema-valid exact writer records plus unique IDs before native, recovery, or utility-imported documents reach activity, renderer-attribution, or asset-reference consumers; missing asset references remain recoverable. Persisted illustration artboard/layer/stroke/object/preference/animation fields now reuse their live-operation schemas with unique ordering/member IDs and record-key identity before renderer/export use; missing references and valid unreferenced records remain recoverable. Persisted pixel documents now retain their established recovery normalization for incomplete frame/cel metadata, key aliases, absent preferences, and missing targets, but the normalized canonical result must satisfy strict palette/library/link/conversion, sprite/frame/cel/chunk, tileset/tile/collision/Wang, and tilemap/layer/object/chunk schemas; duplicate raw entity identities reject before overwrite, and new pixel asset/library operations share the nested contracts. Canonical chunk key/origin/base64/decoded-length checks and tileset limits reuse existing writer/import geometry and Tiled ceilings. Canonical sprite-cel bytes and reusable-stamp indices must stay within the current palette, and frame overrides must match its length and ID order across persisted and live ingress; missing cross-target and broader cross-record reconciliation remain open. A tiny spec-derived 8×8 RGBA Adam7 fixture now imports through the production utility-safe static-PNG path with exact header/decoder geometry; a deterministic decoder-boundary mismatch fails closed, while interlaced 8,193×1 and 4,097×4,097 headers hit the axis/16MP limits before any decode call. Metadata-driven sprite-sheet frame geometry and its 64MP expanded-output budget are now evaluated before reading or decoding the Adam7 companion. The editable-frame APNG decoder now separately reconstructs exact generated 8×8 RGBA and one-bit indexed Adam7 controls under its unchanged frame/expanded-pixel limits; a contradictory noninterlaced row layout rejects before canonical construction. A locally generated 52-byte 2×2 RGB PSD now exercises the standalone utility decoder under a 320 MiB image-data budget derived from the existing 64MP-layer plus 16MP-composite ceilings. Truncated or hostile color-mode, image-resource, and layer/mask section lengths reject before decoder entry; decoded canvas/header and RGBA byte geometry must agree exactly. A writer-produced 246-byte one-layer PSD also imports exactly; changing only its inner record's 48-byte extra-data length to extend one byte past EOF deliberately reaches `ag-psd` under the 320 MiB cap, then rejects before channel decode or canonical construction with no result. Its first 12-byte channel declaration also has a same-size hostile twin declaring 100 MiB plus three bytes; the pinned decoder rejects the resulting 100 MiB-plus-one payload read before fallback allocation or decode, while the 320 MiB budget remains scoped to decoded image data and no AIDraw serialized-byte ceiling is inferred. A one-byte field mutation to 75 bytes separately ends that channel exactly one byte past EOF; the pinned short-read path pads the missing byte, then rejects the next channel header before image decode or canonical construction with no result. A separate 12→13 in-file overlap consumes the next channel's first compression byte; the pinned realignment path reaches bounded ZIP-with-prediction decode, propagates the original `incorrect header check` string, and returns no canonical result. The PDF utility path now covers a valid local page, truncation, hostile unterminated container, and fixed-size missing-root, missing-page-kid, and missing-`endstream` mutations through real pdf.js load/page/render stages; its exact malformed-input importer replay passes 27/27. Encoded page Canvases reset immediately, processed page proxies clean before the next render, preflight-only proxies are swept on failure, and loading tasks are always destroyed; neither page nor task cleanup rejection masks a primary malformed-input error, while cleanup-only failure still fails closed. The 256-page ceiling rejects before page access, and all page viewports must satisfy the existing 8,192-axis/16MP per-page and 64MP cumulative budgets before any page render begins. pdf.js remains the PDF syntax/recovery authority, so recoverable malformed forms are not newly rejected. A fixed-date real Flate `/ObjStm` control imports, while exact one-byte unknown-filter and invalid-zlib-header twins both reject during pdf.js document load before page/render and destroy their loading tasks; production remains unchanged. A separate 3×2 RGB Flate `/DecodeParms` control imports; one-byte predictor `15→16` corruption reaches render and rejects with no result, while one-byte columns `3→0` corruption follows pdf.js's bounded render recovery, and every loading task is destroyed without stricter AIDraw policy. A real `/ASCIIHexDecode`→`/FlateDecode` image control also imports; reversed filter order follows bounded render recovery, while a two-byte early ASCIIHex end-marker mutation rejects during render with no result, and every loading task is destroyed without an AIDraw filter allowlist. A separate `/ASCII85Decode`→`/FlateDecode` control imports, while moving its `~>` terminator after the first two encoded characters produces a same-size four-byte mutation that reaches render, rejects with no result, and still destroys its loading task without stricter AIDraw syntax policy. Generated TMJ and inline TMX maps each prove root depth 0 through depth 64 produces the exact 64-group/one-tile canonical parent-child chain and retained GID. Placing either tile leaf at depth 65 rejects before deliberately hostile geometry/data is validated or expanded and before any canonical document/layer is constructed; the TMX case specifically reaches `xmlLayer` before generic normalization. A shallow TMJ with 4,095 empty object-layer siblings plus one marker tile imports with exactly 4,096 top-level canonical layers and GID 7; adding sibling 4,097 makes the global counter reject before that sibling's invalid geometry/base64 data or any canonical construction. A deterministic generated TMJ family additionally varies mixed root/group/object/tile ordering across depths 0–64, preserves exact canonical parent/child order and marker GIDs, derives three hostile depth-65 variants, and combines a nested/broad exact 4,096-layer boundary with a hostile 4,097th child that rejects before expansion or canonical construction. A separate generated inline-TMX family exercises mixed XML hierarchy at depths 0–64 under the established tile/object/group canonical category order, preserves parent links and GIDs, rejects three depth-65 variants before invalid CSV parsing, and combines a 64-level spine with the exact 4,096-layer boundary plus a deepest hostile 4,097th layer. The exact 16,777,216-cell aggregate boundary is now independently earned: one audited v2 Node run accepted four maximum layers as four canonical layers in 2,957.4787 ms at maxRSS 1,248,976 KiB and rejected a valid one-cell overage in 361.9904 ms at maxRSS 466,216 KiB with the established error and zero canonical layers. Its 179,583-byte retained tree is frozen at SHA-256 `A50BFB38…C2E3`; product source stayed exact and no child survived. The first v1 opt-in attempt remains a separate Vitest-startup failure and is not acceptance evidence. Native document migration now performs one iterative input-linear cycle check over illustration group-layer, illustration object-group, pixel-sprite group-layer, pixel-tilemap group-layer `childIds`: writer-produced acyclic archives open, while CRC-valid twins with one inner-to-outer back-edge reject before native open can return them to recursive consumers. The same internal graph kernel rejects normalized native sprite cel-link cycles while preserving absent-target recovery. Per-checkpoint migration isolates that validation: a two-cel cycle confined to one CRC-valid checkpoint excludes only that checkpoint with the exact corrupt-checkpoint warning while the acyclic main document and checkpoint index metadata remain intact. Sprite and tilemap normalization preserve child-derived root membership, while sprite cel completion still sees every ordered layer; isometric and Tiled interchange behavior is unchanged. Metadata/Tiled companions are realpath-contained to the approved root; XML DTD/entities and compressed layer bombs are rejected. Add broader pdf.js recovery/additional-filter-chain/other-predictor/object-stream and other inner PSD record/resource families, other cyclic-reference fuzzing, broader property-based parser tests, and external-producer/color-profile/chunk/filter interlaced-APNG corpora. |
| QA-09 | Three-level independent Luna/high QA workflow | 🟡 Partial | P0 | Level 1 smoke, Level 2 regression, and Level 3 release-exhaustive requirements, report template, and independent-task rules are documented; every level mandates automated, isolated MCP, native Computer Use, and bidirectional cross-surface evidence. A separate deterministic preflight now verifies the exact clean commit/version, full report and artifact hashes, Level 3 result sections, automation/MCP/Computer Use/cross-surface result, defects, native matrix, and documentation reconciliation, while keeping Level 3 RC and stable-v1 tracker closure distinct. Earlier Level 1 and macOS Level 2 failures remain immutable. Final independent macOS run `20260809T154905Z-macos-level2` is strict PASS against launcher `8E1523B8…1BDE` / ASAR `1B2C8E40…909D`, with exact automation, two-actor cross-surface behavior, mixed-state restart, and cleanup. QA-09 remains Partial because no fresh Level 3 release-exhaustive manifest or run exists. |
| QA-10 | Exact-build isolated MCP/UI identity harness | 🟢 Working | P0 | Node-24 discovery, runtime enforcement, package artifact verification, isolated interactive/headless session launch/status/stop, coordinator checkpoints, persisted MCP client state, and an explicit unsandboxed Windows/macOS launch guard are implemented. Normal stop remains graceful-only; an explicit `--force-on-timeout` fallback verifies the live executable/hash/PID/profile/connection/loopback identity before terminating only the recorded PID and redacting credentials after exit. Packaged Playwright shares native artifact resolution, isolated profiles, readiness diagnostics, native modifiers, and graceful cleanup across **27 clean and 14 explicit retained routes**. FND-05's first crash run remains a graceful controller failure and corrected r2 its narrow crash PASS. Its alive-hang route requires correlated fresh package/profile/failure children, exact hashes, hardened package-security/fuse inspection, a private mode-0700/0600 list-reporter wrapper, strict target/process/error attribution, and graceful-only cleanup. Immutable alive-hang r1 failed pre-launch discovery; r2 never supplied an ACK-tracked input; r3 supplied ACK-pending F24 but kept Playwright debugger attachments alive and suppressed Chromium's unresponsive report; r4 detached every debugger but awaited a child command that never settled. The synchronized controller retains real one-selector `--list` discovery, disconnects only the public `connectOverCDP` transport before the ACK deadline, forbids protocol owner close/private fields, observes pending child commands without hanging, and reconnects only after the exact product marker. It creates no production hook. Immutable alive-hang r5 then passed the narrow macOS/arm64 current-package recovery route once with private evidence and graceful zero-survivor cleanup; it is non-reusable. UX-01 density r1 remains a graceful pre-assertion controller/evidence-security failure; its dedicated wrapper creates 0700 roots, rejects credential-capable reporters, uses list-only reporting with automatic artifacts off, and checks encrypted storage through generic non-reporting errors. Density r2 exposed the macOS content-height product seam. Secure density r3 proved corrected default geometry and cleanup, then exposed a controller-only focus race; corrected secure density r5 is the narrow density PASS. The fourteenth retained route preserves consumed window-chrome r1/r2/r3 as distinct pre-input controller failures and r4 as a later posted-input-only controller/evidence result: r1 disproved origin equality, r2 disproved decorated-frame size equality after exact owner/renderer/layer-zero-window admission, r3 exposed stale expected bounds without retaining the new action-time bounds, and r4 completed one exact-PID post but lacked exact-app readiness and renderer-delivery evidence before the menu stayed absent. Its corrected source model retains dynamic visible-frame containment, complete renderer hit-test neighborhoods, a bounded paired stability barrier, fresh paired action-ready geometry, no-prompt event-posting preflight, and exact-PID/current-native-bounds rechecks; it now also binds default-option activation to the exact regular owner PID, exact frontmost plus trusted renderer focus, and a test-only ordered trusted delivery record before the separate product outcome. Option-click standard zoom, private evidence, and graceful-only cleanup remain exact. It earns no native claim until one wholly fresh authorized run passes. Run retained paths independently/hosted before broader platform claims; none of this widens release status. |
| QA-11 | Native desktop OS matrix | 🟠 Scaffolded | P0 | Verify/package jobs target Windows, macOS, and Ubuntu; tag builds target Squirrel/DMG/DEB/RPM/ZIP with per-platform checksums. The release job fails if its declared platform/architecture disagrees with the native Node host. Apple Silicon evidence now covers locked setup, native raster loading, strict ad-hoc-signed ARM64 identity, verified DMG/ZIP artifacts, authenticated headless MCP, same-artifact Keychain restart, canonicalized representative native import, the performance budget, isolated install lifecycle, login-item toggling, and a strict independent Level 2 bidirectional MCP/Computer Use PASS. Clean hosted/clean-machine runs, old ad-hoc Keychain ACL migration, login-start suppression, Developer ID/notarization/Gatekeeper acceptance, Level 3, Intel, and Linux remain open, so no new platform is release-verified. |
| QA-12 | External technical-artist task benchmark | ⬜ Missing | P1 | No external technical-artist competitiveness study exists. Before using feature-count parity as evidence, run a documented task-based comparison with **8–12 external technical artists** on representative sprite construction, indexed palette work, cel/onion/tag animation, tileset slicing/properties/collisions, terrain/map authoring, and engine-ready package export. Compare AIDraw with the combined Aseprite + Tiled workflow and record per-task completion time, errors/recoveries, final package quality under a declared rubric, and manual export repair; retain anonymized raw observations, participant/tool experience, package identities, task order, failures, and limitations. P1 keeps this task validation paired with the selected core workflow, and its critical-path position requires a usable PIX/MAP candidate before execution rather than allowing an unfinished benchmark to preempt product depth. This validates workflow competitiveness and prioritization, not statistical market proof, universal artist preference, Aseprite file-format parity, or broad Tiled compatibility. |
| REL-01 | Clean Git history and reviewable baseline | 🟡 Partial | P0 | Root commit `73d0712` remains historical baseline context. The recovered **283-path** Layers 1–119 checkout has been separated into 119 ordered ordinary unsigned commits; later product-focus, lifecycle, density, package-generation, interoperability, credential, diagnostic, and authoring checkpoints are clean and live-synchronized through base `1d5af1f7290bb6ddf376556da8ec8100aa37eb05`. This bounded **eight-path** source/headless candidate adds only PIX-08 seamless sprite Wrap edit, its exact core/renderer regressions, and literal architecture/testing/changelog/tracker truth; it does not package, launch, post native input, create/reuse a profile/evidence root, or change canonical schema/export/AGT-11 behavior. Consumed native identities remain ignored, immutable, and non-reusable. Exact selected-tree audits preserve predecessor boundaries and stash `codex-pre-pull-macos-compatibility`. Packaged wrap interaction/visual proof, authored axes, setting persistence, animated-cycle export, broad parity, hosted/broader-platform evidence, and Level 3 remain open, so review or later ordinary synchronization of this checkpoint is not an RC or release. |
| REL-02 | Honest versioning and changelog | ✅ Verified | P0 | Root/core/lock metadata use `0.1.0-alpha.1`; a dated changelog and explicit prerelease/stable release checklist exist. An automated metadata test prevents a premature stable version or workspace-version drift. |
| REL-03 | Desktop CI | 🟠 Scaffolded | P0 | Verify/package jobs are matrixed across Windows/macOS/Ubuntu, Windows keeps the 27-case self-contained packaged suite with an explicit native-session declaration, and every CI/release job runs the same fail-closed complete dependency gate. The **14 explicit-profile cases** require explicit declarations; UX-01 window-chrome r1/r2/r3 are retained immutable coordinate/controller failures before input, while r4 is a distinct posted-input-only controller/evidence result without activation/delivery attribution. None is a native PASS, and any retry requires wholly fresh correlated roots. FND-05 and UX-01 evidence remains macOS/arm64-limited: FND-05 retains one crash-controller failure plus corrected crash-recovery r2 PASS; alive-hang r1/r2/r3/r4 are separately attributed immutable failures and r5 is the distinct one-run narrow current-package recovery PASS. UX-01 retains its earlier density r1/r2/r3 failures, narrow secure density r5 PASS, and separate consumed window-chrome r1/r2/r3/r4 results. Existing retained roots are immutable and require explicit declarations; none may be reused. Portability and host-label checks remain, and each clean hosted package job can use the default first generation, but no hosted run yet proves the new refusal policy, native-maker portability, the historical FND-05 renderer-loss trigger, broader-platform hang recovery, or UX-01's broader exits. Run retained paths independently/hosted and retain credential-safe artifacts/diagnostics before widening any claim. |
| REL-04 | Native packages, archives, checksums, license report | 🟢 Working | P0 | Windows Squirrel/ZIP and Apple Silicon DMG/ZIP are locally produced; Forge also defines Linux DEB/RPM/ZIP. Package verification requires the exact locked Electron runtime (43.4.0 at this checkpoint) and a deterministic single-use generation marker matching package version, target, and selected workspace-relative output. Before Forge Packager's forced overwrite can run, the supported package/make path atomically reserves a fresh output root; an existing file/directory/symlink, a concurrent reservation, or a consumed failed attempt is refused without deletion or process authority. Default `out` remains a usable first generation, while a fresh `AIDRAW_FORGE_OUT_DIR` gives exact run-owned/CI identities and is shared by verification, checksums, default license/provenance output, and packaged-E2E fallback. The unpatched appdmg chain is replaced by a bounded macOS-native `ditto`/`hdiutil` maker that adds the ordinary Applications link, verifies the image before returning, and cleans partial output; current Apple Silicon package/make, independent DMG checksum verification, ZIP integrity, artifact SHA-256 manifest, license report, ad-hoc designated requirement, fuses/security surface, and ARM64 binding pass locally. This policy prevents silent supported-workflow bundle-generation replacement; it does not inspect/restart live owners or define customer update semantics. Local system policy correctly rejects the ad-hoc, unstapled app. Clean-machine installation/uninstallation, current-image mounted-content inspection, Developer ID/hardened runtime/notarization/stapling/Gatekeeper acceptance, old-Keychain ACL migration, Intel macOS, Linux desktop integration, hosted/tagged verification, and reproducibility remain. |
| REL-05 | README, contributor guide, security policy, issue templates | 🟢 Working | P1 | Files and templates exist. Reconcile all claims with this tracker and add developer architecture/testing recipes. |
| REL-06 | MIT GitHub publication and tag-driven release | ⬜ Missing | P0 | Repository owner, remote, credentials, published tag, draft release, and public artifacts are not established. |
| REL-07 | Reproducibility and dependency/license review | 🟡 Partial | P0 | Lockfile, audit notes, checksum, license, and per-platform provenance scripts exist. The current registry snapshot reports zero vulnerabilities in both production and the explicitly included production/development/optional/peer complete graph, verifies 641 installed-package signatures plus 141 attestations, pins packaged Electron 43.4.0, keeps Forge 7.11.2 on compatible Packager 18.4.4, and substitutes only reviewed vulnerable build roots. The lock policy requires all 701 external non-link entries to use exact npm-registry tarballs with SHA-512 integrity and exempts only the two declared repository workspaces. Source-contract tests plus a real package/make prove that exact macOS pairing. The license report inventories the aliased extractor/rebuild graph and four inlined Liberation Sans asset hashes plus the complete OFL 1.1 notice. Capture still fails on dirty source, unsupported host identity, unpinned runtime/toolchain metadata, symlink/empty maker output, checksum drift, or missing Windows license reports, then writes without overwrite. Current release provenance is schema 2 because the single-use package generation is now required source identity; retained pre-policy schema-1 manifests remain historical and fail current comparison rather than being silently upgraded. The separate comparison report remains schema 1. Stable-v1 preflight requires hashed zero-difference Windows x64, macOS arm64, and Linux x64 reports plus separate dependency/license review. No independent native rebuild pair, hosted multi-platform security run, or deterministic exception review exists, so reproducibility and release certification remain unearned. |

Evidence: [test suite](../tests), [testing workflow](TESTING.md), [Luna/high prompt](testing/LUNA_HIGH_PROMPT.md), [QA session harness](../scripts/qa-session.mjs), [QA MCP client](../scripts/qa-mcp.mjs), [CI workflow](../.github/workflows/ci.yml), [release workflow](../.github/workflows/release.yml), [package scripts](../package.json), [dependency audit](DEPENDENCY_AUDIT.md).

## 10. Explicitly deferred from v1

These are not defects against the selected v1 unless the product scope changes:

- mobile, web hosting, 32-bit builds, and multiple native windows;
- accounts, telemetry, cloud sync, remote human multiplayer, and built-in assistant/chat;
- CMYK/16-bit workflows, ABR brushes, Aseprite round-trip, and lossless Photoshop/PDF round-trip;
- broad Photoshop/Illustrator replacement parity; maintain current illustration and interchange contracts, but direct new feature-depth work to the selected local 2D game-asset workflow unless a game-production or defect requirement needs otherwise;
- custom autotile scripting, automatic updates, provider accounts/model downloads, and code signing.

## Recommended critical path

**Binding interoperability order before resuming the numbered product path:** preserve the raw AGT-01/AGT-03 evidence and keep AGT-14 Working/P0. First contain the Windows incident at **P0** by preserving any unsaved artwork, confirming the historical OpenCode session has ended, executing the supported MCP-token rotation/revocation path on the affected runtime, refreshing both profiles, performing only separately authorized cleanup/history decisions, and rejoining both actors. Next complete **P1** compatibility and exact installed-client acceptance: the flat discovery/protocol source checkpoint is necessary but OpenCode 1.18.18 native nonempty actions, Antigravity 2.8.1 restart/404 recovery, stable dual-client attribution, bearer-boundary scanning, and graceful cleanup remain unproved. The lower-priority **P2** source/wire diagnostic and support contract is now implemented; preserve it during P0/P1 execution rather than treating it as installed-client or incident-closure evidence. This incident is separate from FND-05's older default-profile blank-window observation and UX-01's too-small/dense controls; neither product finding is evidence that installed MCP clients pass or fail.

1. **Resolve observed customer-facing defects before parity work:** preserve FND-05's successful fresh-profile exact-package crash/replacement/close-show/relaunch result and consumed alive-hang r5 macOS/arm64 input-ACK recovery result as two narrow, distinct passes; preserve alive-hang r1/r2/r3/r4 as four distinct consumed failures and never rerun any retained identity. Read-only forensics now establish the older owner/default-profile/no-renderer process shape and its unlinked old bundle mappings after pathname replacement; the supported package path separately refuses silent same-root generation replacement. Current source also gives the main process a user-confirmed **View → Recover Editor Window…** fallback for a visibly blank or opaque shell that evades automatic failure signals; confirmation is bound to the exact current shell so a concurrently attached successor is shown rather than replaced, while an unchanged shell is retired before one replacement under the existing retry limit. The prompt explicitly excludes renderer-local unsubmitted input. Treat this as source/headless product behavior pending packaged/native use, not as the historical trigger or repair. Do not treat the forensic or manual-recovery evidence as exact old-byte identity, cache/profile repair, updater/restart behavior, proof the fresh isolated launch fails, a renderer-local-work promise, broader-platform evidence, or a reason to weaken FND-07/UX-05. Preserve immutable UX-01 r5 as the one-run narrow PASS for exact supported content geometry, compact disclosure, synchronized long-document pointer/keyboard routes, sampled controls/workflows, timeline separation, screenshots, zero observed renderer requests, and graceful cleanup. Preserve native window-chrome r1/r2/r3 as three separate pre-input controller failures and r4 as a distinct posted-input-only controller/evidence result: r1 disproved origin equality, r2 disproved decorated-frame size equality, r3 exposed stale expected bounds without proving why the window moved, and r4 completed one exact-PID post but lacked exact-app readiness and trusted renderer-delivery attribution before the menu stayed absent. The next bounded UX-01 exit retains the dynamic contained-frame, complete-hit-neighborhood, stable paired-observation, fresh action-snapshot, and exact current-bounds route while adding exact-owner default-option activation, frontmost/focused readiness, and a test-only trusted event-delivery record that cannot satisfy the separate product-menu assertion. It remains unearned until a separately authorized wholly fresh exact package executes it once; do not rerun any consumed identity or promote density r5 into that, accessibility, hardware/display, customizable-workspace, broader polish, RC, or stable-v1 evidence. FND-05 and UX-01 both remain Working for their respective open criteria.
2. **Complete the professional input/workspace baseline:** add persistent customizable workspaces and progressive disclosure (UX-01), shortcut remapping and complete keyboard routes (UX-08), assistive/200%-text acceptance (UX-09), and measured high-DPI, tablet pressure/latency, touchpad navigation, and mixed-scale multi-monitor behavior (UX-10). Do not substitute headless size/coordinate tests for packaged visual, assistive, or hardware evidence.
3. **Deepen the selected local 2D game-asset studio:** close the practical PIX/MAP exit criteria for sprites, indexed palettes, cels/onion/tags, pixel animation, tilesets, collisions, Wang terrain, finite/infinite maps, project links, engine-facing interchange, and the corresponding AGT-11 semantic workflows until representative work can compete with the combined Aseprite + Tiled workflow. Preserve local auditability and exact canonical transactions. This is not Aseprite round-trip, broad Tiled compatibility, or Photoshop/Illustrator replacement parity.
4. **Validate tasks with artists, not feature counts:** execute QA-12 with 8–12 external technical artists and compare completion time, errors/recovery, final package quality, and manual export repair against Aseprite + Tiled. Feed observed failure clusters back into steps 1–3 without inflating row status.
5. **Keep release trust as a real gate:** complete the packaged regression/native matrix, hosted CI, clean-machine package/archive checks, security and dependency review, reproducibility, exact documentation reconciliation, and independent Level 3 evidence in QA-06, QA-09, QA-10, QA-11, REL-01, REL-03, REL-04, REL-06, and REL-07 before any RC, signing, tag, publication, or release claim.
6. **Then strengthen the game-production moat:** implement AGT-17's auditable multi-asset briefs, coherent character/style propagation, deterministic sprite/map package validation, engine-ready export profiles, and agent repair/revalidation of failed production checks by composing existing semantic and file-authority contracts rather than bypassing them.
7. **Stop at the selected boundary:** broad Photoshop/Illustrator parity, remote service tenancy/multiplayer, and mobile/web products remain separate programs rather than hidden prerequisites for the local agent-native 2D game-asset studio.

## Maintenance rule

Every feature change should update this file in the same change set. Record the automated acceptance that justifies **Verified**; if a regression invalidates an exit criterion, move the item back immediately. Design documents may remain aspirational, but this tracker must remain literal.
