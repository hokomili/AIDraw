# AIDraw feature tracker

Last audited: 2026-08-04

This is the source of truth for implementation status. The README and design documents describe the intended v1 product; an item is not complete merely because its data type, dependency, or menu entry exists.

## Status rules

| Status | Meaning |
| --- | --- |
| ✅ Verified | The workflow is implemented and covered by an automated acceptance, contract, or regression test. |
| 🟢 Working | The workflow is usable in the current editor, but its acceptance coverage is incomplete. |
| 🟡 Partial | A useful subset works, but a material part of the promised workflow or fidelity is absent. |
| 🟠 Scaffolded | The model or interchange path exists, but human authoring is too thin to call the feature usable. |
| ⬜ Missing | No meaningful implementation exists yet. |
| ⏸ Deferred | Explicitly outside the selected v1 scope. |

Priorities are `P0` release blocker, `P1` core v1 workflow, `P2` completeness/fidelity, and `P3` polish. A feature may move to **Verified** only when its exit criteria have an automated test.

## Current release truth

- AIDraw is a capable **pre-v1 prototype**, not a completed v1 release. Workspace metadata now honestly identifies `0.1.0-alpha.1`; stable `1.0.0` remains gated by this tracker and the release checklist.
- The strongest areas are the application-owned document/reducer model, native ZIP persistence, indexed sprite storage, headless authenticated MCP, attributed trace/replay, basic illustration drawing, basic pixel animation, and presentation-scaled pixel export.
- The largest product gaps are packaged pointer/frame-pacing and hardware proof, deeper natural-media simulation, advanced third-party SVG/PDF/PSD fidelity, live provider compatibility, broader external interchange fixtures, and release evidence.
- The current fast verification baseline passes **79 Vitest files / 326 tests**, plus clean TypeScript and ESLint. The current Windows package also passes all **13 packaged Playwright scenarios**; that matrix remains separate from `npm run verify`, and native Computer Use validation is intentionally deferred during this implementation pass.
- A clean reviewable source baseline now exists on `codex/pre-v1-critical-path`; generated packages, QA output, credentials, and user artwork are excluded. No remote CI run or published release exists yet, so local artifacts remain non-release proof.

### Audit snapshot

| Measure | Count |
| --- | ---: |
| Tracked items | 144 |
| Verified | 40 |
| Working | 92 |
| Partial | 7 |
| Scaffolded | 2 |
| Missing | 2 |
| Deferred | 1 |
| P0 / P1 / P2 | 57 / 73 / 14 |

Requirements added during hands-on testing are included explicitly: autonomous headless work and trace replay (FND-04, AGT-10), model/effort attribution (AGT-16), natural-media and custom brushes (ILL-07/08), polished material construction (ILL-19), illustration animation (ILL-21), presentation upscaling and CLI export (PIX-15), large-run save/export/close UX (UX-11), and independent three-level Luna/high MCP plus Computer Use QA (QA-09/10).

## 1. Foundation, process model, and storage

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| FND-01 | Node 24 npm workspace, Electron/React/TypeScript shell | 🟢 Working | P0 | Workspace and Forge/Vite build exist. Packaging/formal-QA runtime preflight now rejects non-24 Node and post-package verification rejects a missing executable after a misleading Forge exit. Verify clean-clone `npm ci`, package, and launch in CI. |
| FND-02 | Sandboxed renderer and narrow IPC boundary | 🟢 Working | P0 | Context isolation, sandboxing, sender checks, CSP, denied navigation/permissions, and fuses are implemented. Add automated negative security tests. |
| FND-03 | Application-owned schemas, migrations, reducer, inverses | ✅ Verified | P0 | Schema rejection, migration, atomic reducer inverses, stale revisions, layer grouping, and revision-checked cross-layer/object-order moves are tested. |
| FND-04 | Canonical editor-independent engine | ✅ Verified | P0 | Packaged E2E covers authenticated drawing and durable replay with no editor window. |
| FND-05 | Single-instance attach/show lifecycle | ✅ Verified | P0 | Packaged Playwright now covers headless ownership → show/attach → close the editor while health/MCP remain live → show/reattach → explicit engine quit. Keep the isolated QA-harness scenario as a release regression. |
| FND-06 | Native `.aidraw` ZIP, assets, preview, atomic save | ✅ Verified | P0 | Temporary ZIP validation, editable named-checkpoint persistence, content-addressed checkpoint assets, schema-1 container migration, unsafe-path rejection, per-entry and total expansion budgets, and round-trip are tested. Add interrupted-write fault injection and CRC-specific fixtures. |
| FND-07 | Recovery journal, compaction, discard semantics | ✅ Verified | P0 | Snapshot replay, malformed trailing transaction protection, and explicit-discard cleanup are tested. Keep the prior resurrected-document bug as a packaged regression. |
| FND-08 | Durable attributed transaction trace | ✅ Verified | P0 | Trace survives compaction and drives non-mutating replay. |
| FND-09 | Utility-process isolation for conversion/render/generation | 🟢 Working | P0 | Image quantization, import/conversion, document export, MCP observation compositing, generation providers, comparison rendering, and accepted-result quantization run through supervised single-lane Electron utility workers with timeout, AbortSignal cancellation, crash rejection/restart, progress relay, and clean shutdown. Generation has a separate lane; cancellation first lets the provider remove/interrupt remote work, then replaces the worker before queued work starts. Supervisor/provider contracts pass; add packaged crash/cancel/resource-pressure acceptance. |
| FND-10 | Application-owned rendering adapters | 🟢 Working | P1 | v1 deliberately uses shared custom Canvas2D renderers over the application-owned model; Paper.js remains a narrow geometry kernel and `perfect-freehand` a stroke-outline kernel. Unused Fabric.js/PixiJS/tilemap runtime dependencies were removed. Deterministic cross-mode goldens and the non-GUI vector/4K-paint/large-map scale gate pass; packaged pointer/frame-pacing evidence remains. |
| FND-11 | Versioned format migrations beyond schema 1 | ✅ Verified | P1 | Canonical schema 2 adds illustration animation. Unversioned/schema-1 illustration and pixel fixtures migrate to schema 2, native schema-1 ZIP containers reopen, and newer unsupported versions fail explicitly. |
| FND-12 | Performance instrumentation and budgets | 🟢 Working | P0 | `npm run test:performance` records machine-readable Windows evidence and gates 5,000 vectors, four populated editable and materialized-sparse 4K paint layers, 65,536 visible map tiles, native save, PNG export, one-million-sample playback accounting, and RSS growth. Packaged pointer latency, frame pacing, four visible lanes, 200% scaling, and tablet latency remain for Level 3 Computer Use. |
| FND-13 | Cross-OS desktop runtime services | 🟢 Working | P0 | Platform-neutral engine status, Windows/macOS/Linux secure-storage policy, macOS login-launch suppression, Windows login items, and XDG headless autostart are implemented and unit-tested. Linux refuses Electron `basic_text`. Windows remains the only locally packaged/interactive proof; pass native macOS/Linux CI and Computer Use before promotion. |

Evidence: [core model](../packages/core/src/model.ts), [operations](../packages/core/src/operations.ts), [document service](../src/main/document-service.ts), [engine runtime](../src/main/engine-runtime.ts), [persistence](../src/main/persistence.ts), [journal tests](../tests/main/journal.test.ts), [packaged E2E](../tests/e2e/editor.spec.ts).

## 2. Agent-native MCP and collaboration

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| AGT-01 | Authenticated stateful localhost MCP | ✅ Verified | P0 | Bearer auth, stateful sessions, health endpoint, persistent port range, and 32 concurrent clients are contract-tested. |
| AGT-02 | Public resources and subscriptions | ✅ Verified | P1 | Documents, manifest, snapshot, changes, and trace resources are contract-read; a negotiated SSE subscription receives `notifications/resources/updated` after commit and the matching changes resource returns the committed transaction. |
| AGT-03 | Public tool surface | ✅ Verified | P0 | All nine named tools are registered and schema-discoverable. This verifies presence, not complete semantics of every action. |
| AGT-04 | Headless bootstrap and launch-time folder trust | ✅ Verified | P0 | Explicit connection handoff and process-lifetime trusted folders are exercised in packaged E2E. |
| AGT-05 | Presence, named cursors, activity, background-tab badges | 🟢 Working | P1 | Presence and actor cursors render; activity and tab indicators exist. Add two-document background-edit and cursor-position E2E coverage. |
| AGT-06 | Fair four-lane playback and backpressure | 🟢 Working | P0 | Scheduler integration tests prove all four lane IDs, next-lane cross-actor fairness, queue cleanup, and the one-million-sample busy gate. Add one packaged four-agent visual-lane scenario before graduating to Verified. |
| AGT-07 | Human locks and optimistic human priority | 🟢 Working | P0 | Automated service tests cover same-object replace/delete, overlapping pixel regions, unrelated work, and human bypass priority; packaged human drawing during agent playback also passes. Add a packaged pointer-down-held conflict for object and region locks before graduating to Verified. |
| AGT-08 | Partial cancellation and per-actor undo | ✅ Verified | P0 | Unit integration retains a visible pixel prefix as one undoable/redoable actor transaction, and packaged Playwright covers Stop All Agents plus Activity-panel agent undo/redo without removing the human stroke. |
| AGT-09 | Idempotency and revision conflicts | ✅ Verified | P0 | Duplicate operation IDs and stale entity revisions are covered. Add conflict payload/retry-guidance contract assertions. |
| AGT-10 | Durable autonomous trace inspection and human-triggered replay | ✅ Verified | P0 | Agents can inspect complete traces, and headless transactions remain replayable from Activity after an editor attaches. Agent-triggered replay is separately tracked as PAR-05. |
| AGT-11 | Rich semantic canvas operation families | 🟢 Working | P1 | MCP expands validated pixel fill/replace/adjust/dither/quantize/selection-scale, animation/stamp/tilemap/map-object/per-tile-collision, Wang-authoring, deterministic tile variants, and project-link embed/pack operations plus illustration align/distribute/material/gradient/styled-text/image-crop/object/layer-filter/mask/boolean, document fragments, and Bézier inspect/move-handle/insert/delete/convert/open-close/arc-convert/split/join into canonical revision-checked operations. Approved file tools also relink/extract project sources. Richer cross-asset graph cascades remain. |
| AGT-12 | Approval jobs and folder authority | ✅ Verified | P0 | Structured review shows actor, exact path/provider/prompt, scale/count, paid-request exposure, overwrite targets, and expiry. Packaged UI exposes Deny/once/session/persistent choices; contract tests prove session trust, persistent trust across host restart, and overwrite re-approval. |
| AGT-13 | Job inspect/wait/cancel/approval dependency | ✅ Verified | P1 | Owner-scoped list, inspect, bounded wait, and cancel are contract-tested across sessions without leaking private payloads. `approve-dependent` reports the human-only boundary; durable batch start/resume/progress/cancel also rebinds only with an opaque resume token. |
| AGT-14 | First-class Codex/Claude Code/OpenCode/Antigravity onboarding | 🟢 Working | P0 | Consent-driven profiles back up configuration and replace only the AIDraw entry using each client’s documented Streamable HTTP schema; OpenCode JSONC comments and sibling settings are preserved. Unit tests cover every writer and the MCP contract initializes under all four client identities. Add live installed-client restart/discovery acceptance and persistent connection health. |
| AGT-15 | Generic MCP-client onboarding | 🟢 Working | P2 | The UI exposes/copies a client-neutral Streamable HTTP JSON profile with bearer header, and docs explain the shared contract. Add token rotation/revocation plus one independent third-party client acceptance. |
| AGT-16 | Agent model/effort/task provenance | 🟢 Working | P2 | A joined session may declare validated model, reasoning effort, and task ID metadata; it persists into actors/activity and the UI formats missing fields without `undefined`. This is client-declared provenance, not platform-attested model identity or proof that an external Codex task has completed. |

Evidence: [MCP host](../src/main/mcp-host.ts), [scheduler](../src/main/playback-scheduler.ts), [MCP contract tests](../tests/mcp/contract.test.ts), [trace tests](../tests/main/trace-store.test.ts), [replay tests](../tests/renderer/replay.test.ts), [human/agent parity audit](PARITY_AUDIT.md).

### 2A. Human/agent parity and autonomous iteration

The shared reducer gives agents access to every low-level operation kind, but low-level write access is not the same as practical parity. These items reconcile the dedicated [human/agent parity audit](PARITY_AUDIT.md) into the release backlog.

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| PAR-01 | New-document option parity | ✅ Verified | P1 | Human IPC and MCP use one strict creation schema for kind/name/dimensions/background and tilemap orientation, finite/infinite mode, and tile geometry. A contract test compares the resulting isometric documents. |
| PAR-02 | Safe document-fragment exchange for agents | ✅ Verified | P2 | Clipboard and MCP share a versioned, self-contained 2 MiB fragment contract. Illustration relation/asset closure, pixel sprite→tileset→map dependencies, legacy clipboard upgrade, ID/reference remapping, palette-preserving import, server attribution, malformed input rejection, and live cross-document MCP exchange are tested without exposing the ambient clipboard to agents. |
| PAR-03 | Agent-callable image-to-indexed quantization | ✅ Verified | P1 | `pixel.image.quantize` converts an embedded raster asset into compact cel runs using document area/OKLab/dither/alpha defaults and records reproducible conversion metadata. Exact pixels, trace metadata, and actor undo are contract-tested. |
| PAR-04 | Human-presence and lock awareness | ✅ Verified | P1 | Session inspection and canvas observation expose coarse human object/region occupancy, advisory editor state, and retry guidance. A contract test proves an authenticated agent sees a direct human lock and receives a retryable conflict without gaining human-priority lock authority. |
| PAR-05 | Agent-callable trace replay | ✅ Verified | P2 | `history_manage:replay` starts one bounded, non-mutating transaction replay and reports missing/busy/already-replaying/too-large states. Contract coverage verifies document revision remains unchanged. |
| PAR-06 | Multi-granularity visual observation | ✅ Verified | P1 | `canvas_observe` targets current or named-checkpoint state plus asset/frame/layer/region with integer scale and document/transparent/solid background, validates exact targets, and enforces pre-render pixel and PNG-byte caps. It can return a race-free before/after pair for the most recent transaction under one combined pixel budget; exact cropped, paired RGBA, and checkpoint output are contract-tested. |
| PAR-07 | Explicit workspace and job observation | ✅ Verified | P1 | Document listing returns `activeDocumentId`; owner-scoped jobs can be listed/inspected/waited/cancelled through sanitized summaries that omit human prompts, results, and approval review data. Cross-session isolation is contract-tested. |
| PAR-08 | Advisory attached-editor state | 🟢 Working | P2 | While attached, the renderer publishes explicitly advisory document/tool/selection/zoom, visible world viewport, active pixel asset/frame/tag, onion-skin, playback, and direction through narrow IPC; it clears on detach and cannot feed back into canonical state. Sanitization/clamping and MCP exposure are contract-tested; add attached-renderer E2E before Verified. |
| PAR-09 | Bulk region operations and long-run throughput | ✅ Verified | P1 | Canonical RLE pixel/tile writes support one million cells, cross chunks, progressive reveal/cancel, locks, inverse/undo, and sample backpressure. `job_manage` starts/resumes/cancels opaque-token durable batches up to 10,000 numbered `canvas_apply` transactions with progress, sequence/idempotency checks, cross-session ownership rebinding, atomic state, and crash-window reconciliation against the durable trace; unit and live host-restart MCP contracts pass. |
| PAR-10 | Non-destructive branches, variants, and comparison | 🟢 Working | P2 | Humans and agents can create attributed editable document checkpoints, observe them headlessly, compare them side-by-side or overlaid, and restore safely. Checkpoint review can selectively merge up to 32 top-level illustration layer trees or pixel assets with dependency closure as one canonical undoable transaction; native/service/MCP contracts cover persistence and merge. Cheap in-place layer variants, recovery-journal checkpoint state, and packaged comparison acceptance remain. |
| SEC-01 | Server-authoritative entity attribution | ✅ Verified | P0 | Commit policy recursively normalizes new/replaced entity creator, timestamps, and revisions from the authenticated actor, renderer IPC cannot impersonate agents, and undo/redo restores exact normalized metadata. Service and MCP regressions cover the boundary. |
| SEC-02 | Asset deletion referential integrity | ✅ Verified | P0 | The reducer reports image, paint-tile, linked-preview, and provenance references; deletion is rejected until references are removed. An explicit ordered object/asset cascade and its exact inverse are tested. |
| SEC-03 | Trusted provenance creation | ✅ Verified | P0 | Generic human/agent canvas transactions cannot add or remove verified provenance. Generation acceptance uses the engine-only path, timestamps are server-issued, referenced assets must exist, and MCP forgery is contract-tested. |
| SEC-04 | Strict conversion-settings validation | ✅ Verified | P0 | Nested conversion settings are strict: area/OKLab, three supported dither modes, finite 0–1 alpha, and no unknown keys. Malformed enums, NaN, ranges, and extra fields are rejected by schema tests. |
| SEC-05 | Inline asset ingestion policy | ✅ Verified | P0 | Inline assets require canonical base64 safe-raster data with declared length/SHA/MIME agreement, header and codec decode, 1.5 MB, 8192 px, and 16 MP limits; dangerous formats and forged generated origin are rejected/normalized. The trust boundary is documented and tested. |

## 3. Illustration editor

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| ILL-01 | Bounded custom artboard, background, sRGB defaults | 🟢 Working | P1 | Dedicated creation and Artboard inspector UIs support dimensions, DPI, and solid/transparent background after creation. Reusable mode-specific document presets persist atomically. Revision-checked replace and translate/expand operations have strict bounds and exact editable-state undo; accepted outpaint uses the atomic form to shift content, guides, paint, and animation consistently. Add packaged preset and outpaint acceptance. |
| ILL-02 | Selection and lasso | 🟢 Working | P1 | Freeform pointer lassos support replace/add/subtract/intersect and containment modes; clicks use actual local fill/stroke paths where available and Alt cycles overlaps. Lasso intersection still uses rotated world bounds rather than exact curve/polygon intersection, and packaged pointer acceptance remains. |
| ILL-03 | Transform workflow | 🟢 Working | P1 | Single/multi-selection world bounds, drag translation, four scale handles, rotation handle, Shift-uniform scale, Shift-15° rotation, live previews, and one lock-safe revision-checked commit are implemented. Skew/pivot handles and explicit reset remain missing; pure geometry is covered but packaged pointer acceptance remains to add. |
| ILL-04 | Snapping and guides | 🟢 Working | P1 | Persistent colored/lockable guides can be added, edited, deleted, and dragged; shared snapping targets artboard, objects, guides, configurable grid, and integer pixels with a document-owned tolerance. Rulers/origin control and packaged pointer acceptance remain. |
| ILL-05 | Pressure pen and vector pencil | 🟢 Working | P1 | Pressure samples and `perfect-freehand` outlines render. Add real stylus-device acceptance for pressure, tilt policy, latency, and long strokes. |
| ILL-06 | Six essential raster brushes and eraser | 🟢 Working | P1 | Hard/soft round, pencil, marker, airbrush, and eraser use a deterministic pressure-aware dab engine with spacing, stabilization, hardness, flow, scatter, size/opacity jitter, angle, and roundness. Shared live/headless/save-tile rendering, destructive erasing, and a maintained hard-round golden are tested; add tablet latency and a broader preset corpus. |
| ILL-07 | Watercolor/natural-media painting | 🟢 Working | P1 | A bounded watercolor tip implements translucent accumulation, wet edge pooling, deterministic granulation, scatter, pressure, and stabilization through the shared renderer, with a deterministic rendering golden. Paper textures, pigment mixing/bloom simulation, and wet-canvas interaction remain. |
| ILL-08 | Custom brush engine and preset management | 🟢 Working | P1 | Named document-owned presets can be created, edited, selected, tuned across all dab dynamics, and deleted; each stroke embeds the complete recipe and seed for headless replay/export. Import/export, folders/tags, tip images, and application-global libraries remain. |
| ILL-09 | Bézier construction and node editing | 🟢 Working | P1 | Node mode distinguishes anchors from in/out handles, draws tangent controls, drags anchors/handles, mirrors handles with Shift, Alt-click subdivides the actual cubic, Ctrl-click deletes, double-click converts smooth/corner, and the inspector opens/closes paths. Exact SVG arcs convert to editable cubics; open paths split into styled objects, closed paths cut open without losing the closing segment, and transformed paths join by nearest world-space endpoints through shared UI/MCP kernels. Multi-subpaths and packaged pointer acceptance remain. |
| ILL-10 | Lines, arrows, rectangles, ellipses, polygons, stars | 🟢 Working | P1 | Core creation tools and editable model objects exist. World-space transformed bounds drive selection/lasso/handles and click hit testing uses actual fill/stroke paths where supported. Packaged transform coverage and richer per-shape parameter editing remain. |
| ILL-11 | Fill/stroke styles, gradients, eyedropper | 🟢 Working | P1 | Solid/linear/radial styles, interactive placement, and a full 2–32-stop editor support ordered positions, color, opacity, geometry, preview, and semantic agent editing. Reusable swatches, gradient transforms beyond endpoints, and robust composite sampling remain. |
| ILL-12 | Images and crop | 🟢 Working | P1 | Image objects, clipboard images, filters, rotated visual crop handles, centered aspect presets, Shift-constrained free crop, and exact non-stretching/resettable source crops work through shared UI/headless geometry. Replace/relink, numeric crop fields, and missing-asset UX remain. |
| ILL-13 | Styled text ranges and typography | 🟢 Working | P1 | The inspector tracks exact character selections and applies font, size, weight, italic, underline, color, and tracking to only that range; shared live/headless layout supports multiline wrapping, alignment, box dimensions, and line height. In-canvas editing, font fallback/embedding, vertical text, and text-on-path remain. |
| ILL-14 | Layer groups, object groups, masks, clipping masks | 🟢 Working | P1 | Layer/object grouping and ungrouping are authorable; one-click editable layer masks and top-object masks render and enforce reference-safe move/delete behavior. A dedicated isolation/edit mode, clipping-stack ordering UX, feathering, and semantic agent mask helpers remain. |
| ILL-15 | Alignment and distribution | 🟢 Working | P1 | UI and MCP share rotated-world-bounds geometry for selection, key-object, or artboard alignment and center or true equal-gap distribution on either axis. Packaged multi-selection acceptance remains. |
| ILL-16 | Union/subtract/intersect/exclude | 🟢 Working | P1 | UI and headless MCP share exact Paper.js union/subtract/intersect/exclude for transformed rectangles, ellipses, polygons, stars, and paths with one atomic delete/add transaction. Compound-hole editing UX, failure recovery, utility-process isolation, and golden geometry tests remain. |
| ILL-17 | Blend modes, opacity, shadows, non-destructive filters | 🟢 Working | P1 | Common blends, opacity, object blur/shadow, and ordered duplicate-capable brightness/contrast/saturation/hue/blur stacks render identically for objects plus isolated object groups and layer/group composites in live and headless paths. Shared UI/MCP editing and overlap/filter regressions are present; filter masks, SVG/PSD/PDF parity, effect-buffer performance proof, and packaged visual acceptance remain. |
| ILL-18 | Sparse tiled paint-layer storage | 🟢 Working | P0 | Save materializes content-addressed 256×256 PNG tiles with a SHA-256-bound editable-stroke prefix; native open validates dimensions, payload hashes, artboard bounds, and the stroke digest before enabling the cache. Live and headless renderers consume valid tiles then composite newly appended strokes, fall back to source strokes while browser tiles load or replay hides a cached stroke, and generic layer mutations cannot inject stale cache data. Multi-tile equivalence, stale-open invalidation, reducer trust, native round-trip, and editable/materialized 4K performance are gated. Add packaged reopen/erase/edit acceptance and dirty-tile-only save updates before Verified. |
| ILL-19 | Editable material presets and polished gold | 🟢 Working | P2 | The polished-gold preset builds editable gradient/blur/mask layers and exports to SVG. General preset management and more materials are not implemented. |
| ILL-20 | Multiple artboards | ⏸ Deferred | P2 | v1 intentionally has one bounded artboard per illustration document; multiple pages/artboards use separate document tabs. Multi-artboard documents are a post-v1 format change. |
| ILL-21 | Illustration animation timeline | 🟢 Working | P2 | Canonical schema-2 object-pose keyframes cover transform, opacity, visibility, three easing modes, duration/FPS, loop/once/ping-pong playback, scrub/play UI, transform-at-playhead authoring, exact-time MCP observation, and bounded GIF/APNG plus batch export. Reducer inverse, interpolation, migration, export, MCP, and rendering-golden tests pass; add richer property tracks, dope-sheet editing, and packaged playback/export acceptance. |
| ILL-22 | Object list / scene outliner | 🟢 Working | P1 | The Layers panel now has a searchable hierarchical per-layer object tree with type icons, canvas-synchronized range/additive selection, inline rename, visibility/lock controls, revision-checked drag/button ordering, layer/group moves, group creation, duplicate/delete, and reveal-on-canvas centering. It derives directly from canonical snapshots, so agent additions appear live without clearing human selection. Packaged E2E covers live agent insertion, selection preservation, rename, order, grouping, and filtering; add explicit drag/drop, visibility/lock, duplicate/delete, deep-cycle, and reveal-position acceptance before marking Verified. |

Evidence: [illustration canvas](../src/renderer/canvas/IllustrationCanvas.tsx), [geometry](../src/renderer/canvas/geometry.ts), [path booleans](../src/renderer/canvas/path-boolean.ts), [object inspector](../src/renderer/App.tsx), [material tests](../tests/common/material-presets.test.ts).

## 4. Pixel sprites and animation

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| PIX-01 | Pixel Sprite/Tilemap/Project creation and custom sizing | ✅ Verified | P0 | Dedicated creation UI, exact sprite dimensions, resize/crop, undo, and isometric/infinite map options have regressions. |
| PIX-02 | Indexed palette up to 256 entries | 🟢 Working | P1 | Indexed storage, protected transparent index 0, editing, adding, exact slot moves, and atomic replace-and-delete work across every cel, reusable stamp, palette cycle, and per-frame override with exact undo. Bounded portable AIDraw JSON and GIMP GPL import/export preserve order/names, offer replace-by-index or append-unique behavior, never delete indexed artwork implicitly, and create durable reports; core and live MCP regressions cover replacement. Add packaged palette UX acceptance. |
| PIX-03 | Integer coordinates and nearest-neighbor rendering | ✅ Verified | P0 | Low-zoom pointer mapping and CSS-stretched canvas coordinates have regressions. Add high-DPI/multi-monitor/stylus tests. |
| PIX-04 | Pixel-perfect pencil, eraser, fill, replace, line, shape tools | 🟢 Working | P1 | Core tools commit exact indexed samples. Add pixel-perfect corner cleanup, fill limits, ellipse goldens, drag cancellation, and large-region performance tests. |
| PIX-05 | Selection and magic wand workflow | 🟢 Working | P1 | Rectangle, true freeform lasso, and connected-color wand selections render marching ants and support replace/add/subtract/intersect modifiers. Drag move, copy/cut/paste at cursor, Select All, Escape cancel, delete, flips, rotation, and scaling use exact indexed/GID cells with union locks, clipping feedback, palette-preserving cross-sprite paste, and actor undo. Cross-application clipboard publication and packaged pointer acceptance remain. |
| PIX-06 | Stamps | 🟢 Working | P1 | Exact indexed irregular selections can be copied locally or captured into a persistent named library, previewed, placed, flipped, rotated, and deleted; semantic MCP placement shares the tested kernel. Tilemap selections use a separate exact 32-bit-GID stamp library. Library import/export and packaged pointer acceptance remain. |
| PIX-07 | Ordered dithering and lighten/darken | 🟢 Working | P2 | UI and semantic MCP share deterministic 2×2/4×4/8×8 Bayer kernels, exact foreground coverage, base/mix palette indices, phase-aware region filling, and selection-clipped previews. Lighten/darken steps to the nearest visual-luminance palette entry instead of assuming palette slot order. Add stroke-phase controls, preset persistence, and packaged visual acceptance. |
| PIX-08 | Symmetry, wrap preview, palette cycling | 🟢 Working | P2 | Horizontal/vertical symmetry, repeated preview, and named validated palette-cycle ranges with direction and timing are authorable and preview exactly without rotating unrelated entries. Authored axes, tile-offset wrap editing, and animated export behavior remain. |
| PIX-09 | Bitmap-font text | 🟢 Working | P2 | Pixel documents own strict reusable bitmap-font assets. A shared deterministic glyph kernel supports multiline text, scale, letter/line spacing, left/center/right anchors, exact locked painting, SVG glyph preview, JSON copy/import, and authenticated semantic MCP placement. Add a glyph/sheet editor, mapping UI, broader bundled character coverage, and packaged acceptance. |
| PIX-10 | Layered cels, linked cels, durations, onion skin | 🟢 Working | P1 | Layers/cels, duplicate/delete/reorder, exact duration editing, link/unlink with safe source deletion, onion skin, and playback are authorable through UI and semantic MCP kernels. Add cel exposure/grid editing, onion settings, and packaged replay regressions. |
| PIX-11 | Animation tags and loop/ping-pong preview | 🟢 Working | P1 | Named ranges can be created, edited, selected, deleted, and previewed forward/reverse/ping-pong through validated UI and MCP operations; UI, MCP, batch, and CLI GIF/APNG/sprite-sheet export resolve the chosen tag sequence exactly. Nested/overlap policy and packaged acceptance remain. |
| PIX-12 | Per-frame palette overrides | 🟢 Working | P1 | Enable/edit/clear authoring, validation, rendering, frame reordering, and semantic MCP replacement work. Import/export fidelity and packaged acceptance remain. |
| PIX-13 | Integer selection transforms and 90-degree rotation | 🟢 Working | P1 | Pure kernels preserve exact palette indices/GIDs for move, flips, clockwise/counterclockwise 90° rotation, and independent 1×–64× nearest-neighbor X/Y scaling of rectangular or irregular masks while clearing source and clipping finite destinations. UI and compact-run MCP scaling share tested geometry; counterclockwise UI exposure and packaged acceptance remain. |
| PIX-14 | GIF/APNG/sprite-sheet import and animation export | 🟢 Working | P1 | GIF/APNG/sprite-sheet export is tested with exact durations, transparency, presentation scale, disposal modes, and UI/MCP/batch/CLI named forward/reverse/ping-pong sequences. Bounded APNG and GIF parsers reject unsafe dimensions, frame rectangles/counts, truncation, and expanded-pixel budgets. The sprite-sheet image wizard and approved agent import share exact grid/margin/spacing/order/count/duration settings, optional empty-frame removal, alignment-preserving shared trim, embedded source retention, companion containment, and expanded-frame limits. Add representative third-party fixtures and packaged acceptance. |
| PIX-15 | Presentation upscale in UI and headless CLI | ✅ Verified | P1 | Integer 1×–64× nearest-neighbor export and overwrite-safe batch CLI are tested. Add batch APNG/sprite-sheet/Tiled coverage and a documented exit-code contract. |
| PIX-16 | Standalone/project links, embed/extract, Pack Project | 🟢 Working | P2 | Imported project sources retain portable relative paths, SHA-256 pins, and embedded cached content. The Assets panel supports per-link Embed, Extract, and Relink plus whole-project packing; MCP has semantic embed/pack and approval-backed relink/extract paths. Stale caches are refused, replacement cache deletion is reference-safe, and document-revision guards prevent long-open approvals/dialogs from overwriting concurrent edits. Pure transitions, file preparation, schemas, reducer conflict/undo, and live MCP contracts are tested; add packaged native-dialog and approved-job external-file round-trip acceptance. |

Evidence: [pixel canvas](../src/renderer/canvas/PixelCanvas.tsx), [pixel storage](../packages/core/src/pixel.ts), [pixel tests](../tests/core/pixel.test.ts), [coordinate tests](../tests/renderer/pixel-coordinates.test.ts), [CLI tests](../tests/main/cli.test.ts), [export tests](../tests/main/export-document.test.ts).

## 5. Tilesets, Wang terrain, and tilemaps

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| MAP-01 | Tileset/map/Wang/collision data model | ✅ Verified | P1 | Sparse chunks and deterministic Wang matching are unit-tested; the complete model is schema-validated. |
| MAP-02 | Tileset slicing | 🟢 Working | P1 | Tile width/height, margin, spacing, rows, columns, exact source coordinates, selected-tile preview, and metadata-aware re-slicing are authorable. Add source-sheet crop overlays, remap choices for dropped metadata, and packaged acceptance. |
| MAP-03 | Per-tile properties, probability, and animation editor | 🟢 Working | P1 | A selected-tile editor authors probability, typed custom properties, and animation frame/timing lists; Tiled interchange preserves them. Add drag reordering, visual animation preview, and fixture round trips. |
| MAP-04 | Collision-shape editor | 🟢 Working | P1 | A nearest-neighbor tile preview renders rectangle, ellipse, polygon, and polyline collisions; shapes move directly, bounded shapes resize from a corner, polygon/polyline points drag independently, Alt-click inserts a projected edge point, Ctrl-click deletes while enforcing topology minimums, numeric fields stay synchronized, and each collision has typed custom properties. Authenticated agents share revision-checked upsert/delete semantics. Multi-selection and packaged pointer/Tiled-fixture acceptance remain. |
| MAP-05 | Edge/corner/mixed Wang set editor | 🟢 Working | P1 | Multiple Wang sets/colors, type, probability, deletion, and all eight edge/corner tile slots are authorable through one validated UI/MCP kernel. Visual edge diagrams and external fixture coverage remain. |
| MAP-06 | Terrain painting | 🟢 Working | P1 | Terrain/set/color and paint/erase mode are selectable; a tested 3×3 repair kernel enforces shared edge/corner constraints, reports unmapped transitions, and chooses weighted variants. Add drag diagnostics, larger repair propagation where tilesets require it, and packaged finite/infinite acceptance. |
| MAP-07 | Finite/infinite orthogonal maps | 🟢 Working | P1 | Exact tile painting and 32×32 sparse chunks work. Add navigation, bounds resize, selection/stamps, large-map performance, and round-trip E2E. |
| MAP-08 | Finite/infinite isometric maps | 🟢 Working | P1 | Creation, rendering, fractional/integer inverse pointer mapping, exact painting, stamps, negative infinite-map coordinates, and affine-projected object-layer selection/move/resize now work with unit coverage. Strict tall-tile depth ordering, navigation polish, packaged interaction, and visual goldens remain. |
| MAP-09 | Tile/group/object layers, parallax, opacity | 🟢 Working | P1 | Pixel and map layer panels show true nested z-order with collapse/expand, cycle-safe group assignment, and drag reordering (Alt-drop nests); shared tree kernels are unit-tested. Authored X/Y parallax previews while panning include parallax-correct object hit testing. Orthogonal/isometric object layers render in z-order with lock-safe canvas move/resize and direct drag/insert/delete polygon points, while agents have revision-checked object upsert/delete semantics. Packaged drag/parallax/topology acceptance remains. |
| MAP-10 | Random variants and probabilities while painting | 🟢 Working | P1 | Wang terrain and named non-Wang tile groups use authored probabilities. A persisted signed map seed and stable coordinate hash make UI and `pixel.tile-variants.paint` MCP results reproducible; pure and live-contract tests cover weighting. Add thumbnails, stroke-level reseeding, Tiled convention docs, and packaged acceptance. |
| MAP-11 | Reusable tile/map stamps | 🟢 Working | P1 | Exact 32-bit transformed GIDs can be captured from tile selections into persistent named stamps, previewed, placed on finite/infinite maps, flipped/rotated/deleted, and placed through authenticated semantic MCP. Add cross-document libraries and packaged pointer acceptance. |
| MAP-12 | Tile transformations | 🟢 Working | P2 | H/V/diagonal flags render and round-trip; tileset permissions constrain paint controls, current-tile painting encodes flags, and selection/stamp transforms preserve full unsigned GIDs. Add transform-combination thumbnails and packaged Tiled fixture coverage. |
| MAP-13 | Tiled TMJ/TMX/TSJ/TSX fidelity | 🟢 Working | P1 | JSON/XML, chunks, nested layers, transforms, Wang data, collisions, animation, and typed properties are implemented. Committed external TMJ→TSJ and TMX→TSX fixtures cover isometric/orthogonal maps, typed metadata, geometry, Wang colors, GID flags, and property-preserving current JSON export; XML property double-normalization has a regression. Image-collection tilesets, templates, zstd, newer blend/tint fields, and comprehensive loss reports remain. |

Evidence: [Wang implementation](../packages/core/src/wang.ts), [Tiled importer](../src/main/import-document.ts), [Tiled exporter](../src/main/export-document.ts), [Wang tests](../tests/core/wang.test.ts), [export tests](../tests/main/export-document.test.ts).

## 6. Import, export, clipboard, and file authority

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| IO-01 | PNG/JPEG/WebP import/export | 🟢 Working | P1 | Raster import/export paths exist. Add color/alpha/orientation/large-image fixtures and round-trip visual comparisons. |
| IO-02 | SVG editable import | 🟢 Working | P1 | Committed fixtures cover editable shapes/paths/text/images, nested groups/transforms, defs/use, class and inline styles, linear/radial gradients, clips, and blur filters. Unsupported constructs produce explicit warnings; advanced masks/filter primitives, full CSS cascade, and broad third-party corpus fidelity remain. |
| IO-03 | SVG editable export | 🟢 Working | P1 | Vectors, nested groups, gradients, ranged text, arrows, image crops, object blur/masks, and paint raster fallbacks export and round-trip through committed fixtures. Complex filters, exact font shaping, blend portability, and broader third-party visual goldens remain. |
| IO-04 | PSD import/export | 🟡 Partial | P1 | Nested groups, visibility/opacity/blends, and supported styled-text companions are retained; visible per-layer raster fallbacks and the composite preserve appearance. Vector descriptors, effects/adjustments, uncommon color modes, and lossless text/effect round-trip remain unsupported and are reported explicitly. |
| IO-05 | PDF import/export | 🟡 Partial | P1 | Multi-page import preserves each page as a raster fallback plus hidden extracted text. Export is hybrid: simple solid vector paths/shapes, standard-font text, and embedded images remain native PDF content while paint, effects, gradients, masks, and unsupported transforms fall back transparently with a report. Editable common-operator import remains missing. |
| IO-06 | Clipboard AIDraw JSON + SVG + PNG | 🟢 Working | P1 | Editable AIDraw fragments and bitmap fallback are implemented. Add cross-application and multi-object/mask tests; publish the private MIME/HTML contract. |
| IO-07 | Pixel animated and sprite-sheet interchange | 🟢 Working | P1 | Generated GIF/APNG files round-trip timing, transparency, and disposal into editable frames. Metadata sheets import, and a dedicated image-slicing wizard plus approved MCP path covers grids, margins, spacing, order, count, timing, empty-cell skipping, shared trim, source retention, exact companion-folder authority, and bounded decoded work. Add external-tool fixtures and rotated/trimmed metadata conventions. |
| IO-08 | Tiled interchange | 🟢 Working | P1 | See MAP-13. External JSON/XML map→tileset fixtures now round-trip the supported typed metadata surface. Root/companion byte limits, canonical relative containment, DTD/entity rejection, layer/depth/object/cell/tile budgets, exact array lengths, and bounded compressed expansion are regression-tested. Unsupported modern fields and complete per-field loss reporting remain. |
| IO-09 | Explicit import/export report | 🟢 Working | P1 | Human and approved-agent imports/exports create actor-attributed, restart-persistent reports with exact source/output paths, warnings, rasterized entries, failures, document associations, and JSON export. Activity inspection and atomic bounded storage are tested at the store boundary; deepen adapters to emit per-object reason codes and add packaged workflow acceptance. |
| IO-10 | Exact-path authority and overwrite approval | 🟢 Working | P0 | Structured approval, session/persistent choices, symlink/junction canonicalization, case-normalized trust, race-aware exclusive writes, and planned companion overwrite paths are tested; trusted folders never auto-approve existing targets. Add a packaged sprite-sheet/Tiled companion overwrite scenario and reparse-point corpus before Verified. |

Evidence: [import adapters](../src/main/import-document.ts), [export adapters](../src/main/export-document.ts), [clipboard IPC](../src/main/main.ts), [export tests](../tests/main/export-document.test.ts), [persistence tests](../tests/main/persistence.test.ts).

## 7. Image generation

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| GEN-01 | Provider-neutral request/job/provenance model | ✅ Verified | P1 | Provider, mode, prompt, negative prompt, sources, mask, size/aspect, count, seed, options, outputs, and provenance exist. A shared UI/engine/MCP capability validator tests provider modes, ignored inputs, embedded source/mask authority, counts, dimensions, seeds, and ComfyUI workflow/endpoint requirements before any paid request. |
| GEN-02 | OpenAI `gpt-image-2` adapter | 🟢 Working | P1 | The utility runner maps `gpt-image-2` create plus multipart edit/inpaint/variation requests, normalizes matching PNG source/mask inputs, and builds a same-size transparent expansion mask for true centered outpaint. Mock coverage proves decoded dimensions, invalid-mask rejection before a paid call, rate limits, cancellation, and zero automatic retries for ambiguous failures. Live API compatibility and broader moderation/timeout payload fixtures remain. |
| GEN-03 | Stability adapter | 🟢 Working | P1 | Create/inpaint/directional-outpaint v2beta endpoints, source/mask/style/seed/form mapping, real decoding, rate limits, cancellation, malformed results, no retry, and partial paid-result retention are mock-tested. Unsupported modes and invalid directional options fail before approval/request. Live-version compatibility and broader moderation/timeout payload fixtures remain. |
| GEN-04 | ComfyUI workflow adapter | 🟢 Working | P1 | API-format workflow loading, automatic or explicit node/input mapping UI, upload, `/prompt`, WebSocket progress/reconnect, bounded history polling, multi-output retrieval, malformed history, and queue removal/running-prompt interrupt on cancellation are mock-tested. Live workflow/version compatibility and broader node families remain. |
| GEN-05 | DPAPI credential storage | 🟢 Working | P0 | Hosted keys use the protected credential store. Add rotation/removal, unavailable-DPAPI behavior, and renderer-leak tests. |
| GEN-06 | Agent paid-request approval | 🟢 Working | P0 | Agent jobs wait for a one-request approval with actor, provider/model or workflow, mode, prompt/negative prompt, source and mask thumbnails, result and potential paid-request counts, seed/size/options, expiry, and an explicit no-persistent-trust note. Contract tests cover the structured payload and bounded previews; add packaged approval/deny/timeout acceptance. |
| GEN-07 | Create/edit/inpaint/outpaint/variation workflows | 🟢 Working | P1 | Source/mask selection, provider-specific mode filtering, inline preflight, explicit Comfy workflow mappings, and non-destructive accepted-result layers/cels are shared by human and agent requests. Illustration outpaint now expands and translates the artboard/content atomically with exact undo and provenance. Automatic canvas-source capture and selection-to-mask authoring remain. |
| GEN-08 | Result compare, before/after overlay, accept/cancel | 🟢 Working | P1 | The source asset or canonical canvas is frozen before the provider request; completed results support synchronized side-by-side/opacity-overlay comparison, shared zoom/scroll, accept-to-layer/cel, and explicit unaccepted-result cleanup. Manager integration now covers partial completion and exact accepted asset/provenance creation; add packaged acceptance, memory limits, and multi-result comparison cases. |
| GEN-09 | Pixel palette conversion and reproducibility | 🟢 Working | P1 | Area resize, OKLab nearest palette, alpha threshold, optional Bayer/Floyd–Steinberg, source retention, and conversion metadata exist. Manager integration proves accepted generated output becomes a new indexed layer with engine-owned provenance through the utility quantizer seam. Add golden quantization/dither variants and explicit multi-frame acceptance policy. |
| GEN-10 | No silent provider substitution or ambiguous paid retry | ✅ Verified | P0 | Shared preflight rejects unsupported modes and inputs rather than approximating them. OpenAI is configured with zero SDK retries and its ambiguous server failure is issued once; Stability first/later failures are never retried and retain completed paid outputs with partial-failure metadata. ComfyUI is never silently substituted for a hosted provider. |

Evidence: [generation types](../src/common/generation.ts), [generation manager](../src/main/generation-manager.ts), [quantization](../src/main/quantize.ts), [generation panel](../src/renderer/App.tsx).

## 8. Desktop UX and accessibility

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| UX-01 | Playful light editor shell and context panels | 🟢 Working | P1 | Top tabs, left tools, central canvas, right panels, and bottom status are implemented. Run a coherent spacing/type/contrast pass at supported window sizes. |
| UX-02 | Dedicated New Document configuration | ✅ Verified | P0 | Toolbar, File > New, Ctrl+N, and Ctrl+Shift+N route into the same configuration dialog instead of mutating the document list. A packaged Playwright regression verifies Ctrl+N exposes the 1920×1080 configuration and Cancel creates no document. |
| UX-03 | Reachable overflowing document tabs | ✅ Verified | P0 | Scroll controls and an all-documents menu are covered by packaged E2E. |
| UX-04 | File menu Open and keyboard Open | 🟡 Partial | P0 | Both code paths now call the same open workflow after a prior menu failure. Add a packaged native-menu regression; current E2E does not prove mouse menu activation. |
| UX-05 | Renderer failure containment | 🟢 Working | P0 | Malformed transactions are schema-rejected and an editor-only recovery boundary prevents a permanent white screen. The boundary can save a bounded local-only diagnostic containing runtime and sanitized tab/job/MCP state while excluding artwork, prompts/results, task IDs, tokens, and credentials. Add a packaged boundary/reload test and malformed-event fuzzing. |
| UX-06 | Session restore and close/discard clarity | ✅ Verified | P0 | Explicitly discarded untitled documents no longer restore in unit/E2E coverage. Save All and Close All now consolidate unsaved-folder selection and save/discard decisions; add packaged multi-document save/close/crash recovery scenarios. |
| UX-07 | Replace browser prompts with editor UI | 🟢 Working | P1 | All four browser prompts are replaced by in-editor, focus-trapped, Escape/cancel-safe dialogs with validation and previews; tags also configure range/direction/color. Add packaged keyboard/focus and commit/cancel acceptance before Verified. |
| UX-08 | Keyboard-only operation and shortcut discoverability | 🟡 Partial | P1 | Core shortcuts and some ARIA labels exist. Complete focus order, tool settings, canvas alternatives, menus, modal focus traps, and shortcut remapping/documentation. |
| UX-09 | Accessibility audit | 🟢 Working | P0 | A static regression gate covers named icon controls, canvas names, status live regions, visible focus, reduced motion, increased contrast, forced colors, and keyboard/ARIA tab semantics; actor identity also uses labels/icons rather than color alone. Screen-reader passes, complete focus order, 200% text, and packaged Windows high-contrast/color-vision acceptance remain. |
| UX-10 | High-DPI, tablet, multi-monitor, and touchpad QA | ⬜ Missing | P0 | The coordinate fix has unit coverage, but no hardware/input matrix exists. |
| UX-11 | Batch save/export/close workflows | 🟢 Working | P1 | The all-tabs and native File menus expose Save All, collision-safe folder batch export, and consolidated Close All. PNG/JPEG/WebP plus sprite and keyframed-illustration GIF/APNG exports return per-document saved/exported/closed/skipped/failed reports; pixel outputs support integer presentation scale and named-tag selection. Add cancellation/failure unit seams and packaged multi-document Computer Use acceptance before Verified. |

Evidence: [editor shell](../src/renderer/App.tsx), [styles](../src/renderer/styles.css), [error boundary](../src/renderer/RendererErrorBoundary.tsx), [packaged E2E](../tests/e2e/editor.spec.ts).

## 9. Testing, hardening, documentation, and release

| ID | Feature | Status | Priority | Current truth and exit criteria |
| --- | --- | --- | --- | --- |
| QA-01 | Unit coverage for core schema/reducer/storage | 🟢 Working | P0 | Important happy paths and regressions cover ordered multi-operation inverse revision rebasing, atomic artboard translation, palettes, linked assets, cels/tags, locks, migrations, and storage. Add property/fuzz tests and systematic coverage of every operation/inverse pair. |
| QA-02 | Golden-image rendering suite | 🟢 Working | P0 | A deterministic raw-RGBA SHA-256 corpus gates illustration blend/filter/mask compositing, hard-round and watercolor brushes, indexed sprites, orthogonal maps, isometric maps, and a keyframed illustration midpoint. Expand to path booleans, text/font fallback, all brush presets, tile transforms, and file round-trip goldens. |
| QA-03 | Interchange round-trip corpus | 🟢 Working | P0 | Generated GIF/APNG round trips assert timing, transparency, disposal, and source retention; sprite sheets and JSON/GPL palettes are covered. Committed SVG fixtures exercise editable nested structure and fallback reports, PSD round-trip covers groups/styled text/raster fallback, hybrid PDF tests prove searchable text, and external-style Tiled JSON/XML fixtures cover supported metadata. Broader third-party PDF/PSD/animated/image-tileset corpora and explicit visual loss goldens remain. |
| QA-04 | MCP contract and concurrency suite | 🟢 Working | P0 | Auth, discovery, 32 sessions, all resources plus negotiated subscriptions, duplicate operations, semantic project links, durable batches, checkpoints, observation, locks, cancellation, approvals, and representative failure payloads are covered. Systematically exercise every remaining tool action/limit combination and the four-lane packaged visual scheduler before Verified. |
| QA-05 | Generation provider mock suite | 🟢 Working | P0 | Shared capability tests plus the actual utility runner cover OpenAI create/edit/inpaint/variation/true padded outpaint, mask validation, rate-limit/cancel, and ambiguous no-retry failure; Stability create/inpaint/outpaint/rate-limit/cancel/partial failure; Comfy workflow mapping/progress/reconnect/history/multi-output/queue cancellation; graceful worker cancellation/timeouts; and malformed images. Manager integration covers moderation/permission classification, partial completion, indexed provenance, terminal-state stability, and undoable outpaint expansion. Live compatibility and broader provider error payload matrices remain. |
| QA-06 | Complete packaged editor E2E matrix | 🟡 Partial | P0 | Core regressions and human+agent/headless scenarios exist. The original acceptance scenarios for conflicts, background tabs, approval, generated fill, animated sprites, Wang terrain, infinite maps, Tiled round-trip, and palette conversion are incomplete. |
| QA-07 | Automated performance gate | 🟢 Working | P0 | The repeatable Node-24 Windows gate records build/machine identity and fails budgets for 5,000 vectors, four populated editable and materialized-sparse 4K paint layers, a 65,536-tile view, native save, PNG export, one-million playback samples, and RSS growth. Packaged input latency, frame pacing, four-lane isolation, display scaling, and tablet measurements remain. |
| QA-08 | Corrupted/untrusted-input hardening | 🟢 Working | P0 | Native ZIP, APNG, GIF, inline raster, palette, bitmap-font, sprite-sheet, SVG/XML, PSD-header, and Tiled paths now have explicit byte/dimension/count/expansion limits and malicious regressions. Metadata/Tiled companions are realpath-contained to the approved root; XML DTD/entities and compressed layer bombs are rejected. Add malformed PDF/PSD corpora, CRC/interlace variants, cyclic reference fuzzing, and property-based parser tests. |
| QA-09 | Three-level independent Luna/high QA workflow | 🟡 Partial | P0 | Level 1 smoke, Level 2 regression, and Level 3 release-exhaustive requirements, report template, and independent-task rules are documented; every level mandates automated, isolated MCP, native Computer Use, and bidirectional cross-surface evidence. The coordinated isolated Level 1 workflow completed end to end and accurately failed only on UX-02 after its automated, MCP, pointer, undo/redo, and cross-surface cases passed. Exercise Levels 2 and 3 before graduating this workflow. |
| QA-10 | Exact-build isolated MCP/UI identity harness | 🟢 Working | P0 | Node-24 discovery, runtime enforcement, package artifact verification, isolated interactive/headless session launch/status/stop, coordinator checkpoints, persisted MCP client state, hash/PID/URL checks, post-stop credential redaction, and an explicit unsandboxed-Windows-launch guard are implemented. The harness passed manual MCP and Computer Use self-tests outside the filesystem sandbox. Add automated harness tests, make headless attach reliable, and include it in CI. |
| QA-11 | Native desktop OS matrix | 🟠 Scaffolded | P0 | Verify/package jobs target Windows, macOS, and Ubuntu; tag builds target Squirrel/DMG/DEB/RPM/ZIP with per-platform checksums. No hosted macOS/Linux run or native interaction result exists yet, so those platforms are not release-verified. |
| REL-01 | Clean Git history and reviewable baseline | ✅ Verified | P0 | Root commit `73d0712` establishes 118 reviewed source/test/doc files on `codex/pre-v1-critical-path`. Ignore rules exclude packages, caches, QA output, tokens/secrets, and root drawing artifacts; staged diff and credential scans were clean. |
| REL-02 | Honest versioning and changelog | ✅ Verified | P0 | Root/core/lock metadata use `0.1.0-alpha.1`; a dated changelog and explicit prerelease/stable release checklist exist. An automated metadata test prevents a premature stable version or workspace-version drift. |
| REL-03 | Desktop CI | 🟠 Scaffolded | P0 | Verify/package jobs are matrixed across Windows/macOS/Ubuntu, Windows keeps packaged E2E, and dependency audit runs independently. No hosted repository run proves the definitions yet. Publish the baseline, run all native jobs, and retain artifacts/diagnostics. |
| REL-04 | Native packages, archives, checksums, license report | 🟢 Working | P0 | Windows Squirrel/ZIP is locally produced; Forge also defines macOS DMG/ZIP and Linux DEB/RPM/ZIP. Package verification resolves each native bundle layout and per-platform checksum manifests cannot be polluted by stale maker output. Native clean-machine installation, unsigned macOS launch, Linux desktop integration, and tagged-release verification remain. |
| REL-05 | README, contributor guide, security policy, issue templates | 🟢 Working | P1 | Files and templates exist. Reconcile all claims with this tracker and add developer architecture/testing recipes. |
| REL-06 | MIT GitHub publication and tag-driven release | ⬜ Missing | P0 | Repository owner, remote, credentials, published tag, draft release, and public artifacts are not established. |
| REL-07 | Reproducibility and dependency/license review | 🟡 Partial | P0 | Lockfile, audit notes, checksum, and license scripts exist. Rebuild artifacts independently and compare hashes or document deterministic exceptions. |

Evidence: [test suite](../tests), [testing workflow](TESTING.md), [Luna/high prompt](testing/LUNA_HIGH_PROMPT.md), [QA session harness](../scripts/qa-session.mjs), [QA MCP client](../scripts/qa-mcp.mjs), [CI workflow](../.github/workflows/ci.yml), [release workflow](../.github/workflows/release.yml), [package scripts](../package.json), [dependency audit](DEPENDENCY_AUDIT.md).

## 10. Explicitly deferred from v1

These are not defects against the selected v1 unless the product scope changes:

- mobile, web hosting, 32-bit builds, and multiple native windows;
- accounts, telemetry, cloud sync, remote human multiplayer, and built-in assistant/chat;
- CMYK/16-bit workflows, ABR brushes, Aseprite round-trip, and lossless Photoshop/PDF round-trip;
- custom autotile scripting, automatic updates, provider accounts/model downloads, and code signing.

## Recommended critical path

1. **Close packaged P0 regressions:** native File → Open activation, renderer recovery/reload, attached advisory state, held human locks, four visible lanes, background tabs, approvals, and multi-document save/export/close.
2. **Finish shared studio depth:** richer natural-media/custom-brush behavior, remaining path/text/mask/filter polish, project link/embed/relink workflows, and lower-level checkpoint merge/variant convenience.
3. **Earn external compatibility claims:** broaden third-party SVG/PDF/PSD/animated/Tiled fixtures, finish provider error/mode matrices, and validate supported live provider/workflow versions without chargeable automatic retries.
4. **Run the deferred native evidence:** Computer Use pointer/frame-pacing, 200% scaling, screen readers/high contrast, tablet/touchpad/multi-monitor, and the complete Level 2/3 packaged matrix on exact Windows/macOS/Linux artifacts.
5. **Meet the desktop release gate:** hosted native CI, clean-machine package/archive verification, reproducibility review, final documentation reconciliation, Level 3 Luna/high, and a checksummed tagged GitHub release.
6. **Stop at the selected boundary:** remote service tenancy/multiplayer and mobile/web products remain separate programs rather than hidden prerequisites for the local agent-native desktop product.

## Maintenance rule

Every feature change should update this file in the same change set. Record the automated acceptance that justifies **Verified**; if a regression invalidates an exit criterion, move the item back immediately. Design documents may remain aspirational, but this tracker must remain literal.
