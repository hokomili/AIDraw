# September 6 rendering and semantic-operation correction

Status: resolved for the consolidated correction. Final source checks, independent emitted-source review, mandatory package verification, local exact-package behavior, ModelBenchmark dependent validation and the distinct clean-agent public-discovery exercise all pass. All owned test engines stopped without force and left zero exact-profile survivors. This is a local correction checkpoint, not release certification.

## Problem and resulting behavior

ModelBenchmark's September 6 audit of source `28cec1334d4de936f85816416a2e668406076810` found an off-center radial PNG despite a centered SVG, compound boolean exclusion rejected by the editable-path validator, populated-group transfer refused across vector layers, and missing semantic payload fields in public discovery. The consolidated request is `shared/requests/2026-09-06-modelbenchmark-aidraw-rendering-and-semantic-edits.md` in the live Secretary shared directory; resolve it through [AGENTS.md](../AGENTS.md).

- Editor Canvas and native raster rendering now use one paint helper. A radial gradient has its center at local-object `x1/y1`; the distance to `x2/y2` is its radius. Both Canvas circles use the same center. A zero radius uses the final stop, matching SVG behavior.
- `aidraw_help({topic:"operations"})` lists 97 public operation kinds. Adding `operation:"<kind>"` returns the operation's input JSON Schema from the admission validator and runtime preconditions. The nine tools and existing eight structured `canvas_apply` branches remain available. Retired provenance writes remain unavailable. Schemas describe payload shape; document references, refinements, locks, resource bounds and authority are still validated at execution.
- Editable booleans check their result against the existing single-subpath envelope before submitting an edit. Unsupported compound results return a clear nonretryable conflict and preserve both operands. The geometry kernel and passive legacy readability retain their broader existing behavior. This does not add general compound-path editing.
- Populated groups still move only within their vector layer. A cross-layer request now reports a nonretryable capability conflict and directs the caller to the published limits. Individual objects and empty groups retain supported cross-layer behavior, subject to existing mask and reference constraints. This does not add subtree transfer.

Public guidance is maintained in [MCP.md](MCP.md), the served guide, operation-specific help and [README.md](../README.md). There is no native-format migration, new filesystem authority or persistent credential mechanism.

## Verification and exact subjects

Generated evidence is retained under project-relative `test-results/audit-correction-20260906t120146z/`. Paths below are relative to that root and are not portable artifact downloads. Original ModelBenchmark audit files and package pins were preserved.

| Evidence | Result and limit |
| --- | --- |
| `source-verify-final.log` | `npm run verify` passes portability (641 tracked + 5 prospective paths at execution), TypeScript, ESLint and 242 files / 1,759 tests. Two obsolete Git-state assertions in documentation tests were removed or updated to retain the relevant feature assertions. Earlier failed tooling and stale-documentation runs remain recorded. |
| `clean-install-r2.log`, `tooling.json`, `native-dependency-r2.json` | Fresh disposable source copy; unchanged lockfile; `npm ci` installed 641 packages. Node 24.19.0, npm 11.6.2, Electron 43.4.0, Darwin ARM64; native Canvas loaded successfully. The ordinary checkout's dependency tree was not replaced. |
| `source-r2/out-audit-correction-r2/.aidraw-package-build-input.json` | Exact 256-file / 3,619,104-byte package input set; canonical record SHA-256 `ba38954d6d4299548af1f345856f74291d1a919b0f7f6079fd79705923860028`. Documentation and later verifier/test edits are outside this input set. |
| `review-r2/subject.json`, `review-r2/extracted/`, `review-r2/final-worktree.patch` | Complete extracted first-party runtime and reviewable source correction. The package-only generation is single-use. Its initial postpackage verification stopped at the old reviewed-pair identity mismatch. Independent r2 review then accepted the complete pair; owner admission retained all earlier identities and existing controls. |
| `package-verification-r2.json` | Mandatory verifier passes the exact source input set, strict deep ad-hoc signature, fuses, runtime/resource/preload policy, retired-surface absence, utility result boundaries, 239 negative and 47 safe-drift rejection controls. The receipt is 18,394 bytes; independent static review does not imply general runtime containment proof. |
| `acceptance-r2/report.json`, `acceptance-r2/calls.jsonl` | 164 public MCP calls against the exact new headless package. All 97 served operation schemas compile; the decoded compact gradient result is 2,453 UTF-8 bytes (3,495 bytes in the formatted text block; 6,315 bytes for the complete native result). Two centered/translated fixtures agree across observation PNG, exported PNG, independently rasterized SVG and editable SVG reimport. Native save/close/open retains exact objects and PNG bytes. Unsupported XOR/group requests preserve the complete canonical document and return nonretryable guidance; supported union, undo and individual transfer commit. |
| `stop-r2.json`, `acceptance-r2/cleanup.json` | Graceful terminal shutdown returned zero, recorded engine stopped, handoff authority redacted, no force attempted, and no process survived with the exact run profile. |

| Package component | Bytes | SHA-256 |
| --- | ---: | --- |
| Launcher | 53,056 | `f2dd00e5dfac0ad8f12ccbc600b2dba8f1dbb175fd7f9bef4f7d7fb7e30ea76b` |
| `app.asar` | 43,619,846 | `0c228a72ee8a2719df9fe2256e8a3775722fa6e849621fe97e28e1fab3a87cec` |
| Complete emitted main | 2,806,262 | `61003a7decca3afbf0f6fd327257f3b74dfb15457613e866751033a803511ae0` |
| Complete emitted worker (`utility-worker-CVgWUHEm.js`) | 226,303 | `ca233e58f2d89f0b26df91eab1ac3b7975cb795d2dd75dcceb864265b5f5aa96` |

The local app is `source-r2/out-audit-correction-r2/AIDraw-darwin-arm64/AIDraw.app` under the evidence root. It is an ad-hoc signed development package. Exact source inputs, dependency installation, emitted runtime, package identities and final working-tree changes are distinct subjects; no independent rebuild reproducibility is implied.

## Independent review and fresh-agent acceptance

ModelBenchmark independently accepted the final complete emitted main/worker pair after reverifying 273 package/extracted/input identities and the complete r1/r2 differential. Its record is `var/aidraw-correction-review-20260906/review-r2.md` in ModelBenchmark; the exact AIDraw copy is `review-r2/independent-review.md`, SHA-256 `56ffc55c8a37e0dbcb3cbb6d82911bddf4af0f67f79f635acceb54b7de24fdda`. Only the concise help callback changed from accepted r1; the worker is byte-identical. Owner admission and all package controls then passed. ModelBenchmark also independently passed public-schema-sourced construction of the selected gradient, boolean and group edits, all 97 served schema compilations, radial PNG symmetry at five distances, SVG center/radius, native save/reopen with identical raster, zero-radius half-alpha red, immutable refusals, supported union and individual movement, and exact-profile process cleanup. Evidence is `var/aidraw-correction-review-20260906/r2/` plus `process-cleanup.json` in ModelBenchmark; exact local copies are retained under `review-r2/dependent/`. Its independently measured full bundle digest is `71aa9559b84dfc5a8aef53f22b2f90fc32f601240eebcf8e35f68736076d0fd7` using the ModelBenchmark inventory scheme. Original audit pins remain preserved; ModelBenchmark owns any future adoption of the new pin.

Those ModelBenchmark results are independent maintainer and fresh public-client checks: the dependent receipt explicitly records `schemaSourced=true` and `freshIndependentAgentExperiment=false`. The separate clean-agent exercise below satisfies the remaining discovery criterion without changing that receipt's meaning.

A fresh agent with no inherited conversation used only its private transport handoff, served MCP tools/help/observations/results and its own output against the same final r2 package. It read no implementation, tests, helper client, historical audit or shared manager context. `fresh-agent/report.md`, `result.json`, `requests.json`, `transcript.jsonl`, `evidence-index.json` and `mutation-payloads-and-results.json` preserve discovery, exact payloads and native responses. No necessary field, unit, revision or precondition was undiscoverable for the completed route.

The agent created a translated editable radial orb, united overlapping rectangles into one editable path, moved individual objects between vector layers, grouped them, and confirmed that the documented populated-group cross-layer refusal was nonretryable and preserved the entire document. A targeted path-node edit used the observed index, anchor and revision. Native save/close/reopen preserved editable objects, vector layers, artboard and path nodes. PNG export, final observation and reopened observation matched in exact file hashes and decoded RGBA bytes; the center-to-edge scan had 99 distinct RGB values and monotonic color channels. SVG inspection preserved the gradient, ellipse and edited path geometry; this exercise did not independently rasterize SVG. The separate local package regression above supplies that raster/reimport evidence.

The native artwork is `fresh-agent/work/orb-study.aidraw` (29,532 bytes, SHA-256 `154e7480429428f9d825d601a5a318d5df05e9ef42e3f90001fecc80006d0365`). Its PNG is 25,596 bytes, SHA-256 `112a33123dee0077b3511af7ffc54768dee69c76ea86846bd1e5ea267444fd8f`; SVG is 1,393 bytes, SHA-256 `4e973e6538f1cd3dee81550407b53c90d7c48781e1316fd7180b80122fdb7043`.

One literal whole-layer equality check failed because reopen added `tileCache` metadata to an untouched empty paint layer (version 1, zero strokes and the empty-stroke hash). `fresh-agent/artifact-checks-initial.json` preserves that failure; the final checks explicitly keep `nativeReopenLayersIdentical=false` alongside the exact vector/pixel equality. The report also preserves three corrected local harness mistakes: a pre-request schema-reader typo, reused IDs in independent initial discovery responses, and replacement of an insufficient image equality helper with exact RGBA/file checks. No product mutation validation mistake or unchanged refusal retry occurred. These observations do not broaden the scoped PASS into literal whole-document identity or exhaustive operation acceptance.

`launch-fresh-agent.json` binds the exact launcher, fresh owned profile and explicitly granted output folder. `stop-fresh-agent.json` records graceful exit zero, stopped engine and redacted authority without force; `fresh-agent/cleanup.json` records zero exact-profile survivors. `final-source-binding.json` confirms all 256 current first-party package inputs still match the built subject, with an empty index and five preserved recovery stashes. `final-evidence-manifest.json` hashes the completed evidence and reviewable working-tree patch.

## Remaining scope

The bounded fresh-agent result establishes the selected public authoring route. Every semantic operation's authoring coverage, installed/vendor-client behavior, agent adoption, productivity and subjective quality remain separate evidence questions.

The current registry audit (`dependency-audit.json`) reports high-severity URI-normalization advisories in the existing runtime AJV helper `fast-uri`, and a moderate serialization advisory in build-only `@xmldom/xmldom` through plist. The lockfile was unchanged; no forced dependency update was performed. This checkpoint does not certify current dependency security or release readiness.

General compound-path editing and populated-group cross-layer transfer remain unsupported. Broader UI/installed/vendor-client acceptance, Windows/Linux/Intel execution, DMG/updater delivery, trusted signing/notarization, Level 3/RC and stable-v1 remain governed by the [release checklist](RELEASE_CHECKLIST.md). No GUI desktop-control acceptance was performed for this correction.

The operation-specific response refinement followed the accepted first package: its lookup had repeated about 18 KB of general guide/catalog text. The final response returns the requested schema and preconditions first in a concise envelope, while general discovery remains available separately. The gradient lookup is regression-bounded below 4,096 characters. The original package, its local behavior evidence and the independent r1 review remain preserved under `review/` and `acceptance-r1/`; the final package is r2.
