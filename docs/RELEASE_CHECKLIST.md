# AIDraw release checklist

This checklist complements `FEATURE_TRACKER.md`; it cannot override a missing or partial release gate.

RC1 preparation (2026-09-09): `codex/rc1` triggers the native release-build matrix without creating a tag or publishing a release. Only tag events can enter the publishing job. Candidate build artifacts and package-review evidence remain bound to their exact commit; a green hosted matrix alone does not satisfy independent Level 3 or Windows 11 clean installer/portable acceptance. The current candidate is untagged and the gates below remain open until their evidence exists.

Historical local evidence (2026-08-25): a retained disposable source copy reused its clean 641-package lockfile tree and produced one fresh single-use Apple Silicon package-only generation. The exact first-party package source-input manifest is 42,883 bytes / `ff96cd2d…8d85`, covers 252 files / 3,598,388 bytes including `index.html`, and has record digest `3f580053…6a12`; later documentation bytes are not package source inputs and the final worktree manifest remains separate. Dependency installation and toolchain bytes are not folded into that first-party label; the lock/dependency census and exact artifact identities are separate. The 53,056-byte launcher is `fcb4bce0595a81d4da2f948424fb8d6d8c94e72bc15c2e8e121e3fcb7505b6fc`; the 43,572,200-byte ASAR is `974559010dac79fef3d73141bb659f88e44ea39643af1cd2671e60a52b24dc96`. Package verification passed the exact dependency census, deep ad-hoc signature/designated requirement, all 15 first-party JavaScript chunks, provider/generation/protected-storage absence, published vector/pixel/tilemap discovery markers, and `exact-emitted-subject-identity-admission` for main `89fb2350…4386` / 2,792,944 bytes plus worker `67e5684f…581b5` / 226,266 bytes. The 239 negative and 47 intentionally safe drift subjects all fail exact identity; this is not a general JavaScript or current-ASAR FND-09 runtime claim.

The exact installed receipt `205970a0…2ab1` binds the package-verification receipt `e134daba…38f`, first-party source-input manifest, both byte-identical installed copies, complete 606-entry bundle tree (`29497777…e0ca`), harness, static no-secret client configuration, negotiated protocol, redacted v2 handoffs, process identities, relocation/restarts, exact nine tools, all eight published structured vector/pixel/tilemap branches, editing requests derived from served discovery, native save, PNG/Tiled interchange, and cleanup. Three ordinary launches prove automatic MCP availability without launch-time folder authority; an untrusted save waits for a human, and only one separately declared fourth launch carries the exact run-scoped QA file grant. Real utility crash/hang/cancel/restart behavior remains historical on the preceding ASAR and was not re-earned. The 2026-08-22 DMG/ZIP/checksum/license outputs also predate the current source. Three missing historical ASARs remain protected receipt-only chronology. These observations do not check any box below: the subject is wholly unstaged and dirty, lacks clean-commit provenance, uses only ad-hoc signing, has no current DMG/ZIP or online audit/signature refresh, used no ordinary GUI or Computer Use, and has no fresh Windows/Linux/Intel, updater, hosted, reproducibility, clean-machine, vendor-client, or independent Level 2/3 result.

## Prerelease

- [ ] Choose a SemVer prerelease version and add its dated changelog section.
- [ ] Run `node scripts/npm-node24.mjs run check:portability` and `node scripts/npm-node24.mjs run verify` on Windows, macOS, and Linux native runners.
- [ ] Install the complete locked tree and run `node scripts/npm-node24.mjs run security:verify`. Require zero reachable high/critical findings in both the production audit and the explicitly included production/development/optional/peer complete audit, plus successful registry signature/attestation verification where npm supports it; never let ambient npm `omit` configuration narrow either gate.
- [ ] Run the packaged Level 2 automated regression matrix.
- [ ] Record known limitations and ensure README claims match the tracker.
- [ ] Build Squirrel/DMG/DEB/RPM/ZIP artifacts on their native OS runners; generate per-platform SHA-256 and license reports.
- [ ] Start each package/make attempt in one absent single-use generation root, retain its `.aidraw-package-generation.json` identity through verification, and never reuse or silently clear a consumed root.
- [ ] Retain and verify the exact first-party package source-input manifest captured inside that generation. Require the renderer entry document and every runtime/configuration source root; bind its manifest hash plus the launcher, ASAR, and bundle-tree hashes in installed evidence. Keep later documentation, dependency/toolchain evidence, and the final worktree/status manifest separately attributed.
- [ ] Require package verification to report the exact manifest/lock Electron version from the packaged runtime itself. On macOS, require the native DMG maker and an independent `hdiutil verify`; treat the ad-hoc/unsigned image as development evidence until signing/notarization gates pass.
- [ ] Require package verification to find the exact `LiberationSans-OFL-1.1.txt` resource in every native app layout, and require the generated license inventory to agree with its four bundled font hashes and complete OFL notice.
- [ ] Retain each native job's schema-2 `RELEASE_PROVENANCE-<platform>.json`; it must bind a clean commit, single-use package generation, and exact source/toolchain/checksum/artifact inventory. Preserve pre-policy schema-1 manifests as historical evidence only; do not mix, migrate, or compare them as current release inputs.
- [ ] Verify no credentials, transient MCP run-state/handoff files, user profiles, QA artifacts, or personal artwork are staged, and generated client snippets contain only the stable stdio launcher/arguments.
- [ ] Run `node scripts/npm-node24.mjs run rc:audit`; treat its ranked output as planning evidence only, not as a release certificate.
- [ ] After fresh independent Level 3 evidence exists, fill an ignored copy of `docs/testing/RC_EVIDENCE_TEMPLATE.json` and require `node scripts/npm-node24.mjs run rc:verify -- --evidence=test-results/astra-high/<run-id>/rc-evidence.json` to pass on the exact clean candidate.
- [ ] Repeat the proven macOS background-engine route against an exact installed candidate: `--headless` must remain windowless and prohibited through non-show activation; intentional show, editor detach/reattach, and terminal quit must preserve the lifecycle and end with zero app/helper survivors. The 2026-09-03 fresh unpacked-package receipt is supporting evidence, not installed/update proof.

## Stable v1 gate

- [ ] Every P0 is Verified and every selected-v1 P1 has met its documented exit criteria.
- [ ] Utility-process, corrupt-input, performance, accessibility, and complete packaged E2E gates pass.
- [ ] Level 3 independent Astra/high automated + MCP + Computer Use QA passes on the exact checksummed build.
- [ ] Install/uninstall/archive behavior passes on clean Windows 11, macOS, and supported Linux VM profiles.
- [ ] Every native package proves the canvas binary, hardened fuses, login-item lifecycle, archive contents, and absence of protected-secret storage modules or legacy provider-store creation on the exact architecture. With one unchanged no-secret client configuration, prove its product-owned wrapper survives/refreshes across an actual Squirrel update and supported macOS/Linux install relocation; stdio remains inherited; the bridge creates no window, activation, Dock/taskbar presence, or competing app instance; ordinary GUI and supported headless launch both connect; stop/restart uses fresh internal authority/session while restoring joined attribution/document intent and resource subscriptions; stale direct authority is rejected; a committed mutation with an injected response failure is not replayed; whole initialize-response loss retires its unknown-ID provisional session within the five-second server bound; transient known-session DELETE failure is retried without forgetting cleanup; PID/instance discovery is private and atomic; and MCP, health, and identity routes are authenticated. Inspect native Windows owner/protected-DACL/exact child-inheritance behavior plus every lexical reparse component for default and caller-selected profiles, POSIX modes on macOS/Linux, and exact archive/source scanning rather than treating source/verifier wiring as package proof.
- [ ] On installed packages, kill a confirmed stdio bridge while all explicit session DELETEs fail and prove the host releases its capacity through authenticated lifetime-stream loss. Separately interrupt an idle subscribed bridge's SSE with EOF/error while stdio remains open and prove automatic single-session reconnection, actor/document/subscription restoration, and notification delivery before another client request. On native Windows, prove mapped/network/substituted drive rejection, exact equality of pre/post physical volume identities, and ancestor owner/effective-right checks—including unrelated delete-child/replacement authority—without rejecting an ordinary safe local user profile.
- [ ] Developer ID signing, hardened-runtime entitlements, notarization, and stapling are either independently verified or the ad-hoc/unsigned Gatekeeper limitation is explicit in every release artifact and note.
- [ ] Rebuild each Windows x64, macOS arm64, and Linux x64 artifact set independently; require `node scripts/npm-node24.mjs run reproducibility:compare -- --left=<first-provenance.json> --right=<second-provenance.json> --output=<comparison.json>` to produce a hashed PASS report, or stop for an explicitly reviewed deterministic exception rather than normalizing a mismatch.
- [ ] Dependency/license review is complete, covers runtime, packaged-Electron, and build/supply-chain surfaces separately, and agrees with the compared lockfile, registry-signature result, and retained license reports.
- [ ] Repository baseline, owner, remote, CI evidence, signing status, and publication credentials are confirmed.
- [ ] Require the same evidence manifest to pass `node scripts/npm-node24.mjs run rc:verify -- --evidence=test-results/astra-high/<run-id>/rc-evidence.json --stable-v1`; this machine check supplements rather than replaces independent evidence review.
- [ ] Replace prerelease metadata with `1.0.0`, finalize the changelog, tag, and publish only after all prior boxes pass.
